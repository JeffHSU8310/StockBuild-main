
## ADR-116：籌碼 / 選股 加上「開啟完整視窗」

### 使用者要什麼

底部分頁的高度只夠顯示兩三列，而籌碼與選股的輸出動輒數十列 —— 得一直捲動，
等於看不到全貌。量化交易早在 ADR-057 就有獨立視窗，這兩個分頁也要有。

### 關鍵決定：搬家，不是複製

量化面板支援「分頁一份、獨立視窗一份」**同時存在**，代價是要維護 `_qt_uis`
清單、每次刷新都要走訪所有存活的 UI。

籌碼與選股不能照抄，因為它們的建構函式把 widget **直接掛在 `self.*`**
（`self.tree_chips`、`self.tree_sc`、`self.btn_sc_run`…）——單一實例設計。
硬要複製會變成：`self.tree_sc` 指向視窗那一份，**分頁的表格從此再也不會更新**，
而且畫面上看起來完全正常，只是資料永遠停在舊的。這種 bug 極難察覺。

所以改成**搬家**：

| 動作 | 行為 |
|---|---|
| 開視窗 | 拆掉分頁的內容 → 在視窗裡重建 panel → 重播內容 |
| 關視窗 | 拆掉視窗的內容 → 在分頁重建 panel → 重播內容 |

永遠只有一份 panel，`self.*` 永遠指向它，**孤兒不可能發生**。分頁在視窗開著
時顯示一段說明（「此面板已在獨立視窗開啟，關閉那個視窗就會搬回這裡」），
讓使用者知道面板去哪了、不是壞掉。

### 重建會清空表格，所以要能重播

- **籌碼**：`_chips_refresh_view()` 純讀本機檔案，直接重呼叫即可
  —— 不會觸發下載（ADR-100 的保證仍然成立）。
- **選股**：記住最後一次渲染（`_sc_last_render`），搬家後重播。

**「記住畫面上是什麼」寫在畫它的函式裡，不寫在呼叫端。** 這條是實測踩出來
的：我原本寫在 worker 那一條路徑上，結果直接呼叫 `_sc_fill_tree()` 的路徑
沒記到，面板一搬家結果就消失，而且不會有任何錯誤訊息。寫在渲染函式本身，
任何呼叫端都自動涵蓋。

### 修掉一個會產生重複視窗的寫法

原本沿用 ADR-057 的寫法：

```python
try:
    if win is not None and win.winfo_exists():
        win.deiconify(); win.lift(); win.focus_force()
        return win
except Exception:
    pass
```

`try` 包太大了：只要 `deiconify` / `lift` / `focus_force` 任何一個失敗，就會
掉進下面的「新建」分支，**開出第二個視窗**——兩份 panel，`self.*` 只指向
其中一個，另一個變成永遠不更新的孤兒。已拆成「先單獨判斷還活著嗎，再逐一
嘗試帶到最前面」。

這個問題是診斷環境**先發現**的：mock 的 widget 沒有 `deiconify`，一呼叫就
AttributeError，於是重複開啟真的開出了第二個視窗。順手把 `deiconify` /
`iconify` / `withdraw` / `focus_force` / `attributes` / `resizable` 補進 mock
（同 P-57：mock 缺方法 = 該路徑等於沒被測）。

### 涉及檔案

| 檔案 | 內容 |
|---|---|
| `stock_app_pro.py` | `open_panel_window()` 通用機制、兩個工具列按鈕、`_sc_last_render` |
| `diag_mock_tkinter.py` | 補上 Toplevel 的視窗操作方法 |
| `diag_repro_issues.py` | +1 案例（搬出／搬回／不重複開／結果不遺失） |

### 已驗證（headless）

- `diag_repro_issues.py` **42 案例全過**（新增 1 個）
- 新案例涵蓋：開窗後分頁舊 widget 確實已銷毀（不留孤兒）、重複開啟不新建、
  關窗後搬回且 widget 是活的、**關掉再開第二輪也不壞**、選股結果搬出搬回都在
- **突變測試**（確認斷言不是空的）：
  - 關視窗時不搬回分頁 → FAIL
  - 重複開啟時新建第二個視窗 → FAIL
- `tests/test_core.py` 540、`tests/test_brokers.py` 42 全過、`crossref` 乾淨

### 需使用者實機驗證（版面 headless 完全測不到）

1. 籌碼分頁 → 按「🗖 開啟完整視窗」，確認視窗開得夠大、表格看得到很多列。
2. 分頁那邊應顯示「此面板已在獨立視窗開啟…」的說明文字。
3. 關掉視窗 → 面板回到分頁，**而且原本的表格內容還在**。
4. 選股跑一次有結果後再開視窗，確認**結果跟著過去**；關掉後也還在。
5. 在獨立視窗裡按「開始選股」「更新籌碼」等按鈕，確認功能正常（不是死的）。
6. 籌碼與選股兩個視窗**同時開著**，確認彼此不干擾。
7. 反覆開關幾次，確認不會愈開愈慢或出現第二個視窗。
