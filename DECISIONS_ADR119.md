
## ADR-119：關閉沒反應（GIL 餓死 UI）、櫃買無資料誤報、期貨金額單位、指數帶入編輯器

使用者實機回報的四項。其中第 4 項（關不掉）最嚴重。

### 四、「執行一陣子後按 X 沒反應」

**根因是籌碼下載把 UI 執行緒餓死。** 截圖顯示當時正在跑 10 年下載
（34/2364）。原本的下載迴圈**每一天都做 4 次 `upsert`**，而 `upsert` 是
「整檔讀入 → concat → 去重 → 寫回」。個股月檔會長到數 MB，10 年就是
**2364 次整檔重寫**。

pandas 這些運算是**握著 GIL** 做的 —— 背景執行緒不會因為是 daemon 就把 CPU
讓出來，主執行緒的 tkinter 事件迴圈拿不到 GIL，點 X 根本沒被處理（P-48 的
典型情境，只是這次量級大得多）。

**修法一：批次寫檔。** 累積約 20 天才落地一次，寫檔次數從 2364 降到約 120。
不是全部堆到最後才寫 —— 那樣中途按停止或關程式，整批就白抓了（下載很貴，
不能有這種風險）。中止路徑上也會先把已抓到的沖出去再返回。

**修法二：關閉保底看門狗。** `on_app_close()` 每一步都已經有時限，但只要有
任何一個第三方呼叫（shioaji logout、子行程 wait）卡在不會回來的地方，整個
關閉就停在那裡。新增一個 daemon 執行緒：不管主流程走到哪，**8 秒後強制
`os._exit(0)`**。正常情況它根本不會醒來（原本的 `os._exit(0)` 會先執行到）。

> **這個看門狗差點默默殺掉診斷腳本。** 診斷會呼叫 `on_app_close()` 驗證關閉
> 流程，但它自己還要跑完其餘案例 —— 看門狗 8 秒後把整個診斷行程結束掉，
> 結果是**輸出整個空白、結束碼卻是 0**（看起來像「沒有測試」而不是「失敗」）。
> 加了 `CLOSE_FORCE_EXIT` 開關讓診斷關掉它。記為 P-74。

### 二、櫃買「版面驗證失敗」洗版

`verify_tpex_layout()` 把「**沒有資料**」和「**版面壞了**」都回傳 `False`。
回溯較早的日期時櫃買端經常就是沒那天的資料（端點涵蓋範圍有限、或當日無交易），
於是每一個沒資料的日子都印一次紅字警告 —— 使用者看到滿螢幕的「版面驗證失敗」，
反而會**忽略真正的版面問題**。

改成三種狀態 `LAYOUT_OK` / `NO_DATA` / `BAD_LAYOUT`。沒資料靜靜跳過，最後
彙總成一行「其中 N 個交易日櫃買端沒有資料（多為較早的日期，屬正常）」。

刻意用字串而不是 bool：兩者的處理方式完全不同，用 bool 只能表達兩種狀態，
呼叫端就**被迫**把它們當成同一件事。

### 三、期貨未平倉金額 千元 → 億

沿用 ADR-118 的 `unit_format`。口數維持原樣（那本來就是對的單位）。

### 一、點上方指數也能帶進策略編輯器

「看A做B」的 A 常常就是指數，但原本只有**自選股**能帶入 —— 那段程式碼直接
寫在點自選股的處理裡，上方指數走的是 `load_index_chart()`，完全沒經過它。

抽成共用的 `_feed_symbol_to_editor()`，兩個入口行為一致，日後多一個入口也
只要呼叫這一個函式。

### 已驗證（headless）

- `tests/test_core.py` **555 全過**、`diag_repro_issues.py` **45 案例全過**
- 新診斷案例涵蓋四項，其中「批次寫檔」用**原始碼斷言**守住（逐日
  `upsert` 的舊寫法不可再出現）—— 真正的症狀要跑滿 10 年下載才看得到，
  離線環境重現不了，只能守住「不會退回舊寫法」

### 需使用者實機驗證

1. **最重要**：再跑一次長時間籌碼下載（3 年以上），下載期間**點 X 應該關得掉**。
2. 下載期間 UI 是否還會卡頓（批次寫檔應該明顯改善，但不會完全沒有）。
3. 櫃買沒資料的日子不再洗紅字，改成最後一行彙總。
4. 期貨法人的「未平倉淨額(億)」數量級是否合理。
5. 策略編輯器開著時，點上方的加權/櫃買指數，代碼與種類應自動帶入。
