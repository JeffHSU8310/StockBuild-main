
### P-60　tkinter 物件的 `__del__` 會在「觸發 GC 的那條執行緒」執行 —— 背景執行緒回收 tk 物件會讓 Tcl 直接 abort 行程
- **症狀**：參數最佳化跑一陣子後程式整個跳掉，主控台留下
  ```
  File "...\tkinter\__init__.py", line 416, in __del__
  RuntimeError: main thread is not in main loop
  Tcl_AsyncDelete: async handler deleted by the wrong thread
  ```
- **根因**：tk 物件（Variable/Widget）幾乎必然參與參照循環（widget → command
  閉包 → Variable → widget），不會被 refcount 立即釋放，而是交給**循環 GC**。
  循環 GC 會在**任何一條執行緒**配置記憶體超過門檻時觸發；最佳化 worker 每組
  參數都 deepcopy + 產生 DataFrame，是全程式最會配置記憶體的地方，GC 幾乎
  必然在那裡被觸發。它一旦回收到已關閉對話框留下的 `tk.Variable`，
  `__del__` 就在非主執行緒呼叫 Tcl，Tcl 的 C 執行期直接 abort 整個行程。
- **為什麼 try/except 沒用**：`__del__` 是 Python 直譯器自己呼叫的，
  應用層攔不到。ADR-056 曾把 `safe_after` 的 except 擴大到 `RuntimeError`，
  完全沒有效果 —— **崩潰不在 `safe_after` 裡**。症狀相似不代表是同一個 bug，
  一定要看 traceback 指的是哪個函式。
- **正確做法**：`gc.disable()` 關閉自動循環 GC，改由主執行緒定期
  （本專案 20 秒，`_gc_tick`）主動 `gc.collect()`，讓所有 tk 循環垃圾的
  `__del__` 都在主執行緒執行。代價是兩次回收之間循環垃圾會暫時累積
  （非循環物件仍靠 refcount 立即釋放）。**記憶體吃緊時調短間隔，不要改回
  自動 GC**。任何「會長時間大量配置記憶體的背景執行緒」都受此規則保護。
- **出處**：ADR-057（ADR-056 的誤判更正）。

### P-61　診斷 mock 的「重複方法定義」與「no-op 實作」會讓測試靜默失效
- **症狀**：`diag_mock_tkinter.py` 的 `_Treeview` 同時存在兩個 `selection`
  定義，後定義覆蓋前者，導致 `selection_set()` 寫進去的值永遠讀不回來；
  相關程式碼在診斷環境靜默走進 `except` 分支，測試「通過」但其實什麼都沒驗到。
  另外 `destroy()` 是 no-op、`winfo_exists()` 永遠回 `True`，等於完全測不到
  「元件被銷毀後不可再碰」這整類 bug —— 而那正是本專案最常見的崩潰來源。
- **根因**：mock 是為了讓程式碼「跑得起來」而寫的，容易為了省事把方法寫成
  `pass`；久了之後沒人記得哪些是真的模擬、哪些只是佔位。加上 Python 允許
  類別內重複定義方法而不報錯，重複定義可以潛伏很久。
- **正確做法**：
  1. 收尾檢查加一步「重複定義掃描」（AST 掃 class 內同名 method 與模組層級
     同名函式），mock 檔案也要納入掃描範圍，不能因為它是「測試工具」就豁免。
  2. 當某個診斷案例需要用到 mock 的某個行為時，**先寫一個直接驗證 mock
     本身行為的小測試**（例如 `selection_set` 後 `selection()` 讀得回來、
     `destroy()` 後 `winfo_exists()` 為 False），確認 mock 真的模擬了它，
     再去寫依賴這個行為的案例。否則「案例通過」可能只是因為 mock 什麼都沒做。
  3. mock 若要修正語意，必須同時確認**既有案例不被破壞**（本次 `selection`
     的合併就刻意保留「沒設定過就退回 focus」的舊行為）。
- **出處**：ADR-057。

### P-62　把「使用者的需求句」讀成「bug 回報」，會做出與需求相反的修改
- **症狀**：使用者說「損益不會有小數點，小數點後面的數字**無條件刪除**」，
  ADR-056 讀成 bug 回報（以為小數被錯誤截斷），反而把顯示從 `.0f` 改成
  `.2f`，讓問題更嚴重；下一輪使用者只好再講一次「只要不是 %，小數點後面的
  數字不要」。
- **根因**：中文的「A 不會有 B」既可以是現況陳述（bug 回報），也可以是
  規格描述（需求）。「無條件刪除/無條件捨去」是明確的**動作指令**，出現這類
  動詞時應優先解讀為需求，而不是症狀。
- **正確做法**：
  1. 需求句中出現動作指令（無條件捨去、四捨五入、改成、要、不要）時，
     優先當成需求；若仍有歧義，**先問一句**再動工，不要猜。
  2. 顯示格式類的改動，交付時附上「改動前後的實際字串範例」
     （例如 `-53438.54 → -53,438`），讓使用者一眼就能確認方向對不對。
  3. 「無條件捨去」不等於 `f"{v:,.0f}"` —— 後者是四捨五入
     （-53438.54 → -53,439），會憑空多算 0.46 元。要截斷請用 `int()`。
- **出處**：ADR-056 的誤判、ADR-057 更正。

### P-63　驗算/檢查類程式碼最容易犯「單位不一致」與「空集合退化」兩種錯
- **症狀**：新寫的回測報告驗算 `audit_result()` 自己被抓出兩個 bug：
  (1) 拿「損益（金額，已乘口數與契約乘數）」跟「進出場價差（點數）」比大小，
  期貨契約乘數 200 讓金額比點數大兩個數量級，對真實回測必然誤報；
  (2) 用 `min(所有損益)` 當「最大單筆虧損」，策略全勝時那是**最小獲利**
  （正數），導致完全正確的報告被判定不一致。
- **根因**：檢查類程式碼的錯誤不會讓功能壞掉，只會產生**假警報**或
  **漏報**，比一般 bug 更難察覺 —— 使用者反而會開始不信任正確的報告。
- **正確做法**：
  1. 比較任何兩個數值前，先問「它們單位一樣嗎」。金額 vs 點數、
     百分比 vs 比率、口數 vs 股數都是本專案踩過的地雷。
  2. 對 `min()`/`max()`/除法一律先想「集合是空的怎麼辦」「全正/全負怎麼辦」，
     並為這些邊界各寫一個測試（本次補了全勝、全敗兩個案例）。
  3. 檢查條件寧可**只抓單位無關的必然矛盾**（例如「不利價格方向不可能獲利」），
     也不要為了多抓一點而引入需要猜測單位的啟發式規則 —— 假警報的代價
     是使用者失去對工具的信任。
  4. 驗算工具必須誠實標示能力邊界（能保證什麼、不能保證什麼），
     避免使用者把「驗算通過」誤解為「策略在真實市場會這樣成交」。
- **出處**：ADR-057。
