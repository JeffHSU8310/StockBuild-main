# ADR-126:主圖活K棒 blit 失效時要有退路 + 報價上屏加速

## 狀態

已實作,**尚未實機驗證**(分支 `claude/live-chart-refresh`)。

## 背景:使用者實測

> 「主圖的K線不會即時更新,以前會,現在又不會了。然後,即時報價更新的速度
> 需要再加快」

問出來的兩個關鍵事實(P-89:先問事實再讀程式):

| 問題 | 回答 |
|---|---|
| 是完全不動,還是只有夜盤/縮放後? | **完全不動,白天盤中也一樣** |
| 手動按週期按鈕重載會跳到最新嗎? | **會 —— 但之後又不自己更新** |

「重載會、之後不會」把範圍縮得很小:**下載那一層是好的,壞的是自動更新那
一層**。而自動更新有兩套:活K棒 blit(400ms,tick 驅動)與 K 棒邊界重抓
(每 5 分鐘)。使用者說「完全不動」,指的是前者。

## 根因:blit 走不通時**安靜地什麼都不做**

`_live_bar_painter` 把所有前置條件寫成一個大 if:

```python
if (lb and lb.get('dirty') and arts and self.current_df is not None
        and len(self.current_df) > 0 and getattr(self, '_hover_bg', None) is not None
        and not self._fetch_in_progress and not self._login_in_progress):
```

任何一條不成立 → 什麼都不做,**而且日誌一個字都沒有**。

其中 `_hover_bg`(blit 底圖快取)特別脆弱,至少有四條路會把它變成 None:

1. `_on_canvas_draw` 在 `is_panning` / `_zoom_active` 期間**刻意不快取**
2. `_blit_hover` 發現視野變了(平移/縮放後)會作廢它
3. `draw_chart` 建新 canvas 時先設 None,等 `draw_event` 才補回來
4. `copy_from_bbox` 本身失敗

**一旦變成 None 而後續又沒有一次完整重繪把它補回來,活K棒就永遠停住。**
這正好符合使用者的描述:手動重載(= 完整 `draw_chart`)會恢復,但很快又停。

> **誠實說明**:我沒有辦法從靜態閱讀確定使用者那次是上面哪一條觸發的 ——
> 這四條都會產生一模一樣的症狀,而且都不留痕跡。所以這次的修法是**把整類
> 問題修掉**(不管哪一條,畫面都一定會動),並且**補上診斷**,萬一還有殘留
> 問題,日誌會直接說出是哪一條,不必再猜。

## 決定

### A. blit 走不通就退回節流過的完整重繪

```python
def _live_bar_blocked_reason(self):   # 回傳卡在哪一條 ('no_blit_bg' / ...) 或 None
def _live_bar_fallback_repaint(self, reason):   # 節流過的 draw_idle + 第一次記一行原因
```

- 完整重繪比 blit 貴很多,所以**最快每秒一次**(`LIVE_BAR_FALLBACK_SEC = 1.0`)。
  目標是「一定會動」,不是「動得很順」。
- 第一次退回時記一行日誌並附**原因代碼**,以後排查有據可查。
- `_blit_hover()` **當場**失敗(底圖剛被作廢)也走同一條退路 ——
  原本連它的回傳值都沒看。

### B. 上屏加速

| 項目 | 原本 | 現在 | 有 API 成本嗎 |
|---|---|---|---|
| 活K棒上屏 `LIVE_BAR_PAINT_MS` | 400ms | **200ms** | 否(本地 blit)|
| 自選股串流上屏 `WL_STREAM_UI_SEC` | 0.25s | **0.1s** | 否(tick 是券商推播,已在記憶體)|
| 股票批次快照 `WL_SNAPSHOT_SEC` | 2.5s | **2.5s(不動)** | **是**(鐵則 5)|
| 五檔/成交上屏 | 0.1s | 0.1s(本來就夠快)| 否 |

**關鍵設計**:原本「上屏節奏」與「快照 API 節奏」綁在同一個 `i % 10` 上 ——
改快上屏就會**連帶把 API 打快**,直接違反鐵則 5。這次把「幾秒打一次快照」
寫成秒數常數,輪數由除法算出來:

```python
every = max(1, int(round(self.WL_SNAPSHOT_SEC / self.WL_STREAM_UI_SEC)))
```

以後只調上屏速度,API 節流一秒都不會變。

## 順手抓到並修掉:我自己在 ADR-124 埋進 main 的時鐘相依

跑這次的突變測試時,**每一輪**都看到一個跟本次改動無關的紅:

```
ADR-123: ... FAIL  一般期貨策略仍應被即時停損平倉 (守門不可過度攔截)
```

原因:**ADR-124 給 `_qt_check_realtime_futures_stops` 補上交易時段閘門之後,
ADR-123 那個案例就悄悄變成看真實時鐘了。**

| 當下時刻 | `is_market_open('期貨')` | ADR-123 該案例 |
|---|---|---|
| 08:45~13:45(日盤)| True | 綠 |
| **13:45~15:00(空檔)** | **False** | **紅** |
| 15:00~05:00(夜盤)| True | 綠 |

我當時**連跑 5 次全綠就把它併進 main**,正是因為那 5 次都落在日盤內。
這是 P-94 才剛寫過的教訓的現場重演:「跑很多次都沒紅」只是沒有反證,
不是證明。**這個紅現在已經在 main 上了**,本次一併修掉。

**修法不是只改那一個案例**,而是讓 `run_case` 統一把時鐘相依的表面凍結住
(預設「市場開著」「不在開盤暖機窗口內」),需要休市/暖機的案例自己在案例內
再 patch。這樣**任何新案例都不會不小心又依賴真實時鐘**。

順帶把 ADR-123 診斷裡那兩行「直接斷言 `just_opened(15:00:05)`」拿掉 ——
那是**純函式的行為**,`tests/test_core.py` 的 `TestMarketSession` 已經完整
覆蓋(含 15:00 夜盤那條)。純函式歸單元測試、GUI 接線歸診斷,各司其職。

## 突變測試抓到一條空殼斷言

「加快上屏連帶把快照打快」這個突變**第一次是綠的**。因為我的斷言是:

```python
every = max(1, int(round(app.WL_SNAPSHOT_SEC / app.WL_STREAM_UI_SEC)))
assert abs(every * app.WL_STREAM_UI_SEC - 2.5) < 1e-6
```

—— 這只是在診斷裡**把同一條除法再算一次**,測到的是我自己的公式,不是程式。
把 worker 裡的 `every` 改成寫死的 10,診斷照樣綠。

改成**真的把 worker 跑起來數**(patch `time.sleep` 讓它跑 200 輪後跳出,
數 `_wl_fetch_quotes_once` 被呼叫幾次),同一個突變就紅了,而且直接報出
違規數字:`實際 1.00s (20 次 / 20.0s)`。

## 驗證

- `python tests/test_core.py` → **609 個全過**
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **52 案例全過,0 FAIL**
  —— 特地在 **14:03(13:45~15:00 空檔,正是會露餡的時段)** 跑,確認時鐘
  相依已經解掉
- `python diag_crossref.py` → 乾淨

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| 拿掉 blit 失效的退路 | 診斷紅 |
| 退路不節流(每 200ms 全圖重繪)| 診斷紅 |
| 上屏速度改回 400ms | 診斷紅 |
| 加快上屏連帶把快照打快 | 診斷紅(**第一次是綠的**,見上)|
| `blocked_reason` 認不出 `no_blit_bg` | 診斷紅 |
| `run_case` 不再凍結時鐘表面 | 診斷紅(ADR-123 那條會回來)|

## 需使用者實機驗證

1. **主圖 5 分K,最後一根 K 棒要跟著跳動**(不用碰滑鼠)。
2. 若日誌出現「**【主圖】活K棒改用完整重繪更新 (原因: xxx)**」——
   畫面仍會動,但**請把那個原因代碼貼回來**,那就是這次沒能靜態定位的那一條。
3. **縮放/平移之後**,K 棒要繼續跳(這是原本最可能卡死的情境)。
4. 報價與自選股跳動應該比之前明顯順一些。
5. **不要**出現「打太快」的徵兆:日誌沒有新的 API 流量/管制警告 ——
   這次刻意沒有動任何 API 節流。
