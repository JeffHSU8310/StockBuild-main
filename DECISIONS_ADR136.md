# ADR-136:停損停利即時觸發改成可勾選 + 自訂策略的風控真的進得了回測 + 合併規則變更

## 狀態

已實作,**尚未實機驗證**;依本次變更後的規則**已合併進 main**。

## 需求(使用者原話)

> 1. 四個「看不見卻一直在生效」的設定,在自訂策略中也要有。
> 2. 停損停利即時觸發,**要有這個選項可以勾**。只要勾,就是不需要等收盤價,
>    就可以即時。**不分商品**。
> 3. 我的目的就是,自訂策略,程式不要寫的太複雜,只要專注在買賣的條件行為就好。
>    其他的就交給系統,在系統勾選就可以做到我要的。
>    **並且可以有效執行回測(這一點非常重要)。**
> 4. 請合併 main,並且修改規則成:「**每一次都自行合併回 main**」。

---

## 第 1 點:已經在 ADR-135 做好了

每日進場上限 / 冷卻秒數 / 期貨時段 / 交易時段閘門四項,ADR-135 已經加進自訂
策略編輯器(`_risk2` 區塊)。這一輪確認無誤,不重做。

---

## 第 2 點:即時觸發改成可勾選

ADR-135 是「一律即時、不分商品」。使用者要的是**可勾選**。

```python
def intrabar_stop_enabled(strategy):
    if 'intrabar_stop' in strategy:      # 有明確設定 → 照設定
        return bool(strategy['intrabar_stop'])
    return trade_type_of(strategy) == '期貨'    # 舊策略 → 沿用它原本的行為
```

### 相容性的關鍵決定

**沒有這個欄位的舊策略,一律沿用它原本的行為**,不可以因為新增欄位就悄悄改變:

| 舊策略 | 原本的行為 | 沒有欄位時的判定 |
|---|---|---|
| 期貨 | ADR-087 起就是即時 | `True` |
| 股票 / 零股 | K棒收盤才判定 | `False` |

新策略 (`new_strategy()`) 明確帶 `intrabar_stop: True` —— 使用者要這個功能。

兩個編輯器(內建、自訂)都加了勾選框,初始值用同一個 `intrabar_stop_enabled()`,
所以打開時顯示的**就是這檔策略目前的實際行為**。

---

## 第 3 點:自訂策略的停損停利在回測裡「完全沒有被套用」

這是這一輪最重要的發現。

`core/backtest.py` 的主迴圈:

```python
if is_custom:
    intents = custom_fn.decision_to_intents(...)   # 只把 BUY/SELL/CLOSE 轉成 intent
else:
    intents = strategy_engine.evaluate_strategy(...)   # ← 停損停利住在這裡
```

**停損/停利的判斷住在 `evaluate_strategy()` 裡,而自訂策略那條路根本不經過它。**
所以使用者在自訂策略編輯器設了停損,**回測完全當作沒設** ——
回測數字與實際行為對不上,而那正是使用者說「這一點非常重要」的地方。

### 修法

1. 把 `evaluate_strategy()` 裡那段 K 棒收盤停損抽成
   **`strategy_engine.check_bar_stop(strategy, runtime, close)`**(行為逐字不變)。
2. 回測的自訂策略分支呼叫同一個函式。
   **程式碼自己已經要出場時就不重複** —— 尊重策略作者的決定。
3. 新增 **`stop_levels()`** 把停損停利換算成**價位**,以及
   **`bar_touch_exit()`** 用當根 K 棒的**高低點**判斷有沒有觸價。
   勾了「即時觸發」的策略,回測就用這個模型 —— 否則實盤盤中觸價就走、
   回測只看收盤價,兩邊永遠對不上。

### 回測觸價模型的兩個保守選擇

| 情況 | 取法 | 理由 |
|---|---|---|
| **跳空**穿過停損價 | 成交在**開盤價**(比停損價更差)| 不做「一定成交在停損價」的樂觀假設 |
| 同一根**同時**碰到停損與停利 | 算**停損** | 分不出誰先到,取對使用者不利的那一邊才不會高估績效 |

兩者都有單元測試,而且把它們改成樂觀版本的突變都會紅。

### 「一律用開盤價成交」的例外

ADR-064 規定回測所有訊號一律成交在隔根開盤價。觸價出場**必須例外** ——
否則即時停損在回測裡永遠成交在開盤價,模擬就失去意義。

但**看A做B 時不適用**:觸價是在 A 的 K 棒上算出來的,拿 A 的價格當 B 的成交價
是錯的,那種情況仍退回 B 的開盤價(保守,且與既有模型一致)。

### 沒有動的東西:回測的「即時風控放寬」

`_relax_realtime_guards()` 會在回測時把 `max_trades_per_day` 放到無限大、
`cooldown_sec` 歸零。這是既有的設計(所有策略都一樣,不只自訂策略),
**這次沒有動**。

> 但要講明白:如果你在編輯器設了「每日進場上限 3」,**回測不會套用它**,
> 所以回測的交易次數會比實盤多。要不要讓回測也照這個上限跑,是另一個決定
> (會改變所有既有策略的回測數字),等你說。

---

## 第 4 點:合併規則變更

`CLAUDE.md` 的「**不可以自行合併回 main**,一定要等使用者實機驗證 + 明確要求」
改成「**每一次都自行合併回 main**」,並寫明配套:

- 合併**前**:單元測試 + `test_brokers` + 診斷 + `crossref` + `py_compile` 全過,
  新功能要有突變測試證明斷言不是空殼。
- 合併**後**:在 `main` 上重跑一次完整驗證(曾在這一步抓到偶發紅)。
- 每一筆仍要在 `DECISIONS_ADR113.md` 追記,**照實寫明哪些還沒經過實機驗證**。

> **「自動合併」改變的是合併時機,不是「可以假裝驗過了」。**
> 交付說明仍然要列出「請使用者實機驗證哪些操作」。

---

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| 回測不套用自訂策略的停損(等於沒修第 3 點)| 單元紅 |
| 舊策略的相容預設寫反(期貨↔其他)| 單元紅 |
| 跳空時樂觀假設成交在停損價 | 單元紅 |
| 同根同時觸發時算停利(高估績效)| 單元紅 |
| 即時觸發的開關不生效(永遠即時)| 單元紅 |

## 兩個測試自己寫錯的紀錄

1. **`new_strategy()` 預設 `stop_loss_pct=2.0`**,我的測試 fixture 只設了
   `stop_loss_abs` 就斷言價位 —— 結果每個案例都同時有兩個停損在跑,測到的
   不是自己設的那一個。程式是對的(取離進場價最近的那個),fixture 錯了。
2. 假的自訂策略寫成「回傳字串 `'BUY'`、讀 `ctx.state`」—— 正確是
   **回傳 `ctx.buy()`、讀 `ctx.position`**(`ctx.state` 是使用者自己的暫存 dict)。
   一筆單都沒開,是測試的假策略寫錯,不是回測錯。

## 驗證

- `python tests/test_core.py` → **734 個全過**(原 714,新增 20)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **61 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

## 需使用者實機驗證

1. **兩個編輯器都有「停損停利即時觸發」勾選框**;打開既有策略時,
   勾選狀態應該反映它**目前的實際行為**(期貨已勾、股票未勾)。
2. 勾起來 → 盤中觸價就出場;取消 → 等該週期 K 棒收盤才判定。**不分商品。**
3. **自訂策略設停損後跑回測** → 交易明細應該出現「停損出場」。
   (修正前這裡是 0 筆平倉 —— 這是本輪最重要的一條。)
4. 同一檔自訂策略,**勾與不勾「即時觸發」跑出來的回測結果應該不同**
   (勾了會用當根高低點判斷觸價,停損次數通常更多)。
5. 期貨既有策略的回測數字會變 —— 因為回測現在會模擬「盤中觸價就走」,
   那本來就是實盤的行為。**這是修正,不是迴歸。**

## 不在這次範圍

- 回測仍會放寬 `max_trades_per_day` / `cooldown_sec`(見上,等你決定)。
- 自訂策略仍沒有「方向」欄位(ADR-045 的設計,多空由 `on_bar` 決定)。
