# ADR-137:手機雙向查詢 —— 指定帳戶的損益/持倉、單一策略的完整狀態

## 狀態

已實作,**尚未實機驗證**;依 ADR-136 變更後的規則已合併進 main。

## 使用者的問題

> 在我的手機可以回傳給系統跟他說我要看某資訊(比如:我要看目前 XX 帳號的損益)
> ……等等,系統就會傳相關資訊到我的手機,做雙向的資訊流通。有辦法做到這個功能嗎?

## 直接回答:可以,而且**大半已經在系統裡了**

ADR-108 就已經把單向通知擴充成雙向:

- `_tg_poll_worker` 用 `getUpdates` **輪詢** Telegram(不用 webhook,
  因為 webhook 需要公網 IP 與憑證)。
- 收到訊息 → `parse_command` → **白名單檢查**(只有設定檔那一個 `chat_id`)
  → 執行 → 回覆。
- 已有的查詢指令:`/status`(登入/總開關/啟用策略數)、`/list`(策略清單)、
  `/positions`(持倉)、`/pnl`(今日損益)、`/help`。

所以「手機問、系統答」這條路是通的。**缺的是「指定對象」** ——
使用者的例子是「XX **帳號**」,而 `/pnl` 只會把所有帳戶一次列出來,
帳戶多的時候要自己在訊息裡找。

---

## 這次補的三件事

| 指令 | 用途 |
|---|---|
| `/pnl [帳號]` | 不帶=全部(原行為);帶了就**只報那一個帳戶** |
| `/positions [帳號]` | 同上 |
| `/accounts` | 列出所有模擬帳戶**與編號**,讓使用者知道上面兩個可以打什麼 |
| `/strategy <策略名或編號>` | 單一策略的**完整設定 + 目前狀態** |

### 帳戶怎麼指定:與策略用同一套規則

`match_account()` 的比對規則刻意與既有的 `match_strategy()` 一致
(編號 / 完全符合 / 唯一部分符合),**使用者只要學一套**。

**多個符合時要求講清楚**,不自己挑一個 —— 遠端沒有畫面可以確認,
回錯帳戶的損益會直接誤導決策。有測試守著,把它改成「隨便挑第一個」會紅。

編號的順序取自 `sorted(keys)`,**穩定** —— 每次問同一個編號必須是同一個帳戶,
否則遠端指令會指錯對象。也有測試。

### `/strategy` 為什麼要同時給設定與狀態

設定決定它**會**做什麼、狀態決定它**現在**在做什麼。只給其中一半,
使用者一定要再問一次。所以一次給齊:商品/週期/數量/看A做B/委託方式/
停損停利/即時觸發/每日上限/冷卻/目前部位/今日進場次數。

---

## 刻意沒有做的兩件事(重要)

### 1. 沒有加「查即時報價」

看起來很自然的下一步是 `/quote 2330`。**沒有做**,因為它會違反兩個既有決定:

- `positions_text()` 的註解寫得很清楚:「遠端查詢**不該觸發券商流量**
  (鐵則 5 的精神)」。
- 鐵則 5 要求 `snapshots()` 節流;一個可以從手機無限次觸發的指令
  正好繞過那層設計。

要做的話得先決定節流策略,那是另一筆 ADR。**沒問過就先不做。**

### 2. 仍然沒有任何下單指令

ADR-108 的安全設計原文:「**完全不提供下單/改單指令**。要買賣一律回到有畫面
的主程式,走鐵則 14 的確認視窗。遠端只能控制『策略要不要跑』,不能直接動錢。」

這次擴充的全部是 `CMD_READONLY`。有一條**反向對照測試**斷言
`/buy` `/sell` `/order` `/close` 都不存在於指令表 —— 免得日後有人「順手」加上去。

---

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| `/pnl` 的帳號參數被忽略(等於沒修)| 單元紅 |
| 帳戶多個符合時隨便挑第一個 | 單元紅 |
| 把 `/accounts` 歸成 `CMD_RISKY` | 單元紅(查詢不可以變成會動錢的指令)|

## 兩個測試自己寫錯的紀錄

1. `paper_account.new_account(initial_cash, name, account_id)` —— 我照
   `new_account(aid, name)` 呼叫,把帳號 id 傳成了初始現金,整批 ERROR。
2. 改好之後又因為**手拼的持倉 dict 少了 `market`/`share_per_unit`**,
   `equity()` 直接 KeyError。

> 教訓:**測試資料的形狀要由產生它的正式程式碼決定,不要自己捏。**
> 改成用 `new_account()` + `apply_fill()` 產生,一次就對。

## 驗證

- `python tests/test_core.py` → **747 個全過**(原 734,新增 13)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **61 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

## 需使用者實機驗證

前提:Telegram 設定裡的「**遠端控制**」要打勾(`remote_control`),
而且 bot_token / chat_id 都填好。

1. 手機傳 `/help` → 應看到新的 `/accounts` 與 `/strategy`,
   `/pnl` `/positions` 的說明也變成「[帳號]」。
2. 傳 `/accounts` → 列出模擬帳戶與編號。
3. 傳 `/pnl 1` 或 `/pnl 主帳戶` → **只回那一個帳戶**的損益。
4. 傳 `/pnl` (不帶參數) → 仍然列出全部(原行為沒被弄壞)。
5. 傳 `/strategy 1` → 該策略的完整設定與目前部位。
6. 打一個不存在的帳號 → 回「找不到帳戶…(先用 /accounts 查看)」。
7. **用另一支手機/另一個 Telegram 帳號傳指令 → 應完全沒有回應**
   (白名單;刻意不回覆,不讓對方確認 Bot 存在)。

## 不在這次範圍

- 即時報價查詢(見上,需要先決定節流策略)。
- 實單庫存查詢(要打券商 API,同上)。
- 自然語言(「我要看損益」)—— 目前仍是 `/` 指令。要做自然語言得先決定
  「解析錯誤時怎麼辦」,而這是一個會動到交易系統的介面,寧可精確。
