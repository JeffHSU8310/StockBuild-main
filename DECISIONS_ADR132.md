# ADR-132:盤勢判斷每天收盤後推播到手機,並加入 60分K

## 狀態

已實作,**尚未實機驗證**(分支 `claude/regime-daily-notify`)。

## 背景:使用者的要求

> 「主圖盤勢判斷,如果有出現相關盤勢,要用傳訊息到我的手機通知我,跟我說是
> 什麼型態或是支撐壓力。**每天一次,收盤後通知即可。**
> 另外,也額外增加對 **60分K** 的盤勢判斷,一樣是對加權指數。同上,也一併要
> 通知我,而且要**註明是 60分K 出現**。」

## 先查清楚:為什麼原本收不到通知

ADR-120 的 `_regime_check()` 偵測到型態時只做一件事:

```python
self.log_message(f"【盤勢判斷】{name} {bar_date} {tf}偵測到:...")
```

而 Telegram 推播的閘門是:

```python
if (getattr(self, '_qt_running', False)
        and telegram_notify.is_quant_message(msg)      # 前綴要是「【自動交易」
        and telegram_notify.config_ready(...)):
```

**兩個條件都不成立**:前綴是「【盤勢判斷】」不是「【自動交易」,而且還要求
自動交易總開關開著。所以那些訊號從來只進系統日誌,一次也沒進過手機。

---

## 決定

### A. 獨立的每日推播通道,**不綁自動交易總開關**

盤勢判斷是**看盤輔助**,跟自動交易是兩件事 —— 使用者沒開自動交易時一樣該收到。
所以:

- 自己一條 daemon thread `regime_daily_notify_worker`(每 60 秒檢查一次
  「時間到了沒」),**不掛在量化 runner 上**(那條由總開關控制)。
- 送出時**直接呼叫 `_send_telegram_async()`**,不經過 `log_message` 的推播閘門。
- 診斷有一條專門守這件事:案例把 `_qt_running` 設成 **False**,推播仍然必須送出。
  (把它綁回總開關的突變會紅。)

在自己的執行緒裡 `sleep` 是安全的 —— P-90 講的是**量化 runner 那條單執行緒**
不可以 sleep(會拖住即時停損),這裡沒有那個問題。

### B. 「過了時刻就送」,不是「剛好那一分鐘才送」

```python
def should_notify_now(settings, dt, last_notify_date):
    ...
    if str(last_notify_date or '') == today: return False    # 每日去重
    return cur_min >= notify_minute(s)                       # 過了就送
```

程式可能 14:00 沒開機、或正忙著別的事而錯過那一分鐘。**錯過就整天不發,
對一天只發一次的通知是最糟的失敗模式** —— 使用者不會知道自己漏掉了。
配合 `last_notify_date` 的每日去重,「過了就送」不會變成重複轟炸。

預設時刻 **14:00**:台股 13:30 收盤,留半小時讓當天的日K 落定。可在設定裡改。

### C. 收盤後要用**今天**那根K棒 → `drop_last=False`

`_qt_fetch_closed_bars()` 一律丟掉最後一根(盤中它一定未完成)。但這條路徑在
收盤後才跑,**當天的日K 已經定案,丟掉它等於永遠看不到今天的型態** ——
對「每天收盤後告訴我今天出現什麼」這個需求是致命的。

所以加一個 `drop_last=True` 的預設參數,只有推播路徑傳 `False`。
**下單路徑一律維持 True**,絕不可以拿未完成K棒去產生委託。
診斷斷言 `('日K', False)` 與 `('60分K', False)` 都出現在抓取紀錄裡。

### D. 60分K 納入型態判斷(允許清單,不是全面開放)

ADR-120 當時寫死只認日K,理由是「型態的定義(頸線/缺口/區間)以一天一根為
前提,套到 5 分K 上沒有意義」。這個理由對**分鐘級短週期仍然成立**,
但 60分K 不一樣:一天只有 4~5 根,一個型態要好幾天才走完,語意跟日K 同量級。

```python
PATTERN_TIMEFRAMES = ('日K', '60分K')     # 順序即通知訊息裡的排列順序
```

**刻意不是「任何週期都能跑」** —— 5分K/15分K 會產生大量沒有意義的型態訊號,
而 ADR-132 之後這些訊號會**推播到使用者手機上**,噪音的代價比以前高得多。
單元測試與診斷都各有一條「5分K/15分K/30分K/周K 一律不評估」的反向對照。

順帶:主圖切到 60分K 時,面板也會評估了(同一個 `should_evaluate`)。

### E. 訊息格式:每一段都標明週期

```
【自動交易-盤勢】加權指數 2026-03-10 收盤後盤勢判斷
[日K] 盤勢/型態:多頭趨勢[偏多]；接近區間上緣[中性]
[日K] 支撐壓力:現價 20450.00|壓力 20600.00 / 20800.00|支撐 20200.00|POC 20380.00
[60分K] 盤勢/型態:上升三角形[偏多]
[60分K] 支撐壓力:...
```

使用者明確要求「要註明是 60分K 出現」→ `[週期]` 前綴放在每一行開頭。
支撐壓力用 `volume_profile.summarize()`(既有函式,一行摘要)。

**沒有任何內容的那天不發**(`format_daily_report` 回 `None`)——
一天一次的通知一旦變成噪音就沒人看了(P-99 的同一個道理)。
但**只有支撐壓力、沒有型態時仍然要發** —— 使用者要的是「型態**或**支撐壓力」,
有一條反向對照測試守著。

### F. 沒登入時不可以吃掉當天的機會

抓不到指數資料就直接返回,**且不記 `last_notify_date`** —— 否則使用者晚一點
才登入,當天就再也不會補送了。診斷有正控:登入之後同一天必須補送。

反過來,**有跑完(不論有沒有內容)就一定要記**,否則沒訊號的那幾天這個 pass
會每 60 秒重跑一次(含兩次資料抓取)直到半夜,白打 API。

---

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| `PATTERN_TIMEFRAMES` 拿掉 60分K | 單元紅 ×2;診斷 ADR-120 紅、ADR-132 紅:`訊息要標明 60分K` |
| 拿掉每日去重 | 診斷紅:`一天只能一則,實際又送了 1 則` |
| `drop_last=True`(丟掉今天那根)| 診斷紅:`兩個週期都要抓、且 drop_last=False` |
| **推播綁回「自動交易總開關」**(重現原本收不到的成因)| 診斷紅:`收盤後應該送出剛好一則推播,實際 0 則` |
| 沒登入也記成「今天已處理」 | 診斷紅:`否則登入後整天都不會補送` |

## 驗證

- `python tests/test_core.py` → **668 個全過**(原 656,新增 12、改寫 1)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **58 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

時間一律由呼叫端傳入(`_regime_daily_notify_pass(now_dt=...)`),
診斷完全不依賴真實時鐘(P-94/P-97)。

## 需使用者實機驗證

1. **先確認 Telegram 已設定好**(bot_token / chat_id)—— 沒設定不會送,
   而且不會有錯誤訊息(這是刻意的,避免每天洗一則失敗訊息)。
2. 【盤勢判斷】⚙ 設定裡新增「**每日推播 (Telegram)**」區塊:開關、推播時刻
   (預設 14:00)、要納入的週期(日K / 60分K 各自可勾)。改完按「套用並儲存」。
3. **今天 14:00 之後**(或把時刻改成比現在早幾分鐘再存)→ 手機應收到一則,
   內容含 `[日K]` 與 `[60分K]` 兩段。
4. **同一天不會再收到第二則**;隔天會再收到。
5. **自動交易總開關關著時一樣要收得到** —— 這是本次刻意改的行為。
6. 主圖切到 **60分K** 時,系統日誌也會出現【盤勢判斷】的偵測結果(原本只有日K)。
7. 若當天完全沒有型態、也算不出支撐壓力 → **不會收到訊息**(這是預期行為)。

## 不在這次範圍

- 只對**加權指數**(使用者指定)。個股/櫃買不推播。
- 沒有做「盤中即時推播」—— 使用者明確說「每天一次,收盤後通知即可」。
- 週K/月K 不納入(型態語意與日K/60分K 不同量級,使用者也沒要求)。
