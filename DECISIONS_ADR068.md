
## ADR-068：切換標的太慢 (股票~15 秒/期貨~25 秒才能 hover) — 股票補上兩段式快速出圖、過期查詢立即中止背景下載、加上分段計時

### 背景

ADR-067 把報價退訂/訂閱移到背景執行緒後,使用者回報切換標的仍慢:「股票平均
要 15 秒、期貨平均要 25 秒,才可以切換成功、滑鼠可以點 K 棒數據」。這個量級
遠超過訂閱延遲 (訂閱頂多幾秒),追下去確認**真正的瓶頸是歷史 K 線下載**,
不是訂閱。

---

### 根因

1. **shioaji `kbars` 一律回「1 分 K」原始資料**再由我們重採樣。所以抓 5 分 K
   60 天 = 要傳 60 天的 1 分 K,資料量很大。

2. **分 K 大範圍要分段下載**:`is_min_tf and (_t - _f).days > 5` 會走
   `_download_kbars_chunked(chunk_days=10, subsplit_days=5)`,60 天 = 6 段,
   段與段間還有 `pace_sec=0.35` 秒節流 (ADR-048 防券商流量管制)。6 次 kbars
   來回 + 節流,合計十幾秒。

3. **股票完全沒有「兩段式快速出圖」**:`slow_asset = asset_type in
   ("future","index_tw")`,快速段只給期貨/指數。股票要等上面那 6 段**全部
   跑完**才第一次 `_publish` 出圖 → 這就是股票約 15 秒才換到圖、才能 hover
   的直接原因。

4. **切走的舊查詢不會停手,反而霸佔下載資源**:`_downloads_should_abort()`
   只看「登出/關程式/回測取消」,不看「使用者已經切到別檔」。使用者連點
   A→B 時,A 的完整下載 (6 段) 還在背景跑,每段都要搶 `_kbars_lock` 才能呼叫
   kbars,把 B 的搶先出圖卡在後面——連點不同標的時尤其明顯。

### 正確做法 (本 ADR)

**一、股票的「分 K」也走兩段式快速出圖**
`want_quick = slow_asset or (asset_type == "stock" and 分K)`。先用 `QUICK_DAYS`
的小範圍 (5 分 K = 5 天) **單次**下載搶先 `_publish(full_ui=True)`,K 線立刻
可看可 hover,完整 60 天歷史在同一背景執行緒接著補全,補完就地換圖 (視角不動,
沿用 ADR-049 的 `prev_len` 平移)。股票日 K 以上維持原樣不加快速段——日 K 會做
`_extend_with_yahoo` 延伸,避免快速段+完整段各打一次 yahoo。

**二、過期查詢立即讓路,不再空跑下載**
* `_download_kbars_chunked` 新增 `abort_cb` 參數;主圖呼叫時傳
  `lambda: seq != self._fetch_seq`。分段迴圈每段開始前、每次重試前都用
  `_should_abort()` (= 原本的 `_downloads_should_abort()` **或** `abort_cb()`)
  檢查,使用者一切走,舊商品剩下的段立刻停抓,把 `_kbars_lock` 與 API 配額
  讓給新商品。
* 完整下載開始前先擋一次:若已搶先出圖 (快取或快速段) 而 seq 已過期,直接
  `return`,連完整下載都不啟動。
* 下載結束後、寫快取前再擋一次:分段中止會回傳部分/空資料,這是「刻意讓路」
  不是失敗,安靜結束——不記誤導的「下載失敗/查無資料」訊息,也不拿殘缺資料
  去污染快取 (`_kbars_cache_get` 會誤判成已涵蓋而不再補抓)。

**三、加上分段計時,方便繼續定位**
台股分支進來記 `_t0`,在「搶先出圖」「完整歷史載入完成」兩個點把
`time.time() - _t0` 寫進系統日誌 (⚡ 開頭)。使用者切一次標的就能從日誌看到
「幾秒出圖、幾秒補完」,若還有殘留的慢,能精準指出是卡在哪一段。

### 預期效果

* 股票分 K:第一次出圖從「等 6 段跑完 (~15 秒)」變成「單次小範圍下載 (~1~3 秒)」
  就能看、能 hover;完整歷史背景補。
* 連點不同標的:舊商品下載立即中止,新商品搶先出圖不再被卡。
* 期貨本來就有快速段;若期貨仍偏慢,新的計時日誌會顯示是卡在快速段下載、
  完整段下載,還是 (日 K 的) 期交所延伸,再據此對症。

### 維持不變 / 沒有動的部分

* `SJ_DAYS` 的歷史深度沒有調降 (使用者要看更多分 K 歷史,見 dev_notes 2026-07-21);
  只是把「深歷史」挪到背景補,不擋第一次出圖。
* 分段下載的 `chunk_days=10 / subsplit_days=5`、`pace_sec`、退避重試 (ADR-048)
  一律不動,券商流量管制的防護維持原樣。
* `_publish` 的 seq 防護、`prev_len` 視角平移 (ADR-024/049) 沿用。
* 同商品換週期、快取新鮮秒開的路徑不受影響。

### 相關程式位置 (stock_app_pro.py)

* `_download_kbars_chunked`:新增 `abort_cb` 參數與 `_should_abort()`;兩處
  中止檢查改用 `_should_abort()`。
* `_fetch_data_worker_impl` 台股分支:
  * `_t0` 計時起點。
  * 快速段條件 `want_quick` 納入「股票分 K」;搶先出圖後記耗時。
  * 完整下載前 `already_shown && seq 過期 → return`;
    `_abort_if_superseded` 傳入兩處 `_download_kbars_chunked`。
  * 下載後、寫快取前補「seq 過期 → 安靜 return」。
  * 完整段發布時記總耗時。

### 需使用者實機驗證 (此環境 headless,無法實測 GUI/shioaji)

1. 登入後點一檔**沒看過**的股票 (5 分 K):看系統日誌
   「⚡ 已搶先出圖…耗時 X 秒」是否約 1~3 秒、且此時滑鼠已能 hover K 棒;
   稍後「⚡ 完整歷史載入完成…耗時 Y 秒」補上完整歷史。
2. 期貨同樣點一檔沒看過的:記下兩個耗時數字回報,好判斷期貨的慢是卡在哪段。
3. 快速連點多檔不同標的:是否每點一下新圖都很快出來 (舊商品下載會在日誌看到
   「已停止…已切換其他標的」)。
4. 切回剛看過的商品/換週期:應維持「秒開」(走快取)。
