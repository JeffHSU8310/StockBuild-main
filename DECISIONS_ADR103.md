
## ADR-103：全方位選股工具 (技術面 + 籌碼面 + 基本面)

### 背景

使用者要一個涵蓋上市上櫃、結合技術分析／籌碼／基本面的選股工具。

### 決策一：完全不動用 shioaji

全市場約 1900~2300 檔。若逐檔呼叫 `kbars` 取 K 線，就是 2000+ 次 API
呼叫，直接撞上鐵則 5 的配額上限（ADR-094 查證：snapshots/ticks/kbars 合計
10 秒 50 次）。

改用**「一次拿全市場」的官方免費端點**，實測全部可用：

| 資料 | 端點 | 實測 |
|---|---|---|
| 全市場日 OHLCV（可回補歷史） | TWSE `MI_INDEX?type=ALLBUT0999&date=` | 1312~1371 檔／日，回溯數月 |
| 本益比／淨值比／殖利率（上市） | TWSE `BWIBBU_d` | 1080 檔 |
| 本益比／淨值比／殖利率（上櫃） | TPEx `tpex_mainboard_peratio_analysis` | 889 檔 |
| 月營收（含官方年增率） | `openapi t187ap05_L` / TPEx `mopsfin_t187ap05_O` | 1082 / 891 檔 |
| 綜合損益表（EPS／毛利） | `openapi t187ap06_L_ci` | 1044 檔 |
| 資產負債表（權益） | `openapi t187ap07_L_ci` / `mopsfin_t187ap07_O_ci` | 1044 / 884 檔 |

**日K 用 `MI_INDEX` 而不是 `STOCK_DAY_ALL`**：後者沒有 date 參數、只回最新
一日，拿它建歷史要等 N 天才有 N 根 K 棒，技術面篩選等於不能用。`MI_INDEX`
吃 date 參數，逐日抓就能一次補齊。抓 180 天約 2 分鐘（1 秒節流）。

### 決策二：技術面與籌碼面「重用，不重寫」

`strategy_engine.CONDITIONS` 已有 60+ 技術條件與 ADR-101 的 15 個籌碼條件。
選股直接呼叫 `strategy_engine.eval_conditions`，不另寫一套判斷邏輯。

好處：使用者在策略裡熟悉的條件，選股時是**完全相同的語意**——不會出現
「策略的黃金交叉」與「選股的黃金交叉」定義不一致這種最惱人的落差；條件只有
一份實作與一份測試；新增策略條件時選股自動就有。

延伸這個想法，UI 上提供「**用某個已存策略的進場條件掃全市場**」——策略編輯器
已經有完整的條件編輯 UI，選股不必再做一套一模一樣的。

### 決策三：空值一律不通過（本工具最關鍵的正確性規則）

本益比顯示「-」代表公司**虧損**（沒有本益比），與「本益比等於 0」意義完全
不同。`to_float()` 對這類佔位符一律回 `None` 而非 0，`eval_fundamental()`
遇到空值一律判定**不通過**而非跳過。

若把空值當成「跳過此條件」，使用者設「本益比 <= 15」時會把一堆虧損公司選進
價值股清單——**這是選股工具最嚴重的錯誤類型，因為使用者會照著它下單**。
有兩條測試專門守著（`test_to_float_distinguishes_missing_from_zero`、
`test_missing_value_never_passes`）。

### 決策四：篩選順序＝效能關鍵

先跑基本面（單一 DataFrame 比較，極快，通常先篩掉九成），再對存活者做
技術面／籌碼面（需逐檔組 K 棒、較慢）。

### 已知缺口與處理（誠實聲明）

> **【本節已於後續驗證中更正，見文末「驗證修正」】** 原本寫的「上櫃沒有可用
> 的綜合損益表端點」是**錯誤結論**——實際上有，只是當時那一次請求剛好失敗，
> 我據此下了結論。詳見文末。

**ROE 是推算值**：`EPS ÷ 每股淨值 × 100`，其中每股淨值 = 收盤價 ÷ 股價淨值比。
EPS 是季更、PB 是日更，期別不一致；用於初步篩選足夠，但**不應拿它跟財報公告
的 ROE 直接對帳**，欄位標籤也標示為「ROE (推算)」。

### 架構

```
core/fundamental_parser.py  六個來源的解析 + EPS/ROE 推導（純邏輯）
core/market_screener.py     篩選引擎（重用 CONDITIONS）+ 5 個預設範本
data/market_store.py        全市場日K（按年月分檔）+ 基本面快照
stock_app_pro.py            「選股」分頁：更新／範本／策略條件／基本面門檻
```

基本面存「當前快照」而非時間序列：使用者關心的是現在的值，保留歷史對選股
沒有實質幫助卻會讓檔案膨脹數十倍。日K 一年約 36MB（與籌碼同量級）。

### 測試

- `tests/test_core.py` 新增 21 個測試，共 **463 個全數通過**。涵蓋空值與 0
  的區別、權證過濾、MI_INDEX 挑表（不寫死索引）、毛利率營收為 0 的處理、
  EPS/ROE 推導、虧損股不可通過本益比條件、技術面重用、範本有效性、
  日K 冪等 upsert、前導零保留、跨月分檔。
- `diag_repro_issues.py` 新增 ADR-103 案例，共 **35 案例全數通過**（切分頁
  不觸發下載、基本面門檻生效、虧損股不誤選、技術面重用、結果填表）。

### 誠實限制

1. **首次更新約 2~4 分鐘**（180 天日K + 基本面），之後每天只補 1 天。
2. **基本面是盤後／月更／季更資料**，沒有即時性。月營收每月 10 日前公布、
   財報季度公布，選股結果的新鮮度受此限制。
3. **MI_INDEX 的歷史深度未逐日驗證到底**（實測回溯至 6/1 可用，更早未測）。
   若某日回「沒有符合條件的資料」，該日會被跳過並在下次更新時重試。
4. **未做產業別篩選**：資料源有產業別欄位（月營收表），但這次未納入 UI。
5. **未做回測**：選股結果無法直接回測「這組條件過去選出的股票表現如何」。
   要做需另開 ADR（牽涉每日重建候選池的計算量）。
6. ~~**上櫃的技術面歷史仍有落差**~~ → **已於 ADR-104 解除**。
   後續找到可回溯的櫃買端點 `afterTrading/otc?date=`（實測可回溯至 2026/03），
   日K 下載改為同一天同時抓上市 + 上櫃，兩市場歷史對齊。詳見
   `DECISIONS_ADR104.md`。

### 需使用者實機驗證

1. 底部「選股」分頁 → 按「⬇ 更新全市場資料」，確認進度顯示、可中止、
   日誌有「基本面已更新 N 檔」。
2. 選範本「價值股」→ 按「🔍 開始選股」，確認結果表出現低本益比高殖利率的股票。
3. 自行在「本益比≤」填 15、「殖利率≥」填 5，再選一次，確認結果符合。
4. 選範本「技術轉強」→ 確認技術面條件有作用（需先完成日K下載）。
5. 在「或用策略條件」下拉選一個已存策略，確認會用該策略的進場條件掃全市場。
6. **雙擊結果列**，確認會把該股帶到主圖查詢。

---

## 驗證修正（使用者要求「確定基本面資料都可以抓到」後的實測結果）

逐一實測所有端點後，找出**三個真實 bug**，都已修正並補上測試。

### 1. 漏掉全部金控股（最嚴重）

財報依產業別分成六個端點：`ci`（一般業）/`basi`/`bd`（建設）/`fh`（金控）/
`ins`（保險）/`mim`。原本只抓 `_ci`，結果**富邦金 2881、國泰金 2882、
玉山金 2884 等全部金控股都沒有 EPS**——它們在 `_fh`。金控是台股權值股的
重要部分。現已六種全抓。

金控與保險沒有「營業收入／營業毛利」欄位（損益結構本就不同），毛利率為空是
**正確的**，不是資料缺失。

### 2. 上櫃財報整批被濾掉（欄位名差異）

上市用中文欄名「公司代號」，上櫃用英文「SecuritiesCompanyCode」——**同一張
財報、兩種欄名**。原本的 parser 只讀中文，導致上櫃 884 筆損益表全被過濾，
只剩 7 筆，而且不會報錯（筆數少了但看不出來）。這也是原本誤判「上櫃沒有
損益表端點」的真正原因。

另外上櫃資產負債表**沒有「權益總額」**欄位，只有資產總計／負債總計，
現以會計恆等式 `權益 = 資產 − 負債` 推導。

### 3. 官方大檔端點會間歇性失敗

實測同一網址連續請求：`mopsfin_t187ap06_O_ci` 第 1 次 JSONDecodeError、
後 3 次都正常回 884 筆。沒有重試的話，使用者更新時只要碰上那一次失敗就會
**靜默少掉一整批資料**，而畫面上看不出來——這是最危險的情況，使用者會以為
篩選結果是完整的。

已加入 `_sc_http_json_retry`（重試 3 次、間隔遞增），且**逐一回報每個來源的
實際筆數**；重試後仍失敗會在日誌明確標示「⚠ 這批股票的相關欄位會是空值」。
同時把 JSON 解碼改為 `utf-8-sig`（部分 OpenAPI 端點帶 BOM）。

### 4. 補上櫃日行情（ROE 覆蓋率）

櫃買的估值端點沒有收盤價，而 ROE 需要每股淨值（＝收盤價 ÷ 股價淨值比），
導致上櫃股票 ROE 全空。已補抓 `tpex_mainboard_daily_close_quotes`
（977 檔），並修正其 `Date` 欄（民國 7 碼）的解析——原本沒解析，呼叫端沒傳
日期時整批資料日期會是 `None`，存檔後讀不回來。

### 修正前後的覆蓋率（實測，全市場 1974 檔）

| 欄位 | 修正前 | 修正後 |
|---|---|---|
| 收盤價 | 未統計 | **98%** |
| 本益比 | 74% | 74%（其餘是虧損股，本來就沒有本益比） |
| 股價淨值比 | 100% | 100% |
| 殖利率 | 100% | 100% |
| **EPS** | 55% | **100%** |
| **毛利率** | 53% | **98%**（缺的是金控／保險，正確） |
| 營收年增率 | 99% | 99% |
| **ROE（推算）** | 55% | **98%** |

關鍵個股實測：台積電 EPS 22.08／毛利 66.2%／ROE 9.7%；富邦金 EPS 2.40／
毛利 --（正確）／ROE 3.7%；上櫃環球晶 EPS 3.97／毛利 20.8%／ROE 2.0%。

### 新增測試

`tests/test_core.py` 增加 5 個測試守住上述 bug（中英文欄名、金控無毛利率、
權益推導、民國日期解析、上櫃日行情自解日期），共 **468 個全數通過**。
