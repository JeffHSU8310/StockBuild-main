
### P-70　相對路徑 + 安靜失敗 = 功能明明做好了，使用者卻永遠看不到效果
- **症狀**：使用者匯入了期交所歷史，系統照樣狂發分段下載，而且**完全沒有
  任何訊息說明為什麼**。連續三輪回報「沒有解決」。
- **根因（兩層疊加，缺一都不會這麼難查）**：
  1. `TAIFEX_BASE_DIR = "."` 解析成「啟動時的當前工作目錄 (CWD)」，不是程式
     所在資料夾。從 Anaconda Prompt 或桌面捷徑啟動時 CWD 不是 `G:\StockBuild`，
     檔案自然找不到。（`broker_config.json` 等七個資料檔全有同樣問題。）
  2. `load_daily()` 找不到檔案就回傳空 DataFrame——**不拋例外、不寫日誌**。
     於是「找不到」和「這個商品本來就沒資料」在畫面上完全無法區分。
- **正確做法**：
  1. 桌面應用的資料檔一律以 `os.path.dirname(os.path.abspath(__file__))`
     為基準，**永遠不要依賴 CWD**。
  2. 「查詢外部資料失敗」不可以安靜退化。至少要寫一行日誌，而且**要把實際
     查找的完整路徑印出來**——只說「找不到」對排查毫無幫助。
  3. 只要某個功能是「背景自動生效」的（快取、延伸、跳過下載），就必須提供
     一個讓使用者**主動確認狀態**的入口（本例加了「🔎 期交所資料狀態」按鈕）。
     否則使用者唯一能回報的就是「好像沒用」，而你無從判斷是沒生效還是沒讀到。
- **更根本的教訓**：功能「有沒有做」和使用者「能不能確認它有沒有生效」是
  兩件事。前三輪都只做了前者。
- **出處**：ADR-060。

### P-71　「連線已死」的判斷關鍵字漏了認證類錯誤，背景工作會整批空轉
- **症狀**：使用者登出後，背景分段下載仍逐段重試，整批 21 段全部噴
  `AuthError: Not authenticated` 才收工。日誌被洗版，還白白對券商送出
  幾十次必定失敗的請求。
- **根因**：`_looks_like_session_dead()` 只認
  `sessionnotestablished` / `session expired` / `disconnected` 等連線層字眼，
  沒有認證層的 `not authenticated` / `AuthError` / `unauthorized`。
  不被認定為致命錯誤 → `_try_seg` 照常重試 → 迴圈繼續下一段。
- **正確做法**：
  1. 「不能用了」的判斷要涵蓋**連線層與認證層**兩類訊息。
  2. 更重要的是**不要只靠例外訊息判斷**。長時間背景迴圈應該有一個明確的
     `_downloads_should_abort()`：程式關閉中、已登出、使用者取消，任一成立
     就停手；**每一輪迭代開始前檢查、每次重試前再檢查一次**。
     靠字串比對是補救，靠狀態旗標才是設計。
  3. 中止與失敗要在日誌上分開講。使用者看到「失敗」會以為是券商在擋，
     看到「已中止（因為你登出了）」才知道是自己的操作造成的。
- **出處**：ADR-060。

### P-72　做「跳過某個步驟」的最佳化時，要檢查下游有沒有假設那個步驟一定有輸出
- **症狀**：ADR-058 讓「期交所已完整涵蓋」的情況完全跳過券商下載——方向正確，
  但下游 `extend_shioaji_df()` 的第一行就是
  `if sj_tf_df.empty: return sj_tf_df`，於是圖表與回測全部變成
  「取不到歷史資料」。**最佳化把功能弄壞了。**
- **根因**：那個 early-return 原本的語意是「shioaji 沒抓到東西，沒得延伸」，
  在跳過下載之後語意變成「這次刻意不需要 shioaji」——同一段程式碼，
  前提改變後意思就不一樣了，但沒人回頭檢查。
- **正確做法**：
  1. 新增「跳過 X」的路徑時，把**所有讀取 X 輸出的下游程式碼**列出來逐一檢查，
     特別是 `if not x: return` 這種 early-return——它們通常隱含「x 一定會有值」
     的舊前提。
  2. 為「X 為空但整體仍應成功」寫一個明確的測試（本例：
     `test_empty_shioaji_returns_taifex_data`），否則這種 bug 只會在使用者
     實機遇到。
  3. 空值有兩種語意——「失敗」和「不需要」——程式若無法區分，就該在呼叫端
     用不同的路徑或旗標表達，而不是都丟一個空表下去。
- **出處**：ADR-060（ADR-058 的回歸修正）。
