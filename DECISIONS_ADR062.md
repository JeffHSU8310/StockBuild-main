
## ADR-062：條件可點選編輯、買進持有三模式（含定期定額）、策略比較

### 一、進場/出場條件可以直接點選修改（使用者需求 #1）

**原本的問題**：條件加進清單後就只能「移除再重加」，而且重加時參數要自己
重打一次。條件一多就非常難改。

**修法**（兩種操作互補，不必二選一）：

* **單擊清單項目** → 把上方「條件類型 + 參數欄」同步成你點的那一條。
  接著按「加入進場/出場」就是複製一份，很順手。
* **雙擊清單項目** → 跳出小視窗直接改參數，確定後**就地更新**那一條
  （不是新增一條）。有下拉選項的參數（SMA/EMA）一樣用 Combobox。

編輯視窗的參數收集沿用「數字轉數字、轉不動保留字串」規則（P-57 的教訓），
否則 `SMA`/`EMA` 會被靜默吃掉。

---

### 二、買進持有三模式並存（使用者需求 #2）

ADR-059 做成「只買一次」、ADR-061 改成「累積加碼」——但使用者要的是**三種
都留著，才能互相比較**。新增 `bnh_mode` 欄位：

| 模式 | 行為 | 用途 |
|---|---|---|
| `single` 單筆長抱 | 只買一次，之後不再進場 | 最單純的長期持有基準 |
| `accumulate` 累積加碼 | 每次條件成立就再買**固定數量** | 逢低承接／訊號加碼 |
| `dca` 定期定額 | 每期投入**固定金額**，數量隨價格變動 | 最貼近一般人的實際投資方式 |

**定期定額的實作重點（誠實處理三個細節）**：

1. **金額 → 數量的換算**：`n = floor(預算 / (價格 × 單位規模))`。
   股票 1 張 = 1000 股，所以每月 1 萬買不起一張 50 萬的股票——這是事實，
   不是 bug。
2. **餘額累積到下一期（carry-over）**：買不滿一個單位的錢**留到下期**，
   不會憑空消失、也不會硬買。真實的定期定額就是這樣運作。
   測試 `test_dca_carry_over_when_too_expensive` 釘住這個行為
   （每月 10 萬、一張 50 萬 → 約每 5 個月才買得起一張）。
3. **同一期只投入一次**：週期用 `每根K棒/每週/每月/每季`，
   同一週期內第一個符合條件的交易日買入，之後跳過。
   進場條件仍然生效，所以「每月 + 收盤跌破均線」= 每月找一次逢低機會。

**順帶消除一處重複實作**：單位規模（股票 1000／零股 1／期貨契約乘數）原本只
寫在 `core/backtest.py`，定期定額也需要同一個數字。抽成
`strategy_engine.unit_size()` 共用——兩處各寫一份遲早分歧（P-67 的教訓，
這個專案已經因為同類問題吃過兩次虧）。

---

### 三、策略比較（使用者需求 #2 後半）

新增「📊 策略比較」按鈕與視窗：

* 勾選 2 個以上策略 → 在**同一段期間、同一組成本與盤別設定**下各跑一次回測。
* 並排比較欄位：淨損益 / 報酬率 / 交易數 / 勝率 / 最大回撤 /
  **交易成本(費+稅)** / **總持有成本** / **每年交易次數**。
* 淨損益最高的那列以綠底標示；失敗的策略標紅並顯示原因，不影響其他策略。
* 期間快選鈕 1Y/3Y/5Y/10Y/15Y/20Y。

**兩個誠實的設計決定**：

1. **強制共用設定**。分開跑再自己抄數字，期間或成本設定一不小心就不一樣，
   比較就沒有意義。
2. **商品/週期不同時主動警告**：
   「⚠ 你選的策略商品/週期不同，比較結果反映的是商品差異而非策略優劣！」
   不阻止你比，但要讓你知道你在比什麼。

取資料的流程抽成 `_qt_prepare_df()`，與 `_qt_backtest_worker` 共用同一套
（下載／期交所涵蓋跳過／延伸／裁切），避免兩份分歧。

---

### 需使用者實機驗證

1. `python tests/test_core.py` → **237 項全過**；`diag_repro_issues.py` → 全 PASS。
2. **條件編輯**：編輯策略 → 單擊進場條件清單裡的項目 → 上方條件類型與參數應
   自動變成那一條；**雙擊**同一項 → 跳出視窗改參數 → 確定後清單那一列就地更新。
3. **三模式**：勾「買進後持有不賣」後，下方「模式」下拉可選三種；
   選「定期定額」時右側「每期金額 / 週期」才會啟用。
4. 建三個策略（單筆長抱／累積加碼／定期定額，同商品同週期）→ 按
   「📊 策略比較」→ 勾選三個 → 10Y → 開始比較。
5. 定期定額的交易明細，**每筆數量應該不一樣**（價格低時買得多）——
   這是定期定額的重點，也是它與累積加碼的本質差異。

### 相關程式位置

* `core/strategy_engine.py`：`unit_size()`、`BNH_MODES`/`BNH_MODE_LABELS`、
  `DCA_INTERVALS`/`DCA_INTERVAL_LABELS`、`_dca_period_key()`、
  `evaluate_strategy` 的三模式分支與 DCA 金額換算＋餘額累積、
  `new_strategy()` 新欄位、`validate_strategy` 的模式與金額檢查。
* `core/backtest.py`：`contract_size` 改用 `_se.unit_size(s)`。
* `stock_app_pro.py`：`_sync_builder_from()`/`_edit_cond_dialog()` 與清單事件綁定、
  買進持有模式下拉與 DCA 參數欄、`_qt_compare_dialog()`/`_qt_compare_worker()`/
  `_qt_prepare_df()`、「📊 策略比較」按鈕。
* `tests/test_core.py`：`TestBnhModesADR062`（225 → 237）。
* `diag_repro_issues.py`：新增 ADR-062 案例。
