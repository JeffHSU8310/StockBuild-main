# ADR-129:終極波段的停損「觸發」與「確認」合併成同一個點位

## 狀態

已實作,**尚未實機驗證**(分支 `claude/merge-stop-levels`)。

## 背景:使用者的截圖與原話

終極波段編輯器,方向=做空:

| 欄位 | 使用者填的值 |
|---|---|
| X — 進出場分界點位 | 42500 |
| **S1 — 空單停損觸發點位 (突破)** | **41701** |
| **S2 — 空單停損確認點位 (隔天12點仍需高於)** | **41700** |

底下紅字:`儲存失敗:空單停損觸發點位 S1 必須大於進出場分界 X`

> 「這兩個應該是要一樣,不需要分兩個,只需填一個S1就可以。(多單也是一樣),請修正」

**S1 與 S2 只差 1 點。** 這正是「多一個沒有實質作用、卻多一次填錯機會的欄位」
的現場證據 —— 使用者為了填第二個欄位要想一次,而想出來的答案幾乎等於第一個。

---

## 原本的設計與它的問題

停損原本是兩段式,每一段一個參數:

| | 觸發 | 確認 |
|---|---|---|
| 多單 | Y:收盤跌破 Y → 記為待確認停損 | Z:隔天12:00 仍低於 Z 才真的平倉 |
| 空單 | S1:收盤突破 S1 → 記為待確認停損 | S2:隔天12:00 仍高於 S2 才真的平倉 |

「兩段式確認」本身是這個策略的核心設計(ADR-084/085),**不動**。
被合併掉的只是「這兩段可以用不同點位」這個彈性 —— 而使用者的實測證明,
那個彈性只帶來填表負擔。

`Y/Z` 用不同值的意義是「留一個緩衝帶」,但緩衝帶其實已經由「隔天 12:00 才確認」
這個**時間**維度提供了,再加一個**價格**維度只是把同一件事說兩遍。

## 決定

### A. 唯一的收斂點放在 `params_of()`

```python
MERGED_STOP_PAIRS = (('z', 'y'), ('s2', 's1'))

def params_of(strategy):
    ...
    for dst, src in MERGED_STOP_PAIRS:
        out[dst] = out[src]
    return out
```

`validate()`、`on_daily_close()`、`on_noon_confirm()` **都經過 `params_of()`**
拿參數,所以三邊自動一致。production 的兩個呼叫端(`_quant_eval_pass` 的
日K 分支、`_qt_chukuangren_confirm_pass`)也都走它 —— 查證過,沒有別的路。

**方向是「確認值 ← 觸發值」**,不是反過來:使用者的原話是「只需填一個 S1」,
S1 是觸發值。有一條單元測試把這個方向釘住(寫反了會變成「使用者沒填的欄位
覆寫使用者填的欄位」)。

### B. 資料格式保留 `z` / `s2`,但不做遷移

`PARAM_KEYS` 維持七個 key。理由:
- 舊策略檔讀得進來,使用者若降版回舊程式也不會壞。
- **不動使用者的策略檔**是本專案一貫做法(ADR-123 起)。

既有檔案裡不同的 `z`/`s2` 值**不會再生效** —— 因為 `params_of()` 覆寫它。
這是 ADR-123 那個教訓的直接應用:

> 一個使用者看不到的欄位若還能左右出場行為,遲早會出事。
> (ADR-123 就是一個看不見的 `stop_loss_pct: 2.0` 砍掉了 -968,250。)

編輯器 `_collect()` 另外把值鏡射一次(`ck_z ← ck_y`),讓存檔資料本身也不
自相矛盾 —— 真正的防線在 A,這只是不留下誤導後人的舊值。

### C. 狀態機本身**不動**

`on_noon_confirm()` 仍然接受 `z` / `s2` 參數並照它判斷。**刻意不把狀態機改成
只認 Y/S1**:
- 狀態機的契約是「給我一組參數,我照它跑」,那是它可離線測試的基礎;
- 既有的單元測試用手寫的 `z != y` 參數在測狀態機的兩段式邏輯,那些測試仍然
  有價值,不該為了這次的簡化而拆掉;
- 「這個策略的 z 一定等於 y」是**策略層**的規則,收斂在參數邊界比塞進狀態機
  更好維護。

> 代價要講明:狀態機裡「z ≠ y」那條路徑在 production 已經是死路。
> 這是刻意的取捨,不是遺漏。

### D. 順手修掉使用者踩到的那個看不懂的錯誤訊息

截圖裡的紅字只說「S1 必須大於進出場分界 X」,而使用者填的是 41701 < 42500。
他顯然不覺得自己填錯 —— 因為訊息沒說**為什麼**。改成:

```
空單停損點位 S1 (41701) 必須大於進出場分界 X (42500) —— 做空是指數「漲上去」才要停損。
```

帶上實際數字 + 一句原因。做多的訊息同樣處理。

**注意:這個驗證規則本身沒有放寬。** 使用者那組參數(做空、停損填在 X 之下)
即使在合併之後**仍然會被擋下來** —— 那是真的填錯,不是欄位太多造成的。

---

## 行為差異

只有一個地方看得出差別,而且只影響**舊策略檔裡 z ≠ y** 的情況:

| 情境(做多,Y=90、舊檔 Z=92)| 舊行為 | 新行為 |
|---|---|---|
| 隔天12:00 指數 = 91 | 91 < Z=92 → **平倉** | 91 > Y=90 → **作廢、繼續持有** |
| 隔天12:00 指數 = 89 | 平倉 | 平倉(相同)|

也就是說:**新行為在「觸發線與確認線之間」那個帶狀區間會選擇繼續持有。**
對使用者而言等於「停損就是我填的那一個點位,沒有第二條線」。

空單對稱(S1=110、舊檔 S2=108,指數 109:舊平倉 / 新繼續持有)。

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| `params_of` 不覆寫(等於沒修)| 單元紅 ×4;診斷紅:`指數 91 已站回 Y=90 之上,不可以平倉` |
| `MERGED_STOP_PAIRS` 方向寫反 | 單元紅 ×10;診斷紅 |
| `_collect()` 不鏡射 | 診斷紅:`存檔資料才不自相矛盾` |
| 編輯器仍顯示兩個欄位 | 診斷紅:`做多方向的編輯器應只剩一個停損欄位 Y` |
| 覆寫成 0(等於把停損關掉)| 診斷紅:`指數 89 < Y=90,停損確認要成立` ← **正控有牙齒** |

**正控是這次的重點**:只驗「91 不該平倉」是不夠的 —— 把停損整個關掉也會讓那條
綠,而那是嚴重得多的問題(該停損的不停損)。所以診斷與單元測試都各有一條
「真的跌破就必須平倉」的反向對照。

### 一個必須走 GUI 路徑才測得到的環節

core 的單元測試是拿**手寫的 params dict** 餵狀態機,所以它們**測不到
「GUI 有沒有經過 `params_of()` 去拿參數」** —— 而覆寫就發生在 `params_of()` 裡。
若哪天有人在 GUI 裡改成直接讀 `s['ck_z']`,單元測試會全綠。

因此診斷案例刻意走 `_quant_eval_pass` → `_qt_chukuangren_confirm_pass` 的
完整路徑(P-64)。

## 驗證

- `python tests/test_core.py` → **637 個全過**(原 626,新增 11)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **55 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

## 需使用者實機驗證

1. **開啟終極波段編輯器**:做多只看到 **Y** 一個停損欄位、做空只看到 **S1** 一個;
   切換「方向」下拉時正確換欄位。
2. **重現截圖那組參數**(做空、X=42500、停損 41701)→ 應該**仍然被擋**,
   但訊息會告訴你原因(做空要填**大於** X 的數字,例如 43000)。
3. 把停損改成大於 X 的值 → **存得起來**。
4. **既有的兩檔終極波段策略**:打開看一下停損欄位顯示的是原本的 Y / S1
   (不是 Z / S2);按儲存後資料會一致化。
5. 有部位時的停損判斷以你填的那一個點位為準。

## 不在這次範圍

- **不動使用者現有的策略檔**(不自動遷移 `ck_z` / `ck_s2`);覆寫已經讓它們
  不生效,按一次儲存就會一致化。
- `on_noon_confirm()` 的狀態機沒有改成只認 Y/S1(見決定 C 的理由與代價)。
- 停利那一段(C / F / SMA20)完全沒動 —— 使用者只提停損。
