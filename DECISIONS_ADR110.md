
## ADR-110：多券商下單 — 策略層級指定「哪一家券商、哪一個帳號」

### 使用者要什麼

> 「要執行某個策略，他可以在策略中就直接指定是哪一個帳號。可以不同券商。」

也就是：策略 A 走永豐證券戶、策略 B 走凱基、策略 C 走兆豐期貨戶，全部在
同一個介面裡跑。

### 先排除的一條路

先評估過把下單交給「下單大師」這類外部下單機（它整合 24～26 套券商 API，
提供萬用 API 讓外部程式餵訊號）。使用者明確不採用，因此走自建 adapter。

### 關鍵設計決定

#### 一、報價與下單分離

台股報價繼續**只走永豐**（ADR-011 已如此規定），只有**下單**分流。

這讓每一家新券商的 adapter 範圍從「整套 SDK 封裝」縮小到三件事：登入、
送委託、收回報。報價訂閱、K 線下載、五檔那些才是工作量大宗，而且會牽動
鐵則 2/3/4/5 一整串——不做，成本差一個數量級。

#### 二、切在「委託意圖」這一層

原本 `_place_strategy_order()` 把三件事混在一起：算讓價與檔位、決定限價／
市價、組 shioaji 的 Order。想送到別家券商就得整段複製改常數——同一份下單
規則存四份，遲早分歧（同一份規則存多份遲早分歧的老問題）。

拆成兩層：

```
core/order_intent.py      算出「要下什麼單」→ 券商中立的 dict
brokers/<券商>.py         把那個 dict 翻成自家 SDK 的委託物件
```

分界線是**「這是不是交易所層級的規則」**：

| 留在 core/（四家共用） | 各家 adapter 自己做 |
|---|---|
| 檔位計算（鐵則 7） | 買進／賣出用什麼常數 |
| 讓價方向與幅度 | 限價／市價／ROD 怎麼表示 |
| 零股只能限價 ROD（鐵則 6） | 零股要不要額外參數 |
| 數量與欄位驗證（`order_rules`，鐵則 9） | 期貨是不是另一組欄位 |

左欄不因為換券商而有第二種寫法，這是重點。

#### 三、`account` 為 `None` 時，adapter 不得帶任何帳號參數

這條是「零行為改變」的具體保證。舊策略沒有帳號欄位 → `account_of()` 回
`None` → adapter 完全不帶 `account=` → shioaji 用它自己的預設帳號，跟加這個
功能之前送出的委託**逐欄位相同**。有診斷案例逐欄位比對守著。

#### 四、指定了帳號卻找不到 → 拒單，絕不退回預設

這個功能最嚴重的失效模式不是「送不出去」（那會報錯，看得見），而是
**默默送到別的帳戶**——沒有任何錯誤訊息，對帳時才發現真錢跑錯戶頭。

所以：`SinopacBroker.place_order()` 在帳號解析不到時直接 `raise`；策略編輯器
的下拉在原設定不存在時退回「預設帳號」項並**顯示橘色警告**，不靜靜地選到
別人的帳號。

#### 五、鐵則 14 的延伸：會動真錢的畫面都要顯示下單目標

多券商帶來一個全新風險：策略設定改一個字，單就送到別家券商去了。因此
`_qt_live_target_text()`（券商 / 帳號）出現在兩個地方：

- **策略清單**的帳戶欄（原本實單固定顯示 `--`，等於看不出下單目標）
- **啟動總開關的確認視窗**——使用者按下「確認啟動」前最後一次能發現下錯
  帳戶的機會

### 順帶修掉的一個潛在陷阱：`self.sj_api` 與 adapter 可能不同步

ADR-097 階段 0 之後，系統裡有兩個名字指向同一個 shioaji 實例
（`self.sj_api` 與 `self.brokers['sinopac'].api`）。它們**當時**是同步的，
但那只是因為兩處賦值剛好都有一起更新。

這在本次階段 1 就真的出事了：診斷腳本設 `app.sj_api = fake`，下單卻走
adapter，委託送到另一個物件去，測試才抓到。**如果反過來——production 改了
adapter 而某處還讀舊的 `sj_api`——就會變成「查詢用 A 連線、下單用 B 連線」
這種極難查的錯誤。**

改法：`sj_api` 變成 property，讀寫都轉到 `brokers['sinopac'].api`。兩者共用
同一份儲存，divergence 從「要小心避免」變成「不可能發生」。上百處讀寫
`self.sj_api` 的程式完全不用改。

### 順帶修掉的診斷盲點：Combobox 的 `current()` 永遠回 0

`diag_mock_tkinter.py` 的 Combobox mock 是 `current: lambda self, i=None: 0`
——設定值被忽略、讀值永遠是 0，等於「下拉選單選了什麼」在診斷環境**完全
測不到**。而「策略指定哪一個實單帳號」正是靠這個讀出來的。已改成真的記住
索引，讓 `current(i)` → `current()` 能往返。

### 分階段與目前進度

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1 | 委託意圖抽象化 + 永豐 adapter 實作（行為不變） | ✅ 本次完成 |
| 2 | 策略指定券商/帳號、編輯器欄位、畫面顯示下單目標 | ✅ 本次完成 |
| 3 | 第一家新券商 adapter | ⬜ 需真帳戶，只能實機 |
| 4 | 其餘兩家 | ⬜ 同上 |

### 階段 3 之前必須先解決的兩個前提

1. **Python 版本**：凱基 `kgisuperpy` 支援 3.09~3.13，本專案執行環境是
   **3.14**，現在裝不起來。要嘛降版，要嘛凱基跑獨立環境（獨立行程 + IPC）。
2. **群益的 SDK 型態未經查證**：印象中是 `SKCOM.dll` 這類 Windows COM 元件，
   執行緒模型與 shioaji 的 typed callback 差很多，可能得放進獨立行程。
   **動工前一定要先確認**，它直接決定 `BrokerClient` 介面能不能統一。
   建議階段 3 先做兆豐或凱基，避開這個未知數。

### 涉及檔案

| 檔案 | 內容 |
|---|---|
| `core/order_intent.py`（新） | 券商中立的委託意圖：讓價、檔位、限市價、路由欄位 |
| `brokers/base.py` | `build_order` / `place_order` / `list_accounts` / `account_object` 介面 |
| `brokers/sinopac.py` | 永豐的翻譯實作 + 帳號解析（id 用「分公司-帳號」，重登後不變） |
| `stock_app_pro.py` | `_place_strategy_order` 改為委派；`sj_api` 改 property；編輯器加「實單帳戶」；清單與確認視窗顯示下單目標 |
| `tests/test_core.py` | +17 個測試（讓價方向、檔位、零股、路由、預設值語意） |
| `diag_repro_issues.py` | +2 案例（委託逐欄位不變、帳號路由與拒單） |
| `diag_mock_tkinter.py` | Combobox `current()` 改為真的記住索引 |
| `PITFALLS.md` | 新增 P-56 (別名不同步)、P-57 (mock 把行為簡化成常數導致假測試) |

### 已驗證（headless）

- `tests/test_core.py` **505 個全過**
- `diag_repro_issues.py` **38 案例全過**，連跑三次穩定
- **突變測試**：故意把讓價方向寫反，新案例會 FAIL——確認斷言不是空的
- 階段 1 的驗收標準是「送給 shioaji 的 Order 逐欄位與重構前相同」，
  期望值是照重構前的程式碼**硬編碼**抄出來的，不是從新程式反推
- `diag_crossref.py` 乾淨、`py_compile` 全過

### 需使用者實機驗證

**版面（headless 完全測不到）**
1. 開三個策略編輯器（一般／自訂 Python／終極波段），確認新增的「實單帳戶」
   那一列**沒有擠掉或蓋住**原有欄位。

**功能**
2. 登入永豐後開編輯器，確認「實單帳戶」下拉列得出你的證券戶／期貨戶。
3. 挑一個策略設成實單並指定帳號 → 存檔 → 重開編輯器，確認選擇有被記住。
4. 看策略清單的帳戶欄，實單策略應顯示「永豐金 / 你的帳號」（原本是 `--`）。
5. 按「🟢 啟動自動交易」，確認確認視窗的每一行實單策略後面都有
   「→ 永豐金 / 帳號」。
6. **未登入時**開編輯器，確認下拉只剩「(用券商預設帳號)」且不會當掉。
7. 設定一個帳號後登出再開編輯器，確認出現橘色的「⚠ 原設定 … 目前不在清單中」。

**最重要的一條**
8. 拿一個**沒有設定過帳號的舊策略**，用實單模式送一張真單（建議零股 1 股），
   確認它跟以前一樣正常成交——這是「零行為改變」的最終驗證。
