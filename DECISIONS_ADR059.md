
## ADR-059：買進持有 (Buy & Hold) 回測基準、回測期末結算、持有成本指標，以及報酬率分母的既有 bug 修正

### 一、買進後持有不賣，也要能回測（使用者需求 #1）

**原本為什麼不行（兩道關卡）**

1. `validate_strategy()` 硬性要求「至少一種出場方式（出場訊號／停損／停利）」，
   否則拒絕存檔——理由是「持倉永遠不會出場」。
2. 就算存得起來，`run_backtest()` 跑到最後一根若還有未平倉部位，**直接忽略**：
   `trades` 是空的 → `_compute_metrics` 回傳全 0 的空指標 → 報告什麼都看不到。

**修法**

* 策略新增 `buy_and_hold` 旗標；勾選後 `validate_strategy` 不再要求出場方式。
  但加了兩道相反方向的防呆：
  - **不可同時設停損／停利／出場訊號**——既然標明永不出場，又設出場條件是
    自相矛盾。寧可擋下來要使用者想清楚，也不要讓他以為在跑 Buy & Hold，
    其實中途被停損出場了。
  - **不可用於實單**——永不出場的實單等於把風險完全交給市場，沒有任何保護。
    只能回測／模擬。
* 新增條件 `always_true`（「永遠成立（第一根就進場，配合買進持有）」），
  讓 Buy & Hold 有合法的進場條件，且語意明確不會誤導。
* 內建策略對話框新增勾選框「📌 買進後持有不賣 (Buy & Hold，當作比較基準)」。

**回測期末結算 `settle_open_at_end`（預設 True）**

跑到最後一根若仍有未平倉部位，用最後一根收盤價做市價結算，產生一筆
`exit_reason = "回測期末結算(未平倉,以最後收盤價計)"` 的交易。

⚠ **這會改變「期末仍持倉」的既有策略的回測數字**。這是**修正而非回歸**：
舊行為把最後那筆未實現損益（可能大賺也可能大賠）完全藏起來，是系統性偏差。
報告會明確標示已做期末結算；不想要的話可傳 `settle_open_at_end=False` 回到舊行為
（測試有涵蓋這條路徑）。

結算的成本比照真實平倉計算——因為要跟主動策略比成本，若不算出場成本，
Buy & Hold 的成本會被系統性低估，比較就失真了。

---

### 二、總持有成本（使用者需求 #1 後半）

「總持有成本」在中文語境有兩種常見意思，**兩個都給，標籤寫清楚**：

| 指標 | 意思 | 為什麼要看 |
|---|---|---|
| **建倉成本（首筆／最大單筆）** | 進場價 × 數量 × 單位規模 | 「要準備多少資金」。Buy & Hold 只建倉一次 |
| **總成本（費+稅）**（既有） | 手續費 + 交易稅總和 | 摩擦成本，Buy & Hold 相對主動策略的主要優勢來源 |
| **每年交易次數** | 交易筆數 ÷ 年數 | Buy & Hold 一年 0.x 次 vs 當沖上百次，一眼看出成本結構差異 |
| **每年交易成本** | 總成本 ÷ 年數 | 跨不同回測長度時可比較 |

**Buy & Hold 的誠實揭露**（直接寫在報告上，不是藏在文件裡）：

* **期貨**：長期持有實際上必須每月換月。R1 連續合約的價格雖然已接續，但
  **換月的手續費與價差成本本回測未計入**，真實成本會比報告高。
* **股票**：本回測使用未還原權息的價格，**長期持有的股利收益未計入**，
  真實報酬會比報告高。

---

### 三、回測報告顯示期間（使用者需求 #2）

標題列與報告內容各顯示一次：
`2020-01-02 ~ 2021-12-01  (1.9 年 / 500 根 日K)`

刻意以**實際餵進引擎的 df 頭尾**為準，而不是使用者輸入的起訖日——因為實際
涵蓋範圍會被資料源深度限制（券商只給到某天、期交所只到昨天），顯示使用者
輸入的日期會讓人以為真的跑了那麼久。

---

### 四、【既有 bug】報酬率分母漏乘單位規模

實作 Buy & Hold 時實測發現報酬率顯示 **43922%**，明顯不合理。追查後確認是
`_compute_metrics()` 的既有 bug：

```python
base += abs(t['entry_price']) * t['qty']      # ← 漏了 contract_size
```

分母因此小了「股票 1000 倍／期貨 200 倍」，導致
`total_return_pct`、`ann_return_pct`、`max_drawdown_pct` **三個百分比全部被
系統性放大**。

**使用者先前回報的畫面就有這個症狀**：0050 的報酬率顯示 `-53093.43%`，
正確值是 `-53.09%`（正好 1000 倍）。而同一份報告裡「每筆交易明細」的報酬%
欄卻是對的——因為那一欄的分母有乘 `contract_size`。**兩處算法不一致，
正是這個 bug 潛伏這麼久沒被發現的原因**（報告上兩個數字並列，一個對一個錯，
反而讓人以為是不同定義）。

已修正並加測試：只有一筆交易時，總報酬率必須等於那筆的報酬%。

---

### 需使用者實機驗證

1. `python tests/test_core.py` → 214 項全過。
2. `python diag_repro_issues.py` → 全 PASS（含新增 ADR-059 案例）。
3. **報酬率修正**：重跑之前那個 0050 策略，報酬率應顯示約 **-53.09%**
   而不是 -53093.43%，且與交易明細的「報酬%」欄一致。
4. **Buy & Hold**：新增策略 → 商品 0050 → 條件類型選「永遠成立」→ 加入進場 →
   停損停利全部歸零 → 勾選「📌 買進後持有不賣」→ 儲存（應成功）→ 回測 20Y。
   報告應顯示 1 筆交易、出場原因「回測期末結算」、建倉成本與每年交易次數。
5. 拿同一商品的主動策略回測，跟上面的 Buy & Hold 對比
   「淨損益 / 總成本(費+稅) / 每年交易次數」三欄——這就是你要的比較。
6. 故意在勾了 Buy & Hold 的情況下填停損 2% → 儲存應被擋下並說明矛盾。

### 相關程式位置

* `core/strategy_engine.py`：`_c_always_true`、`CONDITIONS['always_true']`、
  `new_strategy()['buy_and_hold']`、`validate_strategy` 的 Buy & Hold 分支。
* `core/backtest.py`：`run_backtest(settle_open_at_end=)` 與期末結算區塊、
  `cost_basis_first/max/total`、`trades_per_year`、`cost_per_year`、`years`、
  `settled_open_at_end`、`buy_and_hold_mode`、**報酬率分母修正**。
* `stock_app_pro.py`：策略對話框的 Buy & Hold 勾選框與 `_collect` 收集、
  報告標題與內容的回測期間、Buy & Hold／期末結算的誠實揭露、
  建倉成本與週轉率四格指標。
* `tests/test_core.py`：`TestBuyAndHoldADR059`（200 → 214）。
* `diag_repro_issues.py`：新增 ADR-059 案例。
