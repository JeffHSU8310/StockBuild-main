
## ADR-109：同名方法被重複定義而靜默蓋掉 — 成交量 Y 軸失去「從 0 起算」

### 怎麼發現的

使用者問「還有沒有未完成的工作」，依 `CLAUDE.md` 的收尾檢查跑 AST 稽核
（孤兒 `self.xxx` + 重複方法）時抓到：`stock_app_pro.py` 的
`StockTradingAppPro` 裡，這兩個方法**各被定義了兩次**：

| 方法 | 第一份 | 第二份（實際生效） |
|---|---|---|
| `auto_scale_y` | 第 2504 行 | 第 3099 行 |
| `auto_scale_indicator_panels` | 第 2522 行 | 第 3112 行 |

Python 對此不會有任何警告，後定義的直接蓋掉前面那份。`py_compile` 過、
`diag_crossref` 也過（名字確實存在），所以它安靜地錯了很久。

### 被蓋掉的是什麼（這才是真正的傷害）

兩份內容**不一樣**。第一份裡有成交量副圖的特例：

```python
if name == 'Volume':
    padding = max(high * 0.15, 1.0)
    ax.set_ylim(0.0, max(high + padding, 10.0))   # ← 從 0 起算
else:
    ...
```

第二份沒有這個分支，成交量跟其他副圖走同一條路：
`ax.set_ylim(low - padding, high + padding)`。

**後果**：`low` 是視角內**最小**的那根量。假設畫面上的量介於 80 萬～100 萬，
軸底就會被設在 77.6 萬——而成交量是長條圖，底邊代表 0。於是每一根長條都
被從底部裁掉一大截，畫面上所有量看起來都差不多高，**量能的相對關係整個失真**。
「今天爆量」跟「今天量縮」在圖上長得幾乎一樣。

這對一個以「量價關係」找支撐壓力（ADR-102）的系統來說不是外觀問題。

另外第二份的 `auto_scale_y` 在「視角內價格完全沒有波動」時（漲停鎖死一整天、
或只剩一根 K 棒）直接 `return` 不設範圍，結果是沿用 mplfinance 依整個資料集
算出來的 Y 軸——那根 K 棒會被壓成畫面中間一條看不出來的線。第一份有處理。

### 決定

1. **刪掉被蓋掉的那一份**（第 2504/2522 行），保留較新、有 NaN 防護的那份。
2. **把被蓋掉的兩個行為併回來**：
   - 成交量 Y 軸從 0 起算（`continue` 跳過通用邏輯，語意更清楚）
   - 價格無波動時給對稱小範圍，而不是放著不管
3. **把「重複定義」加進 `diag_crossref.py`**，讓這一類錯誤從此是**看得見的失敗**
   而不是靜默行為改變。已用一個刻意重複定義的樣本檔驗證偵測確實會觸發。
   `@overload` / `@property` 的 setter/getter 這類合法重名不誤報。
4. 順手刪掉 `_extend_with_taifex` 裡一段前次 session 留下的編輯備忘註解
   （`# TODO: ... this is just to anchor the new method` / `# Actually I don't
   need to replace...`）——那不是給讀者看的內容。

### 為什麼是「加掃描」而不只是「修好它」

只修這一處，下次同樣的事還是會發生：這個類別有數百個方法、檔案超過一萬行，
新增功能時肉眼根本不可能確認「有沒有同名的」。`CLAUDE.md` 的驗證流程本來
就寫著「AST 掃描（無孤兒 `self.xxx`、無重複方法）」，但**沒有任何腳本真的在做
後半段**——規則寫了卻沒有工具執行，等於沒有。這次補上。

### 涉及檔案

| 檔案 | 內容 |
|---|---|
| `stock_app_pro.py` | 刪除重複定義；合併回成交量從 0 起算、無波動時給範圍；清掉殘留註解 |
| `diag_crossref.py` | 新增 `scan_duplicate_defs()`，重複定義即 exit 1 |
| `diag_repro_issues.py` | 新增案例：量軸下限必須是 0、MACD 不可從 0 起算、無波動時仍給範圍 |

### 已驗證（headless）

- `diag_crossref.py` 乾淨；用刻意重複定義的樣本檔驗證偵測會觸發（exit 1）
- `diag_repro_issues.py` 38 案例全過
- `tests/test_core.py` 510 全過（本次未動 `core/`）

### 需使用者實機驗證

1. 主圖叫出任一檔股票的日 K，看**成交量副圖的長條是不是從底部長上來**
   （修正前會像浮在半空、每根都差不多高）。
2. 縮放/平移到「量能落差很大」的一段（例如爆量隔天量縮），確認高低差
   在圖上看得出來。
3. 找一檔當天漲停鎖死的股票或把範圍縮到只剩一根 K 棒，確認 K 棒畫得出來、
   不是中間一條線。
