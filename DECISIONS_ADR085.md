
## ADR-085：「楚狂人之終極波段」三項修改 — 做多/做空單選、移除移動停利基準H、明確記錄進場指數點位

### 背景

ADR-084 交付「楚狂人之終極波段」後,使用者在實機看到編輯器截圖時提出三項修改:
1. 直接給「做多/做空」選項:選做多就只顯示做多的條件參數、做空只顯示做空的,
   一個策略只做單一方向 (原本 ADR-084 是自動雙向:突破X做多、跌破X做空)。
2. 詢問「移動停利基準起始值 (H)」是做什麼的 (使用者自己也忘了),截圖裡 H 填了 0。
3. 強調「若有進場,務必記錄進場當時的加權指數點位,因為之後都是看加權指數點位
   來計算」。

### 修改一:做多/做空 單選 + 條件參數依方向顯示

- 策略新增沿用既有的 `direction` 欄位 ('做多'/'做空'),`core/chukuangren_band.py`
  的 `on_daily_close(params, rt, daily_df, direction)` 依方向決定進場:做多只在
  收盤突破 X 記待確認多單、做空只在收盤跌破 X 記待確認空單,不會反向。
- 參數依方向分組:`LONG_PARAM_KEYS=(x,y,z,c,f)`、`SHORT_PARAM_KEYS=(x,s1,s2,c,f)`,
  UI 用「方向」下拉選單動態切換顯示 Y/Z (做多) 或 S1/S2 (做空);X/C/F 兩方向共用。
- `validate()` 改成方向感知:做多檢查 Y<X、做空檢查 S1>X;做空沿用系統既有規則
  只限期貨 (股票/零股融券不在自動交易範圍)。

### 修改二:移除「移動停利基準起始值 H」,改成從進場點 D 自動起算

**根因**:H 原本是「點數移動停利那條停利線的絕對起始點位」,但進場點 D 要進場
當下才知道。使用者預先填一個絕對值 (截圖填 0) 會讓點數移動停利**幾乎永遠不會
觸發** —— 加權指數在 4 萬多點,永遠不會跌破 0,等於這段停利完全失效,只剩
SMA20 那段有作用。這是 ADR-084 的設計缺陷 (把一個「執行期才知道的相對基準」
做成了「使用者預先輸入的絕對值」)。

**修法**:移除 H 參數。點數移動停利的停利線一律「從進場點 D (多單) / E (空單)
自動起算」:
- 獲利 (收盤 − D) 首次超過 C 點 → 啟動,停利線設在進場點 D。
- 獲利每再上升 F 點,停利線就往上鎖 F (棘輪式,只升不降;空單往下鎖)。
- 收盤跌破停利線 (多單) / 突破停利線 (空單) → 待確認點數停利。

效果等於「獲利超過 C 點就開始保護獲利,價格回撤到接近成本 (約 C 點的回撤容忍度)
就出場」。**C 同時決定了回撤容忍度**,少一個容易填錯的參數,也不會再發生截圖那種
填 0 導致失效的情況。SMA20 移動停利那段完全不受影響 (原本就不依賴 H)。

### 修改三:明確記錄進場當時的加權指數點位

其實 ADR-084 就已經在 `on_noon_check` 進場確認成立時記錄
`rt['entry_index_price'] = confirm_price`,且 `strategy_engine.apply_fill` 也把
`rt['entry_price']` 設為 intent 的價 (= 加權指數確認價),所以「進場點位有被記錄、
且後續停損停利都以它為基準」本來就成立。這次依使用者要求把它做得更明確、可稽核:
- `on_noon_check` 進場 intent 的 reason 訊息加上「記錄進場加權指數點位
  D/E={confirm_price}」,系統日誌一眼就能看到記錄了哪個點位。
- 點數移動停利改成從 `entry_index_price` 起算 (修改二),讓「之後都看加權指數
  點位計算」這件事在程式邏輯上更直接 —— 停利線的原點就是進場指數點位本身。

註:記錄的是「隔天中午12點確認進場時的加權指數 5分K收盤價」(= 真正進場的時間點
與價格),不是前一天觸發訊號的日K收盤價;這與「進場時記錄」的語意一致。

### 相關程式位置

- `core/chukuangren_band.py`:`PARAM_KEYS` 移除 h;新增 `LONG_PARAM_KEYS`/
  `SHORT_PARAM_KEYS`/`param_keys_for`/`direction_of`/`DIRECTIONS`;`default_strategy`
  加 `direction`、移除 `ck_h`;`validate` 改方向感知;`on_daily_close` 新增
  `direction` 參數並依方向閘門進場、停利線改從 `entry_index_price` 起算;
  `on_noon_check` 進場 reason 補記錄點位說明。
- `stock_app_pro.py`:`_qt_open_chukuangren_editor` 加「方向」下拉 + 依方向動態
  顯示 Y/Z 或 S1/S2 的參數框 (移除 H 輸入框);`_quant_eval_pass` 呼叫
  `on_daily_close` 時傳入 `direction_of(s)`;策略清單/啟動確認視窗的方向欄改顯示
  實際 `direction` (不再是「多空自動判斷」)。

### 測試

`TestChukuangrenBand` 更新為 32 個測試,涵蓋方向閘門 (做多只做多、做空只做空、
反向不進場)、移除 H 後停利線從進場點起算的棘輪行為、進場一定記錄指數點位、
以及方向感知的驗證。`python tests/test_core.py` 全數 297 個測試通過。

### 需使用者實機驗證

1. 新增/編輯「楚狂人之終極波段」:應有「方向」下拉,選「做多」只顯示 X/C/F +
   Y/Z;選「做空」只顯示 X/C/F + S1/S2 (且做空只能選期貨);不再有「移動停利
   基準起始值 H」欄位。
2. 進場後看系統日誌:應能看到「記錄進場加權指數點位 D/E=xxxxx」,之後停損/停利
   都以這個點位計算。
3. 點數移動停利:獲利超過 C 點後,價格回撤約 C 點就會觸發 (隔天12點確認),
   不會再像填 H=0 時那樣完全不觸發。
