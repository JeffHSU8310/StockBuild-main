
## ADR-086：「楚狂人之終極波段」SMA20 移動停利模式切換 — 加上「獲利需先超過C」前提

### 背景

ADR-085 交付「移除移動停利基準H、停利線改從進場點自動起算」後,使用者確認
SMA20 模式的切換邏輯時發現:原本的切換條件只看「收盤是否穿越SMA20」,跟C
(點數移動停利的啟動門檻) 完全無關 —— 即使剛進場、獲利極小甚至還沒啟動點數
移動停利保護,只要收盤站上SMA20 (多單) / 跌破SMA20 (空單),下一輪評估就會
切進 SMA20 模式 (且一旦切換不會切回)。使用者要求:多單/空單都要「獲利先大於
C,且加權指數也站上/跌破SMA20」才切換到SMA20 模式。

### 修改內容

`core/chukuangren_band.py` `on_daily_close()`:多單與空單的 SMA20 模式切換
判斷,原本只有 `close > sma20` (多) / `close < sma20` (空),改成
`profit > C and close > sma20` (多) / `profit > C and close < sma20` (空)。
`profit` 的計算從原本只在 `entry_px > 0` 區塊內定義,提升到區塊外
(entry_px<=0 時視為 profit=0,不會滿足 `profit > C` 因此不會誤切換)。

點數移動停利 (trail_armed/trail_base 棘輪) 邏輯完全不變,只是把原本內縮在
`if entry_px > 0:` 裡的 `profit` 賦值往外提一層,讓 SMA20 切換條件也能讀到
同一個 `profit` 值,避免重複計算或兩處定義不一致。

`PARAM_HELP['c']` 補充說明:C 除了是點數移動停利的啟動門檻,也是切換到
SMA20 移動停利模式的前提;獲利還沒過 C 時即使站上/跌破 SMA20 也不會切換。

### 效果

- 剛進場、獲利還沒超過 C 點時,即使加權指數當天收盤剛好站上/跌破 SMA20 也
  不會誤切換到 SMA20 模式,避免用雜訊般的價格波動提早放棄點數移動停利的
  保護。
- 獲利真正超過 C 點 (代表點數移動停利已經或即將啟動保護) 之後,才會依
  SMA20 的位置決定是否切換到用均線當移動停利基準,語意更貼近「先用固定
  規則保護基本獲利,獲利夠大之後才改用均線這種更寬鬆但更貼近趨勢的停利」。
- 一旦切換仍然不會切回 (沿用 ADR-084/085 既有行為),固定停損 Y/S1→Z/S2 全程
  持續監控不受影響。

### 相關程式位置

- `core/chukuangren_band.py`:`on_daily_close()` 多單/空單兩段 SMA20 模式
  切換判斷都加上 `profit > C` 前提;`PARAM_HELP['c']` 補充說明;模組頂部
  docstring 同步更新。
- `tests/test_core.py`:新增
  `test_long_does_not_switch_to_sma20_mode_when_profit_not_over_c`、
  `test_short_switches_to_sma20_mode_only_after_profit_over_c` (含空單獲利
  超過C後才真正切換的正向驗證),共 300 個測試全數通過。

### 需使用者實機驗證

1. 「楚狂人之終極波段」持有多單/空單期間,獲利尚未超過C點時,即使加權指數
   當天收盤剛好穿越SMA20,系統日誌也不應顯示切換到SMA20模式的任何跡象
   (沒有相關 pending_exit 產生)。
2. 獲利明確超過C點後,加權指數收盤跌破 (多單) / 突破 (空單) SMA20,才會出現
   「待確認SMA20停利」,隔天中午12點依SMA20位置二次確認。
