
## ADR-061：Buy & Hold 語意更正 —— 「條件成立就再買一次、永不賣出」的累積模型

### 背景：ADR-059 把需求做錯了

ADR-059 把「買進後持有不賣」實作成**只進場一次、之後不再動作**。
使用者更正：**「不是只能進場一次，是只要我設定進場買入的條件成立，就買進，
但是不會賣出」**——這是**累積加碼**模型（定期定額／逢低承接那一類），
用來當長期持有的比較基準。

兩者差很多：前者是「單筆長抱」，後者是「持續投入、部位越滾越大」。
使用者要的「總持有成本」在累積模型下才真正有意義——那就是**總共投入了多少錢**。

---

### 一、引擎層（`core/strategy_engine.py`）

**`evaluate_strategy`**：`buy_and_hold` 時**不看持倉狀態**，
只要進場條件成立就發 OPEN；且**永遠不產生 CLOSE**。
（停損停利／出場訊號在 `validate_strategy` 已被擋掉不可能存在，
這裡再明確地不走那條路。）

**`apply_fill`**：同方向再次開倉時要**加碼**而不是覆蓋——
數量累加、進場價改成**加權平均成本**：

```
new_entry = (prev_px × prev_qty + fill_px × add_qty) / (prev_qty + add_qty)
```

舊版直接覆蓋 `entry_price`/`qty`，在累積模型下成本基礎會完全失真。
**非 `buy_and_hold` 的一般策略行為完全不變**（仍是覆蓋），有測試釘住。

**安全性**：`validate_strategy` 已禁止 `buy_and_hold` 用於實單，
所以這條「會無限累積部位」的路徑不可能出現在真實下單流程，只服務回測與模擬。

---

### 二、回測層（`core/backtest.py`）

* 每次條件成立記一筆 **lot**（不覆蓋前一筆），期末**逐筆**以最後收盤價結算
  → 交易明細每一列 = 一次買進，看得到每次買在什麼價位。
* **資金曲線**的浮動損益改為把「所有已買進的 lot」逐筆算進去；
  舊寫法只反映最後一筆，長期累積的部位完全看不出來。
* 新增累積專屬指標：

| 指標 | 意義 |
|---|---|
| `bnh_buys` | 買進次數 |
| `bnh_total_qty` | 累計部位 |
| `bnh_avg_cost` | 加權平均成本 |
| `bnh_total_invested` | **總持有成本（投入本金）** |
| `bnh_total_invested_with_cost` | 含手續費稅後的總投入 |
| `bnh_final_price` / `bnh_final_value` | 期末價 / 期末市值 |

---

### 三、報酬率分母（又一次，這次是累積模型專屬）

ADR-059 才修過「分母漏乘 `contract_size`」，這次是另一個問題：
`base` 取的是**每筆的平均**成本基礎，但累積模式的 `total_pnl` 是
**119 筆的總和**——分母只有一筆的規模，報酬率被放大成 **1473%**。

修法：`_compute_metrics` 新增 `base_override`，累積模式傳入**總投入資金**。
實測從 1473.88% → **12.39%**，並與手算 `(期末價−平均成本)×總量×單位規模−總成本`
完全一致（差 0.00）。

---

### 四、驗算的誤報修正

`audit_result` 的「持有K棒數 ≥ 1」在累積模式下會誤報：
**最後一根K棒才成立的那次買進，期末隨即結算，持有根數就是 0**，完全合法。
改為「≥ 0」（只擋負數）。

---

### 五、報告呈現

Buy & Hold 回測的報告最上方獨立一列（綠色粗體）：

```
📌 累積買進彙總:買進 164 次 → 累計部位 164 張 | 加權平均成本 107.54 | 期末價 116.27
   總持有成本(投入本金) 17,636,878 | 含手續費稅後 17,708,252 | 期末市值 19,068,864
```

這就是使用者要拿來跟主動策略比較的那組數字。

`always_true` 條件的名稱同步改為「永遠成立（每根都成立；配合買進持有＝定期定額）」，
勾選框說明也改成累積語意，並舉例：
「條件選『永遠成立』＝每根K棒都買（定期定額）；選『收盤跌破均線』＝逢低承接」。

---

### 需使用者實機驗證

1. `python tests/test_core.py` → **225 項全過**；`diag_repro_issues.py` → 全 PASS。
2. 新增策略 → 0050 → 勾「📌 買進後持有不賣」→ 條件選「**收盤價位於均線之下**」
   （期間 20）→ 加入進場 → 停損停利全部 0 → 儲存 → 回測 10Y。
   報告最上方應出現「累積買進彙總」，交易明細有多列，每列都是「期末結算」。
3. 換成條件「**永遠成立**」再跑一次 → 買進次數應接近總K棒數（定期定額效果）。
4. 拿同商品的主動策略對比「總持有成本 / 淨損益 / 總成本(費+稅) / 每年交易次數」。

### 相關程式位置

* `core/strategy_engine.py`：`evaluate_strategy` 的 `buy_and_hold` 分支、
  `apply_fill` 的加權平均加碼、`always_true` 名稱與說明。
* `core/backtest.py`：`is_bnh`/`bnh_lots`、逐筆期末結算、資金曲線多 lot 浮動損益、
  `bnh_*` 指標、`_compute_metrics(base_override=)`、`audit_result` 持有根數 ≥ 0。
* `stock_app_pro.py`：報告的「累積買進彙總」列、勾選框說明改累積語意。
* `tests/test_core.py`：`TestBuyAndHoldADR059` 全面改寫為累積語意（219 → 225）。
* `diag_repro_issues.py`：ADR-059 案例更名並改寫為累積語意驗證。
