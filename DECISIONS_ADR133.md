# ADR-133:主圖黃金切割律(費波南希回撤)+ 盤勢判斷自動掃描,不必人工切換週期

## 狀態

已實作,**尚未實機驗證**(分支 `claude/fib-and-auto-regime`)。

## 需求(使用者原話)

> 1. 主圖指標,再增加**黃金切割律(費波南係數)**這個功能。費波南係數這個功能
>    使用方式,你可以去搜尋網路上的資料。
> 2. 「主圖切到 60分K 時,系統日誌也會出現【盤勢判斷】結果」——關於你做的這一點,
>    我要你做到,**不需要我人工切換,你就可以自動判斷**。(日K也是希望可以做到)

---

## 第一部分:黃金切割律

### 查證來源(依使用者指示搜尋公開資料)

| 項目 | 結論 |
|---|---|
| 主要回撤比率 | **0.236 / 0.382 / 0.5 / 0.618 / 0.786**;最常看的是 0.382、0.5、0.618 |
| 0.5 | 嚴格說**不是**費波南希比例,但各家軟體一律列入(半分位實務上同樣有效)|
| 畫法 | **上升段從擺盪低點拉到擺盪高點;下降段從高點拉到低點** |
| 擺盪點定義 | 高點 = 明顯回檔前的最高點;低點 = 反彈起漲前的最低點 |
| 0.236 | 「趨勢動能區」,強勢趨勢常在此止跌/止漲 |
| 0.618 | **黃金比例**,深度回檔的關鍵位 |
| **次級分割律(台股/波浪理論)** | **0.191**(0 與 0.382 的中間值)、**0.809**(0.618 與 1 的中間值);台股說法「弱勢反彈 0.191/0.382,強勢反彈 0.809」 |
| 延伸 | 1.272 / 1.382 / **1.618**(0.618 的倒數)/ 2.0 / 2.618 |

來源見文末。

### 比例的定義:百分比是「**已經回撤掉多少**」

```
上升段 (低→高):0% 在高點(完全沒回撤)、100% 在低點(全部回撤掉)
                價位 = high - (high - low) * r
下降段 (高→低):0% 在低點、100% 在高點
                價位 = low + (high - low) * r
```

兩個方向**同一條算式,只差起算端**。這樣寫的好處是 `r > 1` 自然落在「起點的
另一側」——正好就是延伸目標的意義,不需要第二條公式。

有一條單元測試專門守方向:上升段 100→200 時 0.618 必須是 **138.2**、
0.382 必須是 **161.8**;算式寫反的突變會紅。

### 擺盪點自動偵測(不必手動拉線)

```python
高點與低點誰「比較晚出現」,就代表最近這一段往那個方向走:
  低點在前、高點在後 → 上升段(回撤往下量)
  反過來             → 下降段
```

這是各家「自動費波」的通用作法,也是**唯一不需要使用者手動拉線的判準**。
取樣根數預設 120 根,可設定 —— 只看最近 N 根,免得很久以前的極值綁架現在的
切割區間(有測試守著)。

### GUI

- 工具列新增「**黃金切割**」勾選鈕(旁邊的 ⚙ 改叫「均線/布林/黃金切割」)。
- 設定裡可調:取樣根數、色彩、**要畫哪幾條比率**(勾選清單,★ 標出 0.382/0.618、
  次級分割律與延伸各自標註)。
- 畫法:端點實線、**0.382/0.618 加粗虛線**、其他回撤細虛線、延伸淡點線;
  每條線左側標「比率 + 價位」。
- 勾選或套用後把所有點位寫進系統日誌,方便直接拿數字去掛單/設停損。
- **任何例外都不可影響 K 線圖本身** —— 跟支撐壓力同樣的理由(輔助資訊不可以
  害主功能掛掉)。

**沿用支撐壓力的取樣語意**:「可見範圍」模式下算的是畫面上看得到的那一段,
所以縮放/平移時切割線會跟著重算。這是刻意的(切割線本來就該跟著看的區間走),
診斷註解裡有寫明,免得下一個人以為是 bug。

---

## 第二部分:盤勢判斷自動掃描

### 問題

ADR-120 的 `_regime_check()` 掛在 `draw_chart()` 上,**只判斷主圖現在顯示的
那個商品與週期**。所以要看 60分K 的盤勢,使用者得自己把主圖切過去 ——
ADR-132 我還把這件事當成功能寫進交付說明,使用者直接指出那不是他要的。

### 決定

在 ADR-132 那條背景 worker 上加一個 `_regime_auto_scan_pass()`:

- **不看主圖現在顯示什麼**,固定對加權指數的 `PATTERN_TIMEFRAMES`
  (日K + 60分K)各評估一次。
- 結果沿用 ADR-120 的去重(`plan_notifications`),只把**新出現的**寫進系統日誌;
  去重狀態的 key 加上 `AUTO|` 前綴與週期,跟主圖那條路徑互不干擾。
- **Telegram 推播維持「每天收盤後一次」**(ADR-132)—— 使用者要的是
  「不用手動切換也看得到」,不是「一直被通知」。

### 用 K 棒格子避免白打 API

```python
def _regime_scan_slot(tf, now_dt):
    分K 類 → 對齊 K 棒邊界(60分K 就是每個整點)
    日K 類 → 每 10 分鐘一格
```

同一格內不重新評估:同一根 K 棒重複算只會得到一樣的結果,白打 API
(ADR-127 的同一個教訓)。**抓到資料才記 slot** —— 抓不到就讓下一輪重試,
不要把這一格用掉。

另外只在**台股盤中**掃(`is_stock_open`):收盤後資料不再變動,一直重掃沒有
意義;收盤那一輪由每日推播收尾。

日K 這條路徑刻意用 `drop_last=False`(ADR-132 加的參數),所以看得到
**今天正在形成**的型態 —— 對「盤中就想知道」這個需求是必要的。

---

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| 上升段的回撤算式寫反 | 單元紅 ×2;診斷紅:`0.618 的價位算錯` |
| 擺盪方向判斷寫反 | 單元紅 ×3;診斷紅:`這段資料是上升段,實際判成 下降段` |
| 拿掉「同一格不重掃」 | 診斷紅:`同一格不該重抓,實際又抓了 ['日K','60分K']` |
| **自動掃描改回只看主圖週期**(等於沒修)| 診斷紅:`日K 與 60分K 都要自動掃,實際掃了 ['5分K']` |
| 黃金切割不看勾選狀態、永遠畫 | 診斷紅:`沒勾黃金切割不該畫任何線,實際畫了 7 條` |

第 4 條是這次的重點:診斷刻意把主圖停在**完全無關**的商品與週期
(`2330` / `5分K`),自動掃描仍然必須掃日K 與 60分K —— 那正是使用者要的
「不用人工切換」。

## 驗證

- `python tests/test_core.py` → **684 個全過**(原 668,新增 16)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **59 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

時間一律由呼叫端傳入(`_regime_auto_scan_pass(now_dt=...)`),
`is_stock_open` 在診斷內 patch,**不依賴真實時鐘**(P-94/P-97)。

## 一個測試自己寫錯的紀錄

「完全沒有波動應該回 None」第一版是紅的 —— 因為測試的 `_df()` 輔助函式把
High/Low 撐開了 ±1,所以「收盤價都一樣」**不等於**沒有波動。是測試的前提錯了,
不是程式錯。改成自己造一份 High == Low 的資料,並補一條反向對照。

## 需使用者實機驗證

### 黃金切割律
1. 工具列勾「**黃金切割**」→ 主圖出現水平線,0.382/0.618 兩條比較粗。
2. 「⚙ 均線/布林/黃金切割」→ 最下面可調取樣根數、色彩、要畫哪幾條比率
   (含次級分割律 0.191/0.809 與延伸 1.272/1.382/1.618/2/2.618)。
3. 系統日誌會列出每一條的價位,可直接拿去掛單。
4. **縮放/平移主圖** → 切割線會跟著可見範圍重算(這是刻意的)。
5. 上升段時 0 在上、1 在下;下降段相反 —— 對照一下走勢是否合理。

### 盤勢判斷自動掃描
6. **把主圖停在任何商品/週期都不要動**(例如個股 5分K),盤中觀察系統日誌:
   應該會自動出現「【盤勢判斷】加權指數 ... 日K偵測到:...」與
   「... 60分K偵測到:...」——**完全不用切換**。
7. 同一根 K 棒內不會重複洗版;跨到下一根 60分K 才會再判斷一次。
8. 收盤後不再持續掃(由每日推播收尾)。

## 不在這次範圍

- 黃金切割只做**自動擺盪點**,沒有做「手動拉線指定起訖點」(使用者沒要求;
  要做的話是滑鼠互動,量級不同)。
- 自動掃描只對**加權指數**(與 ADR-132 一致)。
- 自動掃描的結果只寫系統日誌,不即時推播(使用者:「每天一次,收盤後通知即可」)。

## 來源

- [How to Draw Fibonacci Retracement Levels in Forex Trading — Taurex](https://www.tradetaurex.com/forex-insights/how-to-draw-fibonacci-retracement/)
- [Fibonacci Retracement: Definition & Key Levels for Traders](https://www.heygotrade.com/en/blog/fibonacci-retracement-explained/)
- [How to Use Fibonacci Retracements — Babypips](https://www.babypips.com/learn/forex/fibonacci-retracement)
- [Fibonacci Retracement Levels: The Complete Guide — altFINS](https://altfins.com/knowledge-base/what-are-fibonacci-retracement-levels/)
- [黃金分割率教學:0.618怎麼畫? — 元大期貨TOP](https://www.yuantatop.com/news-post/golden-ratio/)
- [費波那契回撤完整教學 — 量化通 QuantPass](https://quantpass.org/fibonacci/)
- [神奇的斐波那契回撤,教你用黃金分割率 0.618 建立交易策略 — DA Labs](https://dalabs.org/technical-analysis/fibonacci/)
