
## ADR-079：主圖不再延伸多年歷史,只顯示券商 API 原生範圍 — 修正切換/縮放/平移的根本效能問題

### 背景

ADR-078 修正了「快速段被延伸拖垮」的迴歸,但使用者接著實測貼圖 (5 個系統日誌
截圖) 證實問題比 ADR-078 想的更根本:

> 1. 切換時還是覺得很慢
> 2. 當我放大縮小移動K線圖,畫面超級慢

日誌顯示即使是「已搶先出圖」那次,渲染耗時仍高達 900~1700ms、K棒數高達
6,952 / 6,228 / 4,902 / 9,321 根。ADR-078 只讓快速段跳過延伸,但**背景補全的
完整段**(以及快取命中路徑) 依然會延伸出上千~上萬根,使用者切換完成後只要
一縮放/平移,matplotlib 就要重繪這整批資料,每一幀都要 900ms+,這才是
「畫面超級慢」的根本原因——延伸出來的巨量資料留在圖上,不是切換那一瞬間的事。

### 決策 (使用者提出,經確認採用)

問使用者「要不要限制圖表同時渲染的 K 棒數量」時,使用者提出更好的方案:

> 「只有在做回測&最佳化時,才需要讀取歷史數據,一般的時候,就只需要讀取
> 券商API提供的時間範圍數據即可,這樣應該比較快一點。」

這比「加一個任意的 bar cap」更乾淨:**看盤 (主圖) 和回測/最佳化本來就是兩種
不同需求**,不需要用同一份資料硬撐:
- 看盤:只需要「最近一段時間」判斷趨勢/抓進出點,不需要 20 年歷史攤開在
  同一張圖裡。
- 回測/最佳化:需要長歷史驗證策略,但這兩個功能**本來就各自獨立下載/延伸**
  資料 (`_qt_backtest_worker`/`_qt_optimize_worker` → `_qt_bt_load_df` →
  `_extend_with_taifex`/`_extend_with_yahoo`),完全不經過主圖的 `_publish`。

### 做法

**`_fetch_data_worker_impl` 裡的 `_publish()`** (主圖唯一的出圖入口):整段移除
`_extend_with_taifex`/`_extend_with_yahoo` 呼叫。四個呼叫點 (快取命中、快速段、
背景完整段×2) 全部不再延伸,一律只顯示 `SJ_DAYS` 這個券商 API 原生範圍:

| 週期 | SJ_DAYS (未變) | 約略根數 |
|---|---|---|
| 日K | 365 天 | ~250 根 |
| 周K | 1095 天 (3年) | ~150 根 |
| 月K | 1825 天 (5年) | ~60 根 |

比起延伸後的 4,900~9,300+ 根,少了一個數量級以上,matplotlib 渲染成本 (以及
縮放/平移每一幀的重繪成本) 直接等比例下降。

**回測/最佳化完全不受影響**:`_qt_backtest_worker`/`_qt_optimize_worker` 有自己
獨立的 `_qt_bt_load_df` 載入器,直接呼叫 `_extend_with_taifex`/`_extend_with_yahoo`,
和主圖的 `_publish` 是兩條互不相通的路徑——這次只動了主圖那一條,回測/最佳化
的歷史深度、結果完全不變。

### 已知/預期的副作用 (誠實告知)

- **長週期均線在主圖可能顯示不全**:預設指標設定含 MA240 (`ma_periods` 有
  '240')。日K 只有 ~250 根原生資料時,MA240 只有最後約 10 根有值,線條會很短。
  週K/月K 的 MA240 基本不會出現 (根數遠不到 240)。這是「原生範圍變小」直接的
  代價,使用者已在決策時被告知「一般時候只讀取券商 API 範圍」,視為可接受的
  取捨;若之後想要主圖也能看到長週期指標的完整線,可以考慮加大對應週期的
  `SJ_DAYS` (仍遠小於延伸的 20 年,可以在速度與指標覆蓋率之間找一個新平衡點),
  但這是後續使用者若有感覺再調整,這次不主動加大。
- 若使用者想確認某檔的長期趨勢/長歷史,現有「🔬 回測」功能本來就會抓完整延伸
  歷史並畫出對應報告圖,可以作為「看長歷史」的替代路徑。

### 相關程式位置 (stock_app_pro.py)

- `_fetch_data_worker_impl` 的 `_publish()`:移除延伸呼叫 (取代 ADR-078 加的
  `extend` 參數,該參數本次移除,四個呼叫點全部統一不延伸)。
- 相關過期註解一併更新 (`_pub_state`/`cached_len` 的「含延伸」字樣)。
- `_qt_backtest_worker`/`_qt_optimize_worker`/`_qt_bt_load_df`:未改動,回測/
  最佳化的延伸邏輯照舊獨立運作。

### 需使用者實機驗證

1. 切換不同標的的日K/周K/月K:出圖應明顯更快 (系統日誌的渲染毫秒數應大幅下降,
   K棒數應降到 SJ_DAYS 對應的原生根數,不再是四位數以上)。
2. 縮放/平移應該順暢很多 (圖上總根數少了一個數量級)。
3. 若有開啟 MA240 等長週期指標,日K 圖上該線可能只剩最後一小段或看不到,這是
   預期的取捨;若你希望日K 的長期均線也完整,告訴我要把 SJ_DAYS 拉多長 (拉長
   會讓渲染變慢一些,需要抓一個你能接受的平衡點)。
4. 「🔬 回測」/「🎯 參數最佳化」的歷史深度與結果應與改動前一致。
