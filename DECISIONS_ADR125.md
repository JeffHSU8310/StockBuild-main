# ADR-125:啟用中的策略不可刪除(編輯本來就已是唯讀)

## 狀態

已實作,**尚未實機驗證**(分支 `claude/session-gate-realtime-stops`)。

## 背景:使用者實測

策略清單截圖裡,「空」這檔的 **狀態=啟用、運轉狀態=運轉中**,而底下日誌寫著
`【自動交易】已刪除策略「test」`。使用者:

> 「只要在啟動模式,就不能被編輯 & 刪除,請記住。」

## 查證:編輯早就擋住了,刪除沒有

**編輯 `_qt_edit_strategy()`** 本來就有 `readonly = bool(s.get('enabled'))`,
而三個編輯器(一般 / 自訂 Python / 終極波段)在 `readonly=True` 時都把
「儲存策略」換成**停用的「唯讀無法儲存」按鈕**;三者又都在
`json.loads(json.dumps(strategy))` 的**深拷貝**上操作 —— 就算在對話框裡亂改,
也碰不到正在跑的那一份。**這條路是安全的。**

**刪除 `_qt_delete_strategy()`** 只有一道檢查:

```python
if rt.get('state') in ('LONG', 'SHORT'):
    ...return
self.strategies = [x for x in self.strategies if x['id'] != s['id']]
```

**完全沒有看 `enabled`。** 截圖裡持倉是 `--`(FLAT),那道檢查於是放行,
一檔**正在運轉中**的策略就這樣被刪掉了。

全專案只有這一個刪除點;Telegram 遠端控制只能查詢/開關,不能刪。

## 決定

規則收斂成 `core/strategy_engine.py` 的純函式(比照 ADR-123/124):

```python
def is_locked(strategy):        # 啟用中 = 鎖住 (不可編輯、不可刪除)
def can_delete(strategy, runtime) -> (ok, reason)
```

`_qt_delete_strategy()` 改走 `can_delete()`;`_qt_edit_strategy()` 的
`bool(s.get('enabled'))` 改成 `is_locked(s)` —— 行為不變,但**規則從此只有
一份**,日後再多一個「要改動策略」的入口不會又漏掉。

**檢查順序刻意是「先看啟用、再看持倉」**:兩者都不成立時,使用者要做的第一件
事都是「先停用」,訊息就該直接指向那一步,不要先叫他去處理持倉、繞一圈才發現
還得停用。有一條單元測試守著這個順序。

**刻意保留唯讀檢視**,不把編輯器整個擋掉:能看正在跑的策略掛了什麼條件是有
價值的,而唯讀模式已經確保改不動。

### 為什麼不只是「使用者要求」

策略正在跑的時候,底下有 runtime 狀態、隨時可能成交。從腳底下把它抽掉是
資料競爭:量化 runner 每 2 秒迭代 `self.strategies`,即時停損每 3 秒迭代一次,
刪除發生在 UI 執行緒 —— 刪掉的瞬間可能正好有一筆成交要記到它的 runtime 上,
結果就是一筆對不上任何策略的紀錄。

## 驗證

- `python tests/test_core.py` → **609 個全過**(原 602,新增 7)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **51 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| `is_locked` 永遠回 False(等於沒修)| 單元 + 診斷都紅 |
| `is_locked` 永遠回 True(刪除被鎖死)| 單元 + 診斷都紅(反向對照先紅)|
| `can_delete` 拿掉持倉檢查 | 單元 + 診斷都紅 |
| `_qt_delete_strategy` 不呼叫 `can_delete` | 診斷紅 |
| `_qt_edit_strategy` 不看 `is_locked` | 診斷紅 |

診斷案例**走完整的 GUI 路徑**(`_qt_delete_strategy` / `_qt_edit_strategy`),
純函式測不到「呼叫端有沒有真的用到守門」那一層(P-64)。每個方向都配了
**反向對照**:停用且無持倉**要刪得掉**、停用後編輯器**要可寫** —— 少了它們,
把守門寫成「永遠擋」也會一片綠,而那等於把功能鎖死。

## 需使用者實機驗證

1. 啟用中的策略按「🗑 刪除」→ **刪不掉**,日誌提示要先停用。
2. 按「✏ 編輯」→ 開得起來但標題有「(唯讀 - 策略執行中)」,儲存鈕是停用的。
3. **按「停用」後,刪除與編輯都要恢復正常** —— 這條一定要驗,不然可能是
   整個功能被鎖死而不是只鎖啟用中的。

## 順帶發現,但不在這次動

刪除策略**完全沒有確認視窗** —— 選中就刪、不可逆。這次的鎖擋得住「啟用中誤刪」,
但停用狀態下手滑點到「🗑 刪除」仍然是一秒就沒了。要不要加確認框是取捨
(多一次點擊),留給使用者決定。
