
## ADR-098：CLAUDE.md 瘦身 — 依 Anthropic Claude 5 世代 context engineering 建議重整

### 背景

使用者提供 Anthropic 官方部落格文章〈The new rules of context engineering
for Claude 5 generation models〉，要求依其建議調整 CLAUDE.md。文章核心建議：

1. CLAUDE.md 保持輕量：簡述專案用途，篇幅重點放在陷阱 (gotchas)，
   不重複模型本來就知道的內容。
2. 漸進式揭露：長篇指引拆到獨立檔案，需要時才載入，不全塞進 CLAUDE.md。
3. 新世代模型判斷力較好，過度鉅細靡遺的規則說明反而可能過度約束。

本專案原本的 CLAUDE.md 其實已大致符合方向 (鐵則都是專案特有陷阱、細節已
拆到 PITFALLS/ARCHITECTURE/DECISIONS)，主要問題是：16 條鐵則的長說明與
PITFALLS.md 的 P 條目大量重複 (同一件事寫兩份，改一處忘另一處就會出現
矛盾版本)，且速覽區有多處過時內容。

### 決策

1. **鐵則的規範內容一條不動** (使用者明確要求)，但每條只保留「規則本身 +
   一行要點 + P 編號/ADR 出處」，症狀/根因/完整做法一律以 `PITFALLS.md`
   為唯一出處 (single source of truth)，消除雙份維護。
2. 修正過時內容：
   - 標題「10 條鐵則」實際列了 16 條 → 改為不帶數字的「鐵則」(不再腐化)。
   - 「30 個測試案例」(實際已 384) → 改為不帶數字的描述。
   - core/ 模組清單只列 4 個 (實際 18 個) → 改為指向 ARCHITECTURE.md。
   - 速覽補上 `brokers/` 分層 (ADR-097)。
3. **新增 ADR 慣例說明**：ADR-005~044 在 `DECISIONS.md` 本體、ADR-057 起
   每筆獨立一個 `DECISIONS_ADR0XX.md` 檔案；並明文要求「取新編號前先查
   檔名與程式碼註解兩處的最大既有編號」——這是 ADR-097 開發過程中實際
   發生過的撞號教訓 (只查了 DECISIONS.md 本體與 commit 訊息，漏查獨立
   檔案，導致先取了已被用掉的 092)。
4. 順手修正 `PITFALLS.md` P-03 的過時節流值 (5 秒 → 3 秒，ADR-094 已調整
   但 P-03 未同步)，並補 ADR-094 出處。這正是「同一資訊寫兩份」會發生的
   矛盾實例，也是本次瘦身要消除的問題。

### 刻意不做的

- 不動 `PITFALLS.md` 的結構與其餘內容 (它本來就是「細節放這裡」的正確位置)。
- 不動鐵則的任何規範性要求；瘦身只刪重複的解釋文字，不刪規則。
- 不處理使用者本機的全域 `~/.claude/CLAUDE.md` (遠端環境碰不到)。

### 驗證

- 純文件改動，不涉及程式碼；`tests/test_core.py` 不受影響。
- 逐條核對：16 條鐵則引用的 P 編號 (P-01/02/03/04/05/09/10/13/19/20/22/23/
  26/27/46) 與 ADR 編號都實際存在且內容相符，無憑空引用。
- 請使用者確認瘦身後的 CLAUDE.md 沒有遺漏自己在意的規則細節，
  確認後才合併回 main。
