
### P-79　改了「成交時機模型」卻沒開 ADR、沒同步改舊註解，會讓一次正確的升級偽裝成兩個不相干的 bug
- **症狀**：`test_backtest_equals_live_logic`（回測第一個進場點應等於引擎判定
  的金叉點）跟 `test_dca_invests_close_to_planned`（定期定額不可超支預算）
  同時失敗，乍看是兩個獨立問題，其實同一個根因：`core/backtest.py` 的成交
  時機已經從 ADR-039 原本的「同根收盤即成交」改成「訊號用昨天收盤判定、
  今天開盤才成交 (T+1)」，但這個改動完全沒有 ADR 記錄，模組開頭的 docstring
  也還原封不動抄著 ADR-039 的舊描述，跟下面的程式碼互相矛盾。
- **根因**：程式碼裡雖然有中文註解說明「當天判定，隔天開盤執行」，代表當初
  改的人清楚自己在做什麼，但只改了程式碼，沒有：(1) 開一筆 ADR 記錄「為什麼
  改、什麼時候改」；(2) 回頭檢查模組 docstring 是否還在講舊模型；(3) 檢查
  依賴舊模型假設的既有測試 (`test_backtest_equals_live_logic`) 與下游計算
  (定期定額的 sizing) 有沒有跟著時機模型一起更新。
- **正確做法**：
  1. 任何會改變「訊號判定時間點」或「成交時間點」的修改，都屬於鐵則10
     講的「影響資料正確性的改動」，必須當下就補 ADR，不能事後靠人回想。
  2. 模組 docstring／檔頭註解要跟程式碼同步——docstring 講的是「這段程式碼
     現在做什麼」，不是「它歷史上曾經做過什麼」，兩者不一致比沒有註解更糟
     （會誤導下一個人）。
  3. 改成交時機這類「跨整個引擎」的假設變動時，要主動搜尋所有依賴「訊號與
     成交同價/同時」這個舊假設的地方（測試斷言、部位大小 sizing 公式），
     而不是等測試自己爆出來才後知後覺——這正是 P-45/P-57/P-72 一路以來
     同一種「跨檔案/跨假設變動、下游沒跟上」模式的又一次重演。
- **出處**：ADR-064（回填 ADR-039 之後某次遺失的改動記錄；ADR-045～054
  這段 ADR 編號在 `DECISIONS.md` 裡完全缺席，推測改動就藏在那段遺失區間）。

### P-80　用「預算」限制部位大小時，如果 sizing 的參考價格跟實際成交價格不是同一個，「絕不超支」只能是容忍度，不能是嚴格不等式
- **症狀**：定期定額 (DCA) 的測試斷言「總投入 ≤ 期數 × 每期預算」（只留浮點
  誤差空間），實測卻超出預算 0.1%。
- **根因**：`strategy_engine.py` 的 DCA 分支用「決策當根收盤價」計算能買幾個
  單位（`floor(budget / (close × unit_size))`），但 `core/backtest.py` 的
  T+1 成交模型會把實際成交價改成「下一根開盤價」。兩個價格之間的隔夜跳空，
  讓「用價格 A 算出的數量」乘上「價格 B 的實際成本」不可能精確等於預算——
  這在數學上就不該預期嚴格不等式成立。
- **正確做法**：
  1. 决定部位大小 (sizing) 時，先問一句「我用來算數量的價格，跟真正會拿去
     成交的價格，是同一個嗎？」如果不是（本例是 T+1 模型的必然結果，換成
     任何「決策晚於成交」的系統都一樣），對「總花費」的保證就只能是
     「大致符合預算」而不是「絕對不超支」。
  2. 把「carry-over 機制本身有沒有算對」跟「sizing 價格與成交價格的落差」
     分成兩個獨立的測試場景：前者用固定價格（無跳空缺口）驗證到嚴格相等，
     後者（隨機價格）改用相對容忍度，只抓「明顯超支」的真錯誤。混在同一個
     測試裡，一出錯很難判斷是哪一層出的問題。
  3. 不要為了讓不變式重新變嚴格，就讓 sizing 邏輯偷看「未來」的成交價格——
     那是用測試好看程度換取前視偏誤 (P-49)，方向完全錯誤。
- **出處**：ADR-064。
