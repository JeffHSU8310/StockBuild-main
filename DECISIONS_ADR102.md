
## ADR-102：用量價關係推算關鍵支撐/壓力點位

### 背景

使用者要一個「用量價關係，整理出關鍵支撐/壓力點位 (元)」的功能，且對指數、
期貨、股票都要有效。

### 決策：以成交量分布 (Volume Profile) 為核心

「量價關係」最正統的做法是把價格切成一格一格的價格箱，統計每個價位實際
成交了多少量：量堆得越高的價位代表越多人在那裡換手，價格再次回到該處時
越容易遇到支撐或壓力。產出五種點位：

| 類型 | 意義 |
|---|---|
| **POC** (Point of Control) | 成交量最大的價位，最強的單一支撐/壓力 |
| **VAH / VAL** | 價值區上下緣，預設涵蓋 70% 成交量 |
| **HVN** (High Volume Node) | 高量節點，籌碼密集、盤整過的價位 |
| **LVN** (Low Volume Node) | 低量真空帶，價格容易快速穿越 |
| **放量轉折高/低** | swing high/low **且當時要放量**才採計 |

轉折點要求量能確認，是刻意的：沒有量的轉折點事後看常常只是雜訊，那屬於
純價格型態 (已有 `core/market_pattern.py` 處理)，不是量價關係。

### 三個實作上的關鍵決定

**1. 成交量依 High~Low 分攤，不是全記在收盤價。**
一根從 100 漲到 110 的長紅棒，成交是發生在整段路徑上的，不是全部發生在
110。全記在收盤價是常見的簡化實作但會嚴重失真。有測試守著分攤後的總量守恆
(`test_volume_spread_across_bar_range_not_only_close`)。

**2. 價格箱寬度以 tick 為基準，輸出點位對齊 tick。**
三種商品的差異只在這裡：台積電 0.5 元、台指期 1 點、低價股 0.01 元。箱寬
至少一個 tick——比 tick 還細的箱沒有意義 (那些價位根本不能成交)，只會製造
一堆永遠為 0 的空箱。輸出一律 `round_to_tick` + `fmt_price` (鐵則 7)。

**3. LVN 與支撐壓力分開輸出。**
LVN 語意上是「成交真空帶」，不是支撐壓力。第一版把它們混在同一份清單裡
排序，結果同一個價位出現兩筆重複的線 (實測踩到)。現在 `levels` 只含支撐
壓力，`lvn_zones` 獨立，畫圖時用淡灰點線區隔。

### 實作期間修掉的一個真 bug

第一版的局部極值判定兩側都用 `>=` 與 `<=`，導致**平坦區 (三箱等量) 同時被
判成極大又極小**，產出「低量真空帶 + 高量節點」落在同一價位這種自相矛盾的
標籤。已改為「兩側皆不小於 (不大於)，且至少一側嚴格大於 (小於)」，並加上
互斥檢查 (LVN 不與其他類型合併)，兩者都有測試守著。

### 呈現 (依使用者選擇)

在 K 線主圖畫水平線：**壓力紅、支撐綠** (鐵則 1 的延伸——紅=多方要突破的
價、綠=空方要跌破的價)，線的粗細與透明度對應強度，右側標註價格。真空帶用
淡灰點線。工具列有「支撐壓力」勾選框與區間下拉 (可見範圍 / 固定N根，依
使用者選擇兩種都做)。點位數字也會寫進系統日誌，方便拿去掛單或設停損。

**支撐壓力的計算失敗絕不影響 K 線圖本身**——它只是輔助資訊，整段包在
try/except 裡，出錯只記日誌並略過繪製，有診斷案例驗證這個保證。

### 測試

- `tests/test_core.py` 新增 13 個測試，共 **442 個全數通過**。涵蓋 POC 落點、
  價值區涵蓋率、量分攤守恆、矛盾標籤不得出現、無重複價位、角色依現價分割、
  tick 對齊 (股票與期貨)、轉折需量能確認、缺 Volume 欄位退化、價格全平/零量
  回 None。
- `diag_repro_issues.py` 新增 ADR-102 案例，共 **34 案例全數通過**。用真的
  matplotlib Axes 檢查 `ax.lines` 的實際 artist 與顏色 (不是空殼斷言，P-28
  教訓)，並驗證計算失敗時 K 線圖仍正常畫出。

### 誠實限制

1. **支撐壓力不是精確科學。** 本模組只做「把成交量在價格上的分布算出來」
   這件可驗證的事，並用透明規則挑出節點；它**不預測**價格會不會真的在那裡
   反轉。規則刻意保持簡單清楚，使用者知道我在算什麼，勝過看起來很厲害的黑箱。
2. **指數通常沒有成交量**，此時退化成「等權重」＝純價格停留分布。仍有參考
   價值 (價格待越久的區間箱數越多)，但那不是真正的量價關係。
3. **期貨的量價分布會受換月影響**：跨月合約的成交量堆疊在一起，遠月的量價
   結構可能失真。用 R1 連續合約時尤其要留意。
4. 分K 週期同樣可用，但箱寬受 tick 下限限制，短週期的價格範圍窄時箱數會
   自動變少，解析度不如日K。
5. **尚未接進策略引擎**：目前只有圖上呈現，不能用「價格接近 POC」當進場
   條件。要做需另開 ADR (牽涉「接近」的容差定義與回測口徑)。
6. 未做右側橫向成交量分布圖 (使用者這次未選)。資料都在
   `result['profile']['bin_volumes']`，日後要畫不需重算。

### 需使用者實機驗證

1. 開啟 App，查詢任一股票 (如 2330) → 勾選工具列的「支撐壓力」，確認主圖
   出現紅綠水平線與右側價格標註。
2. 切換區間下拉 (可見範圍 / 固定N根)，確認線會跟著重算。
3. 放大/縮小/平移圖表 (可見範圍模式)，確認點位跟著可見資料變化。
4. 換成台指期 (TXF) 與加權指數 (^TWII)，確認點位是合理的整數/合法檔位。
5. 看系統日誌，確認有列出點位數字與強度。
