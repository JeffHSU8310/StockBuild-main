
## ADR-088：內建條件策略新增「委託方式」選項 — 限價/市價/範圍市價

### 背景

使用者在「編輯策略」對話框截圖圈出「讓價檔數」與「模式」兩個既有欄位附近,
要求新增「限價單/市價單/範圍市價單」三種委託方式選項。原本系統下單一律
寫死限價 (`_place_strategy_order` 固定 `price_type=LMT`,只靠「讓價檔數」
往成交方向讓幾檔提高成交率),沒有市價/範圍市價可選。

### 委託方式與交易種類的對應規則 (依交易所規則,不可全部開放)

- **股票 (整股)**:限價 / 市價 兩種 (TWSE 支援)。
- **零股**:依【鐵則6】只能限價 ROD,這是既有的硬性規定 (`core/order_rules.py`
  的防呆檢查),市價/範圍市價 一律不開放,不因這次新增選項而放寬。
- **期貨**:限價 / 市價 / 範圍市價 三種都支援 (範圍市價 = TAIFEX 的 MKP,
  「Market with Protection」,只有期貨/選擇權才有這個委託方式)。

### 修改內容

**`core/strategy_engine.py`**:
1. 新增 `PRICE_TYPES = ('限價', '市價', '範圍市價')` 與
   `FUTURES_ONLY_PRICE_TYPES = ('範圍市價',)`。
2. 新增 `price_type_of(strategy)`:讀策略的委託方式,防呆三層——
   (a) 沒設定/不合法值退回限價,(b) 零股一律強制限價 (鐵則6),
   (c) 範圍市價只對期貨有意義,非期貨一律退回限價。
3. `new_strategy()` 新增預設欄位 `'price_type': '限價'` (=舊行為,完全相容
   既有已存檔的策略,沒有這個欄位的舊策略讀出來一樣是限價)。
4. `validate_strategy()` 新增檢查:委託方式必須是 `PRICE_TYPES` 之一;
   範圍市價只能搭配期貨;零股搭配非限價一律擋下 (訊息明確標「鐵則6」)。

**`stock_app_pro.py`**:
1. `_place_strategy_order()`:依 `strategy_engine.price_type_of(strategy)`
   決定 `price_type` 常數 (期貨:LMT/MKT/MKP;股票:LMT/MKT;零股固定LMT)。
   限價才計算 `slippage_ticks` 讓價後的價格,市價/範圍市價 一律價格送 0
   (依市場當下成交,鏡射既有手動下單面板 `execute_order` 的市價寫法)。
2. `_qt_open_editor()`(內建條件策略編輯器):在「模式」下拉旁新增「委託方式」
   下拉,選項依當下「交易種類」動態切換 (期貨=三選項、股票=限價/市價、
   零股=只鎖限價一種且提示鐵則6原因)。切換交易種類時自動同步選項清單,
   若原本選的委託方式在新交易種類下不合法就退回限價。存檔寫入
   `s['price_type']`。

**範圍**:這次只加在「內建條件策略」編輯器 (`_qt_open_editor`,截圖中的
畫面)。「楚狂人之終極波段」與「自訂 Python 策略」的編輯器沒有這個下拉,
但因為 `default_strategy()`/`new_strategy()` 都會帶出 `price_type='限價'`
預設值,`_place_strategy_order` 對它們一樣正確運作 (=原本的限價行為,
沒有變化)。手動下單面板 (`execute_order`) 本來就已經有限價/市價可選
(沒有範圍市價),這次沒有變動,不在本次範圍內。

### 效果

- 內建條件策略可以選「市價」或「範圍市價」(期貨限定) 送單,不再只能限價,
  適合想要「訊號一到就立刻成交、不計較滑價」的使用情境。
- 零股/股票交易所規則不允許的委託方式在 UI 上就選不到,且
  `validate_strategy` 存檔時再擋一次 (雙重防呆,鏡射系統既有的「本地端先
  驗證,不完全依賴券商回傳錯誤」原則,鐵則9)。

### 測試

`tests/test_core.py` `TestTradeTypeAndAbsStops` 新增 6 個測試:預設值為限價、
範圍市價搭期貨合法、範圍市價搭股票被擋、市價搭股票合法、零股搭市價被擋且
`price_type_of` 防呆退回限價、不合法字串防呆退回限價但存檔仍被擋。連同既有
測試共 313 個全數通過。

### 需使用者實機驗證

1. 「編輯策略」對話框:交易種類選「期貨」時,「委託方式」應可選 限價/市價/
   範圍市價 三種;選「股票」只剩 限價/市價;選「零股」應鎖死只有「限價」
   且旁邊提示文字說明鐵則6原因。
2. 期貨策略選「市價」或「範圍市價」啟用實單:下單時系統日誌應顯示對應的
   委託方式字樣 (非「限價xxx」),且送出的委託單在永豐看盤軟體上應顯示為
   對應的市價/範圍市價單,不是限價單。
3. 股票策略選「市價」:比照上一點確認委託單類型正確。
