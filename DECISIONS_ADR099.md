
## ADR-099：diag_repro_issues.py 6 個案例長期失效 — 測試沒跟上程式演進

### 背景

使用者要求「再 debug 一次」。全面檢查發現 `diag_repro_issues.py` 有 6 個案例
持續 FAIL。逐一追查後結論是：**程式本身沒有 bug，6 個 FAIL 全部是診斷腳本
自己過時**——程式後續演進 (ADR-046/047/048、ADR-070、ADR-074、ADR-096) 改變
了行為或介面，但診斷腳本沒有同步更新，於是這些案例長期形同虛設。

這比「程式有 bug」更危險：這 6 個案例本來是要保護下單清單、快取秒開、
自動交易安全行為等關鍵路徑的，失效期間等於**完全沒有保護**，而且失敗訊息
還會誤導後人以為是程式壞掉。

### 六個 FAIL 的真正原因

**1~3. 下單清單三案例** (`my_orders` 應有1筆 / 已加入清單日誌 / 委託回報替換)
ADR-096 把 `place_order()` 改成背景執行緒 + `safe_after()` 排回主執行緒
(修主程式當機的根因)。但診斷腳本仍在 `_confirm_and_place_order()` 回來後
**立刻**斷言——此時委託根本還沒寫進 `my_orders`,測到的是中間狀態。
→ 新增 `place_and_settle()` helper:送單後等背景執行緒把結果排入 after 佇列
  (最多 5 秒)、再 `flush_after()` 沖掉,才斷言。

**4. ADR-024「首載應下載1次,實際6」**
6 次是「搶先出圖 1 段 + 分段補全 5 段」,來自 ADR-046/047/048 的
`_download_kbars_chunked` (chunk_days=90)。這是刻意的優化 (P-36:kbars 只回
分K,長週期一次抓太多天會慢到不行),斷言卻還停在分段下載加入之前的預期。
→ 改驗「有下載且每段起點不重複」,不再寫死次數 (寫死次數本來就會隨優化腐化)。

**5. ADR-035/036「模擬模式不可下真實單」**
**這則失敗訊息嚴重誤導**——它把兩個條件寫在同一個 assert:「有模擬日誌」and
「`api.placed==[]`」。實測探針確認 `api.placed==[]`,**真實單一張都沒送出,
不是安全問題**;失敗的是前半條件:ADR-070 的交易時段閘門在非交易時間會直接
跳過評估 (正確行為),所以沒有模擬日誌。診斷腳本是週末/盤後跑的,必然失敗。
→ 診斷用的策略加 `session_gate: False` (程式本來就支援的旁路),讓案例與時鐘
  無關。日後若再遇到「兩個條件塞同一個 assert」,應拆開,避免訊息誤導。

**6. ADR-041「同一K棒邊界內不應重複評估」**
測試把 `_qt_fetch_closed_bars` 換成 `lambda st,c2,a2:` (三個位置參數),但
ADR-074「看A做B」之後,程式呼叫時多帶了 `tf`/`cache_sym`/`cache_market`
三個關鍵字參數 → mock 拋 `TypeError` → 被外層 except 吞掉 → `calls` 永遠是
空的,`n1>=1` 必然失敗。
→ mock 改成 `lambda *a, **k:` 全轉發。**教訓:測試替身的簽名要用 \*args/\*\*kwargs
  轉發,否則被替身的函式一加參數,測試就會靜默失效。**

### 額外發現:測試隔離不足 (修好上述之後才浮現)

修完前 6 項後,ADR-024 又卡在「同商品換週期不應重訂閱/退訂」。加 traceback
探針查出:多出來的 2 次 `subscribe` 來自 `watchlist_quote_worker` 這條**獨立
背景 daemon thread** (自選股報價,ADR-042 期貨/指數會訂閱),跟主圖換週期完全
無關。**ADR-024「同商品換週期不重訂閱」的承諾其實一直有守住**,是斷言比對
全域訂閱計數才誤判。
→ `FQ.subscribe` 記錄呼叫來源,新增 `chart_subs()` 只數主圖 (fetch_data_worker)
  路徑的訂閱,斷言改用它。

### 結果

`diag_repro_issues.py` 從 25 PASS / 6 FAIL 變成 **31 PASS / 0 FAIL**,
連續重跑結果一致 (不再受執行時間、背景執行緒干擾)。
`tests/test_core.py` 384 個測試不受影響、全數通過;`diag_crossref.py` 無斷鏈。

### 誠實聲明

本次**沒有修改任何產品程式碼**,只改診斷腳本。也就是說:這次 debug 的產出
不是「修好了幾個 bug」,而是「確認程式沒 bug,並修好了長期失效的測試」。
被修好的 6 個案例從現在起才真正開始提供保護。

### 給未來 session 的提醒

改動下單流程、快取策略、策略評估路徑、或任何被 diag 腳本 mock 的方法簽名時,
**要順手跑 `python diag_repro_issues.py` 並確認是 0 FAIL**。看到 FAIL 不要
預設「這是既有問題」就跳過——本次 6 個案例正是這樣一路被忽略累積下來的。
