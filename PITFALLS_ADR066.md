
### P-84　共用的過濾/守門函式寫對了,不代表每個「產生動作」的分支都真的有呼叫它
- **症狀**：策略編輯器能設定「進場時間窗」(`entry_time_start`/`entry_time_end`/
  `specific_entry_time`),但內建策略設了之後完全不生效——不管是不是在時間窗內,
  進場條件一成立就進場,使用者不會收到任何錯誤或提示。
- **根因**：`filter_intents_by_time()` 這個過濾函式本身邏輯是對的,
  `core/custom_strategy.py` 的三個分支也都正確呼叫了它;但
  `core/strategy_engine.py` 的 `evaluate_strategy()` 裡「會產生 OPEN intent」
  的 3 個分支 (一般進場、買進持有的 dca、買進持有的 single/accumulate) 全部
  在組好 intent 後直接 `return`,沒有一個真的呼叫這個過濾函式——寫對一個共用
  函式,不代表每個呼叫端都記得接上它,尤其當同一個檔案裡有好幾個平行的
  「產生動作」分支時,很容易改了一個、忘了其他幾個。`tests/test_core.py`
  也從未替這個功能寫過任何測試,等於這個功能上線後從沒被驗證過真的有效。
- **正確做法**：
  1. 新增一個「過濾/守門」類的共用函式時,要把它的所有潛在呼叫點列成清單
     逐一確認 (本例:凡是會產生 OPEN 或 CLOSE intent 的 `return` 之前,都要問
     「這裡有沒有經過這個過濾函式」),不能只驗證函式本身的邏輯正確。
  2. 這類「使用者設定了限制,但限制沒有生效」的 bug 比「動作時明明沒發生」更難
     發現——因為使用者看到的是「有動作」,只是動作發生在不該發生的時間,容易
     被誤以為是策略本身的訊號問題,而不會聯想到是時間窗設定沒生效。
  3. 任何「純過濾、不改變核心決策」的函式,都應該支援可選的除錯/紀錄管道
     (見 P-85 的做法),否則就算補上呼叫,下次某個分支又漏接時還是無聲無息。
- **出處**：ADR-066。

### P-85　過濾規則生效時 (排除了某個原本會發生的動作),也要留下可查的痕跡
- **症狀**：即使補上了進場時間窗過濾,使用者設定的時間窗把某次原本會成立的
  進場/出場擋下來時,使用者依然完全看不到「這一根其實條件成立,只是被你自己
  設定的時間窗擋下」——跟 P-58/P-70/P-83 講的「背景失敗必須看得見」是同一個
  模式,只是這次的「失敗」其實是「使用者自己設定的限制正常生效」,但一樣需要
  可見性,否則使用者會誤以為系統又出現「條件到了卻沒動作」的 bug,回報一次
  本來不需要回報的問題。
- **正確做法**：過濾函式提供可選的 `runtime`/紀錄參數,每次呼叫都把「這次排除
  了什麼、為什麼」寫進去 (沒有排除就蓋成空,不讓舊訊息殘留),呼叫端 (GUI)
  比照既有的 `condition_errors` 模式印出日誌。這樣「這是功能生效」與「這是
  真的 bug」在使用者眼中才有辦法區分。
- **出處**：ADR-066。

### P-86　修正一個「訊號時機」相關的斷言時,診斷腳本裡的等價斷言要一起找出來改
- **症狀**：`diag_repro_issues.py` 裡有兩個案例的斷言,分別對應
  `tests/test_core.py` 已經在 ADR-064 修正過的兩個斷言 (T+1 開盤成交模型
  導致「進場點=金叉點」與「定期定額不超支預算」都要放寬),但這支診斷腳本
  從未被同步,穩定重現「明明邏輯是對的,診斷卻回報 FAIL」的假警報,直到下一次
  複查才被發現。
- **根因**：本專案的收尾檢查同時仰賴 `tests/test_core.py`（unittest）與
  `diag_repro_issues.py`（假 tkinter 環境的端到端腳本）兩套機制,但兩者是各自
  獨立的檔案。當一次修正只想到「改 unittest 的斷言」時,很容易忘記
  `diag_repro_issues.py` 裡可能有語意相同、卻獨立寫死的另一份斷言——這是
  P-57「跨檔案改動沒有同批交付所有呼叫端」在「測試/診斷腳本之間」的變體,
  呼叫端不是程式碼,而是驗證邏輯本身。
- **正確做法**：
  1. 修正一個因為「行為模型改變」(例如成交時機、定價公式) 而導致的斷言時,
     用 `grep` 搜尋整個專案 (含 `diag_*.py`) 有沒有基於同一個舊假設的其他
     斷言,不要只改 `tests/test_core.py` 就收工。
  2. 收尾檢查若同時列了 unittest 與診斷腳本兩道關卡,兩者都要真的跑過、看到
     全過的輸出,不能因為「unittest 全過」就假設診斷腳本也一定沒問題——這正是
     本次能發現這兩處遺漏的原因 (若沒有真的執行 `diag_repro_issues.py`,不會
     發現)。
- **出處**：ADR-066（ADR-064 的收尾遺漏）。
