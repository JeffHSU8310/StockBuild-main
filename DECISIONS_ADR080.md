
## ADR-080：主圖按需延伸歷史 — 只有 MA240 開啟或週期為周K/月K 時才延伸,裁到約 3 年

### 背景

ADR-079 讓主圖完全不延伸歷史 (只顯示 `SJ_DAYS` 券商原生範圍),換來切換/縮放/
平移都明顯變快,但代價是誠實記在 ADR-079 裡:MA240 這類長週期指標在日K上幾乎
顯示不出來 (日K 原生只有 ~250 根),周K/月K 的原生範圍也偏薄。

使用者的下一步要求:「如果可以的話,當我要顯示MA240,或是周K/月K 週期時,再到
資料庫內抓資料載入近3年的數據。」——比「完全不延伸」或「一律延伸 20 年」更好
的折衷:**只在真的需要深歷史的情境才延伸,而且裁到約 3 年**,不是把整段
20 年攤開。

### 決策

延伸的觸發條件 (符合任一即延伸):
1. 目前週期是 **周K 或 月K**(使用者原話)。
2. **日K 但有開 MA240**(推廣使用者的「顯示MA240」為一般規則:不限日K,任何
   週期只要 MA240 這條均線被勾選顯示,都算需要深歷史)。

延伸時**裁到最近約 3 年 (1095 天)**,不是 `_extend_with_yahoo`/
`_extend_with_taifex` 原本能拿到的完整深度 (yahoo 最長 20 年、期交所全 series
可達 20+ 年)——避免重蹈 ADR-079 之前「一延伸就是上千~上萬根」的覆轍。

其餘情況 (日K 未開 MA240) 維持 ADR-079 的快速路徑,完全不延伸。

### 做法

**`_extend_with_yahoo`/`_extend_with_taifex` 新增 `max_days` 選填參數**:
延伸/合併完成後,若有帶這個參數,裁到「最後一根往前 max_days 天」。
**不帶這個參數 (預設 None) 完全不裁切**——回測 (`_qt_backtest_worker`)、
最佳化 (`_qt_optimize_worker`)、compare worker 呼叫這兩個函式時都不帶
`max_days`,拿到的仍是完整延伸深度,**這次改動完全不影響回測/最佳化**。

**主圖 `_publish()` 新增 `allow_extend` 參數 + `want_deep` 判斷**:
- `want_deep = (tf in ('周K','月K')) or ma240_on`。
- `allow_extend=True` 時且 `want_deep` 成立,才呼叫延伸函式 (帶
  `max_days=1095`)。
- **只在「快取命中」與「背景完整段」這兩條路徑套用**(`allow_extend=True`)
  ——這兩條可能是「唯一一次出圖」,必須把深歷史準備好。**「快速段」(搶先
  出圖用) 一律不延伸**,維持秒開,深歷史由緊接著的背景完整段補上,使用者
  幾乎感覺不到差異。

**`ma240_on` 從 UI 執行緒讀取並傳入背景 worker**:`self.ma_shows`/
`self.ma_periods` 是 tk 變數,只能在 UI 執行緒讀。`start_fetch_thread()`
(已經在 UI 執行緒) 讀出「是否有任一條 MA 開啟且週期設為 240」,經
`fetch_data_worker` → `_fetch_data_worker_impl` 傳進背景執行緒,供
`_publish` 內的 `want_deep` 判斷使用。

### 效果

- 日K 沒開 MA240:維持 ADR-079 的快 (~250 根原生資料)。
- 日K 開了 MA240:延伸至約 3 年 (~750 個交易日),MA240 有約 500 根的顯示區間,
  比之前 (幾乎完全不顯示) 好很多,渲染成本仍遠低於延伸前的 20 年全灌
  (~4900 根)。
- 周K/月K:一律延伸至約 3 年 (~150 週 / ~36 月),比 ADR-079 的原生範圍
  (周K 本來就有 3 年、月K 原生 5 年會變成 3 年——見下方取捨) 更一致,且遠比
  延伸前的全歷史快。

### 已知取捨 (誠實)

- **月K 原生 SJ_DAYS 是 5 年,套用 want_deep 後裁到 3 年,反而變少**。這是
  照使用者原話「周K/月K…載入近3年」實作的結果 (視為刻意決策,不是疏漏)。
  若使用者實際用起來覺得月K 3 年不夠,之後可以把 `EXT_MAX_DAYS` 對月K
  另外設定更長,這次先照原話做。
- 只有「日K 且 MA240 開啟」才會延伸;若使用者之後開的是 MA120/MA200 等其他
  長週期,不在本次判斷條件內 (使用者只提到 MA240),之後有需要可以擴充
  `want_deep` 的判斷式 (例如「任一開啟中的 MA 週期 > SJ_DAYS 對應根數的
  一定比例」)。

### 相關程式位置 (stock_app_pro.py)

- `_extend_with_yahoo` / `_extend_with_taifex`:新增 `max_days` 選填參數,
  裁切邏輯。
- `start_fetch_thread`:讀 `ma240_on` (UI 執行緒),傳入 worker。
- `fetch_data_worker` / `_fetch_data_worker_impl`:簽名新增 `ma240_on=False`。
- `_publish()`:新增 `EXT_MAX_DAYS`/`want_deep`/`allow_extend` 邏輯;快取命中
  與背景完整段的呼叫改 `allow_extend=True`,快速段呼叫不帶 (預設 False)。

### 需使用者實機驗證

1. 日K、MA240 關閉:應維持目前的快 (ADR-079 的體感)。
2. 日K、開啟 MA240:MA240 線應能看到約 500 根左右的顯示區間 (不再幾乎全空),
   渲染速度應仍明顯快於延伸前的舊行為。
3. 切到周K/月K:應自動延伸到約 3 年;若覺得月K 3 年不夠長,告訴我,我再幫
   月K 單獨拉長 `EXT_MAX_DAYS`。
4. 「🔬 回測」/「🎯 參數最佳化」的歷史深度與結果應與這次改動前完全一致。
