
## ADR-117：實機回報的四個問題（選股欄位錯位 / nan / 帳戶無法分辨）

使用者升級 shioaji 1.7 並實機操作後回報。其中「`^TWOII`（IX0043）目前沒有
問題」——ADR-114 追記二的修正確認有效。

### 一、實單帳戶下拉三個選項一模一樣

**根因**：`list_accounts()` 用 `type(a).__name__.replace('Account','')` 判斷
帳戶種類。1.5.6 用不同類別（`StockAccount` / `FutureAccount`）區分，**1.7 把
它們統一成同一個 `Account` 類別**，改用 `account_type` 欄位（Stock/Future/Intl）。
於是三個帳戶的種類都算出空字串，顯示文字完全相同。

**修法**（`core/sj_compat.account_label()`）：
- 優先讀 `account_type`，類別名稱只當退路
- **顯示文字一定帶帳號** —— 種類與戶名都可能重複（同一人兩個證券戶就長一樣），
  只有帳號是唯一的；帶上它就算種類判斷失敗也還分得出來
- 未簽署的帳戶標 `⚠未簽署`（那種帳戶送單會被券商退，先看到比較好）

顯示結果：`證券 1234567｜許super` / `期貨 7654321｜許super` /
`複委託 9999999｜許super｜⚠未簽署`

### 二、三、選股表頭與資料對不起來

**根因**：同一個 Treeview 有兩種欄位配置，但**只有回測那邊會重設**——
選股的欄位寫在建構函式裡（只執行一次），回測的寫在 `_sc_show_backtest()` 裡
（每次都設）。於是切換是單向的：選股 → 回測正確，回測 → 選股不會切回來，
12 個值被塞進 7 個欄位。

這也解釋了使用者說的「進行回測條件後，欄位名稱才對的到」——回測那邊本來就
會設，所以只有它是對的。

**修法**：欄位配置集中成 `SC_COLUMNS`，提供 `_sc_apply_columns(mode)`，
**每個顯示函式畫資料前都先呼叫**。變成兩邊都必須做的同一件事，不可能只有
一邊記得。

### 四、結果出現字面上的 `nan`

**根因**：`str(row.get(COL_NAME) or '')` —— **NaN 在 Python 是 truthy**，
`nan or ''` 得到 nan，`str()` 後就是 `'nan'`。

有意思的是同一段程式碼裡 `industry` **有**寫 `pd.isna()` 防護所以顯示空白，
`name` 沒寫就中招 —— 兩個欄位處理方式不一致，正是「同一件事寫兩份」的後果。
這也對應到截圖裡 1591 那一列：名稱 `nan`、產業空白。

**修法**：統一用 `market_screener._text()`（先 `pd.isna()` 再轉字串，順便擋掉
字串 `'nan'`），`code` / `name` / `industry` 都走它。

### 已驗證（headless）

- `tests/test_core.py` **544 全過**（新增 4 個 NaN 測試，含端到端）
- `diag_repro_issues.py` **42 案例全過**（新增 1 個涵蓋三個問題）
- **突變測試**（把三處各自改回原本的錯誤寫法，確認都會被抓到）：
  - 選股不切回欄位 → FAIL「選股應有 12 欄，實際 7」
  - `name` 改回 `str(v or '')` → FAIL「表格出現 nan」
  - 帳戶改回用類別名稱 → FAIL（三個標籤變成 `_Acc …`）
- 診斷斷言除了比對表頭，也比對「資料列欄位數 == 表頭欄位數」——那是錯位的
  直接證據

### 需使用者實機驗證

1. 策略編輯器的「實單帳戶」下拉，確認三個帳號**顯示成不同的文字**，
   且看得出證券／期貨／複委託與帳號末碼。
2. 選股 → 按「回測條件」→ 再按「開始選股」，確認**表頭切回代號/名稱/產業…**，
   資料對得起來。
3. 反過來再按一次「回測條件」，確認表頭也正確切成期別/訊號日/…
4. 確認結果表不再出現 `nan`（缺名稱的股票顯示空白）。
