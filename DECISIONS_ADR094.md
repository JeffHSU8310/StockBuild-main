
## ADR-094：報價更新速度加快 (鐵則5 調整無串流快照節流間隔 5→3 秒)

### 背景

使用者要求把幾個報價更新節奏都加快：

1. 主圖/目前選取商品「有串流時」的上屏迴圈：0.25 → 0.1 秒。
2. 自選股清單「期貨/指數串流」上屏：0.5 → 0.25 秒。
3. 自選股清單「股票批次快照」：5 → 2.5 秒。
4. 主圖/目前選取商品「無串流時」的 fallback snapshot 節流間隔：5 → 3 秒。

第4項 (以及連帶的第3項，兩者都是 `snapshots()` 呼叫的節流間隔) 牽動
CLAUDE.md 鐵則5：「對 `snapshots()` 的呼叫必須節流…目前定案：無串流
fallback 快照間隔 ≥ 5 秒。若要調整這個數字，必須先查 shioaji 官方文件
的流量限制並記錄依據到 DECISIONS.md。」因此動工前先查證。

### 查證：shioaji 官方文件的次數限制

來源：[Shioaji 使用限制](https://sinotrade.github.io/zh/tutor/limit/)

- `credit_enquires`、`short_stock_sources`、`snapshots`、`ticks`、
  `kbars` 這幾類查詢的**總次數**上限是 **10 秒 50 次**(約每秒 5 次)。
- 另外 `ticks` 盤中不得超過 10 次、`kbars` 盤中不得超過 270 次 (這兩個
  跟本次改動的 `snapshots()` 節流無直接關係，本專案的即時報價本來就
  優先用 v1 typed streaming callback，不是用 `ticks()` 查詢)。
- 流量 (資料量) 依近30日成交額/口數分級每日 500MB~10GB，開盤日
  8:00 重置。
- 超過次數限制：暫停服務 1 分鐘；連續違規：暫停該 IP/ID 使用權。
- 官方明確建議：「盤中需要即時資料，請改用行情訂閱 (`api.subscribe()`
  或 SSE 串流)」，「最常見的錯誤做法是以輪詢方式使用這些查詢型行情
  API 替代即時報價」——本專案已遵守這個原則 (鐵則2：有串流一律用
  v1 typed callback，`snapshots()` 只在**沒有串流**的空檔當備援)，
  這次調整沒有違反這個大原則，只是備援輪詢的節流間隔本身。

### 目前 (調整後) 三個輪詢式 `snapshots()` 呼叫點合計負載估算

| 呼叫點 | 間隔 | 換算 (次/秒) | 備註 |
|---|---|---|---|
| 主圖無串流 fallback (單一商品) | 3 秒 | ~0.33 | 只在盤後/剛切換商品時觸發 |
| 自選股股票批次快照 (整批一次呼叫) | 2.5 秒 | ~0.4 | 不管清單幾檔股票都是同一次呼叫 |
| 量化交易即時標記價 (`_qt_update_realtime_pnl`，批次) | 3 秒 (未改動) | ~0.33 | 需要 `paper_accts` 有持倉且已登入才會跑 |

三者同時全開的最壞情況合計約 **1.06 次/秒**，相對官方 10秒50次
(約5次/秒) 的上限，仍只用掉約 **21%**，有接近 5 倍的安全margin。
即使加上其他非輪詢、事件觸發式的 `kbars`/合約解析查詢 (K棒邊界觸發、
不是持續輪詢)，也不會逼近上限。

### 決定

四項全部照使用者要求的數字調整：

- `fetch_realtime_worker`：streaming 上屏迴圈 `time.sleep(0.25)` →
  `time.sleep(0.1)` (純粹「多快把已收到的串流資料畫到畫面」，
  **不會**增加任何 API 呼叫，streaming 分支不受節流限制)。
- `fetch_realtime_worker` 無串流 fallback：`>= 5` → `>= 3` 秒。
- `watchlist_quote_worker`：迴圈本身 `time.sleep(0.5)` → `time.sleep(0.25)`。
  這個迴圈同時控制「期貨/指數串流上屏」(每輪都上屏，改成 0.25 秒
  一次) 跟「股票批次快照」(`i % 10 == 0`，10輪×0.25秒=2.5秒一次，
  原本10輪×0.5秒=5秒)——改一行迴圈間隔剛好同時達成使用者要的兩個
  數字，不用分開兩個計時器。

### 已知風險 (誠實)

- **ADR-042 曾經記錄過的 UI 效能教訓**：「20檔股票串流曾造成全畫面
  遲鈍，5秒已是API配額與流暢度的平衡點」——那次的問題是**串流**
  (逐檔訂閱) 造成的畫面卡頓，不是批次 `snapshots()` 快照本身；這次
  股票快照維持「整批一次呼叫」的設計沒有變，只是重繪頻率從5秒變
  2.5秒，理論上负載加倍但仍是同一種輕量操作 (更新表格文字顏色，
  不是重繪整張圖表)。不過**如果自選股清單檔數很多**，2.5秒批次快照
  +重繪表格是否會感覺卡頓，需要使用者在有畫面的機器上實際感受，
  若覺得變慢，最直接的退回方式是把 `watchlist_quote_worker` 的
  `time.sleep(0.25)` 調回 `0.5`(股票快照連帶變回5秒)。
- API 次數負載估算是「目前程式碼已知的輪詢式呼叫點」加總，如果之後
  再新增其他輪詢 `snapshots()`/`kbars()`/`ticks()` 的地方，需要重新
  評估合計負載是否還在安全範圍內，不能假設這次的margin永遠夠用。

### 測試

純數值/節奏調整，不影響任何可離線測試的 `core/`/`data/` 邏輯，
`tests/test_core.py` 384 個測試全數通過 (不受影響，僅供確認沒有
連帶弄壞其他東西)。GUI/報價相關的實際流暢度與 API 穩定性，
**務必**在有畫面、已登入 shioaji 的機器上實機驗證：

1. 盤中開著主圖走一段時間，確認報價/五檔更新明顯更即時，沒有出現
   `snapshots()` 相關的錯誤訊息或報價忽然全部失效 (那是流量/次數
   超限的徵兆)。
2. 自選股清單放多檔股票 (例如 20 檔以上)，觀察畫面重繪是否流暢，
   若感覺卡頓可考慮把股票快照間隔調回原本的5秒。
3. 盤後 (無串流) 切換商品，確認報價每 3 秒左右更新一次，沒有頻繁
   出現失效或空值。
