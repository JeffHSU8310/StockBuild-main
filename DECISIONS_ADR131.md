# ADR-131:主圖布林參數列被說明文字蓋掉(grid 撞格)+ 改成兩組完整布林通道

## 狀態

已實作,**尚未實機驗證**(分支 `claude/bollinger-two-sets`)。

## 背景:使用者的截圖

「主圖指標參數設定」對話框裡看得到 MA1~MA6 六列(開關/類型/週期/色彩),
**布林那一整列卻不見了** —— 只剩一行:

```
※ 按下方按鈕會記住這些設定,下次開啟程式直接沿用。   (σ2=0不畫第二組)  [青 (#00E5FF) ▾]
```

一行說明文字,旁邊露出半截「(σ2=0不畫第二組)」,再旁邊一個**沒有標題的色彩
下拉** —— 那就是布林的色彩選單,孤零零地掛在那裡。

> 「主圖指標布林的參數不見了。另外,我要可以自己設定中線&上下線各2組的參數。」

---

## 問題一:grid 撞格(從第一版就有,不是這幾輪的迴歸)

```python
tk.Checkbutton(dlg, text="布林通道", ...).grid(row=8, column=0, ...)
bb_frame                              .grid(row=8, column=1, columnspan=2, ...)
ttk.Combobox(dlg, textvariable=self.bb_color, ...).grid(row=8, column=3, ...)
tk.Label(dlg, text="※ 按下方按鈕...").grid(row=8, column=0, columnspan=4, ...)   # ← 又是 row 8
```

最後那行說明文字寫成 `row=8, columnspan=4`,**跟布林的所有控制項同一個 grid
儲存格**。tkinter 的 grid 是「後放的蓋前放的」,於是使用者只看得到說明文字。

`git log -S` 查過:**這一行從 initial commit 就是 row=8**,不是這幾輪改出來的。

修法是把說明文字移到自己的列(row 13),布林區重新編號 row 8~12。

---

## 問題二:使用者要「中線 & 上下線各 2 組」

原本的模型是 **1 條中線 + 2 組上下線**(`bb_period` + `σ1`/`σ2`)。
使用者要的是**兩組完整的布林通道** —— 每組有自己的中線與自己的上下線。
(這一點在動工前跟使用者確認過,選的是「兩組完整布林」。)

### 新的參數模型

| | 開關 | 中線類型 | 中線期間 | 上下線 σa | 上下線 σb | 色彩 |
|---|---|---|---|---|---|---|
| 第1組 | `bb_show` | `bb_type` | `bb_period` | `bb_std1` | `bb_std2` | `bb_color` |
| 第2組 | `bb2_show` | `bb2_type` | `bb2_period` | `bb2_std1` | `bb2_std2` | `bb2_color` |

也就是**中線 2 條、上下線每組 2 對(共 4 對)**,兩組完全獨立開關。
中線類型可選 SMA/EMA/WMA(原本固定 SMA)。

## 決定

### A. 第1組**沿用原本的欄位名**,新功能的風險全壓在第2組

```python
bollinger_set(df, bb_period, bb_std1, bb_std2, ma_type=bb_type, prefix='BB',  with_width=True)
bollinger_set(df, bb2_period, bb2_std1, bb2_std2, ma_type=bb2_type, prefix='BB2', with_width=False)
```

`prefix='BB'` 產生的欄位名(`BB_MID`/`BB_UPPER`/`BB_LOWER`/`BB_UPPER2`/
`BB_LOWER2`/`BB_WIDTH`)與 ADR-029 **完全相同**,所以繪圖、十字線提示、
BBW 副圖、`panel_columns` 一個字都不用改。

`BB_WIDTH` 只由第1組產生 —— BBW 是獨立副圖,只有一條線,不改它的語意。

有一條單元測試與一條診斷斷言專門守著「第1組欄位名沒被改掉」(把 prefix 改成
`BBX` 的突變會紅)。

### B. `moving_average()` 收斂成單一出處

布林中線要支援 SMA/EMA/WMA,而那段算式原本內嵌在 MA1~MA6 的迴圈裡。
抽成 `core.indicators.moving_average(series, period, kind)`,MA 迴圈也改用它 ——
不然就是兩份各自維護、遲早分歧(P-67)。`MA_TYPES` 常數同時給 UI 的下拉用。

### C. 標準差一律取收盤價的 rolling std

中線可以是 EMA/WMA,但**σ 的基準不隨中線類型改變** —— 那是布林通道的定義。
有一條單元測試釘住這件事(換中線類型時 `BB_STD` 必須相同、`BB_MID` 必須不同)。

### D. 舊設定檔不需要遷移

`_load_indicator_settings` 對新 key 一律 `d.get(key, 目前值)`,舊檔沒有這些 key
就沿用程式碼預設 = **第1組維持原樣、第2組預設關閉**。使用者的既有設定不受影響。

### E. 對話框放大到 620×520

布林從 1 行變成「標題 + 欄位名 + 兩組 + 兩行說明」共 5 行。沿用 400×350 會把
「確認並套用」按鈕擠出視窗外 —— 那等於**設定存不了**,比參數看不見更糟。

---

## 診斷:用 AST 靜態檢查 grid 撞格

這個 bug 的**類別**是「兩個 widget 佔到同一個 grid 儲存格」,不是某一行字串。
所以診斷不去比對「說明文字在第幾行」,而是:

1. 用 `ast` 找出 `open_main_settings` 裡所有 `.grid(...)` 呼叫;
2. 取出字面值的 `row`/`column`/`columnspan`/`rowspan`(迴圈產生的 `row=i+1`
   不是字面值,跳過);
3. 展開成儲存格集合,**任兩個 widget 不可以佔到同一格**。

把說明文字改回跟布林第1組同一列的突變,診斷立刻報:

```
主圖指標設定的 grid 撞格:第 2716 行與第 2707 行都佔到 (row=10, column=0)
—— tkinter 會讓後放的蓋掉前放的,參數就會像使用者截圖那樣不見
```

> 這比「檢查某個字串在不在」有價值得多:**換個位置再撞一次照樣抓得到**。
> 診斷環境的 tkinter 是 mock(`diag_mock_tkinter`),量不到真實幾何,
> 靜態分析是這裡唯一能做到「真的檢查版面」的方法。

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| **說明文字改回 row=10(重現原始 bug)** | 診斷紅:`grid 撞格 ... (row=10, column=0)` |
| 第2組沿用第1組的參數 | 單元紅;診斷紅:`兩組中線 (SMA20 / EMA60) 算出來一樣` |
| `σb=0` 也照畫第二條 | 診斷紅:`σb=0 不該畫第二條上下線` |
| 第1組欄位改名 (`BB` → `BBX`) | 診斷紅:`第1組布林欄位 BB_MID 不見了` |

## 驗證

- `python tests/test_core.py` → **656 個全過**(原 643,新增 13)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **57 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

---

## 附帶修掉一個我自己埋進 main 的診斷迴歸(P-97 第三次)

做這一輪時順手用 `import diag_repro_issues` 探路,結果**兩個案例是紅的**:

```
ADR-122: ... FAIL  日K 類快取放了 10 分鐘仍應命中
ADR-127: ... FAIL  35 分鐘內沒有跨過開盤,不可以重抓 (實際多抓了 4 次)
```

當時是 **15:03** —— 剛過夜盤 15:00 開盤。

### 根因

ADR-127 把日K 快取的新鮮度從「TTL」改成
`market_session.any_session_opens_between(t0, now)`。而這兩個案例都用
「把快取時間戳往前撥」模擬時間經過:

| 案例 | 往前撥 | 15:03 跑的話 t0 = | 跨過 15:00 嗎 |
|---|---|---|---|
| ADR-122 | 10 分鐘 | 14:53 | ✅ 跨過 → 判定過期 → 紅 |
| ADR-127 | 35 分鐘 | 14:28 | ✅ 跨過 → 判定過期 → 紅 |

也就是**每天 08:45~09:45 與 15:00~15:35 跑診斷必紅**,其餘時間全綠。
而我先前所有的「連跑 N 次全綠」都落在 **02:4x / 11:5x / 12:2x** ——
一次都沒踩進那兩個窗口。

### 這是 P-97 的第三次

P-97 的第一條就寫著:

> 「加了閘門/前置條件之後,**回頭看既有測試有沒有因此變成時鐘相依**
> —— 新條件會悄悄改變舊案例的前提,而舊案例不會抱怨。」

我寫下了這句話,卻在改 ADR-127 時沒有真的回頭看。**寫下來不等於做到。**

### 修法(跟前兩次一樣:放進 harness,不是逐案修)

`run_case` 除了原本凍結 `is_market_open` / `just_opened`,再加一個
`any_session_opens_between → False`(預設「期間內沒有跨過開盤」)。
需要相反值的案例自己 patch —— ADR-127 的正反對照本來就自己 patch,不受影響。

**驗證刻意挑在露餡的時段做**:15:05(15:00 開盤後 5 分鐘)重跑 → 56/0 全綠。
再把 harness 的預設改成 `True` 做突變 → ADR-122/127 立刻紅,證明那兩條斷言
沒有因為凍結而變成空殼。

---

## 需使用者實機驗證

1. **開啟「主圖指標參數設定」** → 布林區要看得見:標題、欄位名(開關/中線類型/
   中線期間/上下線σa/上下線σb/色彩)、**第1組與第2組兩列**、兩行說明,
   以及最下面的「確認並套用」按鈕(視窗已放大到 620×520)。
2. **第1組**維持你原本的設定(期間 20、σ 2/3、青色)—— 舊設定檔會自動沿用。
3. **勾選第2組**(例如 EMA 60、σa 2、σb 0、紫色)→ 按確認 → 主圖應出現
   第二組通道(中線實線、上下線虛線)。
4. **σb 填 0** → 該組只畫一對上下線;填 2.5 → 多一對點線。
5. **十字線移到 K 棒上** → 應同時看到 `BB上/中/下` 與 `BB2上/中/下` 兩段數值。
6. **關掉程式再開** → 兩組設定都要被記住。
7. BBW 副圖(若有開)行為不變 —— 它只跟第1組連動。

## 不在這次範圍

- 副圖 BBW 沒有第2組(只有一條線的副圖,加第二條要另外決定版面)。
- 第3組以上(目前沒有需求)。
- MA1~MA6 的行為完全未動(只是改用共用的 `moving_average()`)。
