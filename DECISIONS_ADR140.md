# ADR-140：主圖像素導向視窗與分階段效能量測

- **日期**：2026-08-03
- **狀態**：已實作，待使用者實機驗證

## 背景

大量 K 線資料在主圖切換、縮放與平移時仍會頓挫。ADR-082 已將
分 K 固定限制為 1,200 根，日／週／月 K 限制為 1,000 根，但小畫布
仍會為肉眼無法分辨的 K 棒建立 Matplotlib artist。舊的效能日誌又只量
`canvas.draw()`，卻標成 `mpf.plot + canvas.draw`，無法正確判斷將指標
改寫為 C++ 是否真有幫助。

## 決策

1. 新增零 tkinter／零 matplotlib 依賴的 `core/chart_viewport.py`，作為
   畫布 K 棒上限的單一出處。
2. 保留 ADR-082 的 1,200／1,000 硬上限；取得可信畫布寬度時，
   改為每像素最多 1.25 根，並保留最少 300 根的可操作歷史。
3. 完整 DataFrame 仍先計算技術指標，只在交給畫布前取尾端視窗，
   不犧牲 MA240 等長週期指標的 warm-up 正確性。
4. `draw_chart()` 分開量測指標計算、`mpf.plot()` 建圖、
   `canvas.draw()` 點陣化與全程；全程超過 350ms 才寫一行日誌。

## 取捨

- 這一階段不引入 C++ 編譯與部署成本。實機日誌若顯示指標計算
  佔比高，再評估 pybind11；若「建圖／畫布」佔比高，優先做 artist
  重用或更換 renderer，不會誤把計算改寫當成繪圖解法。
- 像素自適應會使較窄視窗的可平移歷史少於舊的固定上限，但完整
  資料與指標結果仍保留在記憶體，不影響數值正確性。

## 驗證

- `core/chart_viewport.py` 的上限、最小視窗、像素自適應、尾端切片
  與副本語意均有離線單元測試。
- 實機需使用大量分 K，回報日誌中的「指標／建圖／畫布」毫秒數，
  作為下一階段技術選擇依據。
