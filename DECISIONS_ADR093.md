
## ADR-093：終極波段策略新增「盤勢/型態提醒」— 加權指數區間/技術型態偵測 (只通知不下單)

### 背景

使用者要求在「終極波段策略」再加一個獨立的提醒功能:

1. 抓一段可設定區間 (預設近60天) 加權指數收盤的最高/最低,某日收盤接近
   高點或低點時發出通知。
2. 市場上常見的技術型態 (M頭、W底、破底翻、島型反轉、區間突破、區間
   跌破、N字形…等),由 Claude 上網搜尋補齊更完整的清單,提供給使用者
   知道會判斷哪些型態,需要設定的參數留給使用者自己調整。
3. 盤勢/型態的判斷主要看日K,60分K只是輔助 (盤中預告)。

這是一個**獨立的提醒功能,不影響任何進出場邏輯**——不新增/不修改任何
買賣 intent,純粹多一個「用日誌 + Telegram 通知使用者」的管道。

### 設計原則

- 型態判斷是一個獨立的技術分析概念,不是「終極波段策略」狀態機
  (core/chukuangren_band.py) 的一部分,所以做成新的獨立純邏輯模組
  `core/market_pattern.py` (零 tkinter/零 shioaji,ADR-009),只吃
  OHLC DataFrame、吐型態描述 dict,完全不知道帳戶/委託/部位是什麼。
- 每個型態的判斷規則刻意簡化、透明、可測試,不是精確複刻教科書幾何
  定義,也不是機器學習式的「像不像」黑箱——例如頭肩頂的頸線用兩個
  波谷收盤的平均值,不是真正兩點連線的斜線頸線;三角形/楔形只用波段
  高低點的線性回歸斜率方向粗分類。這是誠實的取捨,寧可規則簡單讓
  使用者知道在算什麼,也不要看起來很厲害其實無法解釋。
- 除了「盤勢分類」「三角形/楔形」這種本質是持續狀態的判斷之外,其餘
  型態都設計成「只在型態confirm的那一天(=傳入資料的最後一根)才觸發
  一次」,避免同一個舊訊號每天重複通知;呼叫端 (GUI層) 另外對「持續
  狀態」類的訊號做「內容有沒有變」的去重 (rt['pattern_last_notified'])。

### 型態清單 (`core/market_pattern.PATTERN_CATALOG`,共12種)

| 分類 | 型態 | 判斷邏輯摘要 |
|---|---|---|
| 盤勢 | 盤勢分類 | 近N天收盤最高/最低,前後半段均價變化幅度判斷區間整理/上升/下降趨勢；收盤接近邊界(容忍度%)另外提醒 |
| 反轉 | M頭(雙重頂) | 兩個相近波段高點夾一個波段低點(頸線),第二個高點後收盤跌破頸線 |
| 反轉 | W底(雙重底) | M頭鏡像,突破頸線 |
| 反轉 | 頭肩頂 | 三個波段高點,頭明顯高於兩肩且兩肩相近,跌破頸線(兩波谷均值) |
| 反轉 | 頭肩底 | 頭肩頂鏡像 |
| 反轉 | 破底翻 | 近期跌破先前低點(支撐)後,收盤第一次收復支撐之上 |
| 反轉 | 假突破 | 破底翻鏡像:突破先前高點(壓力)後收盤第一次跌破壓力 |
| 反轉 | 島型反轉 | 跳空缺口偵測:一段整理被前後兩個同方向缺口夾住形成孤島,回補缺口當天觸發 |
| 突破 | 區間突破 | 收盤站上不含最近兩天的區間最高收盤 |
| 突破 | 區間跌破 | 區間突破鏡像 |
| 趨勢延續 | N字形 | 低點墊高後突破前波高點(上升N字)/高點降低後跌破前波低點(下降N字) |
| 整理 | 三角形收斂/楔形 | 波段高/低點連線斜率方向:高降低升=對稱三角形,高平低升=上升三角形,高降低平=下降三角形,同向上升=上升楔形(偏空),同向下降=下降楔形(偏多) |

### 修改內容

**新增 `core/market_pattern.py`**:
- `find_swings`/`_find_swings`:波段高低點偵測 (左右各 window 根範圍內
  最高/最低才算),清成嚴格交替 high/low,是其餘型態判斷的共用基礎。
- `classify_regime`：盤勢分類 + 接近邊界判斷 (使用者需求1)。
- `detect_double_top`/`detect_double_bottom`/`detect_head_shoulders_top`/
  `detect_head_shoulders_bottom`/`detect_undercut_reverse`/
  `detect_false_breakout`/`detect_island_reversal`/`detect_range_breakout`/
  `detect_range_breakdown`/`detect_n_pattern_up`/`detect_n_pattern_down`/
  `detect_triangle_wedge`：12種型態各自的偵測函式。
- `DEFAULT_PARAMS`：所有可調參數的預設值 (區間天數60、接近邊界3%、
  波段視窗3根、容忍度3~5%等)，使用者可在編輯器覆寫。
- `PATTERN_CATALOG`：型態清單 (id/分類/標籤/說明)，UI「查看型態說明」
  按鈕直接讀這份清單顯示，程式碼與說明文件同一份來源不會分歧。
- `evaluate_all(df, params)`：一次跑完所有啟用的判斷，回傳這次觸發的
  訊號 list。

**`core/chukuangren_band.py`**：`default_strategy()` 新增
`pattern_enabled`(預設False)/`pattern_lookback`(60)/`pattern_near_pct`(3.0)/
`pattern_list`(全部啟用) 四個欄位。預設關閉，不會沒問過使用者就開始
發通知。

**`stock_app_pro.py`**：
1. 終極波段策略編輯器新增「📈 盤勢/型態提醒」區塊：啟用開關、區間天數、
   接近邊界%、12個型態各自的啟用checkbox、「查看型態說明」按鈕 (彈出
   視窗列出 PATTERN_CATALOG 全部說明)。
2. 新增 `_qt_pattern_check(s, rt, daily_df)`：跟 `_quant_eval_pass` 既有的
   終極波段策略日K評估共用同一份剛抓到的日K (不多打API)，一天只跑一次，
   呼叫 `market_pattern.evaluate_all()`，新訊號用
   `log_message('【自動交易-盤勢】...')` 通知——這個前綴會被既有的
   `telegram_notify.is_quant_message()` 抓到，總開關開啟時自動額外推播
   Telegram (ADR-090 既有機制，不用重新接線)。
3. 新增 `_qt_pattern_intraday_preview(...)`：使用者需求3的「60分K為輔」，
   日K還沒收盤前先用最新60分K收盤看是否已經接近日K算出的區間邊界，
   給「留意」等級的盤中預告，正式判定永遠等日K收盤才算數，每天每個
   方向最多預告一次。
4. `QT_PATTERN_PERSISTENT_IDS`：標記哪些訊號是「持續狀態」(盤勢分類/
   接近邊界/三角形楔形)，這幾種才需要「內容有變才通知」的額外去重，
   避免同一個狀態每天重複洗版。

### 效果

- 使用者可以針對終極波段策略單獨開啟這個提醒功能，不影響原有進出場
  邏輯，也不影響其他策略。
- 12種型態涵蓋使用者原本列的7種 + 額外搜尋補上的頭肩頂/頭肩底/假突破
  (破底翻的鏡像，原始需求只提破底翻沒提對稱版本，一併補上)/三角形收斂/
  楔形，都在編輯器「查看型態說明」看得到判斷邏輯，不是黑箱。
- 所有需要調整的參數 (區間天數、接近邊界%、要開哪些型態) 都留在編輯器
  給使用者自己設定，不是寫死。

### 已知限制 (誠實)

- 型態判斷是規則式的簡化版，不是精確複刻教科書幾何定義 (例如頸線用
  波谷均值而非真正斜線)，也不保證抓到人眼判斷會抓到的每一種變形——
  這是為了規則透明可測試而做的刻意取捨，使用者仍應自行判斷，不建議
  單獨依賴這個提醒下決策。
- 三角形/楔形的分類只看斜率方向，沒有驗證真正的收斂速度或框線有效性，
  屬於粗分類，供參考。
- 60分K的盤中預告只做「接近邊界」這一項，其餘型態不做盤中版本 (日K
  尚未收盤時,型態的波段結構本身就還沒定案，做了也只是雜訊)。

### 測試

`tests/test_core.py` 新增 `TestMarketPattern`，26個測試涵蓋：盤勢分類
三種狀態、接近邊界、12種型態各自至少一組「應該觸發」+「不應該觸發」
案例、`evaluate_all`的參數過濾與資料不足防呆、`PATTERN_CATALOG`結構。
連同既有測試共 384 個全數通過。GUI 層 (`stock_app_pro.py`) 已過語法
檢查，因需要 tkinter/shioaji 無法離線測試，**務必**依下方清單實機驗證。

### 需使用者實機驗證

1. 開啟終極波段策略編輯器 (新建或編輯既有的)：應該看到「📈 盤勢/型態
   提醒」區塊，預設「啟用」是關閉的；打開後可以調整區間天數/接近邊界%/
   12個型態的checkbox；「查看型態說明」按鈕能正確彈出說明清單。
2. 啟用後儲存，讓策略啟動運轉一段時間 (至少跨過一次日K收盤，或用
   「🔬 回測」以外的方式手動確認邏輯有跑到)：系統日誌應該看到
   「【自動交易-盤勢】策略「xxx」... 日K偵測到:...」這類訊息；若有
   設定 Telegram 且總開關開著，應該同時收到 Telegram 推播。
3. 確認同一個「盤勢分類」結果 (例如連續好幾天都是「區間整理」) 不會
   每天重複通知；但如果盤勢從「區間整理」變成「上升趨勢」，應該會
   收到新的通知。
4. 盤中 (日K還沒收盤) 觀察是否有「60分K盤中已接近...區間高點/低點」
   的預告訊息，且同一天同一個方向不會重複發。
