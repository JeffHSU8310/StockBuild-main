# ADR-138:布林上下線各自一個 σ + 255 色色盤 + 內建編輯器勾選框被蓋掉 + 遠端控制狀態可見

> 狀態:已實作、離線驗證通過、**尚未經使用者實機驗證**
> 相關:ADR-029 / ADR-056 / ADR-108 / ADR-131 / ADR-133 / ADR-134 / ADR-136
> 新增坑:P-114 / P-115 / P-116

---

## 起因(使用者這一輪的三件事)

1. 兩個編輯器並排的截圖,橘線圈住自訂策略的「停損停利即時觸發」勾選框:
   > 「自訂策略有停損停利即時觸發,內建策略也要有這個功能。」
2. 「主圖指標參數設定」截圖,橘勾標在「上下線σa」「上下線σb」兩欄:
   > 「我要上線一個參數,下線一個參數,兩個要分開。」
   > 「然後,所有指標線的顏色可以更多選擇。最好有 255 種。」
3. > 「我在 Telegram message 中 Key 入 指令(比如 /help),應完全沒有回應,
   > 我可以收到系統傳給我的訊息。是我 Telegram 設定的問題還是其他?」

處理過程中順手抓到第 4 件:**ADR-131/133/134 新增的指標設定從頭到尾就沒有
真的持久化過**。

---

## 一、內建策略編輯器的「即時觸發」勾選框:程式有,畫面沒有

### 查證

ADR-136 明明兩個編輯器都加了,原始碼也確實有兩份
(`s['intrabar_stop'] = bool(var_intrabar.get())` 出現 2 次,ADR-135 的診斷
案例還特地斷言過這件事,一直是綠的)。

根因是 **grid 撞格**(P-104,這是第二次):

```python
# ADR-136 放的
tk.Checkbutton(top, text="停損停利即時觸發 …").grid(row=10, column=0, columnspan=6)
# 早就存在、寫在後面的
_bnh_row.grid(row=10, column=0, columnspan=7)      # Buy & Hold 那一列
```

tkinter 的 grid **後放的蓋前放的,而且完全不報錯**。勾選框被 Buy&Hold 那一列
整個蓋住,於是「原始碼有、截圖沒有」。

順帶抓到同一個對話框裡第二處:`lbl_name.grid(row=5, column=0, columnspan=7)`
壓住了 row 5 的 col 2~5(籌碼 checkbox 那一區)。

### 決定

- 勾選框移到 `row=14`(row 10~13 已被 Buy&Hold / 委託方式等佔用),
  `lbl_name` 的 `columnspan` 改成 2。
- **把檢查工具化**:ADR-131 那次是手寫一段 AST 檢查,只看 `open_main_settings`。
  這次抽成 `diag_repro_issues._grid_overlaps(func_name)`,並套用到 8 個
  「改壞了會讓整區 UI 消失」的對話框。

### 工具本身踩到的兩個坑(P-115)

| 版本 | 誤報對象 | 原因 |
|---|---|---|
| 第一版 | `_qt_open_chukuangren_editor` | 沒分**父容器**:`top`/`watch_fr`/`dlg` 各有獨立座標系,同時佔 (0,0) 合法 |
| 第二版 | `open_sub_settings` | 沒分**互斥分支**:`if ind=="MACD" … elif ind=="RSI" …` 每個分支都從 row=0 開始 |

最終版:撞格 key 是 **(父容器, row, col)**,且走訪時記住「在哪些 `if` 的哪一
條分支裡」,只有分支路徑相容的兩個 grid 才拿來比。認不出父容器 / 位置不是
字面常數(`row=i+1`)一律跳過 —— 這種檢查**寧可漏報也不要誤報**,假警報的
下場就是整條被關掉。

> ⚠️ 這條檢查抓的是那個 bug 的**類別**,不是那一行字串。把勾選框搬回 row=10
> 的突變測試會紅,而 ADR-135 原本那條「原始碼有這一行」的斷言仍然是綠的
> —— 那正是它當初擋不住這個 bug 的原因。

---

## 二、布林:上線一個 σ、下線一個 σ

### 語意變更

| | ADR-029 ~ ADR-131 | ADR-138 起 |
|---|---|---|
| 參數 | `σa` = 內圈那**一對**、`σb` = 外圈那一對 | `σ_up` = **上線**、`σ_dn` = **下線** |
| 畫幾條 | 中線 + 最多 **2 對** 上下線(同色,外圈點線) | 中線 + **1 對** 上下線,但**可不對稱** |
| σ=0 | 該組不畫外圈 | 退回 2.0(不可以讓上下線疊在中線上) |

```python
UPPER = MID + σ_up × STD
LOWER = MID − σ_dn × STD
```

要第二條通道請開「第2組」—— 它從 ADR-131 起本來就是完整獨立的一組
(自己的中線類型/期間/顏色)。也就是說功能沒有變少,只是「第二對」從
「同一組裡的附屬品」變成「另一組」。

`BB_UPPER2` / `BB_LOWER2` 欄位**整個移除**,連同繪圖的點線與十字線提示的
「上2/下2」。

### 舊設定檔的遷移(`config_store.migrate_indicator_settings()`)

```
σ_up = σ_dn = 舊的 σ1        # 主要那對,原樣保留
舊的 σ2(外圈)→ 直接捨棄
```

**刻意不拿 σ2 當下線的 σ。** 使用者設 σ1=2 / σ2=3 的意思是「2σ 一對 + 3σ 一對」;
若對映成「上 2σ、下 3σ」,他會在圖上看到一條自己從沒設定過的歪斜通道,
而且沒有任何提示。寧可少畫一對(那是這次功能改動本來就會發生、而且 ADR
寫明的事),也不要偷偷改掉他既有那對的形狀。這條有專門的斷言擋著。

新 key 已存在就完全不動 → 遷移是冪等的。

### UI

「上下線σa / 上下線σb」→ **「上線σ / 下線σ」**;
說明文字改成「上線σ 與 下線σ 各自獨立(可不對稱,例如上 2、下 3)」。

---

## 三、255 色色盤(`core/palette.py`)

原本 `color_map` 只有寫死的 8 色,MA1~MA6 就吃掉 6 色,再加布林兩組、
黃金切割、副圖指標,兩條線常常同色疊在一起。

```
PALETTE = 舊有 8 色 + 255 系統色 = 263
系統色 = 20 色相 × 12 明暗/彩度階 + 15 階灰
```

- **舊有 8 色排最前面,標籤字串一字未改**。這是**強制**的:
  `indicator_settings.json` 存的是**標籤字串**不是色碼,標籤一改,使用者
  存過的顏色會全部對不上而靜默退回預設色 —— 那是資料遺失。
  `list(color_map)[:6]` 也仍然是 MA1~MA6 的預設色,使用者的圖不會變樣。
- 色相間隔 18°,同色相的 12 階**混合明度與彩度**:主圖背景是深色,
  純粹只調明度的話低明度那幾階會整片糊進背景。最暗只到 v=0.45,
  灰階從 `#202020` 起跳(有測試釘住「不可以有看不見的顏色」)。
- `palette.resolve(label, default)` **容錯**:標籤不在色盤裡就從標籤內嵌的
  `#RRGGBB` 解析回來。色盤日後再調整時,舊設定檔不會突然變色。
  繪圖與十字線都改走它(不再是 `color_map.get()`)。

---

## 四、Telegram 打指令沒反應 —— 不是設定壞了,是少勾一個框

### 診斷結果(回答使用者的問題)

程式沒有壞,Telegram 那邊也沒問題。**「啟用 Telegram 通知」與
「⚠️ 啟用手機遠端控制」是兩個獨立的勾選框**(ADR-108 刻意分開:通知是唯讀
推播,遠端控制能讓系統開始下單,風險等級不同)。只勾通知的話:

```python
if (self.telegram_cfg or {}).get('remote_control'):
    self.start_telegram_control()          # ← 沒開,輪詢執行緒根本不建立
```

打什麼指令都不會有回應,**而且當時連一行日誌都沒有**。使用者手上唯一的證據
是「打了沒反應」,而那跟「token 打錯」「chat_id 不符」長得一模一樣(P-116)。

### 決定

- 新增 `core/telegram_control.control_status(cfg) -> (bool, str)`:
  規則與說法的單一出處,回傳「有沒有啟用 + 給人看的原因」。
  三種情況分別講清楚:沒勾遠端控制 / 勾了但 token 或 chat_id 沒填 / 已啟用。
- `stock_app_pro` 啟動時**無條件**呼叫 `_tg_log_control_state()` 印一行,
  Telegram 設定存檔後也印同一份(兩處說法一致)。
- **不動的部分**:未授權 chat_id 的指令仍然只記日誌、不回覆
  (不讓對方確認 Bot 存在)—— 這條安全設計不因為「要好診斷」而放寬。
  遠端控制一樣不提供任何下單/改單指令(ADR-108)。

---

## 五、順手抓到:ADR-131/133/134 的設定從來沒真的存起來過(P-114)

`config_store.load_indicator_settings()` / `save_indicator_settings()` 都拿
`DEFAULT_INDICATOR_SETTINGS` 當**白名單**過濾。後來每一輪新增設定時,
GUI 的 `_collect` / `_apply` 都補了,**唯獨沒人回頭補那份預設 dict** ——
於是 `bb_type`、`bb2_*`、`fib_*`、`kd_ob`/`kd_os`、`var_jae`、`jae_params`
**存檔時被濾掉、讀檔時也被濾掉**,每次重開都回到程式碼預設值。

使用者手上那份 `indicator_settings.json` 就是證據:只有 ADR-056 當年那 22 個 key。

「當下有生效」正是最會騙人的地方 —— tk.Variable 改了、圖也重畫了,
只有跨行程的持久化沒生效。

**修正**:兩邊都不再白名單過濾。預設值的角色是「缺欄位時補上」,不是
「決定哪些欄位可以存在」;值合不合法交給 `_apply_indicator_settings()`
逐項 try/except 判斷(它本來就是那樣寫的)。

---

## 檔案

| 檔案 | 變更 |
|---|---|
| `core/palette.py` | **新增**:8 舊色 + 255 系統色、`color_map()`/`labels()`/`resolve()` |
| `core/indicators.py` | `bollinger_set(period, std_up, std_dn)` 語意換掉、移除 UPPER2/LOWER2;`calculate_indicators` 參數改名 |
| `core/telegram_control.py` | 新增 `control_status()` |
| `data/config_store.py` | 新增 `migrate_indicator_settings()`;`load/save_indicator_settings` 不再白名單過濾;預設值改 `bb_std_up`/`bb_std_dn` |
| `stock_app_pro.py` | 勾選框移到 row 14、`lbl_name` columnspan 修正;`bb_std_up/dn`、`bb2_std_up/dn`;色盤接 `palette`;取色改 `palette.resolve()`;`_tg_log_control_state()` |
| `tests/test_core.py` | +19(布林 σ、遷移、持久化往返、色盤) |
| `diag_repro_issues.py` | 新增 `_grid_overlaps()`(父容器+分支感知)、ADR-138 案例;ADR-131 案例改用新語意 |
| `PITFALLS.md` | P-114 / P-115 / P-116 |

**編號檢查**:檔名與程式碼註解兩處都查過,最大 ADR-137、最大 P-113。

---

## 驗證

### 離線(全綠)

- `python tests/test_core.py` — **763 通過**(原 747,+16)
- `python tests/test_brokers.py` — 42 通過
- `python diag_repro_issues.py` — **62 案例全 PASS**(原 61,+1)
- `python diag_crossref.py` — 無斷鏈
- `py_compile` 全過

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| `LOWER` 也用 `s_up`(上下線接錯邊) | 單元紅、診斷紅「第1組下線 σ 不對」 |
| 遷移拿舊 σ2 當下線 | 單元紅 ×3、診斷紅「舊的外圈 σ2 不可以被拿去當下線」 |
| `load_indicator_settings` 恢復白名單過濾 | 單元紅、診斷紅「第2組布林中線類型沒有存回來」 |
| `PALETTE` 縮回只剩舊 8 色 | 單元紅(先補了「255 色要真的進到 color_map」才抓得到 —— 原本只驗 `GENERATED_COLORS` 的長度,是空殼) |
| 舊 8 色標籤改名(`黃`→`金黃`) | 單元紅 |
| 勾選框搬回 `row=10`(重現使用者截圖) | 診斷紅,而且訊息直接指出「父容器 top (row=10, col=0) 第 10399 行與第 10415 行」 |
| 拿掉啟動時的 `_tg_log_control_state()` | 診斷紅 |
| `control_status` 只勾通知也回 True | 診斷紅 |
| `_grid_overlaps` 永遠回空 list | 對 row=10 的 bug 回報 `[]` —— 證明前一條真的是靠它抓到的 |

### 請使用者實機驗證

1. **內建策略編輯器**:開任一內建策略 → 應該看得到「停損停利即時觸發
   (盤中觸價就出場,不等K棒收盤;回測同步照此模擬)」這個勾選框
   (在 Buy&Hold 那一列**下面**)。勾/不勾存檔後再打開,狀態要留著。
2. **主圖指標參數設定**:布林那兩欄標題應為「上線σ / 下線σ」。
   試著把上線設 2、下線設 3 → 圖上通道應該**上窄下寬**。
3. **設定持久化**:改完布林/黃金切割/副圖指標的參數,按「確認並套用」,
   **關掉程式再開** → 參數要還在(這一項以前是壞的)。
4. **顏色**:任一條指標線的色彩下拉,應該有 263 個選項
   (前 8 個仍是原本的黃/白/藍/紫/橘/紅/青/綠)。挑一個新顏色套用,
   關掉再開,顏色要留著。
5. **舊設定遷移**:你原本的設定是 `bb_std1=2.0 / bb_std2=3.0` →
   開起來應該變成 **上線σ=2、下線σ=2**(原本的外圈 3σ 那對會消失,
   這是這次改動的預期行為;要兩條通道請開「第2組」)。
6. **Telegram**:系統日誌開頭應該有一行【遠端控制】說明現況。
   若要用手機下指令,請到「Telegram 設定」把
   **「⚠️ 啟用手機遠端控制」也勾起來**(只勾「啟用 Telegram 通知」不夠),
   存檔後再打 `/help` 就會有回應。
