
## ADR-096：主程式整個當機 (真正根因) — 下單/刪改委託同步網路呼叫卡住主執行緒

### 背景

接續 ADR-095 (量化交易獨立視窗有時候關不掉)，使用者進一步回報：**主程式**
也會關閉不了，好像整個當機，等了 3 分鐘都沒反應。3 分鐘這個時長排除了
ADR-095 記錄的「matplotlib 大量K棒渲染卡住UI」這個已知限制 (那個頂多
1~2 秒等級，不會到 3 分鐘)，代表還有另一個更嚴重的根因。

### 根因：下單/刪改委託的 shioaji 呼叫直接寫在主執行緒，沒有逾時

逐一盤點 `stock_app_pro.py` 裡所有會呼叫 shioaji 網路 API 的地方，
確認 `snapshots()`/`kbars()`/`login()`/`logout()` 這些既有的都已經在
背景執行緒執行 (前幾輪 ADR 已經修過)，但找到兩個**仍然直接在主執行緒
(按鈕點擊 callback) 同步呼叫**的地方：

1. **下單確認 → `_confirm_and_place_order()`**：使用者在下單確認視窗按
   「確認送出」，直接在主執行緒呼叫 `self.sj_api.place_order(...)`。
2. **刪改委託確認 → `_send_order_modification()`**：使用者在刪改確認
   視窗按「確認送出」，直接在主執行緒呼叫
   `self.sj_api.cancel_order(...)`/`update_order(...)`，甚至更早的
   `_find_trade_for_order()` 內部還會呼叫 `update_status()`/
   `list_trades()`，一樣是同步網路呼叫。

這幾個呼叫**完全沒有逾時設定**。Tkinter 整個應用程式 (含所有視窗) 只有
一個事件迴圈、跑在同一個主執行緒——主執行緒被這種呼叫卡住時，不只是
「那個功能沒反應」，是**整個程式的每一個視窗、每一顆按鈕 (含關閉鈕)**
都會沒有反應，因為連「使用者點了關閉鈕」這個事件都要排隊，排到主執行緒
有空才處理得到。如果券商端 (或中間網路) 剛好在這個時間點回應變慢、
連線半通不通 (TCP 連線卡在等待讀取、沒有明確斷線)，這通呼叫可能會卡
好幾分鐘甚至更久，使用者看到的就是「整個程式當機，怎麼點都沒反應」——
跟這次回報的「等了3分鐘」完全吻合，比 ADR-095 的 matplotlib 渲染假說
更能解釋「好幾分鐘」這個時長。

### 修正

比照專案既有的「shioaji 慢呼叫一律背景執行緒 + safe_after() 排回主執行緒
更新UI」慣例 (login/logout 已經是這樣做)，把這兩處也改成同樣模式：

- `_confirm_and_place_order()` 現在只負責啟動背景執行緒
  (`_place_order_worker`)，真正呼叫 `place_order()` 在背景做；拿到結果
  (或例外) 後用 `safe_after()` 排回主執行緒的 `_apply_order_result()`
  處理——這個方法的內容 (逐段防護解析委託回報、寫入「我的委託單」清單、
  防重複委託的race guard) 跟改動前**完全相同**，只是被搬到背景呼叫完成
  之後才執行，行為不變，只是不再卡住UI。
- `_send_order_modification()` 同樣改成只啟動背景執行緒
  (`_send_order_modification_worker`)，`_find_trade_for_order()` (含
  `update_status()`/`list_trades()`) 跟 `cancel_order()`/`update_order()`
  都在背景做完才用 `safe_after()` 排回主執行緒記錄結果。

### 效果

- 下單/刪改委託等待券商回應期間，UI (含所有視窗的關閉鈕) 仍然可以正常
  操作，不會被單一筆委託的網路延遲拖累整個程式「看起來當機」。
- 錯誤處理、防重複委託的邏輯、寫入「我的委託單」清單的順序與內容跟
  改動前完全一致，只是執行的執行緒不同——這是刻意的最小風險改法，
  不重新設計下單流程本身。

### 已知限制 (誠實)

- 改成非同步後，理論上使用者可以在第一筆委託還在等券商回應時，就去對
  另一個商品送出第二筆委託——這在改動前是不可能的 (UI整個凍住，什麼都
  點不了)。這其實是**恢復**正常回應速度該有的行為 (真實下單軟體本來就
  不會因為一筆委託在途就把整個介面鎖死)，不是新增的風險；`ctx` 是每筆
  委託各自獨立的字典，多筆委託平行處理不會互相污染資料。
- `self._last_pending_order_key`/`_last_pending_order_info` 這兩個「防
  重複委託」的暫存欄位設計上只記「最近一筆」，如果兩筆委託幾乎同時送出
  又幾乎同時收到回報，這個防護的命中率可能略降——但這是 ADR-027 當初
  就承認的 best-effort 機制 (註解已明確寫「減少重複顯示的機率」，不是
  「保證不重複」)，不是這次改動新增的問題，也不影響委託本身有沒有正確
  送出，只影響「我的委託單」清單短暫顯示重複的機率。
- 仍未逐一替 shioaji SDK 呼叫本身加上明確的逾時參數 (是否支援、怎麼設
  需要查 shioaji 文件/原始碼，這次沒有查證，只解決「呼叫掛住時不再拖累
  UI」，沒有解決「呼叫本身可能真的要等很久才回應或報錯」)——這點如果
  之後想進一步優化 (例如卡超過N秒就在UI提示「委託仍在處理中」)，需要
  另外討論怎麼設計，這次先不做。

### 測試

純執行緒調度改動，不影響 `core/`/`data/` 任何可離線測試的模組，
`tests/test_core.py` 384 個測試不受影響、全數通過。**這是修改實盤下單
路徑，務必在有畫面、已登入 shioaji 的機器上非常仔細地實機驗證**：

1. 用最小數量 (零股1股或期貨最小口數) 模擬/小額測試下單，確認委託
   確實送出、「我的委託單」清單正確顯示、狀態/成交回報正常更新，跟
   改動前行為一致。
2. 下單送出到收到回報這段等待期間，確認可以正常操作其他視窗、正常
   關閉程式，不會卡住。
3. 測試刪改委託 (改量/改價/刪單)，確認送出後委託狀態正確更新。
4. 如果方便，可以刻意在網路不穩的情況下測試 (例如切到訊號很差的網路)，
   驗證下單卡住等待期間 UI 是否仍保持可操作，且最終會顯示逾時/失敗的
   錯誤訊息而不是永遠卡住 (取決於 shioaji SDK 本身的底層行為，本次
   沒有替 API 呼叫加自訂逾時，只確保UI不受拖累)。
