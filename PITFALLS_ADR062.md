
### P-76　「金額 → 數量」的換算一定要處理餘額，不可四捨五入也不可丟棄
- **症狀（設計階段就要想到）**：定期定額每月投入 1 萬元買股票，但股票
  1 張 = 1000 股，一張 50 萬 —— 每月的錢根本買不起一個單位。
  若直接 `round()` 會買到 0 張或憑空多買；若直接丟棄餘額，長期下來
  「總投入」會與使用者實際匯出去的錢對不上。
- **正確做法**：
  1. 數量一律 `floor(預算 / (價格 × 單位規模))`，**不可四捨五入**
     （成交數量不能無中生有）。
  2. 買不滿一個單位的餘額要**累積到下一期**（carry-over），
     真實的定期定額就是這樣運作 —— 錢不會蒸發，也不會硬買。
  3. 報告要能對帳：`總投入 ≤ 期數 × 每期金額`，且差額就是最後留在手上的餘額。
     這條不變式要寫成測試（本專案：`test_dca_invests_close_to_planned`、
     `test_dca_carry_over_when_too_expensive`）。
- **出處**：ADR-062。

### P-77　測試/診斷的斷言不可以編碼「取決於設定」的大小關係
- **症狀**：診斷案例寫了
  `assert 單筆長抱投入 < 定期定額投入 < 累積加碼投入`，實際跑出來
  累積加碼只投入 72,555（每次買 1 股 ≈ 100 元 × 748 次）、
  定期定額投入 349,990（每月 10,000 × 35 期）—— 斷言失敗，
  但**程式其實是對的，錯的是斷言**。
- **根因**：兩種模式的投入金額分別由「每次幾張」與「每期多少錢」決定，
  大小關係完全取決於使用者怎麼設定，不是模式本身的性質。
  我把「我這次的參數剛好會產生的結果」誤當成「必然成立的不變式」。
- **正確做法**：斷言只寫**與參數無關的必然關係**。本例真正成立的是
  「同樣每次買 1 單位，買越多次投入越多」→
  `single < accumulate`、`single < dca`，而 dca 與 accumulate 之間不可比。
  寫斷言前先問一句：「換一組參數，這個關係還成立嗎？」不成立就不是不變式。
- **出處**：ADR-062。

### P-78　同一份資料有多個入口時，取得流程要抽成一個函式
- **症狀**：策略比較需要跟回測完全一樣的資料（下載／期交所涵蓋跳過／
  盤別聚合／延伸／裁切）。若在比較功能裡重寫一份，兩邊只要有一個環節
  不同步，比較結果就會與單獨回測對不起來 —— 而且這種不一致極難察覺，
  因為兩邊「看起來都合理」。
- **正確做法**：把取資料流程抽成 `_qt_prepare_df(策略, 起訖, 盤別)`，
  回測、最佳化、比較全部走同一個入口。本專案已經因為「兩處各寫一份」
  吃過虧（P-67 報酬率分母、ADR-062 單位規模），**第三次就該養成反射**：
  只要某個計算會被第二個地方用到，先抽函式再說。
- **判斷準則**：如果兩段程式碼的正確性必須「同時」維護才有意義
  （改一邊沒改另一邊就會產生錯誤但不會報錯），那它們就該是同一個函式。
- **出處**：ADR-062。
