
## ADR-092：策略重啟前持倉核對、帳戶/策略順序可調整、全部帳戶總覽

### 背景 / 根因

使用者在 ADR-091 (多個可自訂模擬帳戶) 之後提出四項需求：

1. 策略停用後再重新啟用，應該比對「策略自己記錄的持倉狀態」跟「它指定的
   模擬帳戶內實際部位」是否一致，不一致要通知，並且能手動平倉/加倉。
2. 帳戶順序可以自行排列，名稱可以修改。
3. 策略清單順序也可以上下移動。
4. 新增一個「顯示全部帳戶」的功能，一次看到所有帳戶目前的持倉狀況。

第 1 項不只是錦上添花：策略的持倉狀態存在 `strategy_runtimes[id]`
(`state`/`qty`/`entry_price`)，跟模擬帳戶內的 `positions[symbol]` 是兩份
分開存放、分開落地的資料。使用者若手動重置/刪除過帳戶、或策略中途被
改指到別的帳戶，這兩份資料就可能不同步——若讓策略在不一致的狀態下悄悄
重新啟用，可能會用錯誤的起點繼續交易 (平不到根本不存在的倉，或帳戶裡
留著一筆沒人管的孤兒部位)。

另外在確認這次改動時，發現 ADR-091 把 `self.paper_acct` (單一帳戶) 改名
`self.paper_accts` (帳戶容器) 時，`quant_runner_worker` 裡有一處
`getattr(self, 'paper_acct', None)` 沒有跟著改名，導致這個條件永遠是
`None`——ADR-091 之後，即時標記價更新 (`_qt_update_realtime_pnl`) 與
ADR-087 的期貨即時停損停利檢查其實整段都沒有在跑，只是因為外層包了
`except Exception: pass` 而完全沒有任何錯誤訊息。這次一併修正。

### 設計原則

- 持倉核對邏輯 (比對 runtime 跟帳戶部位) 做成 `core/strategy_engine.py`
  的純函式 `position_mismatch(strategy, runtime, acct_position)`，不碰
  tkinter/shioaji，可離線測試 (ADR-009)；GUI 層只負責「發現不一致時該
  跳出什麼視窗、使用者選了哪個處理方式」。
- 帳戶順序：`self.paper_accts` 本來就是一般 dict，Python 3.7+ 的 dict
  本身就保序，「排序」＝照新順序重建一份 dict，不需要另外加一個
  `order` 欄位或排序演算法。
- 策略順序：`self.strategies` 本來就是 list，清單顯示順序＝ list 順序，
  「排序」＝交換相鄰兩個元素，同樣不需要額外欄位。
- 全部帳戶總覽是純顯示功能，直接讀 `self.paper_accts` 全部帳戶算
  `equity`/`unrealized_pnl` (既有的 `core/paper_account.py` 函式)，
  不新增資料結構。

### 修改內容

**`core/strategy_engine.py`**：
- 新增 `position_mismatch(strategy, runtime, acct_position)`：比對
  runtime 的 `state`/`qty` 跟帳戶部位的 `direction`/`qty`，一致 (含兩邊
  都沒有部位) 回傳 `None`，不一致回傳
  `{'rt_state', 'rt_qty', 'acct_direction', 'acct_qty'}`。

**`stock_app_pro.py`**：
1. `_qt_set_enabled(True)` 在既有驗證通過、真正設定 `enabled=True` 之前
   (僅模擬模式；實單模式的部位在券商端，不適用此核對)，用
   `position_mismatch` 檢查一次；不一致就開
   `_qt_open_reconcile_dialog()` 讓使用者處理，原本設定 enabled 的收尾
   動作拆成 `_qt_finish_set_enabled(s, flag)` 給核對完成後的各個分支呼叫。
2. `_qt_open_reconcile_dialog(s, rt, acct, mismatch)`：顯示「策略記錄」
   跟「帳戶實際」的差異，提供四個選項：
   - ✅ 以帳戶為準同步後啟用 (`runtime` 依帳戶部位覆蓋)
   - 🔻 手動平倉 (帳戶內若有部位就真的送一筆 CLOSE 平掉，`runtime` 設回
     FLAT)
   - 🔺 手動加倉 (依 `runtime` 記錄的方向/數量在帳戶內補一筆 OPEN)
   - ⚠ 忽略差異，強制啟用 (不動任何資料，使用者自行負責)
   都可以在「取消」關閉視窗、維持停用狀態。
3. 修正 `quant_runner_worker` 裡 `getattr(self, 'paper_acct', None)` 應為
   `paper_accts` 的殘留 bug (ADR-091 改名沒有同步修正的地方)，讓即時
   標記價更新與 ADR-087 期貨即時停損停利重新開始運作。
4. 帳戶順序/改名：「💰 模擬帳戶」視窗新增「✏️ 重新命名」「⬆ 上移」
   「⬇ 下移」三顆按鈕，操作對象是視窗目前選取的帳戶。
5. 策略順序：量化交易面板 (完整視窗版) 按鈕列新增「⬆ 上移」「⬇ 下移」，
   操作對象是清單目前選取的策略，交換 `self.strategies` 內相鄰元素。
6. 新增「📊 全部帳戶」按鈕與 `_qt_open_all_accounts_window()` /
   `_qt_refresh_all_accounts_window()`：獨立視窗列出所有模擬帳戶的
   資金/損益/報酬率，以及跨帳戶合併的完整持倉清單。`_qt_refresh_paper_account()`
   結尾順手一併刷新這個視窗 (兩者共用同一份帳戶資料，呼叫端不用每個
   地方都記得補刷新)。

### 效果

- 策略重新啟用前多一道安全確認，帳戶跟策略記錄對不上時不會悄悄用錯誤
  起點繼續運轉；使用者能清楚看到差異並選擇處理方式。
- 順手修正了 ADR-091 造成的即時報價/停損停利迴歸，這是這次審查時額外
  發現、不修不影響本次四項需求但屬於資料正確性問題，依鐵則10 一併記錄。
- 帳戶與策略清單都能依使用者習慣的順序排列，帳戶也能改名，不用重新
  建立帳戶或重新指定策略。
- 一個畫面看到所有模擬帳戶的持倉狀況，不用切換下拉逐一確認。

### 已知限制 (誠實)

- 持倉核對只在「使用者點選單一策略的 ▶ 啟用」時觸發，不會在按「🟢 啟動
  自動交易」總開關時對所有已啟用策略批次核對——若同時有多個策略都不
  一致，會在下次個別重新停用/啟用時才發現。這是刻意的取捨：批次跳出
  多個確認視窗的體驗更差，且使用者原始需求的措辭 (「策略…再次啟動」)
  對應的就是單一策略的啟用動作。
- 「手動加倉」目前只支援帳戶內尚無部位、或帳戶部位方向跟策略記錄相同
  的情況；方向相反時要求使用者先手動平倉，不做自動反手 (避免一次操作
  疊加兩種語意，出錯不好排查)。

### 測試

`tests/test_core.py` 新增 6 個 `position_mismatch` 測試：兩邊都無持倉/
完全一致時回傳 `None`；偵測帳戶內孤兒部位、策略記錄有帳戶卻沒有、數量
不同、方向不同這四種不一致情境。連同既有測試共 345 個全數通過。GUI 層
(`stock_app_pro.py`) 已過 `python -m py_compile`，因需要 tkinter/shioaji
無法離線測試，**務必**依下方清單實機驗證。

### 需使用者實機驗證

1. 選一個「模擬」模式且目前有持倉的啟用中策略，先「⏸ 停用」；到
   「💰 模擬帳戶」視窗手動改變這個帳戶的部位 (例如用「🔄 重置帳戶」
   清空，或另外交易同一檔標的改變數量)；回策略清單「▶ 啟用」該策略：
   應該跳出「持倉核對」視窗而不是直接啟用，四個按鈕分別測試都應該正確
   同步/平倉/加倉並成功啟用。
2. 一致的情況 (沒動過帳戶) 重新啟用應該完全沒有跳出核對視窗，跟原本
   行為一樣順暢。
3. 「💰 模擬帳戶」視窗：「✏️ 重新命名」改名後下拉選單與策略清單「模擬
   帳戶」欄應同步顯示新名稱；「⬆ 上移」「⬇ 下移」調整順序後，重開視窗
   /策略編輯器的帳戶下拉順序應該記住新順序。
4. 量化交易完整視窗：選一個策略「⬆ 上移」「⬇ 下移」，清單順序應立即
   反映，且選取狀態不跑掉。
5. 「📊 全部帳戶」視窗：應該同時看到所有帳戶的資金/損益列，以及下方
   跨帳戶合併的持倉表；開著這個視窗跑模擬交易，數字應該會跟著即時
   報價更新 (驗證順便確認 `paper_acct`→`paper_accts` 的 bug 修正生效：
   模擬持倉的「標記價」/「未實現損益」欄位在有即時報價時應該會自己跳動，
   不是卡住不動)。
