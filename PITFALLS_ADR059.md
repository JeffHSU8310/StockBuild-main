
### P-67　同一份報告裡「明細層」與「彙總層」用不同算法，錯誤會潛伏很久
- **症狀**：回測報告的「報酬率」顯示 `-53093.43%`，而同一份報告的「每筆交易
  明細」報酬% 欄顯示 `-53.09%`——正好差 1000 倍。使用者看到兩個並列的數字，
  很自然會以為是「兩種不同定義的報酬率」，而不是「其中一個是錯的」。
- **根因**：`_compute_metrics` 算總報酬率的分母
  `base += abs(entry_price) * qty` **漏乘 `contract_size`**（股票 1000、
  期貨 200），而每筆明細的 `pnl_pct` 分母
  `denom = abs(entry_px) * qty * contract_size` 是對的。兩處各寫一份算法，
  改了一邊沒改另一邊。連帶把 `ann_return_pct`、`max_drawdown_pct` 一起帶錯。
- **正確做法**：
  1. **同一個量只能有一份算法**。彙總層要用到的分母（本金基準、單位規模），
     應該從明細層已經算好的欄位推導，或抽成共用函式，不要各自重算。
  2. 加「跨層一致性測試」：只有一筆交易時，**總報酬率必須等於那筆的報酬%**。
     這種恆等式測試最能抓出彙總層與明細層的分歧。
  3. 看到「數量級明顯不合理」的數字（報酬率上千%、回撤大於本金）就當成
     bug 追查，不要用「期貨沒有本金概念所以僅供參考」之類的說法自我說服。
- **出處**：ADR-059（實作 Buy & Hold 時意外發現；使用者稍早的畫面就有症狀）。

### P-68　回測結束時的未平倉部位若直接忽略，等於系統性隱藏最後一筆損益
- **症狀**：「買進後持有不賣」的策略回測完，報告所有數字都是 0；一般策略
  若在期末仍持倉，那筆的未實現損益（可能是最大的一筆）完全不出現在報告上。
- **根因**：`run_backtest` 只在 CLOSE 成交時把交易寫進 `trades`，跑完迴圈後
  對還開著的部位沒有任何處理。這不只讓 Buy & Hold 跑不出來，對**所有**期末
  持倉的策略都是往有利方向的系統性偏差（大賠的未平倉被藏起來）。
- **正確做法**：期末用最後一根收盤價做市價結算（mark-to-market），
  並在 `exit_reason` 明確標示「回測期末結算(未平倉)」，報告上也要提示。
  結算的成本要比照真實平倉計算，否則長抱型策略的成本會被系統性低估。
  保留 `settle_open_at_end=False` 可回到舊行為，但預設應該是「結算」——
  因為誠實揭露比維持舊數字重要。
- **附帶原則**：任何「只在事件發生時才記錄」的統計，都要檢查
  **迴圈結束時還在進行中的那一筆**有沒有被漏掉。
- **出處**：ADR-059。

### P-69　放寬一條保護規則時，要同時補上「相反方向」的防呆
- **症狀**：為了支援 Buy & Hold 而放寬「必須有出場方式」的驗證後，使用者可能
  勾了 Buy & Hold 卻又填了停損 2%——自相矛盾，而且更危險的是他會以為自己在
  跑 Buy & Hold，實際上中途就被停損出場了，拿這個結果去當比較基準會得到
  完全錯誤的結論。
- **正確做法**：放寬規則時同時問「放寬之後，使用者可能組合出什麼矛盾狀態」，
  並把矛盾狀態也擋下來：
  - 勾了 Buy & Hold 就**不可以**設停損／停利／出場訊號（互斥）。
  - Buy & Hold **不可用於實單**（永不出場的實單沒有任何保護）。
  錯誤訊息要能指路（沒勾旗標而被擋時，訊息要告訴他「若你就是要買進持有，
  請勾選該選項」），而不是只說「不合法」。
- **出處**：ADR-059。
