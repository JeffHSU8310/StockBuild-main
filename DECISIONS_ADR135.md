# ADR-135:自訂策略補齊內建策略的基本功能 + 停損停利即時觸發(不再限期貨)

## 狀態

已實作,**尚未實機驗證**(分支 `claude/custom-parity-and-intrabar-stop`)。

## 需求(使用者原話)

> 1. 在自訂策略中,也會用到**看A商品做B商品** & **看A週期做B週期** & **下單數量(口)**
>    & **週期(5分、15分、30分、60分、日)** & **市價單、範圍市價、限價單** &
>    能否在自訂策略中設定**快選功能**(同內建策略一樣),也就是說,
>    **我只要寫其他條件的程式就好。其他的都跟內建策略基本功能一樣。**
> 2. 還要有一個功能,**停損停利點數到了,就立即執行。不需要等待到收盤。**

---

## 第一部分:自訂策略缺了什麼(逐項查證)

先查清楚現況,不要重做已經有的東西:

| 使用者列的項目 | 現況 |
|---|---|
| 看A做B | ✅ **已經有**(`_qt_build_watch_panel`)|
| 看A週期做B週期 | ✅ **已經有**(同一個面板裡的「看A週期」)|
| 下單數量 | ✅ 已經有 |
| 週期(可自訂)| ✅ 已經有 |
| 快選(點自選股帶入)| ✅ **已經有**(`_qt_editor_symbol_target`,A/B 都可以)|
| **市價單/範圍市價/限價單** | ❌ **沒有** —— `_collect()` 從來沒寫過 `price_type` |

所以真正缺的只有**委託方式**。但使用者說「其他的都跟內建策略基本功能一樣」,
再逐欄比對之後,還發現一件更值得補的事:

### 更嚴重的:四個「看不見卻一直在生效」的預設值

`new_strategy()` 會給每一檔策略這些預設,而自訂策略的編輯器**一個都沒有顯示**:

| 欄位 | 預設值 | 後果 |
|---|---|---|
| `max_trades_per_day` | **3** | 一天進場超過 3 次就被擋,使用者看不到也改不了 |
| `cooldown_sec` | **300** | 出場後 5 分鐘內不會再進場 |
| `session_gate` | **True** | 非交易時間自動待命 |
| `futures_session` | `day_night` | 期貨含夜盤 |

**這正是 ADR-123 那個「看不見的 `stop_loss_pct: 2.0` 砍掉 -968,250」的同一類問題**
—— 一個使用者看不到的設定值卻在左右系統行為。所以這次一併攤開來可設定。

### 停損/停利也補上(但預設仍是 0 = 停用)

ADR-045 當初刻意不給自訂策略停損/停利欄位,理由是「出場由 on_bar 程式碼決定」。
那個理由仍然成立,所以:**全部預設 0 = 停用 = 完全由程式碼控制**,
有填才生效。既有自訂策略的行為因此完全不變。

而使用者的第 2 點(停損停利即時執行)本來就需要這些欄位才有意義。

### 委託方式收斂成一份

原本「委託方式」這一列在程式裡有**三份複製**(內建編輯器、終極波段編輯器、
以及我為自訂策略新加的)。規則的依據是**交易所規定**:

- 範圍市價僅期貨支援(TAIFEX)
- 零股依交易所規則只能限價(鐵則 6)

三份各自維護遲早分歧,而分歧的後果是**送出去會被券商退的委託**。所以:

- UI:抽成 `_qt_build_price_type_row(parent, s, cb_tt)`,三個編輯器共用。
- 規則:抽成 `core/strategy_engine.validate_price_type(strategy)`,
  `validate_strategy()` 與自訂策略的 `_validate_custom()` 共用。

---

## 第二部分:停損停利即時觸發

`check_intrabar_futures_stop()` 原本第二行就是:

```python
if trade_type_of(strategy) != '期貨':
    return None
```

docstring 還寫著「股票/零股維持原有『K棒收盤才判定』的行為不變
(使用者這次只要求期貨即時)」。**這次使用者要求的沒有限定商品別**,所以拿掉。

GUI 那條 `_qt_check_realtime_futures_stops()` 也有同一道 `!= '期貨'` 的 continue,
一併拿掉 —— 上游 `_qt_update_realtime_pnl()` 本來就同時解析股票與期貨合約,
`live_price_by_symbol` 涵蓋得到,不需要額外抓價。

### 這是刻意的行為變更,要講清楚

> **既有的股票策略若設了停損%/停利%/停損元/停利元,從此會在盤中觸價就出場,
> 而不是等該週期 K 棒收盤。** 這正是使用者要的,但它會改變既有股票策略的
> 成交時點,所以在這裡明確記錄。

### 保留的閘門

**ADR-124 的交易時段閘門沒有動** —— 市場關閉時這條路徑仍然不出場
(那種時候本來也不可能成交)。診斷有一條專門守著它沒被順手拿掉。

### 改名

函式從「只做期貨」變成「所有商品別」之後,名字裡的 `futures` 已經名不副實
(P-100 講過的坑)。改名為 `check_intrabar_stop`,**舊名留成別名**,
既有呼叫端與測試不必一次全改。

---

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| 把「只做期貨」的限制加回來(等於沒修 #2)| 單元紅 ×2;診斷紅:`股票停損點數到了要立刻出場` |
| 即時停損永遠觸發 | 診斷紅:`沒到停損點不可以出場`(反向對照)|
| 自訂編輯器不存 `price_type` | **第一版是綠的** → 見下 → 修好切片後紅 |

### 兩個空殼問題(都是突變測試抓到的)

**其一:只檢查「有沒有建出那一列」是不夠的。** 第一版的診斷只斷言原始碼裡有
`_qt_build_price_type_row`,但沒有斷言 `_collect()` 有把值**存進策略**。
把 `s['price_type'] = _pt['get']()` 那一行刪掉,所有斷言照樣綠 ——
使用者選了委託方式卻存不進去,而測試看不出來。

**其二:切片切錯,被隔壁的程式碼餵飽。** 補上「要有 `s['price_type'] =`」之後
**還是綠的**。原因是我用
`_src[index('def _qt_open_custom_editor') : index('def _qt_open_editor')]`
取自訂編輯器的原始碼,而那兩者中間**還夾著終極波段編輯器** ——
它的 `_collect` 也有 `s['price_type'] = ...`,斷言就被那一份滿足了。
改成切到「下一個同縮排的方法定義」為止,並加兩條防呆斷言
(切片不可以吃到別的編輯器),突變才紅。

> 教訓:**用原始碼切片做斷言時,切片邊界本身也要有斷言**。
> 否則你以為在檢查 A,實際上檢查到的是 B。

## 驗證

- `python tests/test_core.py` → **714 個全過**(原 706,新增 9、改寫 1)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **61 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

## 需使用者實機驗證

### 自訂策略(功能齊備)
1. 開自訂 Python 策略編輯器,應該看得到:**委託方式**(限價/市價/範圍市價)、
   **停損%/停利%/停損(元/點)/停利(元/點)/讓價檔數**、
   **每日進場上限/冷卻秒數/期貨時段/交易時段閘門**,以及原本就有的
   看A做B(含看A週期)、週期、數量、快選帶入。
2. 交易種類切到**零股** → 委託方式只剩「限價」;切到**股票** → 沒有「範圍市價」;
   切到**期貨** → 三種都有。(這三條是交易所規則,不是偏好。)
3. **既有的自訂策略打開後行為不變**:停損/停利欄位應該是 0(=停用,
   完全由你的 on_bar 程式碼控制)。
4. 每日進場上限/冷卻秒數應該顯示 **3 / 300** —— 那是它們**一直以來的實際值**,
   只是以前看不到。若你覺得不合用,現在可以改了。

### 停損停利即時觸發
5. **股票**策略設停損(元)或停損% → 盤中**觸價就出場**,不等 K 棒收盤。
6. **沒到停損點不可以出場**(這比第 5 點更重要,請一起確認)。
7. 期貨的既有行為不變。
8. **市場關閉時不會出場**(ADR-124 的閘門仍在)。

## 不在這次範圍

- 自訂策略仍然**沒有「方向」欄位** —— 做多/做空由 `on_bar` 回傳 BUY/SELL 決定
  (ADR-045 的設計,使用者沒有要求改)。
- 不自動調整任何既有策略的參數。
- 停損停利的判斷邏輯本身沒改(只是適用範圍從期貨擴大到所有商品別)。
