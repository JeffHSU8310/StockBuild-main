
## ADR-108：用手機 (Telegram) 遠端控制系統 — 查詢狀態、啟用/停用策略、緊急全停

### 要解決什麼問題

使用者不可能整天守在跑系統的那台電腦前。人在外面時會遇到兩種情境：

1. **想知道現在怎麼樣了**：總開關開著嗎？哪些策略在跑？有沒有部位？今天賺賠？
2. **想動手改變狀態**：看到盤勢不對想緊急停掉；或反過來，臨時決定要啟用某個
   已經設定好的策略。

原本 ADR-090 的 Telegram 只有**單向推播**（系統 → 手機），解決不了第 2 種。

### 決定

擴充成雙向：手機可以下指令。但**能改變狀態就代表能造成金錢損失**，所以整個
設計是圍繞「怎麼讓誤觸與盜用不會變成賠錢」展開的，而不是圍繞「功能要多完整」。

#### 一、指令分三級，風險決定要不要確認

| 級別 | 指令 | 二次確認 |
|---|---|---|
| 唯讀 | `/status` `/list` `/positions` `/pnl` `/help` | 不需要 |
| 往安全方向 | `/off <策略>` `/stop_all` | **不需要** |
| 會開始下單 | `/on <策略>` `/start_all` | **需要** |

停用類刻意**不要**確認。使用者想緊急停止時，還要他多打一則訊息是有害的——
把摩擦放在「開始冒險」那一側，不要放在「收手」那一側。

#### 二、完全不提供下單/改單指令

`COMMANDS` 裡沒有 `/buy` `/sell` `/order`，而且有測試守住這件事
（`test_no_order_commands_exist`）。要買賣一律回到有畫面的主程式，走鐵則 14
的確認視窗。遠端只能決定「策略要不要跑」，不能直接動錢。

理由：下單需要看報價、看五檔、看部位才能判斷，這些手機上都沒有；一個只看到
文字的人做出的下單決定，品質必然低於在畫面前的同一個人。

#### 三、授權是白名單，且未授權者不給回覆

只有設定檔裡那**一個** `chat_id` 能下指令（`is_authorized` 用字串精確比對，
因為 chat_id 可能是負數群組 ID 或很長的數字，型別轉換出錯的代價是「別人能
控制我的交易系統」）。

未授權的指令**不回覆任何訊息**，只寫進系統日誌。回覆等於告訴對方「這個 Bot
是活的、後面接著一個真的交易系統」，沒有必要。

#### 四、確認碼：4 碼、120 秒、用過即棄

`/on` 或 `/start_all` 會先回一個確認碼，要在 120 秒內回傳 `/yes <碼>` 才生效。
確認訊息**必須寫明這一步會讓什麼開始動**——標的、週期、實單/模擬、數量；
`/start_all` 則列出實單策略的名字。手機上看不到畫面，只丟一個確認碼並不構成
知情同意。

碼用過就作廢（防重播），過期的待確認指令直接丟掉（避免下一次 `/yes` 收到
「確認碼不正確」這種誤導訊息）。

#### 五、遠端啟用走的是跟畫面完全相同的檢查

把原本埋在 `_qt_set_enabled()` 裡的前置檢查抽成 `_qt_enable_blockers()`，
GUI 的「▶ 啟用」按鈕與遠端 `/on` 共用同一份：

- 策略設定合法性（自訂策略/終極波段/一般策略各自的驗證）
- 【持倉核對】策略記錄的持倉與模擬帳戶實際部位是否一致

**持倉不一致時遠端一律拒絕啟用**，並要求回主程式處理。核對需要使用者在幾種
處理方式之間做選擇，手機上做不了這個決定，也不該用預設值幫他決定——起點錯
了，後面整條策略的行為都是錯的。

如果不抽這個共用方法，`/on` 就等於留了一條繞過驗證的側門。

#### 六、開關與通知分離、預設關閉

設定檔多一個 `remote_control` 欄位，跟 `enabled`（通知）**分開存**。兩者風險
等級完全不同：通知只是唯讀推播，遠端控制能讓系統開始下單，不該被同一個勾選
一起打開。舊設定檔沒有這個欄位時讀回 `False`。

沒開這個功能時**根本不建立輪詢執行緒**（不是建立後閒置），讓沒用這個功能的人
的行為跟加這個功能之前一模一樣。

### 為什麼用 getUpdates 輪詢而不是 webhook

webhook 需要公網 IP、網域與憑證，一般家用網路架不起來；輪詢只要能連外就行，
零額外基礎設施。代價是最多 3 秒延遲（`TG_POLL_INTERVAL`），對「啟用/停用
策略」這個用途完全足夠。

輪詢用 long polling（`timeout=25`），閒置時不會空轉燒流量。

### 執行緒與生命週期

輪詢在背景 daemon thread，但**指令一律用 `safe_after(0, ...)` 排回主執行緒**
執行（鐵則 13）：指令會改策略狀態、讀 tkinter 變數，在背景執行緒直接改會與
UI 競態。整個 worker 包在 try 裡，網路不穩不會影響主程式，也不洗日誌。

### 涉及檔案

| 檔案 | 內容 |
|---|---|
| `core/telegram_control.py` (新) | 授權、指令解析、確認碼、策略比對、回覆文字。零 tkinter/shioaji/網路 |
| `core/paper_account.py` | 新增 `realized_pnl_on(acct, day)`：某日已實現損益 |
| `data/config_store.py` | `save_telegram_config` 增加 `remote_control` |
| `stock_app_pro.py` | 輪詢 worker、指令派送、`_qt_enable_blockers()` 抽取、設定視窗勾選 |
| `tests/test_core.py` | +22 個測試（授權/確認/比對/文字/損益） |
| `diag_repro_issues.py` | +1 案例，走 GUI 這條路實測 9 個關卡 |

### 已驗證（headless）

- `tests/test_core.py` 510 個測試全過
- `diag_repro_issues.py` 37 案例全過，新案例涵蓋：
  未授權無效且無回覆 → 唯讀指令不改狀態 → `/on` 需確認 → 錯誤碼不放行 →
  正確碼生效 → 碼不可重播 → `/off` 免確認 → 總開關開需確認/關不需要 →
  過期碼失效且清掉 → 不合格策略擋下 → 持倉不一致擋下 → 無下單指令 →
  沒開遠端控制時輪詢不動作
- `diag_crossref.py` 無斷鏈、`py_compile` 全通過

### 需使用者實機驗證

1. 開「📱 Telegram 通知/遠端控制設定」→ 勾「⚠️ 啟用手機遠端控制」→ 儲存，
   確認系統日誌出現「已啟用手機遠端控制」。
2. 手機對 Bot 送 `/help`、`/status`、`/list`，確認 3 秒內收到回覆。
3. 送 `/on 1`，確認收到的確認訊息裡**標的、模式、數量都正確**；
   回 `/yes <碼>`，回電腦看策略清單那一列確實變成「啟用」。
4. 送 `/stop_all`，確認總開關立刻關閉（不需確認碼）。
5. 用**另一個** Telegram 帳號加這個 Bot 並下指令，確認**完全沒有回應**，
   且電腦端系統日誌出現「⛔ 拒絕未授權的指令」。
6. 關掉「啟用手機遠端控制」後重開程式，確認手機下指令不再有任何反應。
