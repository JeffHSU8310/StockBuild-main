# ADR-128:終極波段的訊號週期回到「日K」;12:00 二次確認獨立成一條通道

## 狀態

已實作,**尚未實機驗證**(分支 `claude/chukuangren-eval-cadence`)。

## 背景:使用者的追問

ADR-127 修掉「^TWII 日K 補資料通知一天推播十幾次」之後,我在說明裡寫了
「這是終極波段(看A 是 5分K,所以每 5 分鐘評估一次)去抓日K 資料」。使用者反問:

> 「可是,我明明設定就是看日K,為什麼,哪裡有看5分K?到底是哪裡有問題?」

**這個追問是對的,而且問到了 ADR-127 的上游。**

---

## 查證結果:三件事都成立

### 1. 使用者的設定沒錯 —— 訊號真的只看日K

```python
# stock_app_pro.py _quant_eval_pass() 終極波段分支
daily_df = self._qt_fetch_closed_bars(s, w_contract, w_asset, tf='日K', ...)
chukuangren_band.on_daily_close(params, rt, daily_df, ...)
```

`tf='日K'` 是寫死的。策略清單與「確認啟動」視窗也**固定顯示「日K」**,
而且原本就有註解說明「顯示殘留的『5分K』會讓人誤以為策略是照5分K在判斷」。

**所以在「訊號正確性」這一層,從來沒有 bug。**

### 2. 「5分K」寫死在兩處,而終極波段編輯器裡**沒有這個欄位**

| 位置 | 內容 |
|---|---|
| `core/chukuangren_band.py` `default_strategy()` | `s['watch_timeframe'] = '5分K'` |
| `stock_app_pro.py` 終極波段編輯器 `_collect()` | 同上,**每次存檔又寫一次** |

第二處是關鍵:使用者就算手動改策略檔,一按儲存又被蓋回去。而
「看A週期」那個下拉選單屬於 `_qt_build_watch_panel`,只有**一般策略編輯器**
在用 —— 終極波段編輯器完全沒有它。**使用者看不到、也改不了。**

### 3. 它的用途不是「訊號週期」,是「評估迴圈的節拍器」

`_quant_eval_pass` 的 K 棒邊界閘門拿 `watch_timeframe_of(s)` 算節拍:

```
watch_timeframe = 5分K → tf_mins = 5 → 每 5 分鐘才放行一次評估
```

為什麼需要 5 分鐘?因為「隔天中午 12:00 二次確認」的窗口是 **12:00~12:04**,
要有 5 分鐘的節拍才踩得進去;確認價格也是抓 5分K 的最後一根。

原始註解把這件事寫得很老實:

```python
s['watch_timeframe'] = '5分K'   # 驅動 _quant_eval_pass 每5分鐘評估一次 (隔日中午確認需要這個頻率)
```

**一個純內部的計時器,借用了一個語意是「A 的訊號週期」的欄位。**

---

## 這個借用造成的四個後果

| 後果 | 性質 |
|---|---|
| (a) 使用者設定/清單顯示都是日K,系統內部跑 5分K 節拍,而且無從查看 | **使用者實機回報的困惑本體** |
| (b) 每 5 分鐘整套重評一次(含抓 ^TWII 日K)→ **ADR-127 症狀的上游成因** | ADR-127 已擋住重抓與重複推播,節拍未動 |
| (c) `watch_timeframe_of()` 的語意是「A 的訊號週期」,任何照語意使用它的新程式碼都會拿 5分K 去判斷日K 策略 | **埋著的真 bug** |
| (d) **當天的二次確認只有一次機會** | 見下,這是查下去才發現的、最嚴重的一項 |

### (d) 值得單獨講:一天只有一次機會,失敗完全無聲

舊做法把二次確認巢狀在邊界閘門底下:

```
12:00:0x  5分K 邊界觸發 → _qt_last_boundary[id] = '12:00' → 做確認
12:05     下一個 5分K 邊界 → 但 in_noon_window 是 minute < 5 → 已在窗口外
```

也就是說 **12:00 那一次是唯一的機會**。那一次若拿不到 5分K
(ADR-122 的背景補齊中、ADR-121 的逾時、或單純空 DataFrame),
`if intraday_df is not None and not intraday_df.empty:` 靜靜地不成立,
**當天的二次確認就整個丟掉,沒有任何日誌**。

而終極波段的進出場**完全靠這個確認** —— 丟掉一次確認等於當天該進的沒進、
該出的沒出。這是比 (a)(b)(c) 都嚴重的問題,而它是在追這個「命名問題」時
才被翻出來的。

---

## 決定

### A. `watch_timeframe_of()` 對這類策略回真話(core 單一出處)

```python
# core/strategy_engine.py
DAY_SIGNAL_KINDS = frozenset({'chukuangren_band'})
DAY_SIGNAL_TIMEFRAME = '日K'

def watch_timeframe_of(strategy):
    if str((strategy or {}).get('kind') or '') in DAY_SIGNAL_KINDS:
        return DAY_SIGNAL_TIMEFRAME
    ...
```

覆寫存檔值,所以**既有策略不必遷移就正確**(同 `OWN_EXIT_KINDS` /
`DAY_SESSION_ONLY_KINDS` 的哲學)。一樣用字串避免循環 import,靠單元測試
把它跟 `chukuangren_band.KIND` 釘在一起。

**這一個覆寫同時解決 (a) 與 (c),而且順便把節拍改對** —— 因為節拍就是讀這個
函式:`timeframe_minutes('日K')` 回 `None` → 走「日K 類每 10 分鐘檢查」那一支。

**刻意不與另外兩份清單合併**:三者目前內容相同,但語意是三件事(出場邏輯 /
交易時段 / 訊號週期)。日後多一個「自己管出場但訊號看分K」的種類時,合併過的
寫法會一起把週期改錯。有一條單元測試守著這個區分。

### B. 12:00 二次確認搬到獨立通道

```python
def _qt_chukuangren_confirm_pass(self):   # 掛在 runner 既有的 2 秒輪詢上
```

- 窗口判斷收斂成 core 純函式 `chukuangren_band.in_noon_confirm_window(dt)`
  (常數 `NOON_CONFIRM_HOUR` / `NOON_CONFIRM_END_MINUTE`),GUI 不再寫
  `now_dt.hour == 12 and now_dt.minute < 5` 的字面值。
- **窗口內每個 tick 都會重試**,直到 `on_noon_confirm` 真的被呼叫 → 解決 (d)。
- 便宜的檢查放最前面(沒有待確認訊號 / 當天已確認過 → 完全不碰 API)。
- 補上 **ADR-124 的時段閘門** —— 三條會動到部位的路徑現在用同一套判斷。
- `KBarsPending` 靜靜 continue(資料在背景補,不是錯誤);其他例外**每檔策略
  每天只記一次日誌**,否則窗口內每 2 秒刷一次會變成 150 則(P-99 的同一類問題)。

`_qt_chukuangren_confirm_one()` 把「單一策略確認一次」抽出來,回傳值的語意是
「要不要下一個 tick 再試」——**資料還沒到就回 False**,這是修正 (d) 的核心。

### C. 資料本身也洗乾淨

`default_strategy()` 與編輯器 `_collect()` 都改存 `'日K'`,舊策略下次儲存時
就一致(真正的防線在 A,這裡只是讓資料不再自相矛盾)。診斷有**原始碼層級**
斷言,確保 `s['watch_timeframe'] = '5分K'` 不會再出現在這兩個檔案裡。

### 刻意拿掉的東西:`_forced` 橋接

中途版本在 `_quant_eval_pass` 留了一個 `if _forced:` 的橋接當測試接縫。
查證後發現 `_quant_eval_pass` 的**唯一呼叫端是 runner 且不帶參數**,
沒有任何測試或 production 路徑需要它 —— 實測拿掉之後 54 個診斷案例全綠。

留著只會變成「只有測試會走的 production 分支」+ 第二個確認呼叫點(P-67 的
風險),所以刪掉。**production 現在只有一條確認路徑。**

---

## 行為差異(逐項確認過)

| 項目 | 修正前 | 修正後 |
|---|---|---|
| 日K 訊號判斷的頻率 | 每 5 分鐘 | 每 10 分鐘 |
| 日K 訊號判斷的**結果** | — | **完全相同**(`on_daily_close` 靠 `last_daily_bar_date` 冪等,而日K 集合一天內不變 —— ADR-127 的同一個論證)|
| 12:00 二次確認的時機 | 12:00:0x 一次 | 12:00:00~12:04:59 內第一個成功的 tick |
| 確認失敗時 | 當天丟掉,無聲 | 窗口內持續重試;真的失敗有日誌(每天一則)|
| 12:01 送單 | 不變 | 不變(`on_execute_armed` 的 60 秒延遲沒動)|
| 一般策略 | — | **完全不動**(有反向對照守著)|

---

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| `watch_timeframe_of` 不覆寫(等於沒修)| 診斷紅:`訊號週期應是日K,實際 5分K` |
| `watch_timeframe_of` 永遠回日K | 診斷紅:`一般策略的訊號週期不可以被覆寫成日K`(反向對照)|
| `in_noon_confirm_window` 永遠 True | 單元測試紅 ×2 |
| `in_noon_confirm_window` 永遠 False | 單元測試紅 |
| 資料還沒到也算「已確認」 | 診斷紅:`資料還沒到就不可以算「已確認」` ← **(d) 的守門** |
| 拿掉新通道的時段閘門 | 診斷紅:`市場關閉時這條路徑也不該確認` |
| **runner 不呼叫 `_qt_chukuangren_confirm_pass`** | 診斷紅:`否則這條通道永遠不會跑` |
| 拿掉當天防重複(**兩層都拿掉**)| 診斷紅:`實際多抓了 1 次` |
| 只拿掉其中一層防重複 | 綠 —— 兩層各自足夠,是刻意的 defence in depth |

### 兩個空殼斷言(都是突變測試抓到的)

**空殼一:「同一天不可以重複確認」。** 原本寫成「確認成功後再呼叫一次,斷言
沒有多抓」。但 `on_noon_confirm` 成立後會把 `pending_entry` 清成 `None`,
於是第二次在**最前面那道「有沒有待確認訊號」**就 `continue` 了 —— 根本走不到
防重複那一行。把兩層 `last_confirm_date` 檢查全部拿掉,斷言照樣綠。
修法:**手動把狀態擺回**「有待確認訊號 + 當天已確認過」,直接測那道守門。

**空殼二(這一條差點漏掉):診斷全部都是直接呼叫 `_qt_chukuangren_confirm_pass()`,
所以完全測不到「production 有沒有人去呼叫它」。** 把 runner 那一行刪掉,
前 9 條斷言全都還是綠,但 12:00 確認一輩子不會發生。補了一條 `inspect.getsource`
的接線斷言(順序也一起檢查:確認要排在送單之前),那個突變才紅。
—— 這是 P-64 的教訓在「新增一條 pass」這種改動上的具體長相。

## 驗證

- `python tests/test_core.py` → **626 個全過**(原 616,新增 10)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **54 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

診斷案例走**完整的 GUI 路徑**(`_quant_eval_pass` → `_qt_chukuangren_confirm_pass`),
時鐘一律 patch,不依賴真實時間(P-94/P-97)。

## 需使用者實機驗證

1. **開啟終極波段策略編輯器按一次儲存**,之後日誌/訊息裡不該再出現任何
   跟這檔策略有關的「5分K」字樣。
2. **最關鍵:12:00~12:01 的二次確認與送單仍然正常。** 這是本次唯一動到
   交易行為的地方 —— 請在有待確認訊號的那一天盯著 12:00~12:02:
   - 12:00 過後應看到「12:00確認成立 ...,約1分鐘後 (12:01) 依...自動送單」
   - 12:01 應看到實際送單(模擬或實單)
   - 若確認條件不成立(價格退回 X 以內),應該安靜作廢、不下單
3. 日K 訊號的判斷結果跟以前一樣(進出場點位不該改變)。
4. 若出現「【自動交易-12:00確認異常】」那一行,**請把內容貼回來** ——
   那是新增的錯誤通道,它會告訴我們資料層還有什麼問題。

## 不在這次範圍

- `QT_TF_DAYS['日K'] = 300` 的天數(300 天 ≈ 205 個交易日,對 MA240 不足)。
- 兩檔同名策略在日誌上分不出來(ADR-123 已記)。
- 終極波段編輯器要不要**顯示**「訊號週期:日K(固定)」當唯讀資訊 —— 會多佔
  版面,而且清單已經顯示日K 了。等使用者實機看過再決定。
