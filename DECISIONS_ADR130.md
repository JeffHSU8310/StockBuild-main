# ADR-130:停損點位不受「進出場分界 X」限制(但仍必須真的填)

## 狀態

已實作,**尚未實機驗證**(分支 `claude/stop-level-free-of-x`)。

## 背景:使用者指出前一版的規則本身是錯的

ADR-129 才剛把驗證訊息寫得更清楚(「做空是指數『漲上去』才要停損」),
使用者直接指出那條規則不該存在:

> 「要改變一個邏輯,用做空來比喻,有時候停損點的位置不一定會高於進場點,
> 因為,實際進場的位置,已經離一開始設定的進場位置太遠,導致虧損會很大。
> 所以,你說停損點必須大於進出場分界,這一點不要被限制,是允許我自行設定,
> 你只要依照我設定的停損位置去判斷是否符合條件即可。」

**使用者是對的,而且指出了我推論裡的一個錯誤前提。**

## 錯在哪裡:把「設定的分界 X」當成了「進場價」

舊規則(做空 `S1 > X`)的推論是「做空要在指數漲上去時才停損,所以停損必須
高於進場點」。這個推論的第二步偷換了概念 —— **X 不是進場價**:

```
Day 1 收盤   指數 42400  (跌破 X=42500)      → 記下待確認空單訊號
Day 2 12:00  指數 41800  (仍在 X 之下) → 確認 → 12:01 進場
                  ↑ 實際進場的指數點位是 41800,不是 42500
```

這是這個策略**設計上的必然**:進場要「收盤跌破 X」+「隔天 12:00 二次確認」,
中間隔了一整個晚上與半個交易日,指數本來就會跑掉。

於是舊規則造成的實際後果是:

| 停損設在 | 舊規則 | 相對實際進場價 41800 的風險 |
|---|---|---|
| 42500 以上(被迫)| ✅ 可存 | **虧 700 點以上才停損** |
| 42000(使用者想設)| ❌ 被擋 | 虧 200 點就停損 ← **合理的風控** |

**規則把「合理的風控」擋掉、只允許「過寬的風控」。** 這不是使用者用錯,
是規則錯了。

## 決定

### A. 拿掉停損與 X 的相對關係檢查

```python
# 移除
if p['y'] >= p['x']:  return False, "多單停損點位 Y 必須小於進出場分界 X ..."
if p['s1'] <= p['x']: return False, "空單停損點位 S1 必須大於進出場分界 X ..."
```

**狀態機本來就不需要改。** 查證過:`on_daily_close()` 的出場路徑只用
`Y` / `S1` 與 `entry_index_price`,**完全沒有用到 X** —— X 只出現在進場條件裡。
所以「依照我設定的停損位置去判斷」這件事,邏輯層一直是這樣做的,
唯一擋路的就是那兩行驗證。

### B. 保留(並補齊)「有沒有真的填」的檢查

這是**刻意保留的限制**,而且跟使用者反對的那條完全不同性質:
`0` 不是合法的加權指數點位,而 0 的後果是災難性的 ——

| | 填 0 的後果 |
|---|---|
| 做多 `Y=0` | `close < 0` 永遠不成立 → **完全沒有停損** |
| 做空 `S1=0` | `close > 0` 永遠成立 → **每天都誤記待確認停損** |

而且**舊規則只「順便」擋到做空那半邊**(`0 <= X` 成立 → 擋);
做多的 `Y=0` 一直是個沒人擋的洞(`0 >= X` 不成立 → 放行 → 一檔沒有停損的
策略可以上線)。ADR-130 把兩邊都補上,順便補 `X <= 0`
(X=0 時做多變成「收盤 > 0」→ 每天都進場)。

> 也就是說:這次**放寬了一條錯的限制,同時補上一個真的洞**。

### C. 說明文字改寫,並把風險講明白

編輯器的參數說明原本寫「必須低於/高於 X」——那句話現在是錯的,改成:

- Y/S1 與 X 的高低**不受限制**,由使用者自行決定;
- 解釋為什麼(實際進場價不是 X);
- **⚠ 並且明講反面**:若做空的 S1 設得**低於實際進場價**,進場當天收盤就會
  滿足「收盤 > S1」→ 隔天 12:00 確認後立刻平倉。**這是設定的必然結果,
  系統不會阻止。**

使用者要的是「自己決定」,那就把決定所需的資訊給足,而不是用規則代替他判斷。

---

## 這次沒有一起放寬的東西(重要)

**進場條件仍然只看 X。** 使用者要改的是停損,不是進場。診斷有一條正控守著:
做空、`42600 > X` 時**不可以**記進場訊號、`42400 < X` 時**必須**記 ——
把進場也改成看 S1 的突變會讓那條紅。

停利那一段(C / F / SMA20 移動停利)完全沒動。

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| 把相對關係檢查加回來(等於沒修)| 單元紅;診斷紅:`檢查應已移除,但還找得到 p['y'] >= p['x']` |
| 連「有沒有填」都不檢查 | 診斷紅:`S1=0 會讓「收盤 > 0」永遠成立` |
| 停損誤用 X 而不是 S1 | 單元紅;診斷紅:`42100 已突破 S1=42000,要記待確認停損 (實際 None)` |
| 停損永遠觸發 | 診斷紅:`41900 還在 S1=42000 之下,不該停損` ← **正控** |
| **進場也被一起放寬** | 診斷紅:`42400 跌破 X,做空應記進場訊號` ← **範圍守門** |

後兩條是這次的重點:放寬一條限制時,最容易犯的錯是「順手放寬太多」。
一條測「不可以變成永遠觸發」、一條測「不可以連進場一起鬆掉」。

## 驗證

- `python tests/test_core.py` → **643 個全過**(原 637;新增 8、改寫 4)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **56 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨
- `py_compile` 全通過

診斷案例走**完整的 GUI 路徑**(`validate` → `_qt_enable_blockers` →
`_quant_eval_pass` → `_qt_chukuangren_confirm_pass`)。特別要走
`_qt_enable_blockers`:那是「啟用」與 Telegram `/on` 共用的閘門,
**只改 `validate()` 而那裡另外擋一次的話,使用者一樣啟用不了**(P-64)。

### 一筆規則的反轉記錄

`diag_repro_issues.py` 裡 ADR-129 案例的第 5 項,原本斷言使用者那組參數
(做空、X=42500、停損 41701)**應該被擋**;ADR-130 把同一組數字改成斷言
**必須存得起來**。同一行、相反的期待 —— 那就是這次規則變更的現場紀錄,
刻意留在註解裡說明為什麼反轉。

## 需使用者實機驗證

1. **重現截圖那組參數**(做空、X=42500、停損 **41701**)→ 這次應該**存得起來**。
2. 停損填 **0** → 應該仍然被擋,訊息會說「必須是大於 0 的加權指數點位」。
3. **啟用**那檔策略 → 不該再被啟用閘門擋(這是跟存檔不同的另一道關卡)。
4. 有空單部位時,**停損判斷以你填的那個點位為準**:指數收盤漲過該點位才進入
   待確認,隔天 12:00 仍在該點位之上才平倉。
5. **進場條件沒有變**:仍然是收盤跌破 X + 隔天 12:00 仍在 X 之下。

## 不在這次範圍

- 不自動調整任何既有策略的參數。
- 不新增「停損離實際進場價太近」的警告 —— 使用者明確要求「你只要依照我設定的
  停損位置去判斷」,系統不該用警告去質疑他的設定;風險已寫在參數說明裡。
- 進場條件、停利邏輯完全未動。
