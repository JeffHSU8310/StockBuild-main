
## ADR-076：繪圖效能 — 切換/縮放/平移變慢的三個熱路徑開銷 (gc、全圖底圖複製) + 渲染計時

### 背景 / 現象

使用者回報 (日K,已登入,首次出圖其實只花 0.5~0.7 秒):
1. 「切換時還是覺得很慢。」
2. 「放大縮小移動K線圖,畫面超級慢。」

第 1 點的資料載入已經很快 (搶先出圖 0.5 秒),所以慢的是**繪圖/重繪本身**;
第 2 點更直接指向 **matplotlib 渲染** 是瓶頸。

### 找到的熱路徑開銷

1. **`draw_chart` 每次都 `gc.collect()`**:full GC 在大 heap 上可達數十~上百 ms。
   而 draw_chart 在「切商品的搶先出圖 + 背景補全 (一次切換就兩次)、版面微調」
   都會跑到,每次都 full GC → 切換體感變慢。`plt.close(fig)` 已釋放圖物件,
   不需要每次都 collect。
2. **`_on_canvas_draw` 每次重繪都 `copy_from_bbox(整張圖)`**:這是給 hover 用的
   底圖快取。但**縮放/平移進行中**時,視野每一幀都在變、底圖每幀都作廢,這時候
   還把整張圖的點陣複製一份純粹是白花錢 —— 平移/縮放每一幀都被這個全圖複製拖累。

### 做法 (只動效能,不改繪圖結果)

1. **gc 挪出熱路徑**:draw_chart 改成每 12 次才 `gc.collect()` 一次 (記憶體仍
   有界,但大多數重繪不再付 full GC 成本)。
2. **互動中不做全圖底圖快取**:`_on_canvas_draw` 在 `is_panning` 或 `_zoom_active`
   時直接清掉 hover 底圖並 return,不做 `copy_from_bbox`;平移放開
   (`on_mouse_release` 已補一次 draw)、或縮放停止 200ms 後 (`_on_zoom_settled`)
   再補一次完整重繪,重新快取底圖,hover 的 blit 照常運作。
3. **縮放加 settle 節流**:連續滾輪縮放期間標記 `_zoom_active`,停止滾動 200ms
   後才補快取,避免每一格滾輪都做全圖複製。
4. **渲染計時 (診斷)**:draw_chart 末端量 `canvas.draw()` 實際毫秒數,只有
   > 350ms 才多印一行 (含 K 棒根數),讓使用者回報時能判斷「切換/縮放慢」是否
   卡在 matplotlib 渲染本身 (K 棒越多、面板越多、視窗越大越慢),不洗版。

### 為什麼這樣安全

- 只移除/延後「與畫面內容無關的額外工作」(gc、互動中的底圖快取),繪出來的圖
  完全一樣;hover blit 在互動結束後照常恢復。
- 沒有動資料、指標、mpf.plot 參數或版面。

### 仍受限之處 (誠實,可能需下一步)

- matplotlib/Agg 是 CPU 軟體渲染,單幀成本主要來自「視窗像素大小 × K 棒/線條
  數量」。日K (~243 根) 主要受視窗大小影響;分K 若一次顯示上千~上萬根,單幀
  渲染會明顯變慢 —— 這是 mplfinance 路線的結構性限制。新加的渲染計時就是要先
  量出實際毫秒數;若偏高 (例如 > 400ms),下一步可考慮:互動時可見範圍外的 K 棒
  降採樣、或評估更快的繪圖後端 (需另開 ADR,屬架構變動)。

### 相關程式位置 (stock_app_pro.py)

- `draw_chart`:gc 改每 12 次;末端渲染計時 (>350ms 才記)。
- `_on_canvas_draw`:互動中跳過 `copy_from_bbox`。
- `on_scroll_zoom` + 新增 `_on_zoom_settled`:縮放 settle 節流與事後補快取。

### 需使用者實機驗證 / 回報

1. 切換商品、縮放、平移是否比之前順一些。
2. 若仍慢:看系統日誌「載入成功 … 本次繪圖渲染 NNN ms (M 根K棒)」那行的數字,
   回報 NNN 與當時視窗大小/週期,好判斷是否要再做降採樣或換繪圖後端。
