# ADR-142：SQLite K 線歷史庫與每日增量更新

- **日期**：2026-08-03
- **狀態**：已實作，待實機驗證

## 決策

1. 程式啟動時自動建立 `data/kbars.sqlite3` 與 schema，使用者無須安裝
   額外資料庫伺服器。
2. 主鍵為 `(symbol, asset_type, timeframe, ts)`，同一根 K 棒重新取得時
   更新 OHLCV，不產生重複列。
3. 所有週期（含分 K）每次發布前先合併 SQLite 舊歷史，發布後
   再 upsert 這次結果；因此每日例行查詢會自動補上新 K 棒。
4. SQLite 已滿十年時不再呼叫深層歷史源，只用近期 API 結果
   更新最新日期；大幅減少重複下載。
5. 資料庫、WAL 與 SHM 檔都不進 Git；它們是每台電腦的本地行情快取。
6. 首次長週期建庫向 shioaji 請求最長 7,300 天，API 實際回傳多少
   就存多少；再以期交所期貨與 Yahoo 十年歷史補齊。

## 實機驗證

- 第一次載入長歷史後應產生 DB；重開程式再載入同商品，
  早期歷史應直接從 SQLite 合併。
- 隔日重新查詢後列數只增加新交易日，同日重查不得重複。
