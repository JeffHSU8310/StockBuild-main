
### P-64　被 API 流量管制擋住時，先問「這些請求真的需要發嗎」，而不是一路加重試
- **症狀**：回測/最佳化下載長期歷史時，`ServerError: kbars: request $P2P/...`
  整段整段失敗洗版。ADR-048 已經加了段間節流、退避重試、失敗切小段搶救，
  但只要期間夠長（20 年日K = 數十段），請求數量本身就會再次觸發管制。
- **根因**：節流/重試都是「想辦法把請求送出去」，沒有減少請求**總量**。
  管制是針對總量的，所以這條路線有天花板。
- **正確做法**：先確認「這段資料是不是已經有別的來源」。本專案的期貨 R1
  日/周/月K，本地期交所歷史往往已經涵蓋 99% 的期間，那部分**根本不必向
  券商要**（`split_coverage()` 算出真正缺的區間，只補尾巴）。實測 20 年回測
  從「數十段請求、頻繁被擋」變成「完全不發請求」。
  加重試是最後手段，不是第一手段；能不發的請求就不要發，順帶也不吃流量配額。
- **出處**：ADR-058（ADR-048 的延伸）。

### P-65　台指期歷史資料的「盤別口徑」會在 2017-05-15 中途改變 —— 長期回測必須明確選定口徑
- **症狀**：用期交所歷史做 1998~2026 的長期回測，前後段的隔夜跳空行為差很多，
  跳空型/開盤型策略的績效被嚴重扭曲，但表面上完全看不出來。
- **根因**：臺灣期貨市場的**盤後交易時段（夜盤）2017-05-15 才上線**。
  那之前根本沒有夜盤，資料必然只有日盤 08:45–13:45；之後
  `build_front_month_daily` 會把「一般＋盤後」合併成近全序列。
  同一個 CSV 的**定義在中途改變**——這不是資料錯誤，是市場事實。
  實測（使用者提供的期交所檔案）：TX 隔夜跳空中位數 0.3794% → 0.0644%
  （**5.89 倍**）、MTX 0.3465% → 0.0631%（5.49 倍），差異極為顯著。
- **正確做法**：
  1. **提供兩種口徑並各自存檔**（`TX.csv` 近全 / `TX_day.csv` 只用日盤），
     匯入時一次產生兩份，換口徑不必重新下載。
  2. **兩端要對齊**：期交所端選日盤時，shioaji 端也必須用
     `session_basis='day'` 只取 08:45–13:45 的分K 再聚合，否則接縫處會出現
     「前段日盤、後段近全」的隱性錯誤。
  3. **自動偵測 + 主動警告**：用「隔夜跳空中位數在 2017-05-15 前後的比值」
     實證判斷（不要相信任何欄位標示），比值 ≥ 2.5 就判定為混合口徑並警告。
  4. 把判斷依據（實測數字）直接寫在 UI 上，讓使用者自己決定，不要替他選。
- **可推廣的原則**：**任何跨越制度變更日的長期歷史資料都要先問「定義有沒有
  變」**。台股類似的斷點還有：漲跌幅 7%→10%（2015-06-01）、逐筆交易上線
  （2020-03-23）、現股當沖稅率調整等。回測期間橫跨這些日子時，同一個欄位
  在前後段可能不是同一個意思。
- **出處**：ADR-058。

### P-66　判斷資料性質時，優先用「行為實證」而不是「欄位標示」
- **症狀**：要判斷一份日K是否含夜盤，若只看資料來源的說明文件或欄位名稱，
  很容易誤判——舊資料可能根本沒有那個欄位，合併後的資料也看不出來源。
- **正確做法**：找一個**只有在該性質成立時才會出現的可量化行為特徵**，
  直接從資料本身算出來。本例用的是「隔夜跳空幅度」：
  含夜盤時 Open 距前一日 Close 只隔 75 分鐘（跳空小），只有日盤時隔約 19
  小時（跳空大）。實測比值高達 5.9 倍，判別力極強且不需要任何外部資訊。
  同樣的思路也適用於：判斷還原權息與否（除息日的跳空）、判斷資料是否含
  盤後定價（成交量的分布）、判斷是否為連續合約（換月日的價格斷點）。
- **附帶好處**：這種檢查可以做成常態化的自動診斷，資料源哪天悄悄改了格式
  也會被抓到，而不是等到回測結果怪怪的才發現。
- **出處**：ADR-058。
