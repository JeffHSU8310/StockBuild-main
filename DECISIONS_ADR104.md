
## ADR-104：補齊上櫃技術面歷史（ADR-103 最大落差的後續處理）

### 背景

ADR-103「誠實限制」第 6 點列為**本次最大的功能落差**：上櫃股票的技術面歷史
補不起來。原因是當時找到的上櫃日行情端點
`openapi/v1/tpex_mainboard_daily_close_quotes` **只有當日、沒有日期參數**，
歷史只能隨著每天更新逐日累積——在累積足夠根數之前，上櫃股票的技術面條件會
因 K 棒不足而永遠不通過。

使用者要求接續處理。

### 決策：改用可回溯的 `afterTrading/otc` 端點

實測找到櫃買另一支端點吃 `date` 參數：

```
https://www.tpex.org.tw/www/zh-tw/afterTrading/otc?date=YYYY/MM/DD&type=EW&response=json
```

回溯測試（實測非推測）：

| 日期 | 筆數 |
|---|---|
| 2026/07/24 | 1013 |
| 2026/07/17 | 1013 |
| 2026/06/15 | 1009 |
| 2026/03/10 | 996 |

回溯到 3 月都正常，足以支撐技術面所需的歷史深度。

日K 下載迴圈改成**同一天同時抓上市 + 上櫃**，兩個市場的歷史才會對齊，
不會出現「上市有 K 棒、上櫃沒有」的不對稱狀態。

### 兩個實測踩到的欄位陷阱

這支端點的欄位設計與其他來源都不同，兩個陷阱都必須靠「欄位名」而非位置解決：

**1. 欄位名帶前後空白**：`'收盤 '`、`' 成交金額(元)'`、`'成交股數  '`。
直接用字串比對會全部找不到。

**2. 欄位順序是「收盤、漲跌、開盤、最高、最低」——收盤在開盤前面**，
與一般 OHLC 慣例相反。這是最危險的一個：若照慣例寫死索引，開高低收會全部
錯位，而且數字看起來**仍然像價格**、不會拋任何錯誤，只會讓所有技術指標
（均線、KD、布林…）算出錯誤的值。

因此 `parse_tpex_daily_history()` 一律用 strip 後的欄位名建索引，缺必要欄位
時回空表而不是硬抓。測試中加了 OHLC 自洽檢查（`High >= max(Open, Close)`、
`Low <= min(Open, Close)`），欄位錯位時這條會立刻失敗。

### 驗證（實際下載，非模擬）

實際抓了 12 個交易日的兩市場資料：

```
2026-07-24: 上市1313 + 上櫃977 = 2290
2026-07-23: 上市1309 + 上櫃981 = 2290
...
2026-07-08: 上市1311 + 上櫃988 = 2299

本地累積:27577 列, 12 個交易日, 2324 檔
```

上櫃個股的技術面條件確認可用：

| 代號 | K棒數 | 技術條件可評估 |
|---|---|---|
| 6488 環球晶（上櫃） | 12 | ✅ |
| 5347 世界（上櫃） | 12 | ✅ |
| 2330 台積電（上市） | 12 | ✅ |

另外對真實下載的 977 筆做 OHLC 邏輯檢查（`High < Low`、`Close > High`、
`Close < Low`），**錯誤筆數 0**——確認欄位沒有錯位。

### 測試

`tests/test_core.py` 新增 2 個測試（欄位名帶空白 + 收盤在開盤前的順序陷阱、
缺欄位回空表），共 **470 個全數通過**。`diag_repro_issues.py` 35 案例全數
通過；`diag_crossref.py` 無斷鏈。

### 對 ADR-103 誠實限制的更新

第 6 點「上櫃的技術面歷史仍有落差」**已解除**：現在上櫃與上市一樣可以逐日
回補歷史，技術面條件對兩個市場都有效。

### 誠實限制

1. **首次更新時間變成約 2 倍**（每個交易日要抓上市 + 上櫃兩次請求）。抓 180
   天約 6 分鐘（1 秒節流 × 2 請求 × 180 天）。之後每天只補 1 天、2 次請求。
2. **回溯深度未測到底**：實測 2026/03 可用，更早未逐一驗證。若某日回空，
   該日會被跳過並在下次更新時重試。
3. 這支端點是**網頁版的查詢介面**（`www/zh-tw/afterTrading/otc`）而非正式
   OpenAPI，官方改版風險比 `openapi/v1/*` 高。已用欄位名比對 + 缺欄位回空表
   降低風險，但若官方大改版仍可能需要調整——屆時會是「上櫃日K 抓不到」的
   明確失敗，不會是無聲的錯誤資料。

### 需使用者實機驗證

1. 「選股」分頁 → 按「⬇ 更新全市場資料」，確認日誌顯示上市與上櫃都有下載。
2. 用「技術轉強」範本選股，確認結果中**出現上櫃股票**（4 碼以 3~8 開頭的
   代號多為上櫃，或雙擊查看確認）。
3. 雙擊一檔上櫃結果，確認能正常帶到主圖畫出 K 線。
