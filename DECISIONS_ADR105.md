
## ADR-105：選股加入產業別篩選

### 背景

ADR-103 誠實限制第 4 點：「資料源有產業別欄位（月營收表），但這次未納入 UI」。
使用者要求補上。

### 決策

**資料源**：月營收表的「產業別」欄位。上市 `t187ap05_L` 與上櫃
`mopsfin_t187ap05_O` **都有這個欄位、都是中文欄名**（與損益表不同——那邊
上櫃用英文欄名，見 ADR-103 驗證修正第 2 點）。實測覆蓋率
**1972/1974 檔（99.9%）**，共 36 種產業別。

**不寫死產業清單**：`industries_of()` 從實際資料讀出有哪些產業，UI 下拉在
載入基本面後才填。官方調整分類（新增/更名）時不會無聲漏掉那些股票。

**產業別為空時視為不符合**：與 ADR-103 決策三一致——指定產業時，分類不明的
股票不可混進來。

**放在篩選鏈最前面**：產業比對是最便宜的判斷（單純字串比對），先擋掉可以讓
後面的數值比較與技術面掃描少跑很多檔。

### 實測結果

以「本益比 ≤ 30」為共同條件：

| 產業 | 符合檔數 | 樣本 |
|---|---|---|
| 半導體業 | 63 | 2329 華泰、2379 瑞昱 |
| 金融保險業 | 31 | 2801 彰銀、2812 台中銀 |
| 光電業 | 36 | 2323 中環、2374 佳能 |
| 水泥工業 | 5 | 1102 亞泥、1103 嘉泥 |
| 不限產業 | 944 | — |

### 順帶修掉一個會崩潰的 pandas 型別問題

實作期間踩到：`base.loc[mask, 'EPS'] = [含 None 的 list]` 在新版 pandas 對
float64 欄位會直接拋 `TypeError: Invalid value 'None' for dtype 'float64'`，
而不是靜默轉成 NaN。

這個崩潰**只在「一批股票缺 EPS、另一批有」的資料組合才會出現**——小樣本
測試很容易漏掉，但真實全市場資料必然觸發。已改為一律包成
`pd.Series(dtype='float64')` 再指派，並補上專門的測試
（`test_merge_handles_mixed_missing_eps_without_dtype_error`）。

### 測試

`tests/test_core.py` 新增 6 個測試（產業篩選、空產業不混入、
`industries_of` 讀實際值、結果帶產業欄、pandas 型別、月營收帶產業別且
中英文欄名都讀得到），共 **476 個全數通過**；diag 35 案例全過。

### 誠實限制

1. **產業分類來自月營收表**，ETF 與少數沒有月營收申報的標的沒有產業別
   （實測 2/1974 檔），指定產業時它們不會出現。
2. **只支援單選**：UI 是下拉選單，一次一個產業。引擎本身支援多選
   （`industries` 參數吃 list），日後要改多選只需換 UI 元件。
3. 官方的產業分類粒度固定（如「半導體業」不再細分上下游），無法自訂。
