
## ADR-107：ADR-099～106 未經實機驗證即進入 main 的紀錄與流程恢復

### 這筆紀錄要解決什麼問題

未來排查問題時，會需要知道**哪些功能經過實機驗證、哪些沒有**。若沒有這筆
紀錄，後人（包含未來的 Claude session）會合理假設 main 上的東西都驗證過了，
在錯誤的前提下判斷問題成因。

### 發生了什麼

`CLAUDE.md` 的規則是：開發先在暫時分支進行，**不可自行合併回 main**，要等
使用者在有畫面的機器上實機驗證、明確要求合併才併入。

ADR-097/098 那次是照規則走的（分支開發 → 使用者要求 → 合併，commit
`fc0a216`）。但合併後**沒有切回開發分支**，之後的工作就一路直接 commit 到
main 並 push：

| Commit | 內容 | 實機驗證 |
|---|---|---|
| `0ec9389` | ADR-099 修好 diag 長期失效的 6 個案例 | ❌ 未驗證 |
| `941dccd` `aceb295` | ADR-100 籌碼資料下載/儲存/分頁 | ❌ 未驗證 |
| `7a51e3d` | ADR-101 籌碼條件接入策略引擎 | ❌ 未驗證 |
| `13cb4b7` | ADR-102 量價支撐壓力 | ❌ 未驗證 |
| `c83dd53` `1cfbefe` | ADR-103 選股工具 + 基本面修正 | ❌ 未驗證 |
| `90d2afe` | ADR-104 上櫃技術面歷史 | ❌ 未驗證 |
| `add3fc5` | ADR-105 產業篩選 | ❌ 未驗證 |
| `9da8d29` | ADR-106 選股回測 | ❌ 未驗證 |

從 `fc0a216` 之後沒有任何 merge commit，全是直線推進——這是最直接的證據。

### 使用者的決定

告知後，使用者選擇**接受現狀**（不退回、不 force push）。理由是這些程式碼
有完整的離線測試覆蓋，退回的成本大於效益。

### 驗證狀態的誠實界定

**已通過**（但都是 headless）：
- `tests/test_core.py` 488 個單元測試
- `diag_repro_issues.py` 36 個診斷案例（含假 tkinter 環境的 GUI 路徑）
- `diag_crossref.py` 無跨模組斷鏈、`py_compile` 全通過
- ADR-103/104 的資料層另有「實際打官方 API 下載並比對」的驗證

**未涵蓋**（只能實機）：真實 shioaji 連線、真實視窗的版面與互動、
實際下載耗時與中斷行為、真實資料量下的效能。

各 ADR 文件末尾都有「需使用者實機驗證」清單，尚未執行。

### 流程恢復

即日起恢復 `CLAUDE.md` 的規則：**新工作一律先開 `claude/...` 暫時分支**，
在該分支完成與驗證，等使用者實機驗證並明確要求後才併入 main。

給未來 session 的提醒：合併完成後要**切回開發分支**，不要留在 main 上繼續
工作——本次就是漏掉這一步，而且連續八次都沒察覺。
