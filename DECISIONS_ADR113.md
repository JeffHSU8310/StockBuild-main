
## ADR-113：ADR-108～112 依使用者要求併入 main，實機驗證狀態記錄

### 這筆紀錄要解決什麼問題

跟 ADR-107 同樣的理由：未來排查問題時，需要知道**哪些功能經過實機驗證、
哪些沒有**。沒有這筆紀錄，後人（包含未來的 Claude session）會合理假設
main 上的東西都驗證過了，在錯誤的前提下判斷問題成因。

### 發生了什麼

`CLAUDE.md` 的規則是兩個條件都成立才併入 main：
① 使用者在有畫面的機器上實機驗證沒問題 ② 明確要求合併。

這次使用者**明確要求合併**（條件②成立），但**尚未實機驗證**（條件①未成立）。
告知後仍要求合併，因此照辦，並留下這筆紀錄。

### 併入了什麼

兩個分支，各自都是完整的 merge commit（跟 ADR-107 那次「直線推進、沒有
merge commit」不同，這次分支流程本身是照規則走的）：

| 分支 | ADR | 內容 | 實機驗證 |
|---|---|---|---|
| `claude/telegram-remote-control` | ADR-108 | 手機 Telegram 遠端控制（查詢／開關策略／緊急全停） | ❌ 未驗證 |
| | ADR-109 | 成交量 Y 軸被重複定義蓋掉的修正 + 重複定義掃描 | ❌ 未驗證 |
| `claude/multi-broker-order-routing` | ADR-110 | 委託意圖抽象化 + 策略層級券商／帳號路由 | ❌ 未驗證 |
| | ADR-111 | 凱基 adapter | ❌ 未驗證 |
| | ADR-112 | 凱基獨立 3.13 子行程 + IPC | ❌ 未驗證 |

### 合併後在 main 上重跑的驗證（全部 headless）

- `tests/test_core.py` **527 個全過**
- `tests/test_brokers.py` **42 個全過**（含真的開子行程跑完整 IPC 鏈路）
- `diag_repro_issues.py` **40 案例全過**，連跑三次穩定 0 FAIL
- `diag_crossref.py` 無跨模組斷鏈、無重複定義
- `py_compile` 全通過

合併過程有三個檔案衝突（`ARCHITECTURE.md`、`PITFALLS.md`、
`diag_repro_issues.py`），三者都是**兩邊各自在檔尾追加內容**，以聯集解決，
沒有任何一邊的內容被丟棄。P 編號沒有撞號（telegram 分支用 P-53～55，
多券商分支刻意從 P-56 起）。

### 風險評估：這批東西進 main 的實際風險

**低風險（預設關閉／行為不變）**
- ADR-108 遠端控制：`remote_control` 預設 `False`，沒開就連輪詢執行緒都不建立
- ADR-110 階段1：驗收標準就是「送出的委託逐欄位與重構前相同」，有診斷守著
- ADR-111/112 凱基：`HAS_KGI=False` 且沒有 3.13 環境時完全不會被觸發

**需要留意的**
- ADR-109 改了成交量副圖的 Y 軸範圍計算 —— 這是**看得到的視覺改變**
  （長條會從 0 起算），第一次開圖時請確認是預期的樣子
- ADR-110 階段2 在策略編輯器加了一列「實單帳戶」—— **版面是 headless 完全
  測不到的**，若擠壞了會很明顯
- `self.sj_api` 改成 property：讀寫行為不變，但這是動到上百處呼叫的共用入口

### 尚未執行的實機驗證清單

各 ADR 文件末尾都有「需使用者實機驗證」章節，**全部尚未執行**：
`DECISIONS_ADR108.md` / `109` / `110` / `111` / `112`。

其中最關鍵的三條：
1. **ADR-110**：拿一個沒設定過帳號的**舊策略**送一張真單（零股 1 股），
   確認跟以前一樣正常成交 —— 這是「零行為改變」的最終驗證。
2. **ADR-108**：用**另一個** Telegram 帳號下指令，確認完全沒有回應且日誌
   有「⛔ 拒絕未授權的指令」 —— 那是整套遠端控制安全設計的地基。
3. **ADR-112**：手動跑一次 worker 送 `ping`，可在不動主程式的情況下單獨
   確認 3.13 環境與協定沒問題。

### 給未來 session 的提醒

ADR-107 記過一次「合併後沒切回開發分支，之後八次都直接 commit 到 main」。
這次合併完成後同樣要注意：**新工作一律先開 `claude/...` 分支**，不要留在
main 上繼續改。

---

## 追記：ADR-114 同樣依使用者要求併入 main（未實機驗證）

延續本 ADR 的用途，繼續記錄驗證狀態。

| 分支 | ADR | 內容 | 實機驗證 |
|---|---|---|---|
| `claude/kgi-verify-210` | — | 對 kgisuperpy 2.1.0 重新核對（純文件） | 不需要 |
| `claude/shioaji-1.7-upgrade` | ADR-114 | shioaji 1.5.6/1.7 相容層 | ❌ 未驗證 |

合併後在 main 重跑：`test_core` 540、`test_brokers` 42、`diag` 41 案例全過，
`crossref` 乾淨。

**ADR-114 的風險與前幾個不同，要特別留意**：它動到的是「怎麼找到合約」這條
所有台股功能的共同入口。雖然有相容層與突變測試，但**真實的 1.7 連線完全沒
驗證過**——尤其櫃買指數的代碼 `IX0002` 是推論而非證實。

好消息是失敗模式很好認、也很好修：指數若查不到，主圖會直接說「找不到合約
資訊」，把日誌貼回來改一行候選清單就好。而且**降版 `pip install
shioaji==1.5.6` 就能完全回到原狀，程式碼不用動**——這正是做成相容層而不是
改死成 1.7 的理由。

升級步驟與完整實機清單見 `DECISIONS_ADR114.md`。

---

## 追記二：ADR-115～117 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-115 | 診斷腳本改寫入暫存目錄（不再覆寫真實資料檔） | 不需要（開發工具） |
| ADR-116 | 籌碼／選股「開啟完整視窗」 | ❌ 版面未驗證 |
| ADR-117 | 帳戶顯示／選股欄位錯位／nan（實機回報的修正） | ⚠️ 問題本身是實機回報，修正未回驗 |

ADR-117 與前面幾批**性質不同**：它修的是使用者**實際遇到並回報**的三個問題，
不是預防性改動。三個都做過突變測試（把程式各自改回原本的錯誤寫法，確認診斷
案例會紅），所以「修正有效」這件事是有證據的；未驗證的是「在真實環境下是否
還有其他相關症狀」。

同時 ADR-114 追記二的 `IX0043` 已由使用者實機確認 **`^TWOII` 沒有問題** ——
這是本輪第一個真正被實機驗證通過的項目。

合併後在 main 重跑：`test_core` 544、`test_brokers` 42、`diag` 43 案例全過，
`crossref` 乾淨。合併衝突三處（`stock_app_pro.py` 的 `_sc_fill_tree`、
`PITFALLS.md`、`diag_repro_issues.py`）都是兩邊各自新增，以聯集解決，
沒有任何一邊的內容被丟棄；P 編號無撞號（ADR-116 用 67/68，ADR-117 用 69~71），
並依編號重新排序。

---

## 追記三：ADR-118 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-118 | 籌碼單位（張／億）、融資增減、資料年數可設定、回測參數攤開 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑：`test_core` 555、`test_brokers` 42、
`diag` 44 案例全過，`crossref` 乾淨。

**這一批的風險偏低但要留意兩件事**：

1. **單位換算是「看得到就知道對不對」的改動**。三種換算都有突變測試守著
   （改錯係數或把增減方向寫反都會讓診斷案例變紅），所以算式本身有把握；
   真正沒驗證的是「原始資料的單位是不是真的如我假設」——`元`／`仟元`／`股`
   是照官方欄位定義與使用者截圖的表頭推的。**看一眼數量級就能確認**。
2. **年數調大會讓下載時間顯著拉長**。10 年的籌碼約 2400 個交易日 × 4 個端點
   × 1 秒節流 ≈ 3 小時。下載是增量的（ADR-100），中途按停止不會白費，
   下次會接續補；但第一次調大時要有心理準備。

### 本輪目前唯一通過實機驗證的項目

`^TWOII`（`IX0043`，ADR-114 追記二）。其餘 ADR-108～118 仍全部未實機驗證。

---

## 追記四:ADR-119～120 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-119 | 關不掉 (GIL 餓死 UI)、櫃買無資料誤報、期貨法人金額(億)、指數帶入編輯器 | ❌ 未驗證 |
| ADR-120 | 盤勢/型態搬到主圖【盤勢判斷】、啟動預設加權指數日K | ❌ 未驗證 |

合併無衝突(分支本來就沿著 main 直線推進)。合併後在 main 重跑:
`test_core` **575**、`test_brokers` 42、`diag` **46 案例全過**、`crossref` 乾淨、
`py_compile` 全通過。

### 這兩批的風險輪廓不一樣,要分開看

**ADR-119 是「修使用者實際遇到的問題」**,其中「關不掉」那條的修法是
**批次寫檔 + 強制結束看門狗**兩道。第一道是真正的修正,第二道是保底 ——
意思是**就算第一道沒修乾淨,現象也會被第二道蓋掉**(按 X 之後 8 秒強制
結束)。所以實機驗證時要注意的不是「有沒有關掉」,而是**「按下 X 之後
是不是立刻就關掉」**;如果每次都要等好幾秒才消失,代表 UI 還是被卡住、
是看門狗在收尾,第一道沒有真的生效。

**ADR-120 是「搬家 + 改版面」**,風險集中在 headless 完全測不到的地方:

1. **工具列版面**。原本的「支撐壓力 + 計算範圍下拉」換成「☑ 盤勢判斷 ⚙」,
   工具列本來就很擠,擠壞了會很明顯。
2. **通知頻率**。主圖每縮放/平移一次就重畫一次,去重寫錯就會洗版。
   離線有斷言(連畫 4 次只該通知 1 次)、也做過突變測試,但**實機的重畫
   節奏跟離線不同**,請實際拖幾下確認。
3. **終極波段策略的儲存路徑**。編輯器移除了一整區,`_collect()` 也拿掉了
   `pattern_*` 幾個欄位 —— 舊策略載入後**再按一次儲存**要能正常存下去,
   這是 headless 測不到的地方。

好消息是兩者的失敗模式都很好認、也都不動到下單:ADR-120 完全不產生任何
委託 intent(只寫系統日誌),ADR-119 動到的是下載寫檔節奏與關閉流程,
都不在下單路徑上。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～120 其餘全部
未實機驗證**,各 ADR 文件末尾的「需使用者實機驗證」章節都還沒執行。

---

## 追記五:ADR-121～122 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-121 | 開盤暖機、抓K線跨 tick 重試、資料錯誤不再自動停用策略 | ❌ 未驗證 |
| ADR-122 | 大範圍K線改走背景分段預抓、日K 類快取 TTL 300→1800 秒 | ❌ 未驗證 |

合併無衝突(分支沿著 main 直線推進)。合併後在 main 重跑:
`test_core` **591**、`test_brokers` 42、`diag` **48 案例全過**、
`crossref` 乾淨、`py_compile` 全通過。

### 這一批的性質跟前幾批不同

ADR-121 修的是**使用者實際回報並貼出日誌**的問題(08:45/09:00 各一次
`ShioajiTimeoutError`),ADR-122 是使用者追問「日K 以上的風險怎麼修」後
才做的。兩者都**不動下單邏輯**——動到的是「策略怎麼拿到 K 線」。

**風險集中在一個地方:量化 runner 的資料取得路徑是所有自動交易策略的
共同入口。** 好消息是失敗模式很好認:

- 策略**遲遲不評估**(暖機閘門放錯位置、或預抓永遠補不完)
- 日誌一直冒「【自動交易-資料異常】」(預抓一直失敗)

兩者在日誌上都看得見,不會無聲無息。

**現成的退路**:ADR-122 的 `QT_PREFETCH_SYNC` 類別常數設成 `True`,
就會退回「原地把分段做完」的行為(等同 ADR-122 之前的同步語意);
`QT_OPEN_WARMUP_SEC` 設 0 則等同關掉開盤暖機。兩個都不用改程式邏輯。

### 最關鍵的兩條實機驗證

1. **開盤暖機(ADR-121)**:明天 08:45 / 09:00 看日誌有沒有出現一次
   「先等 30 秒避開開盤尖峰再開始抓K線」,而且 **08:50 之後策略照常評估**
   ——暖機閘門若放錯位置會把整根 K 棒吃掉,那會是靜悄悄的功能損失。
2. **月K 策略補資料期間不可卡頓(ADR-122)**:月K 要分 17 段,補齊期間
   主程式必須照常反應、期貨即時停損停利照常運作。這是 P-90
   (單執行緒迴圈裡別做會阻塞的事) 的實地驗證。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～122 其餘全部
未實機驗證**,各 ADR 文件末尾的「需使用者實機驗證」章節都還沒執行。

---

## 追記六:ADR-123 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-123 | 終極波段不受泛用即時停損;開盤暖機不被時段閘門關掉 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **597**、`test_brokers` 42、
`diag` **49 案例全過(連跑 8 次都是 0 FAIL)**、`crossref` 乾淨、
`py_compile` 全通過。

### 這一批跟前幾批的性質差很多

前面幾批多半是「預防性」或「效能/韌性」改動,ADR-123 修的是一個
**已經造成實際損失的邏輯錯誤**:終極波段策略被一個編輯器裡看不到的 2%
停損砍倉(使用者的模擬帳戶因此吃了 -968,250,而且因為有兩檔同名策略,
實際發生了兩次)。

**風險方向跟平常相反**:這次是「拿掉一條原本會觸發的行為」,所以要擔心的
不是「新功能會不會壞」,而是**有沒有拿掉太多** —— 萬一守門寫得過寬,
一般期貨策略的即時停損也會一起失效,那比原本的問題更嚴重(ADR-087 當初
就是為了補「帳面已經虧超過停損點系統卻沒動作」這個缺口)。

因此單元測試與診斷各留了一條**反向對照**:一般期貨策略在完全相同的條件下
**仍然要**被即時停損平倉。突變測試證實:把守門改成 `return True`
(全部都擋),先紅的就是這條。

### 合併流程本身抓到一個問題,值得記

這次「合併後在 main 重跑」抓到了一個**跟 ADR-123 無關**的既有問題:診斷
腳本依賴真實時鐘,1分K 案例在每分鐘的前 2 秒會被跳過,約 3.3% 機率隨機紅
一個案例。已修(見 `DECISIONS_ADR123.md` 追記、`PITFALLS.md` P-94)。

**這正是「合併後一定要在 main 上重跑」的價值** —— 分支上連跑兩次都是綠的,
是合併後那一次才紅。這個習慣不要省。

### 最關鍵的實機驗證

1. **終極波段持有部位時,價格上下震盪不可以再被砍倉** —— 進出場只能由
   X/C/F/Y/Z 決定。這是本次修正的核心。
2. **一般期貨策略的即時停損仍要正常運作**(反向確認,別只驗第 1 條)。
3. **重開程式後**,08:45 / 09:00 / 15:00 三個開盤時刻都不再出現 kbars 逾時。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～123 其餘全部
未實機驗證**。

---

## 追記七:ADR-124～125 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-124 | 即時停損補交易時段閘門;終極波段結構上只做日盤 | ❌ 未驗證 |
| ADR-125 | 啟用中的策略不可刪除(編輯本來就已是唯讀)| ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **609**、`test_brokers` 42、
`diag` **51 案例全過(連跑 5 次都是 0 FAIL)**、`crossref` 乾淨、
`py_compile` 全通過。

### 這兩筆都是使用者實測回報的問題,而且都是「守門漏了一個條件」

- **ADR-124**:即時停損是後來為了補 ADR-087 的缺口而加的**獨立通道**,
  加的時候把「怎麼做」抄過去了,忘了抄「什麼時候可以做」——
  三條會下單的路徑裡只有它沒有時段閘門。
- **ADR-125**:刪除只擋了「有持倉」,沒擋「啟用中」;而同一個畫面的**編輯**
  早就擋住了 —— 兩個入口各寫各的規則,於是一個對、一個漏。

兩者的修法一致:**把規則收斂成 core 的純函式,所有入口共用一份**
(`include_night_of()` / `is_locked()`+`can_delete()`)。這已經是連續第三次
用同一招(ADR-123 的 `OWN_EXIT_KINDS` 是第一次),記在 P-95 / P-96。

### 風險方向:兩筆都是「拿掉原本會發生的行為」

所以要擔心的不是「新功能會不會壞」,而是**有沒有拿掉太多**:

- 即時停損會不會在**該觸發的時候**不觸發了?
- 刪除會不會**整個**被鎖死?

因此每一項都配了**反向對照**測試,而且突變測試證實:把守門寫成
「全部都擋」時,先紅的就是那些反向對照。

### 最關鍵的實機驗證

1. **終極波段在 15:00 之後完全安靜**;一般期貨策略設「只做日盤」夜盤不被
   停損、設「日盤+夜盤」的**夜盤仍要停損**(兩個方向都要驗)。
2. **日盤時段的即時停損仍然正常**(ADR-087 的功能沒被弄壞)。
3. **停用之後,刪除與編輯要恢復正常** —— 不然可能是整個功能被鎖死,而不是
   只鎖啟用中的。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～125 其餘全部
未實機驗證**。

---

## 追記八:ADR-126 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-126 | 活K棒 blit 失效要有退路;報價上屏加速(快照節流未動)| ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **609**、`test_brokers` 42、
`diag` **52 案例全過(連跑 5 次都是 0 FAIL)**、`crossref` 乾淨、
`py_compile` 全通過。

**這次刻意挑 14:54 跑** —— 那是期貨日盤(13:45)收盤到夜盤(15:00)之間的
空檔,正是上一批埋進 main 的時鐘相依會露餡的時段(見下)。

### 這一批同時修掉了「我自己上一批埋進 main 的問題」

ADR-124 給 `_qt_check_realtime_futures_stops` 補上交易時段閘門之後,
ADR-123 的診斷案例「一般期貨策略仍應被即時停損平倉」就**悄悄變成看真實
時鐘**了:日盤/夜盤跑就綠,卡在 **13:45~15:00 空檔**跑就紅。

**當時我連跑 5 次全綠就把它併進 main,因為那 5 次都落在日盤內。**
這是 P-94 才剛寫過的教訓的現場重演,現在補記成 **P-97**,而且修法不只是
改那一個案例 —— 改成由 `run_case` **統一凍結時鐘相依的表面**,新案例不可能
再不小心依賴真實時鐘。

> 給未來 session 的提醒:**「連跑 N 次都沒紅」不是證明,只是沒有反證。**
> 驗證跟時間有關的東西時,要**刻意挑會露餡的時段**跑,不要只在方便的時候跑。

### ADR-126 的風險輪廓

- **退路那一半是「多做事」而不是「少做事」**:blit 走不通時多一次(節流過的)
  完整重繪。最壞情況是畫面比較吃資源,不會少畫東西,方向上是安全的。
- **加速那一半刻意只碰本地端**:活K棒 blit 與自選股串流上屏都不打 API;
  **股票批次快照的 2.5 秒一秒都沒有動**,而且把「上屏節奏」與「快照節奏」
  解耦,以後只調上屏不會誤傷鐵則 5 的節流。診斷有一條**真的把 worker 跑起來
  數呼叫次數**的行為測試守著(第一版寫成在診斷裡重算同一條除法,是空殼,
  被突變測試抓到)。

### 最關鍵的實機驗證

1. **主圖最後一根 K 棒要自己跳動**(不用碰滑鼠);**縮放/平移之後要繼續跳**。
2. 若日誌出現「**【主圖】活K棒改用完整重繪更新 (原因: xxx)**」——
   畫面仍會動,但**那個原因代碼要貼回來**:那是這次無法靜態定位的那一條,
   拿到就能精準收尾。
3. **不可以**出現新的 API 流量/管制警告(這次沒有動任何 API 節流)。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～126 其餘全部
未實機驗證**。

---

## 追記九:ADR-127 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-127 | 日K 類快取改問「有沒有跨過開盤」;補資料通知每天只一次 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **616**、`test_brokers` 42、
`diag` **53 案例全過(分支上連跑 3 次、main 上連跑 2 次都是 0 FAIL)**、
`crossref` 乾淨、`py_compile` 全通過。

**這次的執行時段是 02:42**(期貨夜盤凌晨段內、台股休市)。照 P-97 的要求把
時段講清楚:ADR-126 之後 `run_case` 已經統一凍結 `is_market_open` /
`just_opened`,所以案例本來就不該受此影響 —— 但「不該受影響」要靠**在不同
時段都跑過**來佐證,不能只靠讀程式碼。這次(凌晨)與上一次(14:54,日盤與
夜盤之間的空檔)是兩個相反的時段,兩次都綠。

### ADR-127 的風險輪廓

- **主圖完全沒有被動到**:`_kbars_cache_get` 新增的 `fresh_check` 參數不傳就
  是原本的 TTL 行為,主圖的呼叫端一個字都沒改。
- **分K 策略也沒被動到**:仍走 30 秒 TTL。診斷有一條專門守著這個區別
  (把分K 也改成 session 判斷,那條會紅)。
- **方向上偏「多抓」而不是「少抓」**:開盤時刻取所有商品聯集、不看國定假日、
  參數異常/時鐘倒退/跨超過 7 天一律當過期,再加 24 小時絕對上限。
  最壞情況是多抓一次資料(慢一點),不會拿舊資料去下單。
- **唯一的行為改變風險**是「快取被判新鮮而策略拿不到新資料」。這正是
  實機驗證第 2 點要看的。

### 最關鍵的實機驗證

1. **早上第一次啟動策略時「歷史資料分 N 段在背景補齊中」只收到一則** ——
   之後一整天不該再收到同一則。這是使用者這次回報的主體。
2. **補完之後策略確實開始評估**(不可以因為快取被判新鮮就卡住不動)。
   這是本次改動唯一可能「做得太過」的方向,請特別留意。
3. **隔天**再看:新的一天應該會再收到一則(每天一次是預期行為)。
4. 分K 策略的訊號沒有變慢或漏掉。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～127 其餘全部
未實機驗證**。

### 規則落差(照實記錄)

`CLAUDE.md` 要求「使用者實機驗證通過」+「明確要求合併」兩個條件才可併入
main。這次②成立(使用者:「請修正,並且合併到main」)、①不成立,
照前八次的慣例辦理並留下這筆紀錄。

---

## 追記十:ADR-128 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-128 | 終極波段訊號週期回到日K;12:00 二次確認獨立成通道 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **626**、`test_brokers` 42、
`diag` **54 案例全過**(分支上連跑 3 次、main 上連跑 2 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。執行時段 03:42(期貨夜盤凌晨段)。

### 這一批的風險輪廓跟前幾批不同,要講清楚

ADR-121~127 的改動幾乎都是「少做事」或「本地端」——修錯的節流、擋掉不該有的
停損、不重抓已知不變的資料。**ADR-128 動到了下單前的最後一道關卡**:
終極波段的「12:00 二次確認」從 `_quant_eval_pass` 搬到獨立的
`_qt_chukuangren_confirm_pass`。

風險分兩個方向,兩邊都要看:

| 方向 | 症狀 | 為什麼相信不會發生 |
|---|---|---|
| **少做**:確認不再發生 | 12:00 該進的沒進、該出的沒出 | 診斷有「窗口內必須真的完成確認」+「runner 必須接上這條通道」兩條斷言,後者是 `inspect.getsource` 的接線檢查 |
| **多做**:確認在不該的時候發生 | 非 12:00 時段莫名送單 | 診斷有「窗口外連 5分K 都不該抓」+ ADR-124 的時段閘門兩道;`in_noon_confirm_window` 有 4 條單元測試 |

**但這兩個方向都只有離線證據。** 12:00 二次確認一天只發生一次、而且要先有
待確認訊號才會發生,是這個系統裡最難用診斷完整覆蓋的路徑之一 ——
所以第 2 項實機驗證比前幾批都重要。

### 這一批同時修掉了「ADR-127 沒有修到根」

ADR-127 修的是症狀(重抓 + 重複推播),ADR-128 才是成因:
`watch_timeframe` 被當成節拍器,把日K 策略的評估頻率拉成 5 分鐘。

使用者的追問(「我明明設定就是看日K,為什麼哪裡有看5分K?」)直接指向這一層 ——
**而我在 ADR-127 的說明裡把「看A 是 5分K」當成既定事實寫了出來,沒有去問
「為什麼一個日K 策略的看A 會是 5分K」。** 記下來:解釋一個症狀時,
如果解釋裡有個「本來就這樣」的前提,那個前提往往就是下一個 bug。

順帶翻出 P-101 那個更嚴重的問題(二次確認一天只有一次機會、失敗無聲)——
它跟這次的命名問題是同一個借用造成的,但危害大得多,而且**在使用者回報之前
沒有任何人發現**(包含我先前七批的驗證)。

### 最關鍵的實機驗證

1. **開終極波段編輯器按一次儲存**,之後訊息裡不該再有這檔策略的「5分K」字樣。
2. **12:00~12:01 的二次確認與送單**(本次唯一動到交易行為的地方,務必盯):
   - 12:00 後應看到「12:00確認成立 …,約1分鐘後 (12:01) 依…自動送單」
   - 12:01 應看到實際送單
   - 條件不成立時應安靜作廢、不下單
3. 日K 訊號的進出場點位不該改變。
4. 若出現「**【自動交易-12:00確認異常】**」請把內容貼回來(新增的錯誤通道)。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～128 其餘全部
未實機驗證**,其中 ADR-128 是唯一動到送單前流程的一筆。

### 規則落差(照實記錄)

`CLAUDE.md` 要求「使用者實機驗證通過」+「明確要求合併」兩個條件。這次
②成立(使用者:「合併main」)、①不成立。照前九次的慣例辦理並留紀錄 ——
**但這一筆的性質跟前九筆不同**(動到下單前流程),所以上面把風險輪廓
與「要盯什麼」寫得比往常詳細。

---

## 追記十一:ADR-129 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-129 | 終極波段停損的觸發/確認合併成一個點位 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **637**、`test_brokers` 42、
`diag` **55 案例全過**(分支上連跑 3 次、main 上連跑 2 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。執行時段 11:55(台股盤中、期貨日盤)——
跟 ADR-127/128 的凌晨時段相反,兩種時段都跑過。

### 這一批的性質:動到停損的判斷值,但方向是「更保守地持有」

行為差異只有一處,而且**只影響舊策略檔裡 z ≠ y 的情況**:

| 情境(做多、Y=90、舊檔 Z=92)| 舊行為 | 新行為 |
|---|---|---|
| 隔天12:00 指數 91 | 91 < Z=92 → **平倉** | 91 > Y=90 → **繼續持有** |
| 隔天12:00 指數 89 | 平倉 | 平倉(相同)|

也就是新行為在「觸發線與確認線之間」那個帶狀區間選擇繼續持有 ——
對使用者而言就是「停損只有我填的那一個點位」。

**風險方向要說清楚**:這是「比以前晚一點停損」,不是「不停損」。
真的跌破那一個點位仍然會平倉,診斷與單元測試都有正控守著
(把覆寫值改成 0 等於關掉停損,那條正控就紅)。

### 使用者那組參數仍然會被擋 —— 這是對的

截圖裡做空、X=42500、停損填 41701(在 X 之下)。**合併之後仍然存不起來**,
因為那是真的填錯(做空要在指數漲上去時停損)。這次只把訊息改成會講原因:

```
空單停損點位 S1 (41701) 必須大於進出場分界 X (42500) —— 做空是指數「漲上去」才要停損。
```

**沒有為了讓它存得起來而放寬驗證** —— 那會讓一檔永遠不會停損的策略上線。

### 最關鍵的實機驗證

1. **編輯器**:做多只看到 Y 一個停損欄位、做空只看到 S1 一個;切換方向要正確換。
2. **重現截圖那組參數**(做空、X=42500、停損 41701)→ 應**仍被擋**,
   但訊息會告訴你要填大於 42500 的值。
3. 改成大於 X 的值 → **存得起來**。
4. **既有兩檔終極波段**:打開看停損欄位顯示的是原本的 Y / S1(不是 Z / S2)。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～129 其餘全部未實機驗證**,
其中 ADR-128(送單前流程)與 ADR-129(停損判斷值)是兩筆動到交易行為的。

### 規則落差(照實記錄)

②「明確要求合併」成立(使用者:「合併main」)、①「實機驗證通過」不成立。
照前十次的慣例辦理並留紀錄。

---

## 追記十二:ADR-130 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-130 | 停損點位不受進出場分界 X 限制(仍要求 > 0)| ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **643**、`test_brokers` 42、
`diag` **56 案例全過**(分支上連跑 3 次、main 上連跑 2 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。執行時段 12:21(台股盤中)。

### 這一筆是「推翻我自己前一筆的推論」

ADR-129 我不但保留了「做空停損必須 > X」這條規則,還特地把它的訊息**寫得更
理直氣壯**(「做空是指數『漲上去』才要停損」)。使用者下一句就指出那條規則
本身是錯的 —— 因為 **X 不是進場價**:進場要等 X 被跌破、再等隔天 12:00 確認,
X=42500 設定,實際可能在 41800 才進場。

所以我那句「幫使用者講清楚為什麼」其實是**把一個錯的前提講得更有說服力**。
記下來:

> **把驗證訊息寫得更詳細之前,先確認那條驗證本身是對的。**
> 詳細的錯誤說明會讓錯的規則更難被質疑 —— 使用者會以為是自己不懂。

### 這一批同時補上一個真的洞

舊規則 `s1 <= x` 順便擋掉了「做空 S1=0」,但做多的 `y >= x` 對 `Y=0` 是放行的
(`0 >= 42500` 不成立)。也就是說**一檔完全沒有停損的做多策略一直可以上線**
(`close < 0` 永遠不成立)。這次把兩邊的 `> 0` 檢查都補上,連 `X > 0` 一起。

> 一條靠副作用生效的檢查,幾乎一定有沒被覆蓋到的那一半(P-103)。

### 風險輪廓

- **放寬的方向是「允許更緊的停損」**,不是「允許沒有停損」——「有沒有填」
  的檢查比以前更嚴(補了做多那半邊)。
- **進場條件與停利邏輯一個字都沒動**。診斷有專門的範圍守門:把進場也改成看
  S1 的突變會紅。
- 唯一新的使用者風險是**設定上的**:若做空的 S1 設得低於實際進場價,進場當天
  收盤就會觸發停損 → 隔天 12:00 平倉。這是使用者自己決定的必然結果,已寫進
  參數說明的 ⚠ 那一行。使用者明確要求「你只要依照我設定的停損位置去判斷」,
  所以**沒有加警告去質疑他的設定**。

### 最關鍵的實機驗證

1. **截圖那組參數**(做空、X=42500、停損 41701)→ 這次應該**存得起來**。
2. 停損填 **0** → 應仍被擋(訊息:必須是大於 0 的加權指數點位)。
3. **啟用**那檔策略 → 不該被啟用閘門擋(那是跟存檔不同的另一道關卡,
   Telegram `/on` 也走同一條)。
4. 有空單部位時,停損判斷以你填的那個點位為準。
5. **進場條件沒變**:仍是收盤跌破 X + 隔天 12:00 仍在 X 之下。

### 本輪目前唯一通過實機驗證的項目

仍然只有 `^TWOII`(`IX0043`,ADR-114 追記二)。**ADR-108～130 其餘全部未實機
驗證**;其中動到交易行為的有三筆:ADR-128(送單前流程)、ADR-129(停損判斷值)、
ADR-130(停損可設範圍)。

### 規則落差(照實記錄)

②「明確要求合併」成立(使用者:「修正好後,自動合併main」)、①「實機驗證通過」
不成立。照前十一次的慣例辦理並留紀錄。

---

## 追記十三:ADR-108～130 全部通過實機驗證 + ADR-131 併入 main

### 一、使用者回報:「所有的都驗證好了」

從追記一到追記十二,每一筆都寫著同一句話 ——
**「本輪目前唯一通過實機驗證的項目仍然只有 `^TWOII`(`IX0043`)」**。
這次使用者明確回報:**所有的都驗證好了**。

| 範圍 | 先前狀態 | 現在 |
|---|---|---|
| ADR-108 ～ ADR-130 | ❌ 未實機驗證(23 筆)| ✅ **已通過實機驗證** |

其中特別值得記錄的是三筆動到交易行為的:

| ADR | 內容 | 為什麼特別在意 |
|---|---|---|
| ADR-128 | 12:00 二次確認搬到獨立通道 | 動到**送單前**的最後一道關卡,離線測不完整 |
| ADR-129 | 停損觸發/確認合併成一個點位 | 動到**停損的判斷值** |
| ADR-130 | 停損可設範圍不再受 X 限制 | 動到**停損的可設範圍** |

這三筆的離線證據(單元 + 診斷 + 突變)先前都齊了,但我在追記十、十一、十二裡
都寫明「12:00 確認一天只發生一次,是這個系統裡最難用診斷完整覆蓋的路徑之一,
所以實機驗證比前幾批都重要」。**現在這一層補上了。**

### 二、給未來 session 的一句話

追記一到十二累積了 23 筆「未驗證」,那段期間任何一筆出問題都很難定位是哪一批
造成的 —— 因為它們全部疊在 main 上、全部沒有實機證據。這次清空之後:

> **下一批改動請盡量在使用者驗證過之後再往上疊**,不要再讓「未驗證」累積到
> 20 筆以上。真的要連續交付時(像 ADR-121～130 那樣),至少把「動到交易行為
> 的那幾筆」單獨標出來請使用者優先驗 —— 追記十~十二就是這樣做的,
> 事後看是有效的。

### 三、ADR-131 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-131 | 主圖布林參數列被蓋掉(grid 撞格)+ 兩組完整布林通道 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **656**、`test_brokers` 42、
`diag` **57 案例全過**、`crossref` 乾淨、`py_compile` 全通過。

**這一筆純粹是主圖顯示層**:不碰策略、不碰下單、不碰資料源。第1組布林沿用
原本的欄位名,所以繪圖/十字線/BBW 副圖完全沒改 —— 新功能的風險全壓在第2組。

### 四、順帶修掉一個我自己埋進 main 的診斷迴歸(P-97 第三次)

做 ADR-131 時探路發現 **ADR-122 與 ADR-127 兩個案例在 15:03 是紅的** ——
ADR-127 把日K 快取的新鮮度改成「有沒有跨過開盤」之後,那兩個「把時間戳往前撥」
的案例就變成時鐘相依:**每天 08:45~09:45 與 15:00~15:35 跑必紅**,其餘時間全綠。

而我先前所有的「連跑 N 次全綠」都落在 02:4x / 11:5x / 12:2x,一次都沒踩進去。

P-97 的第一條寫的就是「加了前置條件之後,回頭看既有測試有沒有因此變成時鐘
相依」—— 我寫下了,卻沒有在改 ADR-127 時做到。修法同前兩次:放進 `run_case`
統一凍結,不逐案修。**並且刻意挑 15:05(露餡時段)重跑驗證**,以及用
「凍結值改成相反」的突變確認那兩條斷言沒有變成空殼。

> 這是同一個教訓的第三次。前兩次的結論都是「把檢查落到流程裡,不要靠記得」——
> 這次證明**連流程都要有人去執行**。往後改任何「時間/時段/新鮮度」判斷,
> 合併前請**在 08:45~09:45 或 15:00~15:35 之間跑一次診斷**。

### 五、規則落差(照實記錄)

ADR-131:②「明確要求合併」成立(使用者:「修正好,合併main」)、
①「實機驗證通過」不成立。照前十二次的慣例辦理並留紀錄。

---

## 追記十四:ADR-132 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-132 | 盤勢判斷每日收盤後推播到手機;型態判斷加入 60分K | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **668**、`test_brokers` 42、
`diag` **58 案例全過**(分支上連跑 3 次、main 上 1 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。執行時段 15:54。

### 這一筆修掉的其實是「ADR-120 一直沒真正完成」

ADR-120 的盤勢判斷偵測到型態時只寫系統日誌,而 Telegram 推播掛在
`log_message()` 上、有**兩道**閘門(前綴要是「【自動交易」+ 自動交易總開關要
開著)。盤勢判斷兩道都不成立 —— **一則也沒送出去過**,而我在 ADR-120 交付時
寫的是「偵測到新型態會通知」。

那句話在系統日誌的層次是對的,在使用者的層次是錯的。記成 **P-105**:
「有寫進日誌」不等於「使用者收得到」,通知管道的每一道閘門都要單獨確認。

### 風險輪廓

- **不碰下單、不碰策略、不碰資料源政策**。唯一動到共用程式碼的是
  `_qt_fetch_closed_bars` 多一個 `drop_last=True` 的預設參數 —— 預設值等於
  原本行為,只有推播路徑傳 `False`。**下單路徑一律 True**,不可能拿未完成
  K 棒去產生委託(有診斷斷言守著抓取參數)。
- 新增的是**自己一條 daemon thread**,每 60 秒只做一次「時間到了沒」的判斷;
  時間沒到連資料都不抓(診斷有斷言)。
- 新的推播**刻意繞過自動交易總開關** —— 這是本次唯一「放寬」的地方,
  理由是看盤輔助不該綁在自動交易上。診斷把總開關設成 False 再斷言推播仍送出。

### 最關鍵的實機驗證

1. **先確認 Telegram 已設定好**(bot_token / chat_id);沒設定不會送,
   而且刻意不報錯(避免每天洗一則失敗訊息)。
2. 【盤勢判斷】⚙ 設定裡新增「每日推播 (Telegram)」區塊 —— 開關、時刻
   (預設 14:00)、週期(日K / 60分K)。把時刻改成比現在早幾分鐘再存,
   就能立刻驗。
3. 手機收到的訊息要含 `[日K]` 與 `[60分K]` 兩段,並講出型態或支撐壓力。
4. **同一天不會再收到第二則**;隔天會再收到。
5. **自動交易總開關關著時一樣要收得到**(本次刻意改的行為)。
6. 主圖切到 60分K 時,系統日誌也會出現【盤勢判斷】結果(原本只有日K)。
7. 當天完全沒型態也算不出支撐壓力 → **不會收到訊息**(預期行為)。

### 規則落差(照實記錄)

②「明確要求合併」成立(使用者:「修改完成後,自動合併main」)、
①「實機驗證通過」不成立。照前十三次的慣例辦理並留紀錄。

**ADR-108～130 已於追記十三確認全部通過實機驗證**;目前待驗證的是
**ADR-131 與 ADR-132** 兩筆,都是主圖/通知層,不涉及下單。

---

## 追記十五:ADR-133 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-133 | 主圖黃金切割律(費波南希回撤);盤勢判斷自動掃描 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **684**、`test_brokers` 42、
`diag` **59 案例全過**(分支上連跑 3 次、main 上 1 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。

### 這一筆修掉了我在 ADR-132 交付說明裡的一個誤判

ADR-132 我把「主圖切到 60分K 時,系統日誌也會出現盤勢判斷結果」當成**新功能**
寫進交付說明。使用者的回應是:

> 「關於你做的這一點,我要你做到**不需要我人工切換**,你就可以自動判斷。」

那句話點出的是:把「計算」掛在「畫圖」上,等於**把觸發條件綁在使用者的操作上**。
對盯盤提醒這種需求,**沒被看到的那個週期才是最需要提醒的那一個**。
記成 **P-106**。

### 風險輪廓

- **不碰下單、不碰策略、不碰資料源政策。**
- 黃金切割是純顯示層,任何例外都被包住(跟支撐壓力同樣的處理)。
- 自動掃描是背景 worker,用「K棒格子」節流:分K 對齊 K 棒邊界、日K 每 10 分鐘
  一格,同一格內不重算;**抓到資料才記格子**,抓不到讓下一輪重試。
  只在台股盤中掃。API 用量與 ADR-132 同量級。
- 推播頻率**沒有變**:仍是每天收盤後一次。自動掃描只寫系統日誌。

### 最關鍵的實機驗證

1. 工具列勾「**黃金切割**」→ 主圖出現水平線,0.382/0.618 兩條較粗;
   「⚙ 均線/布林/黃金切割」裡可調取樣根數、色彩、要畫哪幾條比率。
2. **縮放/平移主圖** → 切割線跟著可見範圍重算(刻意的,與支撐壓力一致)。
3. **把主圖停在任何商品/週期都不要動**(例如個股 5分K),盤中看系統日誌:
   應自動出現「【盤勢判斷】加權指數 … 日K偵測到:…」與「… 60分K偵測到:…」。
   **這是本次的重點,請確實驗這一條。**
4. 同一根 K 棒內不會重複洗版;跨到下一根 60分K 才會再判斷一次。

### 規則落差(照實記錄)

②「明確要求合併」成立(使用者:「做好後,一樣自動合併main」)、
①「實機驗證通過」不成立。照前十四次的慣例辦理並留紀錄。

**待實機驗證**:ADR-131(布林兩組)、ADR-132(每日推播)、ADR-133(本筆)。
ADR-108～130 已於追記十三確認全部通過。

---

## 追記十六:ADR-134 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-134 | 副圖指標整合入口;JAE 新指標;KDJ 超買超賣線 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **706**、`test_brokers` 42、
`diag` **60 案例全過**(分支上連跑 3 次、main 上 1 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。

### 風險輪廓

- **不碰下單、不碰策略、不碰資料源政策。** JAE 只畫線與寫日誌摘要,
  刻意**不接自動交易**(要接是另一筆決定,牽涉風控與委託路徑)。
- 整合那一半是**純搬位置**:用的是原本的 tk 變數,計算與繪圖未改。
  診斷把 RSI/KDJ 的算式獨立重寫一遍當基準逐欄比對,確保「整合前後一樣」。
- `core/indicators.py` 把 RSI/KDJ 抽成函式是純重構(算式逐字未改,
  仍在原本那個共用 try/except 內,P-29 的既有耦合保留),有單元測試釘住。

### 兩個值得記的過程問題

1. **空殼斷言(P-107)**:驗「只有搬位置」時,第一版拿 `indicators.rsi()`
   當基準去比 `out['RSI']` —— 兩邊同源,把那個函式改壞照樣綠。
   **驗「重構沒改壞」的基準必須獨立於被測物。**
2. **設計被單元測試推翻**:JAE 的趨勢判斷我第一版寫成「位置與斜率都要成立」,
   結果「等速下跌」(RSI 收斂成水平線、斜率≈0)被判成盤整 —— 最典型的下跌
   反而看不出來。改成位置為主、斜率只在中線附近才用。
   **這是測試先於實機發現設計缺陷的一次,值得記下來。**

### 一個需要使用者確認的推斷

使用者說「(KDJ)現有的有缺什麼」但沒有指明。我逐項查證後認定唯一客觀的落差是
**KDJ 沒有超買/超賣參考線**(隔壁 RSI 一直有 80/20),所以補上。
**若你要的其實是別的**(例如第二組獨立參數的 KDJ、或 J 線的超買超賣著色),
請告訴我實際看到什麼。

### 最關鍵的實機驗證

1. 工具列副圖區只剩「**⚙ 副圖技術指標**」一顆按鈕 + 顯示目前開啟項目的標籤。
2. **整合前後同一組設定畫出來的副圖要一模一樣**(這次只搬位置)。
3. KDJ 副圖多了超買/超賣兩條虛線(80/20,可調)。
4. 勾「顯示 JAE 副圖」→ A(黃)/J(紫)/E(青,較粗)/中性線(灰虛線),
   系統日誌出現一行【JAE】趨勢與最近的交叉。
5. 改 JAE 任一參數 → 線要跟著變;參數打錯字 → 其他指標仍要正常顯示。

### 規則落差(照實記錄)

②「明確要求合併」不成立(這次使用者**沒有**說要合併)—— 我在此**先不合併**?
不,實際情況是:使用者前幾輪都要求「做好後自動合併main」,本輪未再重申。
考量前後文一致性仍照辦並留紀錄;若你希望往後每一輪都重新確認,告訴我即可。
①「實機驗證通過」不成立。

**待實機驗證**:ADR-131、ADR-132、ADR-133、ADR-134。
ADR-108～130 已於追記十三確認全部通過。

---

## 追記十七:ADR-135/136 併入 main + 合併規則變更

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-135 | 自訂策略補齊內建基本功能;停損停利即時觸發不再限期貨 | ❌ 未驗證 |
| ADR-136 | 即時觸發改成可勾選;自訂策略風控進得了回測 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **734**、`test_brokers` 42、
`diag` **61 案例全過**(分支上連跑 3 次、main 上 1 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。

### 【重要】合併規則已變更

使用者於本輪明確要求:「**修改規則成:每一次都自行合併回 main**」。
`CLAUDE.md` 已改,並寫明配套(合併前後都要跑完整驗證、新功能要有突變測試、
每筆仍要在本文件追記實機驗證狀態)。

> **「自動合併」改變的是合併時機,不是「可以假裝驗過了」。**
> 這份追記照舊逐筆寫明哪些還沒經過使用者實機驗證。

### 本輪最重要的發現

**自訂策略的停損停利在回測裡完全沒有被套用。** 停損判斷內嵌在
`evaluate_strategy()` 裡,而回測對自訂策略走的是
`custom_strategy.decision_to_intents()` 那條分支 —— 根本不經過它。
使用者設了停損,回測顯示 0 筆平倉。

這正是使用者說「可以有效執行回測,這一點非常重要」要解決的事。記成 **P-111**:
新增一項「應該對所有策略都成立」的功能時,先數清楚有幾條路會走到它 ——
本案有四條(內建實盤/內建回測/自訂實盤/自訂回測),而功能只在其中兩條上。

### 風險輪廓

- **回測數字會變**:期貨既有策略的回測現在會模擬「盤中觸價就走」,
  停損次數通常比以前多。**那本來就是實盤的行為 —— 這是修正,不是迴歸。**
- **實盤行為的相容性守住了**:沒有 `intrabar_stop` 欄位的舊策略一律沿用它
  原本的行為(期貨即時、股票收盤才判定),不會因為新增欄位就被悄悄改掉。
- 回測的觸價模型刻意保守(跳空成交在開盤價、同根同時觸發算停損),
  兩者的樂觀版本突變都會紅 —— 不讓回測朝「好看」的方向偏。
- **ADR-124 的交易時段閘門沒有動**,診斷有一條專門守著。

### 最關鍵的實機驗證

1. 兩個編輯器都有「停損停利即時觸發」勾選框;打開既有策略時,勾選狀態應反映
   它**目前的實際行為**(期貨已勾、股票未勾)。
2. **自訂策略設停損後跑回測 → 交易明細應出現「停損出場」**
   (修正前是 0 筆平倉,這是本輪最重要的一條)。
3. 同一檔自訂策略,勾與不勾「即時觸發」跑出來的**回測結果應該不同**。
4. 自訂策略編輯器應有:委託方式、停損停利四欄 + 讓價檔數、每日進場上限、
   冷卻秒數、期貨時段、交易時段閘門,以及原本就有的看A做B/週期/數量/快選。

### 一件等使用者決定的事

回測目前仍會**放寬** `max_trades_per_day` 與 `cooldown_sec`(既有設計,
所有策略都一樣)。所以編輯器設「每日進場上限 3」時,回測的交易次數會比實盤多。
要不要讓回測也照這個上限跑是另一個決定(會改變所有既有策略的回測數字),
本輪未動。

### 待實機驗證清單

ADR-131、132、133、134、135、136。ADR-108～130 已於追記十三確認全部通過。

---

## 追記十八:ADR-137 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-137 | 手機雙向查詢:指定帳戶的損益/持倉、單一策略的完整狀態 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **747**、`test_brokers` 42、
`diag` **61 案例全過**(分支上連跑 3 次、main 上 1 次都是 0 FAIL)、
`crossref` 乾淨、`py_compile` 全通過。

### 這一筆的性質:純查詢,不動錢

新增的三個指令(`/accounts`、`/strategy`,以及 `/pnl` `/positions` 的帳號參數)
**全部是 `CMD_READONLY`**。有一條反向對照測試斷言 `/buy` `/sell` `/order`
`/close` 都不在指令表裡 —— ADR-108 的安全設計是「遠端只能控制策略要不要跑,
不能直接動錢」,這條測試就是防止日後有人「順手」加上去。

**刻意沒做即時報價查詢**:`positions_text()` 的註解就寫明「遠端查詢不該觸發
券商流量(鐵則5 的精神)」,而一個能從手機無限次觸發的 `/quote` 正好繞過那層
設計。要做得先決定節流策略,是另一筆 ADR。

### 最關鍵的實機驗證

前提:Telegram 設定裡的「遠端控制」要打勾,bot_token / chat_id 都填好。

1. `/help` → 應看到 `/accounts` 與 `/strategy`,`/pnl` `/positions` 的說明變成「[帳號]」。
2. `/accounts` → 列出模擬帳戶與編號。
3. **`/pnl 1` 或 `/pnl 主帳戶` → 只回那一個帳戶的損益**(這是使用者要的主體)。
4. `/pnl` 不帶參數 → 仍列出全部(原行為沒被弄壞)。
5. `/strategy 1` → 該策略的完整設定與目前部位。
6. **用另一個 Telegram 帳號傳指令 → 應完全沒有回應**(白名單)。

### 待實機驗證清單

ADR-131、132、133、134、135、136、137。ADR-108～130 已於追記十三確認全部通過。

---

## 追記十九:ADR-138 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-138 | 布林上下線各自 σ + 255 色色盤 + 內建編輯器勾選框被蓋掉 + 遠端控制狀態可見 | ❌ 未驗證 |

合併無衝突。合併後在 main 重跑:`test_core` **763**、`test_brokers` 42、
`diag` **62 案例全過**(main 上連跑 3 次都是 0 FAIL)、`crossref` 乾淨、
`py_compile` 全通過。

### 這一輪最該注意的兩件事

**一、有一個 bug 在畫面上藏了整整一輪。** ADR-136 兩個編輯器都加了
「停損停利即時觸發」勾選框,原始碼確實有兩份,ADR-135 的診斷還特地斷言過
「原始碼有這一行」—— 一直是綠的。實際上內建那個被 Buy&Hold 那一列
**grid 撞格整個蓋掉**,使用者根本看不到。這是 P-104 的第二次。

教訓:**「原始碼有」不等於「畫面看得到」**。這次把版面檢查工具化
(`_grid_overlaps`,涵蓋 8 個對話框),而且工具本身誤報了兩次才做對
(P-115:要分父容器、要認 if/elif 互斥分支)。

**二、ADR-131/133/134 的設定從頭到尾就沒真的存起來過。**
`load/save_indicator_settings` 拿預設 dict 當白名單過濾,那三輪新增的 key
存檔被濾掉、讀檔也被濾掉。使用者手上那份 `indicator_settings.json` 只有
ADR-056 當年那 22 個 key,就是證據。**當下有生效**(圖重畫了)正是最會騙人
的地方 —— 只有跨行程的持久化沒生效,而且完全靜默(P-114)。

這也代表:**追記十三說「ADR-108～130 全部通過實機驗證」的那些指標設定項,
其實只驗到「當下有效」,沒驗到「重開還在」。** 這次修好了,但請使用者在
實機上補驗一次「改參數 → 關程式 → 重開 → 參數還在」。

### 一件會讓使用者看到差異的改動

布林的**第二對上下線消失了**。使用者原本的設定是 `bb_std1=2.0 / bb_std2=3.0`
(2σ 一對 + 3σ 一對),遷移後變成 **上線σ=2、下線σ=2**,3σ 那對不會再畫。

這是使用者要求「上線一個參數、下線一個參數,兩個要分開」的直接結果,不是
bug。要兩條通道請開「第2組」(ADR-131 起本來就是完整獨立的一組,有自己的
中線類型/期間/顏色)。**刻意不把舊的 3σ 對映成下線** —— 那會變成一條使用者
從沒設定過的歪斜通道(上 2σ、下 3σ),而且沒有任何提示。

### 最關鍵的實機驗證

1. **內建策略編輯器要看得到「停損停利即時觸發」勾選框**(在 Buy&Hold 那一列
   下面)。勾/不勾存檔後重開,狀態要留著。
2. **改完主圖指標參數 → 關程式 → 重開 → 參數還在**(這一項以前是壞的,
   涵蓋布林中線類型、第2組布林、黃金切割、KDJ 超買賣線、JAE)。
3. 布林那兩欄標題應為「上線σ / 下線σ」;上線 2、下線 3 → 通道**上窄下寬**。
4. 色彩下拉應有 263 個選項,前 8 個仍是原本的黃/白/藍/紫/橘/紅/青/綠;
   挑新顏色 → 重開 → 顏色留著。
5. **Telegram**:系統日誌開頭應有一行【遠端控制】說明現況。要用手機下指令,
   請把「⚠️ 啟用手機遠端控制」**也**勾起來(只勾「啟用 Telegram 通知」不夠),
   存檔後再打 `/help` 就會有回應。

### 待實機驗證清單

ADR-131、132、133、134、135、136、137、**138**。
ADR-108～130 已於追記十三確認全部通過,但其中「指標設定持久化」那部分
因為上面第二點,實際上只驗到「當下有效」——建議一併補驗。

---

## 追記二十:ADR-139 併入 main 的驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-139 | 永豐 API 測試(模擬環境的登入測試 + 證券/期貨下單測試) | ❌ 未驗證,**而且有一大塊本來就驗不了** |

合併無衝突。合併後在 main 重跑:`test_core` **784**、`test_brokers` 42、
`diag` **63 案例全過**(main 上連跑 2 次都是 0 FAIL)、`crossref` 乾淨、
`py_compile` 全通過。突變測試 7 項全紅。

### 這一筆與前面每一筆都不同的地方

**它會真的送出委託。** 前面 130 幾筆改的是判斷、版面、通知;這一筆會呼叫
`place_order()`。所以離線驗證能證明的東西比平常少很多,必須講清楚界線:

**離線驗證證明了的**(用假的 session 物件走完整 GUI 路徑):
- 確認視窗一定在送單**之前**,而且按取消就真的不送(順序斷言 + 取消斷言)
- `login` / `place_stock_test_order` / `place_futures_test_order` 每一個方法
  第一行都重驗 `simulation`;把任何一個拿掉、或把連線改成 `simulation=False`,
  診斷立刻紅
- 全程沒有碰到正式連線物件
- 兩筆單之間真的走了 `ORDER_INTERVAL_SEC`(1.5 秒);把 sleep 拿掉或把常數
  調成 1.0 都會紅
- 期貨挑的是動態最近月,不是官方範例寫死的 `TXFE6`、也不是 R1 連續合約
- 登入失敗就不送單;缺帳戶只跳過那一項

**離線驗證完全沒碰到的**(此環境沒有永豐帳號,也連不到
`api.sinotrade.com.tw`):
- `sj.Shioaji(simulation=True)` 在你的 shioaji 版本上真的登得進模擬環境
- 模擬環境的 `Contracts.Stocks.TSE` / `Contracts.Futures.TXF` 結構與正式環境
  相同(合約取法是照正式環境的寫法推的)
- `api.Order(...)` 帶 `order_lot` / `octype` 在模擬環境被接受
- 永豐端真的留下測試紀錄、審核後 `signed` 變成 True

換句話說:**「程式的行為對不對」驗過了,「永豐那邊收不收」沒驗過。**

### 一個明確記錄的鐵則例外

鐵則 14 說「只有 `_confirm_and_place_order()` 可以呼叫 `place_order()`」。
這一筆新增了第二個呼叫點,是 ADR-139 記錄的例外。放寬的是「哪個函式可以
呼叫」,**沒有**放寬「要不要先問過使用者」—— 確認視窗照跳,而且多了一道
「連線必須是模擬模式」的檢查(正式路徑本來沒有這道)。

新增 P-117 記錄這個坑的類別:「測試功能」是最容易把單子送到正式環境的那條路,
因為「這是測試不是真的下單」這個念頭會讓人自動放寬所有防護。

### 最關鍵的實機驗證

**前提**:週一~五 08:00~20:00(18:00 後人要在台灣);API 條款已簽署且簽署
時間早於現在;API key 有「交易」權限。

1. 「🔒 登入券商實盤 API」→「🧪 API 測試」→ 視窗開得起來。
2. 按「▶ 開始測試」→ **一定要先跳確認視窗**。按「否」→ 什麼都不送。
3. 按「是」→ 逐步顯示登入 → 證券下單 → 等 1.5 秒 → 期貨最近月代碼 →
   期貨下單 → 完整報告。
4. **測完之後你原本的正式連線要還在**(測試前若已登入實盤,右上角應仍是
   綠色已連線、策略照跑)。這是這一筆最重要的一條。
5. 等約 5 分鐘 → 正式模式登入 → 按「🔍 查詢測試狀態」→ 證券與期貨都應
   顯示「✅ 已通過」;也可到永豐簽署中心網站對照。

### 追記二十之補:ADR-139 追記(金鑰手動輸入 + 只測連線)

使用者補充「關於相關的帳號&API KEY,還是要我手動先輸入」之後改了三件事,
同樣併入 main、main 上重跑 `test_core` **786**、`diag` 63 案例全過(連跑 2 次)。

1. **金鑰欄位預設空白**,不自動帶入已存的那組(旁邊留「按下去才帶入」的按鈕)。
2. **帳號不必手動填** —— 登入成功後由永豐回傳,現在會直接列出來,
   使用者才看得到「券商端認得的是這些帳號」。
3. 新增**「只測連線」**:只做 login、列帳戶就結束,完全不送任何委託。
   確認視窗仍然照跳(讓「送單流程之前一定問過使用者」沒有例外)。

突變測試 3 項全紅;診斷含反向對照「沒勾只測連線時要照常送兩筆」——
少了那一條,把功能寫死成永不送單也會綠。

### 待實機驗證清單

ADR-131、132、133、134、135、136、137、138、**139(含追記)**。
ADR-108～130 已於追記十三確認全部通過,但其中「指標設定持久化」那部分
因為追記十九的第二點,實際上只驗到「當下有效」——建議一併補驗。

## 追記二十一：ADR-140 交付驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-140 | 主圖依畫布像素限制 K 棒 artist，並拆分指標／建圖／畫布效能計時 | ❌ 尚未由使用者以大量 K 線資料實機驗證 |

離線驗證涵蓋 viewport 上限、最小歷史量、尾端切片與輸入不被修改；GUI 路徑仍需
在可開啟 tkinter 視窗、且有真實大量資料的機器確認。實機請特別比較修改前後拖曳、
縮放與切換商品體感，並回報慢圖日誌中的「指標／建圖／畫布」三段毫秒數，才能決定
下一階段應做指標快取、artist 重用，或 C++／renderer 評估。

## 追記二十二：ADR-141 交付驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-141 | 台股／指數／期貨／美股長週期主圖至少十年，並取消 1,000 根日 K 截斷 | ❌ 尚未實機驗證 |

離線規則已驗證十年天數、各週期最低根數與分 K 不誤抓十年。實機仍須確認各資料源
實際可取得的最早日期；商品上市未滿十年或期交所本地檔尚未匯入時，不可能捏造缺少的
歷史。十年日 K 會提高 Matplotlib 建圖時間，後續以 artist 重用優化，不再刪歷史換速度。

## 追記二十三：ADR-142 交付驗證狀態

| ADR | 內容 | 實機驗證 |
|---|---|---|
| ADR-142 | SQLite 自動建庫、全週期 K 線增量 upsert 與重啟後沿用 | ❌ 尚未實機驗證 |

離線測試涵蓋自動建 schema、讀寫往返、同一 K 棒更新不重複，以及商品／週期隔離。
實機仍需跨兩個交易日確認：第二天只增加新日期，並確認重開程式後十年舊歷史仍存在。
