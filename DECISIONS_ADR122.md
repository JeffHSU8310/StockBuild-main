# ADR-122:策略路徑的大範圍 K 線改走背景分段預抓

## 狀態

已實作,**尚未實機驗證**(分支 `claude/qt-kbars-chunked`)。

## 背景

ADR-121 修掉了使用者實際遇到的開盤逾時,但在「不在這次範圍」留了一條:
**策略路徑對大範圍請求仍是單次下載**,而主圖對同樣範圍早在 ADR-069 就改
分段了。使用者直接問這條要怎麼修。

`kbars()` **一律回 1 分 K**(日/周/月K 是本地重採樣的),所以「天數」直接
等於資料量。查證後發現範圍比原本說的大 —— 主圖的門檻是分K 類 5 天、
日K 類 90 天,對上 `QT_TF_DAYS`:

| 週期 | 取數天數 | 依主圖規則的段數 |
|---|---|---|
| 1分K | 4 | 1 |
| 5分K | 7 | **1**(過門檻但切不出第二段)|
| 15分K | 14 | 2 |
| 30分K | 21 | 2 |
| 60分K | 35 | 4 |
| 日K | 300 | 4 |
| 周K | 700 | 8 |
| 月K | 1500 | **17** |

也就是說 15分K 起就已經在「主圖認為該分段」的範圍內。使用者這次的 5分K
沒事,是因為 7 天切下去也只有一段 —— 這也解釋了為什麼只有開盤尖峰那兩發掛。

## 設計上必須同時滿足的兩個限制

1. **要分段**,否則大請求本來就容易逾時(ADR-069 對主圖的結論)。
2. **不可以在量化 runner 迴圈裡做**。ADR-121/P-90 剛立的規矩:runner 是
   單執行緒,同一條迴圈上還有每 3 秒的 `_qt_update_realtime_pnl`
   (期貨即時停損停利靠它)。月K 的 17 段光段間節流就 5.6 秒。

**直接把 `_download_kbars_chunked` 搬進 runner 會違反自己剛寫的 P-90**,
所以這一版的核心是「分段要做,但做在別的執行緒上」。

## 決定

### A. 分段門檻收斂成 `core/kbars_plan.py`

門檻與段長原本只是 `fetch_data_worker` 裡的字面值。策略路徑要用同一套規則,
只能再抄一份 —— 而「兩份清單各自維護、遲早分歧」正是 P-67 的坑。

主圖那段**維持原樣不動**(已驗證過的路徑,不進這次的風險預算),但 diag
加了一條**原始碼層級斷言**:主圖用的四個字面值必須與 `kbars_plan` 的常數
一致,改岔了會當場紅(同 P-64 手法)。

### B. 需要分段的請求 → 背景預抓執行緒

`_qt_fetch_closed_bars` 分三條路:

| 情況 | 走法 |
|---|---|
| 快取命中且新鮮 | 直接用(**沒變**)|
| `chunk_plan()` 回 None(小請求)| 原地單次下載(**沒變**)|
| 需要分段 | 起背景執行緒 + 拋 `KBarsPending` |

背景執行緒跑 `_download_kbars_chunked`(**retries 照用** —— 在自己的執行緒
上睡不影響任何人),補完寫進 `_kbars_raw_cache`。runner 這一輪則靠 ADR-121
既有的 boundary 還原機制,2 秒後再來看一次快取 —— **不需要新的等待邏輯**。

三個容易寫錯、都寫了測試的地方:

1. **「正在補」不是錯誤**。用 `KBarsPending` / `KBarsUnavailable` 兩個子類
   區分:前者靜默(不計 `data_error_count`、不記日誌、**不進重試計數** ——
   補一份月K 要好幾十秒,用 3 次上限去卡它會讓它永遠補不完),後者才走
   ADR-121 的記錄與流量診斷。
2. **部分成功不可以進快取**。`_download_kbars_chunked` 部分失敗仍會回傳
   DataFrame,呼叫端光看回傳值分不出「完整」與「缺了最近那一段」——
   後者拿去評估會用過期資料下單。因此加了 `stats` 出參,`fail_segs`
   非零就不寫快取(同鐵則 4「沒有真實資料就不要捏造」的精神)。
3. **失敗要被回報出來**。預抓失敗記在 `_qt_prefetch_errors[key]`,下一輪
   先**取出並清掉**再開新的預抓 —— 順序反了會讓失敗被「進行中」蓋掉,
   變成無聲的無限重試。

### C. 日K 類快取的 TTL 從 300 秒放到 1800 秒

日K 類策略每 **10 分鐘**評估一次,而舊 TTL 是 **300 秒** —— **快取必定已
過期,等於完全沒作用**,每次評估都重打一次大請求。

可以放長的理由(非直覺,值得記):`_qt_fetch_closed_bars` 會**丟掉最後
一根**視為未完成。對日K 策略而言,盤中整段時間「最後一根」都是今天,
丟掉之後**評估用的資料從開盤到收盤完全不變** —— 要等到隔天第一根日K
出現,評估集合才會多一根。

分K 類維持 30 秒不動:那個資料每根 K 棒真的會變,拉長會漏訊號,方向相反。

### D. 順手修掉的一個小 bug

`_download_kbars_chunked` 的「切小段搶救」路徑寫死 `_try_seg(sub, sub_end, 1)`,
等於呼叫端說 `retries=0` 在那條路上不被尊重(每個小段照樣睡 1.5 秒)。
改成 `min(1, retries)`:既有呼叫端(retries=2)行為完全不變,
`retries=0` 現在真的是 0。這也讓診斷從 92 秒縮回約 40 秒。

## 實作時真的踩到、被測試抓出來的兩個坑

**一、把使用者正在跑的 5分K 也丟去背景預抓了。**
5分K 的 7 天**過了 5 天的門檻**,所以第一版 `chunk_plan` 判它「要分段」——
但 10 天一段,7 天只切得出**一段**,跟單次下載完全一樣,卻多繞一條背景
預抓的路,平白改變一條使用者每天在跑的路徑。診斷案例 3 當場抓到。
修法:`segments <= 1` 一律回 None(「切不出第二段就不算分段」)。

**二、診斷案例 2 一度是空殼。**
假資料的日期寫死在 2024,而請求範圍是「現在往前推 300 天」,兩者不重疊,
每一段都切出空 DataFrame,快取永遠填不進去 —— 而下游斷言
「第二輪不可再下載」在那種情況下**照樣會過**(預抓失敗時第二輪走的是
回報失敗那條,也不會下載)。修法:假資料日期改成以現在為終點,並補一條
**直接斷言快取真的有 key** 的檢查(P-28 那一類的教訓)。

## 驗證

- `python tests/test_core.py` → **591 個全過**(原 584,新增 7)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **48 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨

### 突變測試

| 把程式改成 | 結果 |
|---|---|
| `chunk_plan` 永遠不分段 | 診斷紅 |
| 拿掉「切得出第二段才分段」的判斷 | 單元測試 + 診斷都紅(5分K 行為被改掉)|
| 拿掉預抓去重 | 診斷紅 |
| `KBarsPending` 也算資料錯誤 | 診斷紅(**第一次是綠的**,見下)|
| 日K 類 TTL 改回 300 秒 | 診斷紅 |
| 預抓部分成功也寫進快取 | 診斷紅(**第一次是綠的**,見下)|
| 主圖分段門檻改成 `> 7` | 診斷紅(與 core 常數不一致)|

**三個突變第一次是綠的,補測試之後才紅**,各自的洞不一樣:

1. **門檻判斷根本是死枝**。`chunk_plan` 原本先比門檻、再比段數,但在
   `threshold <= chunk` 的參數下,門檻那句永遠不會改變結果 —— 改壞它任何
   測試都不會紅。修法不是補測試,是**把死枝拿掉**:判斷只看「切得出第二段
   嗎」,threshold 常數保留下來單純給診斷比對主圖的字面值。
2. **「預抓在途」只跑一輪測不出來**。ADR-121 的重試計數要連續 3 次才會記錄,
   所以把 Pending 誤算成錯誤時,單跑一輪的斷言照樣是綠的。修法:用一個
   `threading.Event` 把預抓確定卡在途中,再連跑超過重試上限的輪數。
3. **「部分成功」要刻意製造**。原本的假下載不是全成功就是全失敗,測不到
   「一部分段成功、一部分失敗」。修法:加 `fail_after`,讓前 N 段成功、
   其餘失敗,再斷言快取**沒有**被寫進殘缺資料。

## 需使用者實機驗證

1. **建一檔日K 策略並啟用**:
   - 第一次評估時日誌出現一次「歷史資料分 N 段在背景補齊中」,
     **幾秒後**策略正常開始評估(**不是卡住不動** —— 這是最重要的一點)
   - 不再出現 `kbars` 逾時
   - 之後 30 分鐘內的評估不會再重新下載(日誌安靜)
2. **月K 策略**(最極端,17 段):補齊期間主程式**不可以卡頓**,
   期貨即時停損停利要照常運作 —— 這是 P-90 的實地驗證。
3. **原本的 5分K 策略行為完全不變**(它走的還是原地單次下載那條)。
4. 策略編輯器的「試跑」對日K 策略仍要能一次跑完(它走 `allow_blocking=True`,
   在自己的 worker 執行緒上原地把分段做完)。

## 不在這次範圍

- **`QT_TF_DAYS` 的天數本身**。順帶查到:日K 的 300 天 ≈ 205 個交易日,
  **不夠 MA240 用**(要 240 個交易日 ≈ 350 個日曆日)—— 現在是「既慢又不夠」。
  但調整它會改變哪些訊號成立,是行為改變,要另外確認。分段做完之後多抓
  一些的成本低很多,適合那時再談。
- **B(執行商品)的現價改用 `snapshots()`**(使用者已決定不動)。
