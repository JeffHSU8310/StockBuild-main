
## ADR-091：多個模擬帳戶 (可自訂增加) — 策略可選帳戶,避免同標的多策略衝突

### 背景 / 根因

使用者要求:「我需要多個模擬帳戶（可自訂增加），避免同標的在同一個帳戶
在多策略時產生衝突。策略中要可以選擇帳戶」。

查證後確認這是一個真實存在的資料正確性 bug,不只是「想要更多帳戶」的
單純需求:`core/paper_account.py` 的 `apply_fill()` 裡,`positions` 是用
**symbol 當唯一 key**:

```python
pos = acct['positions'].get(sym)
if pos and pos.get('direction') == direction:
    ...累加...
else:
    acct['positions'][sym] = {...}   # 直接覆蓋!
```

如果策略A持有 MXFR1 多單,策略B (不同策略) 也交易 MXFR1 但方向不同 (或
甚至方向相同但這是兩個獨立邏輯的策略),B 開倉時會**直接覆蓋** A 的部位
記錄——A 的股數/均價/方向全部消失,只留下 B 那一筆,等於帳面上憑空少了
一個部位。`_qt_refresh_tree` 的持倉欄位顯示也是純用 symbol 查
`self.paper_acct['positions']`,兩個策略若同標的,畫面上會顯示同一份
(可能已經失真的) 資料。這正是使用者「避免衝突」的字面意思。

### 設計原則

`core/paper_account.py` 的既有設計 (一個 dict = 一個帳戶,`apply_fill`/
`mark_price`/`unrealized_pnl`/`equity` 的簽名完全不動) 完全不必更動——
「多帳戶」的解法是「把會衝突的策略分散到不同帳戶」,而不是在單一帳戶內
用 (symbol, 策略id) 複合 key 拆分部位 (那樣會讓「一個帳戶的現金/權益」
失去意義:兩個策略共用一份現金,卻各自以為自己在獨立記帳,槓桿與資金
運用的模擬會完全失真)。多帳戶純粹是 **GUI 層維護一個
`{account_id: 帳戶dict}` 的容器**,呼叫端各自傳對的帳戶 dict 進 core 層
既有函式,core 層完全不需要知道「有多個帳戶」這件事,繼續保持零
tkinter/shioaji、可離線單元測試 (ADR-009/011)。

### 修改內容

**`core/paper_account.py`**:
- `new_account(initial_cash=1000000.0, name='預設帳戶', account_id=None)`:
  新增 `name`/`account_id` (不帶就自動產生 uuid 短碼)。
- 新增 `DEFAULT_ACCOUNT_ID = 'default'` 常數,給遷移舊資料與相容用。

**`core/strategy_engine.py`**:
- `new_strategy()` 新增 `'account_id': 'default'` 預設欄位。
- 新增 `account_id_of(strategy)`:沒設定/空白一律回傳 `'default'`
  (相容舊策略,不用使用者手動改設定就能接上遷移後的預設帳戶)。

**`stock_app_pro.py`** (大量既有呼叫點改動,但都是機械性替換):
1. `self.paper_acct` (單一帳戶) 改成 `self.paper_accts`
   (`{account_id: 帳戶dict}`)。`_qt_load()` 相容舊版單一帳戶檔案
   (頂層直接是帳戶 dict、有 `'cash'` 鍵) ——讀到舊格式就原地搬進新格式
   當「預設帳戶」(id 固定 `'default'`),舊策略不用手動改設定就能接上
   原本的部位與交易史,不會因為這次改動就「帳目歸零」。
2. 新增 `_qt_paper_acct_for(strategy, warn=True)`:依策略的 `account_id`
   解析對應帳戶,帳戶被刪除/設定壞掉時退回任一個現有帳戶並記警告
   (`warn=False` 給高頻率的純顯示呼叫用,避免同一個失效帳戶每次刷新
   畫面就洗一次系統日誌;三個實際成交路徑維持 `warn=True`)。
3. 新增 `_qt_account_choices()`:回傳 `[(id, name), ...]` 給編輯器/
   模擬帳戶視窗的下拉選單用。
4. `_quant_eval_pass`/`_qt_check_realtime_futures_stops`(ADR-087)/
   `_qt_chukuangren_execute_pass`(ADR-089) 這三個實際下單/記帳的路徑,
   全部改用 `_qt_paper_acct_for(s)` 解析出的帳戶,不再寫死單一帳戶。
5. `_qt_update_realtime_pnl`:標記價更新改成遍歷**所有**帳戶的持倉
   (不同策略的部位可能分散在不同帳戶裡)。
6. `_qt_refresh_tree`:策略清單新增「模擬帳戶」欄 (`acct`,只在
   `mode≠實單` 時顯示帳戶名稱),持倉/未實現損益改依策略對應的帳戶查詢。
7. `_qt_open_paper_window`/`_qt_refresh_paper_account`:模擬帳戶視窗頂部
   新增「帳戶」下拉 + 「➕新增帳戶」+「🗑刪除此帳戶」按鈕。用
   `self._paper_win_account_id` 記住目前檢視的帳戶,切換帳戶時原地重繪
   頭部數字/持倉表/交易紀錄表 (不用整個視窗重建)。刪除帳戶前會列出還在
   用這個帳戶的策略名稱並警告 (刪除後那些策略下次成交會自動改記到其他
   帳戶,`_qt_paper_acct_for` 的既有防呆機制接住);至少要保留一個帳戶。
   重置帳戶改成只重置「目前選取的那一個」,不影響其他帳戶。
8. 三個策略編輯器 (`_qt_open_editor` 內建條件策略、
   `_qt_open_chukuangren_editor` 楚狂人之終極波段、`_qt_open_custom_editor`
   自訂 Python 策略) 都新增「模擬帳戶」下拉,選項來自
   `self._qt_account_choices()`,存檔寫入 `s['account_id']`。
9. `diag_repro_issues.py` 裡兩處直接寫 `app.paper_acct` 的舊診斷案例
   同步改用 `app.paper_accts['default']` (歷史診斷腳本,不在
   `tests/test_core.py` 範圍內,但既然改到就順手保持一致,不留下已知會
   炸的殘留程式碼)。

### 效果

- 兩個策略同時交易同一檔標的,只要分到不同模擬帳戶,部位/損益/交易史
  完全獨立,不會再互相覆蓋。
- 使用者可以自訂新增任意數量的模擬帳戶 (自訂名稱、自訂初始資金),用來
  依「策略群組」「風格」「標的類別」等任何自己想要的方式隔離資金與部位
  模擬,更貼近多個實際子帳戶的操作習慣。
- 策略清單一眼就能看到每個模擬策略記在哪個帳戶,不用逐一打開編輯器確認。
- 舊版只有一個模擬帳戶的使用者,升級後帳目 100% 原樣接上 (遷移成
  「預設帳戶」),不用重新設定任何一個既有策略。

### 已知限制 (誠實)

- 帳戶被刪除後,原本設定用它的策略不會自動跳出提示要求重新選帳戶——
  只有等下一次真的成交時,`_qt_paper_acct_for` 才會記一筆警告並自動改記
  到「隨便一個現有帳戶」。比較嚴謹的做法是刪除帳戶時強制把受影響的策略
  改成停用,但那樣使用者可能不樂見「刪個帳戶還連帶把策略停用」,這次先
  採取「儘量不中斷、但明確警告」的路線,若使用者實際用起來覺得不夠嚴謹,
  之後可以再開 ADR 加強。
- 沒有「合併帳戶」「帳戶間轉帳」這類進階操作,目前只有新增/刪除/重置。

### 測試

`tests/test_core.py` 新增 8 個測試:`new_account` 的 id/name 自動產生與
自訂、id 不重複、空白名稱防呆;兩個獨立帳戶各自記帳互不影響 (驗證核心
防衝突機制本身);`account_id_of` 的預設值/自訂值/空白防呆/舊策略相容。
連同既有測試共 339 個全數通過。GUI 層 (`stock_app_pro.py`) 的改動因為
需要 tkinter/shioaji 無法離線測試,已比照既有規範逐一過語法檢查
(`python -m py_compile`),但**務必**依下方清單實機驗證。

### 需使用者實機驗證 (這項改動面很廣,務必仔細驗證)

1. 直接用現有 `E:\StockBuild` 的資料開啟:既有的模擬帳戶餘額/持倉/交易
   紀錄應該完整保留 (自動遷移成「預設帳戶」),不應該歸零或消失。
2. 「💰 模擬帳戶」視窗:應該看到「帳戶」下拉選單、「➕新增帳戶」「🗑刪除
   此帳戶」按鈕。新增一個帳戶,填名稱與初始資金,確認畫面正確切換顯示
   新帳戶的數字 (應該是全新的、跟其他帳戶不共用)。
3. 建立兩個策略,分別設定不同的「交易種類/商品代碼」但**同一個標的**
   (例如都選 MXFR1),但在「模擬帳戶」下拉各自選不同帳戶,啟動模擬讓
   兩個策略都進場:兩個帳戶應該各自正確記錄自己的部位,互不影響,策略
   清單的「持倉」欄位應該分別顯示各自帳戶裡的正確數字。
4. 刪除一個還有策略在用的帳戶:應該跳出警告列出受影響的策略名稱;刪除
   後那個策略之後成交應該正常記到其他帳戶 (系統日誌會有提示),不會
   整個報錯或卡死。
5. 策略清單新增的「模擬帳戶」欄:模擬模式的策略應顯示帳戶名稱,實單
   模式的策略應顯示 `--`。
6. 三個策略編輯器 (內建條件策略/楚狂人之終極波段/自訂Python策略) 都
   應該能看到並正確存讀「模擬帳戶」下拉選單。
