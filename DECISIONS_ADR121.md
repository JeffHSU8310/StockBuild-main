# ADR-121:量化策略抓 K 線的開盤韌性

## 狀態

已實作,**尚未實機驗證**(分支 `claude/qt-kbars-resilience`)。
關鍵驗證只能在真實開盤時做,見文末。

## 背景:使用者回報

```
【自動交易-異常】策略「test」第 1 次錯誤: ShioajiTimeoutError:
Timeout Topic: api/v1/data/kbars, Corr: c12839, Timeout: 30000ms
```

出現兩次:**08:45 與 09:00 各一次**,使用者當下沒有任何操作。

## 查證過程(把錯的假設也記下來)

這一條查了三輪才對,前兩輪的推論都被事實推翻,值得留下來:

**第一輪(錯)**:懷疑資料量。`QT_TF_DAYS` 的日K 是 300 天、周K 700 天、
月K 1500 天,而 `kbars` 一律回 1 分 K,所以日K 策略等於一次要 5.5 萬根。
主圖對超過 90 天的請求早就改成分段下載(ADR-069),策略路徑沒有跟上。
→ **推翻**:使用者的策略是 5分K,只抓 7 天。

**第二輪(錯)**:懷疑「看A做B」的第二次抓取用的是**做B 的週期**
(`_qt_fetch_closed_bars` 第二次呼叫沒帶 `tf`,吃 `strategy.get('timeframe')`),
若 B 是日K 就會變成「A 的 5 分鐘節奏 × B 的 300 天請求」。
→ **推翻**:B 也是 5分K。

**第三輪(對)**:問出**時間點**才找到。`core/market_session.py`:

```python
FUT_DAY_OPEN_MIN = 8 * 60 + 45   # 08:45  期貨日盤開盤
STOCK_OPEN_MIN   = 9 * 60        # 09:00  台股開盤
```

**這兩個時刻正是時段閘門打開的那一秒。**

> 教訓:前兩輪都在讀程式碼找「什麼東西比較大」,第三輪只問了一句
> 「這則訊息什麼時候出現的」就定位了。**時間戳是證據,程式碼是推論**
> —— 下次先問時間戳。

### 根因

`_quant_eval_pass`(`stock_app_pro.py`)在開盤前每 2 秒都被閘門擋掉:

```python
if not market_session.is_market_open(tt, include_night=include_night):
    self._qt_log_session_closed(s, tt, include_night); continue
```

量化 runner 每 2 秒醒一次。時鐘一跨過開盤,`is_market_open()` 翻成 True,
策略**在 2 秒內**發出當天第一個 `kbars` 請求 —— 那一秒全台灣所有 shioaji
用戶端都在做同一件事,券商的 kbars 服務同時還在為新交易日翻檔。

而且**開盤那一發必定是冷的**:分K快取 TTL 只有 30 秒
(`CACHE_TTL_MIN_TF`),開盤前策略一直被閘門擋著、一次都沒抓過。

### 為什麼一次逾時會變成問題

兩則訊息都顯示「第 1 次」,而 `error_count` 只要有一輪完整成功就歸零 ——
代表 08:50/08:55 是成功的,**這確實只是暫時性失敗**。但:

1. **無重試**。主圖那條路(`_download_kbars_chunked._try_seg`)會退避
   1.5s→3s 重試兩次;策略這條路一次都不重試。ADR-048 早就查證過:kbars
   的暫時性失敗多半退一步就會過。
2. **無診斷**。主圖失敗時會呼叫 `api.usage()` 把流量寫進日誌,策略失敗時
   什麼都不印 —— 所以「暫時性管制」與「當日流量耗盡」在日誌上長得一樣,
   查到這裡只能猜。
3. **連續 3 次會自動停用策略**,而停用之後 `_quant_eval_pass` 與
   `_qt_check_realtime_futures_stops` 都會跳過它 ——
   **部位還在,停損卻不跑了**。使用者這次距離這個結果只差一次。

## 決定

### A. 開盤暖機:不要在鐘響那一秒去要 K 線

`core/market_session.py` 新增純函式:

```python
QT_OPEN_WARMUP_SEC = 30

def just_opened(trade_type, dt=None, include_night=True, warmup_sec=None):
    """現在是不是「剛開盤 warmup_sec 秒內」。休市/判不出盤別一律 False。"""
```

`_quant_eval_pass` 在時段閘門通過後、**`_qt_last_boundary` 被寫入之前**擋一下。
位置很重要:寫入之後才擋的話,那一根 K 棒會被吃掉,暖機結束也不會補評估。

**這個延遲不損失任何資訊**:`_qt_fetch_closed_bars` 本來就會丟掉最後一根
視為未完成,開盤後第一根**新的已收盤 K 棒**要等到 08:50(5分K)/ 08:46
(1分K)才存在。等 30 秒完全不影響訊號。

實作細節上有兩個容易寫錯的地方,都寫了測試:
- **夜盤的凌晨段(00:00~05:00)不是「剛開盤」** —— 那一盤是前一天 15:00
  開的;05:00 是**收盤**。把它當開盤會在收盤那一刻莫名停一段時間不評估。
- **盤中零股的開盤時刻是可設定的**(ADR-072,預設 09:10),暖機要跟著
  `ODD_LOT_OPEN_MIN` 走,不可以寫死 09:00。

### B. 重試改用 runner 自己的節奏,**不是** `time.sleep()`

**刻意不沿用主圖那套原地退避。** 量化 runner 是單執行緒:原地睡會一併拖住
其他策略與 `_qt_update_realtime_pnl`(每 3 秒,期貨即時停損停利靠它)。
最壞情況(30s 逾時 + 1.5s + 30s + 3s + 30s)阻塞近 95 秒 ——
**那是拿一個錯誤訊息換掉即時停損,是更糟的交易。**

改用 runner 本來就有的 2 秒節奏:

1. `core/strategy_engine.py` 新增 `KBarsFetchError`,`_qt_fetch_closed_bars`
   把下載例外包成它(斷線類例外 `_looks_like_session_dead` 維持原樣直接
   上拋 —— 那不是暫時性失敗,重試沒有意義,而且上層要靠它
   `_mark_session_dead`)。
2. `_quant_eval_pass` 單獨接 `KBarsFetchError` → `_qt_on_kbars_fetch_error`:
   - 未達 `QT_KBARS_MAX_ATTEMPTS`(3)→ **還原 `_qt_last_boundary`**,
     不記錯誤,下一個 tick(2 秒後)自然重試同一根 K 棒
   - 達上限 → 記一次、**把 boundary 寫回去停止重試**
     (不可以無限每 2 秒重打券商 API)

效果:三發、間隔約 2 秒、零額外阻塞。使用者這次看到的兩則訊息在這個規則下
都不會出現。

### C. 抓資料失敗不再扣「策略錯誤」的點(使用者確認納入)

`error_count` 原本把「抓不到 K 線」與「策略邏輯壞掉」算在同一個池子。拆開:

| 計數 | 來源 | 達 3 次 |
|---|---|---|
| `error_count` | 策略邏輯 / 下單路徑的例外 | **自動停用**(維持原行為)|
| `data_error_count` | 抓不到 K 線 | **永不停用**,只記錄與警告 |

理由:抓不到資料時策略本來就不會產生任何 intent,**停用它得不到任何保護**;
但停用會讓 `_quant_eval_pass` 與 `_qt_check_realtime_futures_stops` 都跳過
這檔策略 —— 部位還在,停損不跑。網路瞬斷不該有這種後果。

自動停用對「真的壞掉」仍然有效,這點有獨立的診斷斷言守著(見驗證)。

**相容性**:`rt` 持久化在 `quant_state.json`,舊檔只有 `error_count`,
語意不變(仍是邏輯錯誤計數),`data_error_count` 缺席時當 0,不需要遷移。

### D. 失敗時補流量診斷

`_qt_log_kbars_usage_once()` 沿用主圖 `_log_usage_once` 的做法,**每天只印
一次**。下次萬一不是開盤尖峰造成的,日誌自己會講是「暫時性管制」還是
「當日流量耗盡」,不用再猜。

## 驗證

離線全過:

- `python tests/test_core.py` → **584 個全過**(原 575,新增 9)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **47 案例全過,0 FAIL**
- `python diag_crossref.py` → 無斷鏈、無重複定義

### 突變測試(每個新斷言都確認「改壞就會紅」)

| 把程式改成 | 結果 |
|---|---|
| `just_opened()` 永遠回 False(等於沒做暖機)| 單元測試紅 |
| 暖機區間右邊界寫成 `<=`(多含一秒)| 單元測試紅 |
| 夜盤凌晨段也被當成剛開盤 | 單元測試紅(**第一次是綠的**,見下)|
| 零股暖機寫死用整股開盤時刻 | 單元測試紅 |
| 拿掉 boundary 還原(等於不重試)| 診斷紅 |
| 重試用完沒有停手(無限重打)| 診斷紅 |
| 斷線例外也被包成 `KBarsFetchError` | 診斷紅 |
| 資料錯誤仍累加到會停用的計數 | 診斷紅 |
| 暖機閘門整個拿掉 | 診斷紅 |

**「夜盤凌晨段」那一條第一次跑出來是綠的**,值得記下來:原本只透過
`just_opened()` 驗這個行為,而 `just_opened()` 裡的 `0 <= elapsed` 剛好會把
錯誤的回傳值擋掉(凌晨段算出來的 elapsed 是負數),所以**契約寫錯了、
結果卻仍然正確**。修法是補一條**直接測 `_session_open_minute` 契約**的
測試,不再靠下游算式湊巧擋住 —— 補完再跑同一個突變,就紅了。
(這正是突變測試的價值:沒跑它,這個洞會一直在。)

## 需使用者實機驗證

**這一項的關鍵驗證只能在真實開盤時做:**

1. **明天 08:45 / 09:00**:
   - 日誌應出現一次「【自動交易-開盤】…先等 30 秒避開開盤尖峰再開始抓K線」
   - **不應再出現** `ShioajiTimeoutError`
   - 08:50 之後策略照常評估(暖機不會把策略卡住)—— 這點要確認,
     暖機閘門若放錯位置會把整根 K 棒吃掉
2. 若仍出現逾時:這次日誌會**附上 API 流量數字**。把那一行貼回來,
   就能分辨是暫時性管制還是流量耗盡,不用再猜。
3. 確認策略整個交易日維持啟用(沒有被自動停用)。

## 不在這次範圍

- **B(執行商品)的現價改用 `snapshots()`**。目前為了拿一個
  `b_df['Close'].iloc[-1]` 把 B 的整段 K 線抓回來(`_quant_eval_pass` 裡
  `watch_enabled` 那一段),確實浪費;但那會把成交價口徑從「上一根已收盤
  K 棒的收盤價」改成「當下現價」,影響實單成交價與模擬帳戶記帳。
  使用者選擇這次不動,另開 ADR。
- `QT_TF_DAYS` 的日/周/月K 天數(300/700/1500 天單次請求)仍未分段。
  這次的策略是 5分K 所以沒踩到,但**日K 以上的策略仍有第一輪推論的風險**
  —— 主圖對超過 90 天的請求早就分段了,策略路徑沒有。留待日後處理。
