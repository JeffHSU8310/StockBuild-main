
## ADR-077：看A做B — 「看A」商品也支援點自選股帶入,並自動判斷 指數/期貨/股票

### 背景 / 需求

使用者:「看A做B,看A的商品也是可以點擊就可以自動帶入及判斷是指數/期貨/股票。」

先前 (策略編輯器帶入) 只有「做B (執行商品)」的代碼欄支援點自選股自動帶入 +
判斷股票/期貨。使用者要「看A」欄也一樣能點自選股帶入,且種類要能判成「指數」。

### 做法

一次自選股點擊沒辦法猜使用者要填 A 還是 B,所以用「**最後點過的欄位**」決定:
- `_qt_editor_symbol_target` 改成 5 元組 `(dialog, entry, type_combo, lookup, kind)`,
  `kind` 為 `'B'` 或 `'A'`。
- 做B 的代碼欄 (兩個編輯器) 綁 `<FocusIn>` → 把作用中目標設為 B (預設也是 B,
  維持原本「沒特別點就填 B」的行為)。
- 看A 的代碼欄綁 `<FocusIn>` → 設為 A;此外「勾選看A做B」的當下也順手切到 A,
  方便馬上點自選股帶 A。
- `on_watchlist_select` 依 `kind` 決定種類判斷:
  - B → `_trade_type_for_symbol` (股票/期貨)。
  - A → 新增 `_watch_trade_type_for_symbol` (指數/期貨/股票):指數優先
    (`strategy_engine.looks_like_index_symbol`,如 ^TWII/^TWOII),其餘沿用 B 的判斷。

UI:看A 欄標題改藍色並加提示「點一下此欄再點左側自選股即可帶入A並自動判斷
指數/期貨/股票」。

### 使用方式

1. (做B) 點自選股 → 帶入做B 代碼、判股票/期貨 (同以前)。
2. 想帶看A:先點一下「看A 商品代碼」欄 (或勾選看A做B),再點左側自選股 → 帶入
   看A 代碼並自動判成 指數/期貨/股票。之後再點自選股會繼續帶 A,直到點回 B 欄。

### 相容 / 安全

- 預設作用中目標仍是 B,既有「點自選股帶B」行為不變。
- 對話框關閉後的殘留目標由 `winfo_exists()` 擋掉 (原本就有)。

### 相關程式位置 (stock_app_pro.py)

- `_watch_trade_type_for_symbol` (新):看A 種類判斷 (含指數)。
- `on_watchlist_select`:5 元組解包 + 依 kind 選種類判斷函式。
- `_qt_open_editor` / 自訂編輯器:B 目標改 5 元組 + `<FocusIn>` 綁定。
- `_qt_build_watch_panel`:A 欄 `<FocusIn>` / 勾選時設為作用中目標 + 提示文字。

### 需使用者實機驗證

1. 開策略編輯器 → 勾看A做B → 點「看A 商品代碼」欄 → 點左側自選股加權(^TWII):
   看A 代碼應帶入、種類自動變「指數」;點期貨→「期貨」;點個股→「股票」。
2. 點回上方「商品代碼」(做B) 欄再點自選股 → 應改帶做B、判股票/期貨。
