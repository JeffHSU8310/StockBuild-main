# PITFALLS.md — 已知陷阱清單

> 台股自動交易系統 (`stock_app_pro.py`) 踩過、且**容易被後人重新踩一次**的坑。
> 開工前先讀 `CLAUDE.md`，再讀本文件確認要改的區塊有沒有已知地雷。
> 每個坑的格式：**症狀 → 根因 → 正確做法 → 出處 ADR**。
> 新踩到坑、或修掉一個坑時，請同步更新這裡與 `DECISIONS.md`。

---

## 一、shioaji 報價 / 資料

### P-01　v0 字典 callback 用 topic 字串分流零股/整股，不可靠
- **症狀**：零股資料混進整股、或整股混進零股，數據時好時壞。
- **根因**：`set_quote_callback` (v0) 只能靠 topic 字串裡有沒有 "ODD" 判斷，
  字串格式一旦變動判斷就失效，本質不可靠。
- **正確做法**：只用 v1 typed callback (`set_on_tick_stk_v1_callback` /
  `set_on_bidask_stk_v1_callback` /`set_on_tick_fop_v1_callback` /
  `set_on_bidask_fop_v1_callback`)，讀物件上的 `intraday_odd` 布林欄位分流。
  絕不要再註冊 v0 callback。
- **出處**：ADR-005，鐵則 2。

### P-02　`Snapshot` 沒有 `bid_ask` 屬性，盤後五檔不能捏造
- **症狀**：盤後五檔量看起來「有資料」，但其實是假的、會誤導委買賣力道判讀。
- **根因**：shioaji `Snapshot` 只有最佳一檔 (`buy_price/sell_price/buy_volume/
  sell_volume`)，**沒有** `bid_ask`。任何「用總量除以 10 展開五檔」都是編造。
- **正確做法**：第 1 檔顯示 snapshot 真實值，第 2~5 檔量一律 `--`，物件帶
  `is_simulated=True`，UI 標「(參考)」/「快照」。沒有真資料就誠實顯示 `--`。
- **出處**：ADR-005，鐵則 4。

### P-03　`snapshots()` 無節流會把當日 API 配額打光
- **症狀**：盤中用著用著，整個系統報價突然全部失效一整天。
- **根因**：shioaji 有每日流量配額，fallback 迴圈若無限制高頻呼叫
  `snapshots()` 會在盤中把配額耗盡。
- **正確做法**：無串流 fallback 快照間隔 **≥ 3 秒** (`self.last_fallback_snap_time`
  節流；ADR-094 查證官方文件 snapshots/ticks/kbars 合計 10 秒 50 次上限後，
  由 5 秒調整為 3 秒)。要再縮短必須先查官方流量文件並記新 ADR，不可憑感覺調。
- **出處**：ADR-005 / ADR-094，鐵則 5。

### P-04　零股/整股暫存共用變數 + 裸讀寫會競態污染
- **症狀**：零股與整股 tick / 五檔互相蓋來蓋去。
- **根因**：callback 在 shioaji 背景執行緒觸發，UI worker
  (`fetch_realtime_worker`) 在另一條執行緒讀，兩邊裸讀寫同一組變數。
- **正確做法**：`current_tick_normal`/`current_tick_odd`、
  `current_bidask_normal`/`current_bidask_odd` 永遠分開；所有讀寫都經
  `self.quote_lock`。
- **出處**：ADR-005，鐵則 3。

### P-05　四路訂閱包在一個大 try，失敗無聲無息
- **症狀**：零股沒資料，卻查不出是哪一路訂閱失敗。
- **根因**：整股 Tick / 整股 BidAsk / 零股 Tick / 零股 BidAsk 四路共用一個
  `try/except`，任一路掛掉整批中斷且無記錄。
- **正確做法**：逐路獨立 try/except，每路成功/失敗都印日誌，格式如
  `【訂閱結果】整股Tick:✓ 整股五檔:✓ 零股Tick:✗ 零股五檔:✗`。
- **出處**：ADR-005，鐵則 8。

### P-06　盤後冷登入拿不到「當日零股收盤價」，這是 shioaji 先天限制
- **症狀**：盤後 (如晚上 21:30) 開 App，零股看板顯示整股參考價，跟當日零股
  實際收盤對不上。
- **根因**：`snapshots()` 的 close 永遠是整股價；零股收盤價唯一來源是整股
  v1 tick 串流物件上的 `closing_oddlot_close`（14:30 盤後零股收完才推送）；
  shioaji **沒有**盤中零股歷史 tick 查詢 API。冷登入時串流早已錯過。
- **正確做法**：`on_tick_stk_v1` 收到 `closing_oddlot_close>0` 就快取；冷登入
  拿不到時退回整股參考價並**明確標註**「今日零股收盤 shioaji 盤後無法回補」，
  不要讓使用者誤以為是 bug。要拿真實零股收盤，App 需在 13:40–14:30 保持連線。
- **出處**：ADR-006。

---

## 二、K 線聚合

### P-07　期貨用 `resample('D')` 會被夜盤污染
- **症狀**：台指期日/週/月K的開高低收跟券商軟體對不上（收盤被夜盤最新價蓋掉）。
- **根因**：`kbars()` 對期貨只回分K，日K靠自行聚合。`resample('D')` 依自然日
  00:00 切割，會把 15:00 起的夜盤混進隔天日盤（夜盤量約佔全日三分之一，不可忽略）。
- **正確做法**：期貨走 `core/futures_session.py` 的**交易日 (session date) 聚合**：
  時間 >13:45 歸下一交易日，≤13:45 維持當天；日盤排在夜盤兩段之後，取 `'last'`
  當 Close 自然落在日盤 13:45（「近全」收盤定義）。股票/指數沒夜盤，維持自然日
  resample。要改收盤定義需另開 ADR。
- **出處**：ADR-007。

---

## 三、下單

### P-08　`order_lot` 有 4 種，不是 2 種
- **症狀**：只支援整股/盤中零股，缺盤後定價、盤後零股。
- **根因**：舊版誤以為只有 Common/IntradayOdd。實際是
  `Common`(整股)、`Fixing`(盤後定價)、`Odd`(盤後零股)、`IntradayOdd`(盤中零股)。
- **正確做法**：四種模式都要支援；委託驗證統一走 `core/order_rules.py`。
- **出處**：ADR-008。

### P-09　零股類交易所規則：只能現股 / 限價 / ROD，數量 1~999 股
- **症狀**：零股掛市價或融資融券被券商退單，浪費一次 API 呼叫。
- **根因**：交易所硬規則，不是可放寬的選項。
- **正確做法**：`execute_order()` 送出前本地擋下不合規的零股委託，這個防呆
  不可被移除或繞過。整股/盤後定價上限 499 張、零股類 999 股
  (`core/order_rules.py` 的 `MAX_QTY_LOT`/`MAX_QTY_ODD`)。
- **出處**：ADR-005 / ADR-008，鐵則 6、鐵則 14。

### P-10　下單必須先跳確認視窗，只有一個方法可以真正送單
- **症狀**：為了「方便測試」把送單直接接回 `execute_order` → 誤送真實委託。
- **根因**：實盤下單，繞過確認 = 高風險。
- **正確做法**：`execute_order()` 只驗證+組裝 Order → `_show_order_confirmation()`
  顯示欄位 → **只有** `_confirm_and_place_order()` 可呼叫
  `self.sj_api.place_order(...)`。不可繞過。
- **出處**：ADR-013，鐵則 14。

### P-11　委託剛送出 (PendingSubmit) 時 `trade.order.id` 可能為空
- **症狀**：日誌印「委託成功 PendingSubmit」，但「我的委託單」清單是空的。
- **根因**：PendingSubmit（交易所尚未完全確認）時 `trade.order.id` 常是空字串，
  舊版 `if order_id:` 判斷為否就整段靜默跳過，外層又 `except: pass` 吞掉例外。
- **正確做法**：取 id 用獨立 try/except（存取真實 shioaji 物件可能丟例外），
  取不到就用 `_pending_` 開頭本地暫時 key 先讓委託顯示；之後
  `_handle_order_event` 收到真實 id 再替換。**seed 成功/失敗都要印明確日誌**，
  `_refresh_my_orders_ui` 例外不可靜默吞掉。數量轉換用 `_safe_int()`。
- **出處**：ADR-018 / ADR-019 / ADR-020。

### P-31　Treeview「先刪光再組列」會在中途炸掉時留下永久空白清單
- **症狀**：日誌印「已加入清單 (共 N 筆)」，分頁卻空白且**一直**空白
  （資料在 `my_orders` 裡都對）。
- **根因**：refresh 流程是「刪光 Treeview → sorted() → 逐列 fmt_price 插入」。
  排序或格式化中途丟例外（實測重現點：seed 的 `ts:0` 與委託回報的
  `exchange_ts` 型別混排，`sorted()` TypeError），畫面停在「已刪光、還沒插」，
  之後每次 refresh 死在同一處。
- **正確做法**：「**先備妥 → 再刪 → 再插**」——所有列先在記憶體組好
  （排序 key 經 `_safe_ts()` 強制轉 float、逐列獨立 try/except 壞一列跳一列），
  組完才動 Treeview。清單類 UI 更新一律遵守此原則。
- **出處**：ADR-021。

### P-33　ttk Treeview (clam 主題) 只設 `'.'` foreground,資料列會「插了卻看不見」
- **症狀**：委託單/已成交分頁,**標題列看得到、資料列整片空白**;日誌證明資料
  已插入 (`Treeview N 列 / my_orders M 筆` 兩數皆 >0) 且無任何例外。
- **根因**：clam 主題下,`style.configure('.', foreground='#FFFFFF')` 只讓
  **標題列**上色,**資料列**的文字色/背景色/列高必須針對 `Treeview` style
  明確設定,否則資料列以主題預設渲染 (文字色可能與底色相近),看起來像空白。
- **正確做法**：建專用 style（如 `Trades.Treeview`）明確設 `background`/
  `fieldbackground`/`foreground`/`rowheight`,Treeview 建構時 `style=` 套用;
  每列插入再套 `tags=('visible_row',)` + `tag_configure(foreground=...)` 雙保險;
  插入後 `update_idletasks()`。並保留「refresh 後印出實際列數」的診斷日誌,
  讓「插了卻看不見」這種最難查的狀況變成看得到的數字。
- **出處**：ADR-022。

### P-12　委託狀態用「主動回報」，不可用 `update_status()` 輪詢
- **症狀**：想每隔幾秒輪詢委託狀態。
- **根因**：官方「使用限制」文件明確要求用主動回報 (callback/SSE)，避免輪詢。
- **正確做法**：`api.set_order_callback()` 註冊 push callback
  (`on_order_deal_callback` → `_handle_order_event`/`_handle_deal_event`)，
  完全不輪詢。callback 在 shioaji 執行緒，更新畫面一律經 `safe_after`。
- **出處**：ADR-018。

### P-13　現沖 checkbox 只在「換股」決定起始值，不可被其他操作覆蓋
- **症狀**：使用者在某檔手動取消現沖勾選後，點了別的按鈕又被悄悄勾回去。
- **根因**：`update_daytrade_checkbox_state()` 在切換交易別/種類時也會被呼叫；
  若它在「合格時」主動把 `daytrade_var` 設回 True，就會覆蓋使用者的手動取消。
- **正確做法**：換股時依 `current_day_trade` 設一次起始值；
  `update_daytrade_checkbox_state()` **只**負責「不合格時強制清空並鎖住」，
  合格時**不主動勾回**。系統決定起始值、使用者可在單筆委託上覆蓋。
- **出處**：ADR-015 / ADR-018，鐵則 16。

### P-14　shioaji 當沖旗標參數名稱有版本差異
- **症狀**：`Order(..., daytrade_short=True)` 在某些版本丟 `TypeError`。
- **根因**：不同 shioaji 版本參數名可能是 `daytrade_short` 或舊版 `first_sell`。
- **正確做法**：`execute_order()` 依序 try `daytrade_short=True` →
  `first_sell=StockFirstSell.Yes` → 無旗標一般委託 (並提示使用者手動確認)。
- **出處**：`stock_app_pro.py` `execute_order()`。

---

## 四、繪圖 / 版面（踩最多次的一區）

### P-15　⚠️ `fig.subplots_adjust()` 對 mplfinance 面板無效
- **症狀**：不管邊界比例、figsize、tight_layout 怎麼調，圖表四周白邊/面板位置
  一動也不動（此坑連續踩了四~五輪才找到根因）。
- **根因**：mplfinance 的每個面板是用 `fig.add_axes([固定矩形])` 建立的，
  而 `subplots_adjust()` **只影響 subplot/GridSpec 建立的軸域**，對 add_axes
  的固定位置軸域完全無效。用 `plt.subplots()` 測會「有效」是假象，因為那不是
  mplfinance 的建法。
- **正確做法**：用 `_apply_chart_margins(fig, axlist, panel_ratios)` 對每個面板
  （`axlist[2i]` 主軸 + `axlist[2i+1]` secondary_y 孿生軸）直接 `set_position(rect)`。
  即時預覽走同一個方法 + `canvas.draw_idle()`，不做完整重繪。
- **出處**：ADR-020。

### P-16　`canvas_widget.config(width,height)` 會被 `pack(fill=BOTH, expand=True)` 覆蓋
- **症狀**：想用像素微調畫布尺寸，滑桿拉到最大也完全沒反應。
- **根因**：`pack(fill=BOTH, expand=True)` 會強制 widget 撐滿容器、忽略 config
  指定的尺寸。兩者同時用，config 等於白設。
- **正確做法**：畫布本來就該用 `pack(fill=BOTH, expand=True)` 自動填滿容器
  （這正是我們要的）；圖表內部留白改用 P-15 的 `set_position` 控制。不要再加
  無效的 config 寬高，也不要提供會誤導的「像素微調」滑桿。
- **出處**：ADR-020。

### P-17　副圖 Y 軸不會自動跟著縮放/平移重算，會被畫面外極端值壓扁
- **症狀**：MACD/RSI 等副圖線被壓成貼底的扁平線，看起來「跑掉/壞掉」，且查詢
  天數越長越容易發生。
- **根因**：`auto_scale_y` 原本只套主圖；副圖 Y 軸是 mplfinance 依「整個資料集」
  算的固定範圍，即使某極端值已捲到畫面外，仍撐開整個副圖 Y 軸。
- **正確做法**：`auto_scale_indicator_panels(xmin, xmax)` 依「目前可見 X 範圍」
  重算每個副圖 Y 軸；在初始載入、還原縮放、滾輪縮放、拖曳平移四處都要呼叫
  （拖曳任一面板都要重算，因為各面板共用 x 軸）。
- **出處**：ADR-018。

### P-18　換股後 hover 資訊列殘留上一檔股票資料
- **症狀**：換股後上方 hover 列還顯示前一檔的名稱/開高低收，跟新圖表標題不一致。
- **根因**：`lbl_hover_info` 只在滑鼠移到圖上才更新；換股後若還沒移滑鼠，會停在
  上一檔最後 hover 的資料。
- **正確做法**：`update_ui()` 呼叫 `draw_chart()` 前，把 `lbl_hover_info` 重置為
  預設提示、`last_hover_idx=-1`。
- **出處**：ADR-018。

### P-19　紅漲綠跌，絕不可換
- **症狀**：某處顏色寫反。
- **根因**：台股慣例紅=漲、綠=跌，與美股相反。
- **正確做法**：所有 K 線、五檔、漲跌文字、MACD Hist 一律紅漲綠跌；寫反視為
  bug 立即修。Hist 依數值正負動態上色。
- **出處**：鐵則 1，ADR-017。

### P-20　價格格式化一律走 `fmt_price()`，不可無腦 `.2f`
- **症狀**：交界價位 (10/50/100/500/1000) 出現非法小數位。
- **根因**：ETF (00 開頭) 與一般股票 tick 表不同，各處自己寫格式化會不一致。
- **正確做法**：一律 `self.fmt_price(p)` → `core/tick_rules.py`。手動輸入不對齊
  tick 時用 `round_to_tick()` 修正（`FocusOut` + 送單前各一道）。
- **出處**：鐵則 7，ADR-018。

### P-21　中文輸入法送「全形句號」，小數點打不出來
- **症狀**：輸入法非英文模式時，價格欄位打不出小數點（或顯示全形「。」）。
- **根因**：中文輸入法句號鍵送出全形「。」而非半形「.」。原本只在 `FocusOut`
  才轉，打字當下畫面仍是錯的。
- **正確做法**：`entry_price` 綁 `<KeyRelease>` → `_normalize_decimal_realtime`，
  打字當下即時把全形句號/小數點轉半形並保留游標；另備「.」按鈕與 `FocusOut`
  修正當保險。（NumLock 關閉送 `KP_Delete` 的舊猜測已非主因。）
- **出處**：ADR-016 / ADR-020。

---

## 五、生命週期 / 執行緒

### P-22　背景執行緒排回 UI 一律用 `safe_after`，不可裸用 `self.after`
- **症狀**：關視窗時主控台噴 `_tkinter.TclError: invalid command name`。
- **根因**：daemon worker 會跑到行程結束；視窗關閉後仍想 `self.after()` 更新已
  銷毀的 widget。
- **正確做法**：一律 `self.safe_after(...)`（兩層防護：排程前檢查 `_closing`、
  執行時再檢查一次並包 `try/except TclError`）。新增任何背景排程都要沿用。
- **出處**：ADR-012，鐵則 13。

### P-23　關視窗要先 `logout()` 再 `os._exit(0)` 保底
- **症狀**：關掉視窗後終端機跳不回提示字元（行程沒真正結束）。
- **根因**：shioaji 內部 WebSocket 執行緒不是我們開的，無法保證是 daemon；只
  `destroy()` 行程可能永遠不結束。
- **正確做法**：`on_app_close()` 依序 (1) 已登入時 `sj_api.logout()` (2) `destroy()`
  (3) `os._exit(0)` 保底。三步都要有。
- **出處**：ADR-014，鐵則 15。

### P-24　`create_widgets()` 裡呼叫依賴其他 widget 的方法，順序要對
- **症狀**：啟動即 `AttributeError` 崩潰（例如 `set_trade_mode` 用到還沒建的
  `log_txt`），連帶整個行程死掉、看起來像「無法登入券商 API」。
- **根因**：初始化呼叫的時間點早於它依賴的 widget 建立。
- **正確做法**：把 `self.set_trade_mode("Common")` 這類初始化呼叫移到
  `create_widgets()` **最尾端**（所有 widget 都建好之後）。不要靠在
  `log_message` 加防呆蓋症狀。
- **出處**：ADR-010。

### P-25　同時登入券商官網會造成 API session 中斷
- **症狀**：`SessionNotEstablished` / session 相關錯誤，報價/下單失效。
- **根因**：同一帳號在官網與 API 並行登入會互踢。
- **正確做法**：用 API 前先登出官網。程式端 `_looks_like_session_dead()` 偵測
  斷線關鍵字、`_mark_session_dead()` 把 `api_logged_in` 撥回 False 並更新狀態列。
- **出處**：ADR-016。

---

## 六、資料源政策

### P-26　台股一律 shioaji，不可偷偷加回 yfinance/FinMind 備援
- **症狀**：未登入時台股查詢安靜退化成別的資料源，數據來源不一致。
- **根因**：多資料源混用會造成資料不一致與難以除錯。
- **正確做法**：台股（股票/ETF/指數/期貨）未登入 API 時**直接報錯退出**並提示先
  登入，不退化；美股才自動用 yfinance（shioaji 不支援美股）。FinMind 及其
  法人/資券籌碼指標已整個移除，不可因「以前有」順手加回，要恢復需先確認並開
  新 ADR。判斷邏輯在 `is_taiwan_instrument`。
- **出處**：ADR-011，鐵則 12。

---

## 七、開發 / 測試

### P-27　`core/`、`data/` 必須零 tkinter、零 shioaji 依賴
- **症狀**：想在 core 裡 `import shioaji` 或讓函式簽名依賴 `self`/tkinter Variable。
- **根因**：這兩個套件存在的唯一理由是「可離線單元測試」，一旦有 GUI/券商依賴
  就破功。
- **正確做法**：純計算/純規則（技術指標、tick、委託驗證、K 線聚合）寫成 `core/`
  純函式並補 `tests/test_core.py`；需要 `self.after`/widget/callback 的留在
  `stock_app_pro.py`。改 `core/`/`data/` 後必跑 `python tests/test_core.py`。
- **出處**：ADR-009，鐵則 11。

### P-28　假 mplfinance 若用空殼 `mpf.plot`，`draw_chart` 內部完全沒被測到
- **症狀**：`draw_chart` 裡打錯方法名/參數，headless 測試卻全過，實機才爆。
- **根因**：早期假 `mpf.plot()` 回傳 `(None, [])`，所有動 `axlist` 的程式碼
  （`set_title`/`.text()`/`set_position`/`axvline`）從沒真正執行。
- **正確做法**：假 mplfinance 要用**真的 matplotlib Figure + `add_axes`** 建面板
  （見 `diag_mock_tkinter.py`），讓 `draw_chart` 對真 Axes 執行才抓得到錯。這也
  是能重現 P-15（subplots_adjust 無效）的關鍵。
- **出處**：ADR-017 / ADR-018。

### P-29　技術指標四塊 (MACD/RSI/KDJ/DMI) 共用一個 try/except 的既有耦合
- **症狀**：其中一個參數轉換失敗，會連帶跳過其餘幾個指標。
- **根因**：`core/indicators.py` 重構時**刻意保留**這個重構前就有的耦合，避免重構
  夾帶邏輯修正。
- **正確做法**：知道這是已知行為即可。若要拆成互不影響的獨立 try/except，請**另
  開一筆 ADR**，不要在其他重構裡順手改掉。
- **出處**：ADR-009。

### P-30　交付前的固定收尾檢查
- **必做**：(1) `python -m py_compile stock_app_pro.py`；(2) AST 掃描（找不到定義
  來源的 `self.xxx`、重複定義的方法）；(3) 改 `core/`/`data/` 就跑
  `python tests/test_core.py`；(4) 動到 GUI/繪圖/下單就跑 `diag_repro_issues.py`
  等假 tkinter 診斷；(5) GUI 深度耦合的部分無法在 headless 驗證，要明確請使用者
  實機驗證並附上「怎麼驗」。
- **出處**：CLAUDE.md 開工流程、歷次 ADR 的驗證段。

### P-34　shioaji 刪改是對 `Trade` 物件操作,不是給 order id
- **症狀**：想做刪單/改單,手上只有回報來的 order id,`cancel_order`/`update_order`
  無從呼叫。
- **根因**：shioaji 的刪改 API 簽名是 `cancel_order(trade)` / `update_order(trade,
  price=/qty=)`,需要 `place_order` 回傳的 `Trade` 物件。
- **正確做法**：seed 時把 trade 存進 `my_orders[id]['trade']`;暫時項目被正式
  回報替換時要把 trade 接續過去。沒有 trade 時用 `list_trades()` 依 id 找回
  (一次性,非輪詢);再找不到就誠實告知請於券商 App 處理,不亂送。
- **出處**：ADR-023。

### P-35　`update_order` 改量的 qty 語意 (減量 vs 新總量) 不可憑記憶硬寫
- **症狀**：改量 10→7 結果變成 7 張被砍剩 3、或方向整個相反。
- **根因**：qty 參數語意依文件版本描述不一,離線環境無法查證,寫錯直接改錯
  實盤數字。
- **正確做法**：目前實作採「qty=要減少的量」(交易所「改量即減量」慣例);
  送出前日誌同時印「意圖 (10→7)」與「實際參數 (qty=3)」;**首次實機用最小
  差距 (10→9) 驗證**,若方向相反只需改 `_send_order_modification` 一處。
  驗證結果務必回寫本條與 ADR-023。
- **出處**：ADR-023。

### P-36　shioaji kbars 回傳「一分K」,長週期抓太多天會慢到不行
- **症狀**：切台指期/指數要等數秒到十幾秒;日K以上週期特別慢。
- **根因**：kbars 沒有日K端點,一律回一分K。台指期一天近 19 小時 ≈ 1140 根/天,
  日K抓 730 天 ≈ 55 萬根,下載+重採樣成本巨大;且若無快取,換週期/切回商品
  都整套重來。
- **正確做法**：(1) `SJ_DAYS` 控制下載天數,勿隨手加大;(2) 原始分K進
  `_kbars_raw_cache`,重採樣自快取,同商品換週期零下載;(3) 慢商品 (期貨/指數)
  日K以上用兩段式:小範圍搶先出圖、背景補全並平移 xlim;(4) 任何發布都要過
  `_fetch_seq` 序號防護,舊查詢不可蓋新圖;(5) 同商品換週期跳過退訂/重訂閱。
- **出處**：ADR-024。

### P-37　matplotlib 互動事件裡呼叫整圖重繪,會把整個視窗拖到滑鼠都卡
- **症狀**：滑鼠移到K棒上十字線跟不上,且滑鼠在本軟體視窗內移動本身就變慢
  (其他軟體正常);拖曳平移也頓。
- **根因**：`motion_notify_event` 每移一像素觸發一次;在裡面呼叫
  `draw_idle()` 等於高頻「整張圖重繪」(幾百根K棒×多面板,單次上百毫秒),
  Tk 主執行緒飽和,連視窗游標渲染都被拖慢。
- **正確做法**：hover 疊加物 (十字線/文字) 一律 `animated=True` + blitting:
  `draw_event` 時 `copy_from_bbox` 快取乾淨底圖,滑鼠移動只
  `restore_region + draw_artist + blit` (毫秒級),blit 失敗才降級 draw_idle;
  所有更新 (含資訊列字串重組) 收進「換K棒才做」的 gating;真重繪類互動
  (平移) 用時間節流 (30ms) + 放開時補最終一繪。新增任何圖表互動都遵守:
  **motion 事件裡絕不整圖重繪**。
- **出處**：ADR-025。

### P-118　只量 `canvas.draw()` 會把主圖效能瓶頸判錯
- **症狀**：日誌寫「`mpf.plot + canvas.draw`」，但實際計時點在
  `mpf.plot()` 之後；看到畫圖慢就想把指標改寫成 C++，卻沒有證據。
- **根因**：指標計算、artist 建立與 Agg 點陣化是三種不同工作，
  單一計時值無法決定該優化哪一層。
- **正確做法**：`draw_chart()` 分開量指標／建圖／畫布／總時間；
  繪圖資料量同時經 `core/chart_viewport.py` 依畫布像素限制。
  先用實機數據決定下一步是快取、artist 重用、C++ 計算還是更換 renderer。
- **出處**：ADR-140。

### P-38　對同一個 Shioaji 物件重複 login 會陷入「重登永遠失敗」死循環
- **症狀**：session 斷線 (常見:官網互踢) 後,不論怎麼點重新登入都出現
  「同一帳號同時間只能有一個生效」,只有重開整個程式才能恢復。
- **根因**：斷線後只撥 `api_logged_in=False` 旗標:客戶端物件內部狀態
  (WebSocket/token) 已壞,券商端舊 session 又還佔著名額,對同一物件再
  login 必被拒。
- **正確做法**：(1) `_mark_session_dead` 時背景 best-effort `logout()` 釋放
  券商端名額;(2) 重登一律「logout 舊物件 → `sj.Shioaji()` 建全新物件 →
  `current_contract` 作廢 → login」;(3) 斷線判斷關鍵字只認明確字樣
  (SessionNotEstablished 等),泛用的 "session"/"not ready"/"connection error"
  會把暫時性錯誤誤標成斷線,引發假性登出循環。
- **出處**：ADR-026 (P-25 的程式端完整解法)。

### P-39　功能只綁「雙擊」而無可見入口 = 使用者當成功能不存在
- **症狀**：功能明明做好了 (如委託刪改),使用者回報「看不到/沒有這功能」。
- **根因**：唯一入口是雙擊列,介面上沒有任何按鈕或文字提示,無從發現。
- **正確做法**：互動功能至少給一個「看得見」的入口 (按鈕/選單) + 操作提示;
  雙擊可作為快捷,但不能是唯一入口。對話框 Entry 明確配色 (bg/fg/
  insertbackground) 避免同色隱形 (P-33 同源),並 lift()+focus_force() 確保
  不開在主視窗後面。
- **出處**：ADR-027。

### P-40　委託回報可能比 place_order() 回傳「更早」抵達,造成清單重複
- **症狀**：下一筆單,清單偶爾出現兩筆一模一樣的委託。
- **根因**：shioaji order callback 在 `place_order()` 還沒 return 時就先觸發,
  正式項目先入清單;seed 隨後又建 `_pending_` 暫時項目 → 永久重複。
- **正確做法**：seed 建暫時項目前,先掃清單有無「同商品/同買賣/同量、數秒內」
  的正式項目,有就不建暫時項目直接返回。(與 P-11/P-32 的「晚到」情境互補,
  兩個方向都要防。)
- **出處**：ADR-027。

### P-41　Treeview 的 selection_set 會發 <<TreeviewSelect>> 事件,Listbox 不會
- **症狀**：自選股按「上移/下移」後,圖表莫名重新載入 (等於誤點了那一檔)。
- **根因**：Listbox 的 selection_set 不發事件,Treeview 的會;把 Listbox 換成
  Treeview 後,程式化選取 (上移/下移/重建列表) 觸發了 select handler。
- **正確做法**：程式化選取前設抑制旗標 (`_wl_select_suppress`),handler 開頭
  檢查;旗標用 `safe_after(50ms)` 延後解除 (事件在 idle 才派發)。任何
  Listbox→Treeview 遷移都要檢查這個行為差異。
- **出處**：ADR-028。

### P-42　純英文代號 ≠ 美股:期貨代號需要使用者指定市場,不能用字元特徵猜
- **症狀**：輸入 ZEF/CDF 等期貨代號被當成美股丟給 yfinance,查無資料。
- **根因**：舊判斷「含數字=台股,其餘=美股」,只對 TXF/MXF 等少數代號硬編
  例外,涵蓋不了全部期貨商品。
- **正確做法**：市場模式由使用者指定 (台股/台期貨/美股 segmented 按鈕),
  台期貨模式走 `_resolve_futures_contract` 通用解析 (`<代號>R1` 連續合約);
  查無代號用 `_log_futures_candidates` 列出可用商品幫使用者找代號。
  下單端的合約解析也要同步通用化 (直接用 current_contract),否則新期貨
  能看圖不能下單。
- **出處**：ADR-028。

### P-43　訂閱期貨 R1 連續合約,推送的 tick.code 是「實際月份合約」,相等比對必失敗
- **症狀**:台指期報價 5 秒才跳一次 (當沖等級完全不夠用),但訂閱明明成功。
- **根因**:訂閱 TXFR1 後,shioaji 推送的 tick/bidask 的 code 是實際月份合約
  (如 TXFG6),`tick.code == contract.code` ('TXFR1') 永遠 False → 串流全被
  丟棄 → 永遠走 5 秒快照 fallback。極隱蔽:訂閱成功、callback 有進來,
  只是被自己的過濾條件丟掉。
- **正確做法**:期貨比對用「商品前綴 (前3碼)」(`_fop_code_match`),一次只訂
  一檔期貨不會誤收;完全相等亦接受 (訂特定月份時)。若未來多檔期貨同時
  串流,需改 per-symbol 暫存並另開 ADR。
- **出處**:ADR-029。

### P-44　tkinter 空間不足時「後 pack 的先被擠出視窗」— 關鍵資訊區要先 pack
- **症狀**:面板加了一列新元件後,最底部的五檔報價「不見了」(標題卡在視窗邊緣)。
- **根因**:pack 順序決定空間分配優先權;空間不足時最後 pack 的元件先被擠出。
  五檔原本是左側面板最後 pack 的,任何讓面板長高的改動都會把它擠掉。
- **正確做法**:關鍵必見區塊「先 pack + side=BOTTOM」(順序=優先權、side=位置,
  兩者獨立);把可壓縮的滾動列表 (如成交明細) 留在後面當犧牲者。在左側面板
  新增任何元件時,都要想「擠掉的會是誰」。
- **出處**:ADR-030。

### P-45　跨檔案簽名變更,主程式要能容忍附屬模組版本落後
- **症狀**:只更新主程式、忘了同步覆蓋 core/ 模組 → TypeError → 整張K線圖
  空白 (主功能全掛)。
- **根因**:主程式以新關鍵字參數呼叫舊簽名的 core 函式;交付含多檔且路徑
  不同 (主程式在根目錄、模組在 core/),使用者漏放其一機率很高。
- **正確做法**:(1) 新參數盡量放簽名尾端給預設值 (向下相容);(2) 呼叫端
  try/except TypeError 降級到舊簽名,主功能照常、印一次性版本提示與確切
  覆蓋路徑;(3) 交付訊息明列「每個檔案 → 哪個資料夾」。
- **出處**:ADR-030。

### P-46　`on_app_close` 在主執行緒同步呼叫會卡死的 I/O (logout),視窗會「沒有回應」
- **症狀**:按 X 關不掉,標題列出現 (沒有回應)。
- **根因**:session 殭屍時 `sj_api.logout()` 永不返回,主執行緒被卡死。
- **正確做法**:關閉流程的網路 I/O 一律「背景執行緒 + join(timeout)」;
  逾時放行,交給 `os._exit(0)` 保底 (P-23)。任何寫在主執行緒的阻塞式
  第三方呼叫都要問:它卡住時使用者還關得掉視窗嗎?
- **出處**:ADR-030 (P-23 的補強)。

### P-47　「含數字=台股」啟發式會誤殺期貨完整代號 (TXFR2/TXF202609 都含數字)
- **症狀**:點自選股的 TXFR2 報「券商合約查無」,該列報價永遠 '--'。
- **根因**:市場自動判斷與自選股報價解析把「含數字」當台股,但期貨完整
  代號 (R2 的 2、月份 202609) 本來就含數字,被搶先誤判成股票。
- **正確做法**:`_looks_like_futures_symbol` (3 英文字母 + R1/R2 或 6 位數字)
  必須排在「含數字」判斷之前;同一份判斷邏輯的每個使用點 (市場切換/
  報價解析/台股模式 fallback) 都要一起改,漏一處就會有一條路徑復發。
  與 P-42 (純英文≠美股) 同族:代號路由一律「樣式/解析優先,字元特徵墊底」。
- **出處**:ADR-031。

### P-48　CPU 密集的第三方 SDK 呼叫即使丟到背景執行緒,仍可能靠 GIL 卡死整個 GUI
- **症狀**:登入/查詢等操作丟給 `threading.Thread` 執行了,理論上主執行緒
  該保持順暢,但 GUI 卻整個「沒有回應」,連視窗都關不掉。
- **根因**:Python 的 GIL 是「整個行程共用一把鎖」,不是「每個執行緒各自
  獨立」。如果背景執行緒在跑的是長時間 CPU 密集且不常釋放 GIL 的 C 擴充
  呼叫 (如編譯過的交易 SDK 解析大量合約資料),GUI 主執行緒 (含 Tk 事件
  迴圈) 一樣會被卡住排不到執行機會——這與「有沒有用 threading.Thread」
  無關,threading 只解決「我方 Python 邏輯」之間的並發,解決不了「別人的
  C 擴充長時間不放 GIL」。
- **正確做法**:(1) 先確定並清除自己程式碼裡任何額外的、可驗證的卡住來源
  (例如對從未連線過的物件呼叫 logout 造成的不必要風險);(2) 防止使用者
  誤觸疊加第二個同類型背景工作,讓 GIL 爭用加倍;(3) 及早且持續給出「這是
  已知現象、預期要等多久、不要強制關閉」的訊息,把「看起來當機」轉換成
  「已知道在等什麼」;(4) 若確認是真正的 GIL 長時間不放,結構性解法是把
  該 SDK 呼叫隔離到獨立行程 (multiprocessing),但這是大改動,值不值得做
  取決於實機卡頓的實際時長與頻率。
- **出處**:ADR-032。

### P-49　自動交易絕不能用「未完成的K棒」評估訊號 (repaint 假訊號)
- **症狀**:策略盤中頻繁觸發又「反悔」——同一根K棒的指標值隨即時價不斷
  變動,交叉成立又消失,若直接拿最新一根K棒評估,會下出大量事後看根本
  不存在訊號的單。
- **根因**:重採樣後的最後一根K棒在該週期結束前是「進行中」狀態,
  Close/High/Low 都還會變。
- **正確做法**:評估一律剔除最後一根 (`df.iloc[:-1]`),只用已收盤K棒;
  搭配「同一根K棒只評估一次」閘門 (記錄 last_bar_ts) 與「交叉條件只在
  交叉當根為 True」,三層合起來才能杜絕重複/假訊號。代價是訊號最多晚
  一根K棒,這是正確性換來的,不要為了「更即時」把這層拿掉。
- **出處**:ADR-035。

### P-50　多執行緒共用單一 broker 連線,併發呼叫同一 API 會互相干擾
- **症狀**:新增背景自動更新後,切換商品時「背景補全下載失敗」、K線圖異常。
- **根因**:手動查詢、主圖自動更新、量化 runner 三個背景執行緒共用同一條
  shioaji 連線;同時呼叫 kbars 造成互相干擾。且自動更新可能讀到「新商品的
  contract + 舊商品的 df」而把兩者資料合併。
- **正確做法**:(1) 單一連線的下載 API 用一把鎖 (`_kbars_lock`) 強制串行化;
  (2) 手動查詢設 `_fetch_in_progress` 旗標,背景自動更新偵測到就完全讓路;
  (3) 自動更新用「df 物件身分守衛」(記下開始時的 df 參照,套用前比對是否
  同一物件) + 序號守衛,確保絕不把 A 商品資料黏進 B 商品的圖。凡是新增
  會碰共用連線/共用狀態的背景執行緒,都要先想清楚與既有流程的併發關係。
- **出處**:ADR-038 (P-44 pack 擠出、ADR-024 序號守衛的延伸)。

### P-51　主程式模組長期缺 import sys/json 卻沒爆,是因為之前剛好沒走到那行
- **症狀**:新增「自訂策略子行程」功能時,`sys.executable` 報 NameError;
  補 sys 後 `json.dumps` 又報 json 未定義。
- **根因**:stock_app_pro.py 檔頭一直沒 import sys 與 json —— 過去 os._exit
  路徑上實際觸發,所以潛伏很久沒被發現。新功能一用到就爆。
- **正確做法**:(1) 檔頭 import 該檔實際用到的所有標準庫;(2) 每加一個用到
  新標準庫的功能,先確認 import 存在;(3) 收尾的 py_compile 只能抓語法、
  抓不到 NameError,真正跑過 (診斷案例/離線測試走到那行) 才會現形 ——
  這也是為什麼每個新功能都要有實際執行的回歸案例,不能只靠編譯通過。
- **出處**:ADR-040。

### P-52　交叉條件的瞬時性與錯誤重置
- **症狀**: 模擬單或實單符合出場條件（如均線死亡交叉），但系統卻沒有自動出場。介面顯示策略仍為「啟用」，且沒有被自動停用。
- **根因**: `core/strategy_engine.py` 中的 `_close_intent` 函式定義被意外刪除或未正確縮排，導致條件成立時拋出 `NameError`。因為交叉條件（如 `ma_cross_down`）通常只有在一根 K 棒上成立，拋出異常後，下一根 K 棒條件不再成立，系統成功執行其他邏輯並將 `error_count` 重置為 0。這使得「連續 3 次錯誤停用」的安全機制失效，出場訊號被無聲吞掉。
- **正確做法**: 修復 `_close_intent` 的定義。未來若有依賴瞬時條件（只有一根 K 棒成立）的邏輯，必須確保其內部呼叫的函式與變數皆已正確定義，否則單次的異常可能被後續的成功執行掩蓋。
- **出處**: 模擬單未自動出場的 Bug 調查。

### P-53　開放遠端控制時,「二次確認」不等於「知情同意」
- **症狀**:手機收到「確認請回傳 /yes AB12」,使用者照做,結果啟動的是一個
  自己以為已經改掉的實單策略。
- **根因**:確認碼只證明「操作是本人發的」,不證明「本人知道自己在啟動什麼」。
  遠端沒有畫面可以核對標的/模式/數量,而下指令的人常常是憑記憶。
- **正確做法**:(1) 確認訊息必須寫明這一步會讓什麼開始動——標的、週期、
  實單/模擬、數量;總開關則列出實單策略名稱。(2) 前置檢查提前到「發確認碼
  之前」,不要讓使用者確認一個註定失敗的操作;確認後再檢查一次 (中間那 120
  秒狀態可能被改)。(3) 遠端啟用必須與畫面上的「啟用」共用同一份前置檢查
  (`_qt_enable_blockers`),否則等於留了一條繞過驗證的側門。(4) 需要使用者
  在幾種處理方式間做選擇的關卡 (例如持倉核對) 一律拒絕遠端執行,不可用
  預設值代替使用者決定。
- **出處**:ADR-108。

### P-54　安全機制的摩擦要放在「開始冒險」那一側,不是「收手」那一側
- **症狀**:想緊急停止自動交易,卻還要先收確認碼、再打一則 /yes。
- **根因**:把「所有會改變狀態的操作」一視同仁地加上確認,沒有分辨方向。
- **正確做法**:依風險方向分級——停用/急停 (`/off`、`/stop_all`) **不需要**
  確認,啟用 (`/on`、`/start_all`) 才需要。同理,未授權的指令不回覆任何訊息
  (回覆等於告訴對方這個 Bot 後面接著一個真的交易系統),但一定要寫進系統日誌。
- **出處**:ADR-108。

### P-55　同名方法被定義兩次,後面那份靜默蓋掉前面那份 (py_compile 抓不到)
- **症狀**:某個明明寫過的行為不見了 —— 例如成交量副圖 Y 軸不再從 0 起算,
  長條被從底部裁掉一截,所有量看起來都差不多高。程式沒報任何錯。
- **根因**:`stock_app_pro.py` 的 `auto_scale_y` 與
  `auto_scale_indicator_panels` 各被定義了**兩次**。Python 不會警告,後定義
  的直接覆蓋前面那份;而兩份內容不一樣,前一份裡的成交量特例就此失效。
  `py_compile` 只看語法、`diag_crossref` 只看「名字存不存在」,兩者都抓不到。
- **正確做法**:(1) 收尾一定要跑 `python diag_crossref.py`,它現在會掃重複
  定義並在發現時 exit 1;(2) 在超過一萬行的類別裡新增方法前,先
  `grep -n "def 方法名"` 確認沒有同名的;(3) 真的發現重複時,**先逐行比對
  兩份的差異**再決定保留什麼——兩份通常是不同時期寫的,各自有對方沒有的
  處理,直接砍掉一份會第二次弄丟同一個行為。
- **出處**:ADR-109。
### P-56　同一個連線有兩個名字時,它們遲早會不同步
- **症狀**:診斷腳本設了 `app.sj_api = fake`,下單卻送到另一個物件去,委託
  完全沒進到假 API。反過來在 production 就會是「查詢用 A 連線、下單用 B
  連線」這種極難查的錯誤。
- **根因**:ADR-097 把連線搬進 adapter 後,`self.sj_api` 與
  `self.brokers['sinopac'].api` 是兩個名字指向同一個實例。它們同步**只是
  因為兩處賦值剛好都有一起更新**,不是結構上保證的;只要有人只改其中一個
  就會分岔,而且不會有任何錯誤訊息。
- **正確做法**:別名不要用「兩份儲存 + 記得一起更新」,改成 property 讓兩者
  共用同一份儲存 (ADR-110 的做法),把「要小心避免」變成「不可能發生」。
  凡是看到「這兩個變數必須保持一致」的註解,就該想想能不能讓它們根本不是
  兩個變數。
- **出處**:ADR-110。

### P-57　假 tkinter 的 Combobox `current()` 永遠回 0,下拉選單的選擇完全測不到
- **症狀**:診斷案例斷言「策略存到的帳號 = 使用者選的那個」永遠失敗,
  或更糟——永遠通過但其實什麼都沒測到。
- **根因**:`diag_mock_tkinter.py` 的 Combobox 是
  `"current": lambda self, i=None: 0`,設定值被忽略、讀值恆為 0。凡是靠
  `cb.current()` 讀使用者選擇的程式 (策略的模擬帳戶、實單帳戶…),在診斷
  環境下都等於沒被測試。
- **正確做法**:已改成真的記住索引。**更一般的教訓**:假環境的元件若把行為
  簡化成常數,靠它的斷言就是空的;寫 GUI 診斷案例前,先確認你要斷言的那個
  行為在 mock 裡真的有實作,不然會得到一個永遠綠燈的假測試。
- **出處**:ADR-110。

### P-58　「守著併發」的測試,沒拿掉保護跑過一次就不算數
- **症狀**:寫了一個多執行緒測試宣稱能抓到「帳號綁定被插隊」,實際上把鎖
  拿掉之後測試照樣全綠——等於完全沒有保護。
- **根因**:延遲放錯位置。真正的競態窗口是「綁定完成 → 呼叫端讀 api.Order」
  這一段;測試把延遲放在**綁定之前**,那個窗口根本沒被撐開,兩條執行緒
  永遠不會交錯。
- **正確做法**:(1) 任何宣稱守著併發/鎖/順序的測試,**一定要先把保護拿掉跑
  一次,確認它會紅**,再放回去。沒紅過的併發測試要當成沒有。(2) 撐開窗口
  的延遲要放在「共享狀態已被改動、但呼叫端還沒讀到」的那個點,不是放在
  修改之前。(3) 同樣的道理適用於所有「這個測試守著 X」的宣稱——突變測試
  (故意寫錯一行看測試會不會抓到) 是唯一能證明斷言不是空的的方法。
- **出處**:ADR-111 (凱基 adapter 的帳號綁定鎖)。

### P-59　wheel 標示 py3-none-any 不代表它跟 Python 版本無關
- **症狀**:`kgisuperpy` 標示為 `py3-none-any`、METADATA 沒有
  `Requires-Python`,pip 完全不擋,但在 Python 3.14 就是 import 失敗。
- **根因**:兩層。(1) 套件內夾帶預編譯的 C 擴充 (`*.cpython-313-*.so`、
  `*.cp313-win_amd64.pyd`),只涵蓋 cp39~cp313;(2) 更關鍵的是**傳遞依賴**
  ——它載入時會 `import numba`,而 numba 沒有 3.14 版。
- **正確做法**:評估一個第三方 SDK 能不能用,不要只看 wheel 標籤與
  Requires-Python。把 wheel 解開看有沒有 `*.so` / `*.pyd`、看它們的版本標籤,
  再看 `Requires-Dist` 裡有沒有 numba/scipy 這類跟 Python 版本綁很緊的套件。
  另外:import 這種套件時 `except ImportError` 不夠,要 `except Exception`
  ——載入失敗的型態不只一種,不能讓它把整個主程式打掉。
- **出處**:ADR-111。

### P-60　跨行程送出委託後失聯,結果是「不確定」,不是「失敗」
- **症狀**:委託寫進子行程的管線後逾時或管線斷掉,程式把它當成失敗回報,
  策略維持 FLAT,下一根 K 棒又進場一次 —— **重複下單**。反過來當成成功,
  則會記一個可能不存在的部位,之後平倉平到空氣。
- **根因**:把「不知道」硬塞進「成功/失敗」這個二元結果。跨行程 (或跨網路)
  的非冪等操作,天生有第三種狀態。
- **正確做法**:(1) 把操作分成冪等/非冪等,`place_order` 這類非冪等操作失聯
  時拋**獨立的例外型別**,讓呼叫端被迫分開處理,不可能混進
  `except Exception` 當成沒送出;(2) **絕不自動重送,也絕不自動重啟行程後
  再送一次**;(3) 呼叫端收到這個狀態要**停用該策略**並大聲告知——在人確認
  券商端狀態之前,策略的持倉認知已經不可信,繼續跑只會把錯誤放大;
  (4) 訊息要明講「無法確認、不會自動重送、請去券商端確認」。
  另外:回應一定要比對請求 id,否則上一個逾時請求的遲到回應,會被當成
  下一個請求的答案——在下單這條路上等於拿 A 單的結果判斷 B 單。
- **出處**:ADR-112。

### P-61　子行程的 stdout 若同時被第三方套件拿去 print,協定會當場錯位
- **症狀**:IPC 用 line-delimited JSON,平常都好好的,一登入就整個對不上。
- **根因**:`kgisuperpy` 內部會直接 `print` (例如 `print('輸入賬號錯誤')`)。
  那些字混進 stdout,主程式讀到一行不是 JSON 的東西,之後每個回應都錯位。
- **正確做法**:worker 一啟動就把 `sys.stdout` 換成 `sys.stderr`,真正的
  stdout 只留給協定 (`out = sys.stdout; sys.stdout = sys.stderr`)。
  並補一個測試斷言「stdout 的行數 == 請求數」。同理:壞行要拋例外不要安靜
  略過 (否則主程式會永遠等一個不會來的回應);子行程的三條管線在收行程時
  都要 close (長時間執行會累積 fd,實測 ResourceWarning 抓到)。
- **出處**:ADR-112。

### P-62　升級 SDK 時,「版本號」不能拿來當「行為」的代理
- **症狀**:寫 `if sj.__version__ >= '1.7': 用新寫法`,結果某個版本部分支援、
  或官方指南與實作不一致,判斷就整個歪掉。
- **根因**:版本號是宣告,不是事實。shioaji 1.7 的升級指南就聲稱
  `fetch_contracts()`、`Contracts.status` 已移除、`SecurityType.Future` 更名為
  `Futures`,但實際讀 1.7.0 的型別定義,**這三者都還在原樣**。照指南寫版本
  分支就會錯。
- **正確做法**:(1) 參數支援與否 → 直接問簽名 (`inspect.signature`),不要問
  版本號 (`core/sj_compat.supported_kwargs`);(2) 資料形狀變更 → 用「候選清單
  依序嘗試」,哪個拿得到就用哪個 (`resolve_index`),過渡期兩種代碼並存也能
  自動處理;(3) 升級前先把 wheel 抓下來讀 `.pyi`/原始碼,不要只信升級指南
  (同 P-59 的教訓:官方文件與套件實際內容經常不同步)。
- **出處**:ADR-114。

### P-63　合約列舉回傳的型別可能跟「用代碼查」拿到的不是同一種
- **症狀**:升級 shioaji 1.7 後,`Contracts.Stocks.get('2330')` 正常,但
  `next(c for c in Contracts.Stocks if c.symbol == '2330')` 全部比不中,
  使用者看到「找不到合約資訊」。
- **根因**:1.7 新增了輕量型別 `StockInfo`/`IndexInfo`/`FuturesInfo` (只有
  security_type/region/exchange/code/target_code/base)。完整的 `Contract`
  仍然有 `symbol`,但**列舉容器時拿到的是輕量型別**,沒有 `symbol` 這個屬性。
- **正確做法**:比對代碼時 `symbol` 與 `code` **兩個都比**
  (`sj_compat.match_contract_code`);取顯示用代號時 symbol 取不到就退回 code
  (`contract_symbol`)。更一般的教訓:同一個容器的「查詢」與「列舉」不保證
  回傳同一種型別,凡是遍歷 SDK 容器再讀屬性的地方都要用 getattr 防守。
- **出處**:ADR-114。

### P-64　純函式測試守不住「只有真連線才走得到」的那幾行
- **症狀**:SDK 升級後補了相容函式,也寫了測試,結果實機一跑還是壞在同一類
  問題上 —— 因為真正出錯的那一行在 GUI 深處,測試根本沒經過它。
- **根因**:`sj_compat.contract_symbol()` 有完整單元測試,但主圖的期貨查詢
  那兩行**仍然裸讀 `contract.symbol`**,壓根沒呼叫到那個相容函式。測試全綠,
  bug 還在。突變測試也抓不到 —— 改壞相容函式時,那兩行本來就沒在用它。
- **正確做法**:對「無法在離線環境走到」的路徑,補**原始碼層級的斷言**
  (例如「主程式裡不可再出現裸讀 `.symbol`」)。這是本專案既有的手法
  (診斷腳本裡already有 `assert 'def xxx' in src` 這類檢查),用在 SDK 升級
  這種「全檔案掃一遍改乾淨」的情境特別有效。補完一樣要跑突變測試確認它會紅。
- **出處**:ADR-114 追記。

### P-65　診斷腳本會覆寫使用者的真實策略與模擬帳戶檔
- **症狀**:每次跑 `diag_repro_issues.py`,`quant_strategies.json`、
  `quant_state.json`、`paper_account.json` 就出現在 `git status` 的修改清單裡。
- **根因**:診斷案例會建策略、記模擬成交,而 `_qt_save()` / `_qt_save_paper()`
  寫的就是使用者**真正在用**的那三個檔案 (`app_path(...)` 絕對路徑)。跑一次
  診斷 = 策略清單與模擬帳戶績效被測試資料覆蓋。在 repo 裡靠「跑完記得
  git checkout 還原」擋著,但使用者在自己機器上跑診斷時沒有這層保護,
  忘記還原就直接進版控。
- **正確做法**:在**建立 App 實例之前**把 `QT_STRATEGY_FILE` /
  `QT_STATE_FILE` / `QT_PAPER_FILE` 這三個**類別屬性**改指到暫存目錄
  (並把真實檔案內容複製過去,免得「載入得到既有策略」的案例前提不成立),
  結束時整個暫存目錄刪掉。改類別屬性的好處是所有實例自動吃到,不必逐一
  修改診斷案例。更一般的原則:**任何會被自動化重複執行的腳本,都不該寫入
  使用者的真實資料路徑**——個別案例自己 patch `_qt_save` 是治標,漏一個就破功。
- **出處**:ADR-115。

### P-66　從幾個樣本看出來的「命名規律」,不等於那真的是規則
- **症狀**:shioaji 1.7 的升級指南只列出加權指數 `001` → `IX0001`。我從這
  一個樣本推出「新代碼 = IX + 舊代碼補零到 4 位」,據此判斷櫃買 (舊代碼 101)
  應為 `IX0101`,還寫進候選清單的第一順位。實際上櫃買是 **`IX0043`** ——
  IX 後面那四位是**指數自己的編號**,跟舊代碼沒有任何對應關係。
- **根因**:一個樣本 (001 → IX0001) 同時符合「補零規律」與「另有編號」兩種
  解釋,而我只看到前者就當成規則。樣本數 1 推不出規律。
- **正確做法**:(1) 只有一兩個樣本時,把推論**標明是推論**並放進候選清單的
  後段,不要當第一順位;(2) 真正的解法是去要一份完整對照表 (本案是使用者
  提供的台灣指數代碼表),或實機列舉一次 `list(api.Contracts.Indexs.OTC)`;
  (3) 這也是為什麼相容層要用「候選依序嘗試」而不是「算出唯一答案」——
  推論錯了只是多試一次,不會整個功能失效。這個設計這次確實救了場。
- **出處**:ADR-114 追記二。

### P-67　把面板複製成兩份,會讓 self.* 只指向其中一份,另一份變成孤兒
- **症狀**:同一個面板同時存在於分頁與獨立視窗,其中一邊的表格再也不會更新。
  畫面看起來完全正常,只是資料永遠停在舊的 —— 沒有任何錯誤訊息。
- **根因**:面板的建構函式把 widget 直接掛在 `self.*`(單一實例設計)。建第二
  份時 `self.tree_xxx` 被覆寫成新那份,舊那份就沒有任何東西指向它,所有
  refresh 都只更新新的。
- **正確做法**:除非面板本來就設計成多實例 (像量化面板那樣維護一份 `_qt_uis`
  清單、刷新時走訪所有存活 UI),否則**用「搬家」而不是「複製」**:開視窗時
  拆掉分頁的內容並在視窗重建,關視窗時再搬回去。永遠只有一份,`self.*` 永遠
  指向它。代價是重建會清空畫面,所以要能重播內容 —— 而「記住畫面上是什麼」
  要寫在**渲染函式本身**,不要寫在呼叫端 (寫在呼叫端時,任何一條沒記到的
  路徑都會讓內容在搬家後消失,而且不會報錯)。
- **出處**:ADR-116。

### P-68　「已經開著就帶到最前面」的 try 包太大,會開出第二個視窗
- **症狀**:重複點「開啟完整視窗」時開出第二個視窗,兩份面板互相搶
  `self.*`(接著就是 P-67)。
- **根因**:
  `try: if win.winfo_exists(): win.deiconify(); win.lift(); ...; return win
   except: pass` —— `deiconify`/`lift`/`focus_force` 任何一個拋例外,都會掉進
  下面的「新建」分支。
- **正確做法**:把「還活著嗎」與「帶到最前面」分開包 try;前者決定要不要
  return,後者失敗就算了 (帶不到最前面只是小瑕疵,開出第二個視窗是真的壞)。
  另外這個 bug 是診斷環境先發現的 —— 假 widget 沒有 `deiconify`,一呼叫就
  AttributeError。mock 缺方法時要補進 mock,不要在 production 加 try 繞過。
- **出處**:ADR-116。
### P-69　`str(v or '')` 在 pandas 資料上是錯的:NaN 是 truthy
- **症狀**:選股結果的名稱欄顯示字面上的 `nan`。
- **根因**:`str(row.get('Name') or '')` —— **NaN 在 Python 是 truthy**,
  所以 `nan or ''` 得到的是 nan,`str()` 之後就是 `'nan'` 三個字。同一段
  程式碼裡的 `industry` 有寫 `pd.isna()` 防護所以正常,`name` 沒寫就中招 ——
  兩個欄位處理方式不一致,正是「同一件事寫兩份」的典型後果。
- **正確做法**:凡是「值可能來自 DataFrame」的地方都用統一的
  `_text()` 之類的 helper (先 `pd.isna()` 再轉字串,順便擋掉字串 `'nan'`),
  不要用 `or`。合併全市場資料時只要某檔在某一份來源缺漏就會產生 NaN,
  這不是罕見情況而是常態。
- **出處**:ADR-117。

### P-70　同一個 Treeview 有兩種欄位配置時,只有一邊記得重設 = 資料與表頭錯位
- **症狀**:跑過「回測條件」之後再按「開始選股」,資料是選股的、表頭卻還是
  回測的 (期別/訊號日/買進日…),12 個值被塞進 7 個欄位,完全對不起來。
- **根因**:選股的欄位寫在**建構函式**裡 (只執行一次),回測的欄位寫在
  `_sc_show_backtest()` 裡 (每次都重設)。於是切換方向只有單向有效 ——
  選股 → 回測會正確切換,回測 → 選股不會切回來。
- **正確做法**:把所有欄位配置集中成一份資料 (`SC_COLUMNS`),提供
  `_sc_apply_columns(mode)`,並讓**每一個顯示函式在畫資料前都先呼叫它**。
  「畫資料前先設好自己的欄位」變成兩邊都必須做的同一件事,不可能只有一邊
  記得。斷言時除了比對表頭,也要比對「資料列的欄位數 == 表頭欄位數」——
  那是錯位的直接證據。
- **出處**:ADR-117。

### P-71　用 `type(obj).__name__` 判斷 SDK 物件的種類,SDK 改版就失效
- **症狀**:實單帳戶下拉出現三個一模一樣的選項,使用者無從選起。
- **根因**:`type(a).__name__.replace('Account','')` —— 1.5.6 用不同類別
  (StockAccount/FutureAccount) 區分帳戶,**1.7 統一成同一個 `Account` 類別**,
  改用 `account_type` 欄位。於是三個帳戶的種類都算出空字串,顯示文字完全相同。
- **正確做法**:(1) 優先讀**資料欄位**而不是類別名稱 (`account_type`),
  類別名稱只當退路;(2) 凡是給使用者選擇的清單,顯示文字**一定要包含唯一
  識別碼** (這裡是帳號) —— 種類與戶名都可能重複,只有帳號不會,帶上它就算
  種類判斷失敗也還分得出來;(3) 順便把「不能用」的狀態標出來 (未簽署的帳戶
  送單會被券商退,先看到比較好)。
- **出處**:ADR-117。

### P-72　官方資料的原始單位不是使用者講的單位,直接顯示等於要人心算
- **症狀**:籌碼表格顯示 `102,513,033`、`545,534,811` 這種一長串數字。看得懂,
  但要在腦中除以 1000 或 1 億才知道是多少,而且欄位之間單位還不一致。
- **根因**:官方原始單位是「股」「元」「仟元」,台灣投資人講的是「張」「億」。
- **正確做法**:換算集中在一個純函式模組 (`core/unit_format.py`),**表頭一定
  要標單位**(`外資(張)`、`三大法人合計(億)`)。三個注意事項:(1) 這類數字錯了
  不會報錯,只會讓使用者據此下判斷,所以一定要有測試 + 突變測試;(2)「沒有
  資料」顯示 `--` 不要顯示 0 —— 兩者在籌碼上意義完全不同;(3) 增減類欄位要
  帶正負號,那既是方向也是紅漲綠跌的依據 (鐵則1)。
- **出處**:ADR-118。

### P-73　自己算「增減」時,先確認序列是由舊到新排好的
- **症狀**:融資增減的正負號跟實際相反 —— 使用者看到「融資減少」其實是增加。
- **根因**:畫面是「日期新到舊」排序的,直接拿那個順序做相鄰相減,得到的是
  「前一日 − 今日」,方向剛好相反。而且**不會有任何錯誤訊息**。
- **正確做法**:算差之前明確 `sort_values('Date', ascending=True)`,算完再轉回
  顯示用的排序。測試要直接斷言方向 (`diff([500, 600])[1] == 100`),並用突變
  測試把 `cur - prev` 改成 `prev - cur` 確認會紅。第一期沒有前值時回 None
  (顯示 `--`),不要當成 0。
- **出處**:ADR-118。

### P-74　「保底強制結束」的看門狗會把診斷腳本一起殺掉
- **症狀**:診斷腳本輸出**整個空白**、結束碼卻是 **0** —— 看起來像「沒有測試」
  而不是「測試失敗」,非常容易誤判成環境問題。
- **根因**:為了解決「按 X 沒反應」加了一個 daemon 看門狗:`on_app_close()`
  一被呼叫就在 N 秒後 `os._exit(0)`。而診斷腳本本來就會呼叫 `on_app_close()`
  驗證關閉流程,但它自己還要跑完其餘案例 —— 時間到就被連帶結束。
- **正確做法**:凡是「無條件強制結束行程」的機制,都要留一個明確的測試開關
  (本案是 `CLOSE_FORCE_EXIT`,production 永遠 True)。更一般的原則:**會終止
  行程的程式碼,不能假設自己所在的行程只做一件事**。另外,診斷腳本結束碼是 0
  但沒有輸出時要當成異常 —— 那通常代表行程被外力結束,不是全部通過。
- **出處**:ADR-119。

### P-75　背景執行緒逐筆做 read-modify-write,量級一大就把 UI 餓死
- **症狀**:籌碼下載跑一陣子後,整個視窗沒反應,按 X 也沒用,只能去工作管理員砍。
- **根因**:下載迴圈**每一天**都對月檔做一次「整檔讀入 → concat → 去重 →
  寫回」。個股月檔數 MB,10 年 = 2364 次整檔重寫。pandas 這些運算是**握著
  GIL** 做的,主執行緒的 tkinter 事件迴圈拿不到 GIL,點擊根本沒被處理
  (P-48 的同一個根因,只是量級大得多)。
- **正確做法**:(1) **批次寫檔** —— 累積 N 筆才落地一次,寫檔次數降一個數量級;
  (2) 但**不要全部堆到最後**才寫,中途停止/關程式會讓整批白抓,中止路徑上也要
  先沖一次;(3) 另外加「保底強制結束」的看門狗當第二道防線 (見 P-74);
  (4) 評估這類迴圈時要算的是「**單次成本 × 次數**」,而不是只看單次很快。
- **出處**:ADR-119。

### P-87　把「每次重畫都會跑一次」的判斷直接接上通知,等於用縮放頻率洗版
- **症狀**:主圖上勾了盤勢判斷後,只要滑鼠拖曳/滾輪縮放,系統日誌就一直冒出
  同一則「偵測到 M頭」。畫面上**沒有任何錯誤訊息**,只是日誌變成噪音,真正
  重要的委託回報被洗掉。
- **根因**:型態偵測掛在 `draw_chart()` 裡,而 `draw_chart()` 是每次縮放/
  平移/切指標都會重跑的。偵測函式本身沒錯 —— 它每次都誠實回報「以最後一根
  為今天,現在確實有這個型態」;錯的是呼叫端把「**狀態**」當成「**事件**」用。
  同一個坑的另一半:原本掛在策略裡時,每天只評估一次,所以這個問題不存在,
  搬到主圖才浮現 —— **功能搬家會改變呼叫頻率,原本夠用的去重就不夠用了**。
- **正確做法**:(1) 呼叫端要記「上次通知到哪裡」的狀態,並且**分兩種**:
  一次性事件用「同一根 K 棒內只通知一次」,持續狀態 (盤勢分類/接近邊界/
  三角形楔形) 用「內容跟上次通知的不一樣才通知」;(2) 這段去重規則一定要
  抽成**純函式** (本案 `core/regime_panel.plan_notifications`) —— 在 GUI 裡
  幾乎測不到,抽出來才能寫「連呼叫 5 次只該通知 1 次」這種斷言;(3) 診斷
  案例要真的連續 `draw_chart()` 好幾次再斷言日誌沒有增加。
- **出處**:ADR-120。

### P-88　設定檔正規化時,「欄位不存在」跟「欄位存在但是空的」是兩種不同的意思
- **症狀**:使用者在設定裡把型態清單**全部取消勾選**、存檔、重開程式,結果
  又全部被勾回來 —— 設定「存不住」。
- **根因**:正規化寫成 `out['list'] = src.get('list') or DEFAULT`。空 list 是
  falsy,於是「使用者刻意選擇什麼都不要」跟「舊設定檔沒有這個欄位」被折成
  同一種情況,一律套用預設值。(同一類的還有 `or` 把 0、空字串、False 一併
  吃掉 —— 跟 P-69 `str(v or '')` 把 NaN 變成 `'nan'` 是同一族的錯。)
- **正確做法**:用 `if key in src` 判斷「有沒有這個欄位」,再另外判斷值本身;
  預設值只在**欄位缺席**時套用。測試要把兩種情況分開斷言
  (`normalize({'list': []})['list'] == []` 且 `normalize({})['list']` 非空)。
- **出處**:ADR-120。

### P-89　排查「偶發的外部失敗」時,時間戳比程式碼更早給出答案
- **症狀**:量化策略偶爾噴
  `ShioajiTimeoutError: Timeout Topic: api/v1/data/kbars ... 30000ms`,
  查了兩輪程式碼都沒查對。
- **根因**:前兩輪都在讀程式碼找「什麼東西比較大」(先懷疑 `QT_TF_DAYS` 的
  日K 300 天、再懷疑看A做B 的第二次抓取吃到做B 的長週期),兩個推論都合理、
  都被事實推翻 —— 因為那是**推論**。第三輪只問了一句「這則訊息什麼時候出現
  的」,答案是 **08:45 與 09:00** —— 正好等於 `FUT_DAY_OPEN_MIN` 與
  `STOCK_OPEN_MIN`,也就是時段閘門打開的那一秒,一秒定位。
- **正確做法**:面對**偶發**的外部失敗,先問三個事實再讀程式碼:
  (1) **什麼時候**發生 (時間戳能直接對上程式裡的常數/排程);
  (2) 頻率 (每次都失敗 = 結構問題;偶發 = 環境或時機問題);
  (3) 當下在做什麼。錯誤訊息裡的「第 N 次」也是事實 —— `error_count` 有成功
  就歸零,所以「第 1 次」本身就證明了這是偶發而非結構性失敗。
- **出處**:ADR-121。

### P-90　在單執行緒的迴圈裡用 sleep 做退避重試,會把同一條迴圈上的即時保護一起拖住
- **症狀**:想幫量化策略的抓資料加重試,最直覺的做法是沿用主圖那套
  `time.sleep(1.5) → sleep(3)` 退避。
- **根因**:主圖的分段下載跑在自己的背景執行緒上,睡多久都只影響它自己;
  量化 runner 是**單執行緒**,同一條迴圈上還有 `_qt_update_realtime_pnl`
  (每 3 秒,期貨即時停損停利靠它) 與其他策略的評估。加上重試後最壞情況
  (30s 逾時 + 1.5s + 30s + 3s + 30s) 會阻塞近 95 秒 —— **等於拿一個錯誤
  訊息換掉即時停損**,比原本的問題更危險。
- **正確做法**:在已經有固定輪詢節奏的迴圈裡,**用迴圈自己的節奏當退避**:
  失敗時把「這一輪已處理」的標記 (本案是 `_qt_last_boundary`) 還原,下一個
  tick 自然重試,零額外阻塞。務必同時做兩件事:(1) 設重試上限;(2) 上限到了
  要把標記**寫回去**停止重試 —— 否則會變成每個 tick 都重打外部 API。
  搬用別處「已驗證過」的做法之前,先確認兩邊的執行緒模型一樣。
- **出處**:ADR-121。

### P-91　「過了分段門檻」不等於「真的會被切成兩段」
- **症狀**:把策略路徑的大範圍請求改走背景預抓之後,**使用者每天在跑的
  5分K 策略也被改了路徑** —— 而規格明明寫著「小請求行為零改變」。
- **根因**:門檻與段長是兩個不同的數字。分K 類的門檻是 5 天、段長是 10 天,
  所以 6~10 天全都是「過了門檻,但只切得出一段」。而 5分K 的實際取數天數
  正好是 7 天。判斷式只寫了 `days > threshold`,沒有再問一句「切出來幾段」。
- **正確做法**:分段的判斷要以**結果**(切出來幾段)為準,不是以門檻為準:
  `segments = ceil(days / chunk_days)`,`segments <= 1` 就回「不需分段」。
  更一般的原則:**當一個決策有兩個參數時,不要只用其中一個去下結論** ——
  尤其當另一個參數會讓結論失效的時候。測試要直接針對「門檻與段長之間」
  那段區間逐一斷言(本案是 6~10 天全部都該回「不需分段」)。
- **出處**:ADR-122。

### P-92　共用的預設值會悄悄套到「自己有一套邏輯」的子類型上
- **症狀**:終極波段策略的 5 口 TXF 被「即時停損出場 (損益 -2.44% ≤ -2.0%)」
  砍掉,模擬帳戶吃了 -968,250。而那個 2.0% **使用者從頭到尾沒看過也沒設定過**
  —— 該策略的專屬編輯器根本沒有這個欄位。
- **根因**:兩件事疊在一起。(1) `chukuangren_band.default_strategy()` 是疊在
  `strategy_engine.new_strategy()` 上面做的,後者的 `stop_loss_pct` 預設 2.0
  就被一起帶進來,而 `default_strategy()` 沒有清掉;(2) 後來新增的「期貨即時
  停損」通道 (`_qt_check_realtime_futures_stops`) 只過濾「有沒有啟用」與
  「是不是期貨」,**完全不看 `kind`**。結果一個自帶完整出場邏輯的策略,
  風控被一個它設計裡不存在的規則整個換掉。
- **正確做法**:(1) 子類型用共用預設值當底時,**要明確列出哪些欄位對它沒有
  意義並歸零**,不要假設「沒人設定就不會生效」;(2) 更重要的是把「這條規則
  對這個種類不適用」做成**核心層的守門** (本案 `strategy_engine.OWN_EXIT_KINDS`
  + `has_own_exit_logic()`),而不是在每個呼叫端各自 if —— 呼叫端遲早會漏掉
  一個 (本案就是新增即時停損通道時漏的);(3) 守門一定要配一條**反向對照**
  測試 (一般策略在同樣條件下**仍然要**觸發),否則把守門寫成「全部都擋」
  也會一片綠,而那等於把整個功能關掉。
- **出處**:ADR-123。

### P-93　保護機制別掛在「使用者可關閉的開關」底下
- **症狀**:ADR-121 加的「開盤暖機」對某些策略完全沒作用,15:00 夜盤開盤
  照樣噴 kbars 逾時。
- **根因**:暖機檢查被巢狀寫在 `if s.get('session_gate', True):` 裡面,
  所以只要使用者把「交易時段閘門」關掉,暖機就一起消失。但這兩件事的目的
  完全不同:時段閘門是**使用者的偏好**(要不要在非交易時間動作),暖機是
  **保護券商 API**(別在開盤鐘響那一秒去打)。更糟的是方向剛好相反 ——
  關掉閘門的策略是 24 小時都在評估的,**撞上開盤瞬間的機率反而更高**,
  卻剛好是唯一沒有保護的那一群。
- **正確做法**:寫巢狀條件之前先問「這兩個判斷是同一件事嗎」。**保護性、
  防呆性的檢查要跟使用者偏好平行,不要巢狀在偏好底下。** 測試要特地把
  偏好開關關掉,再斷言保護仍然生效。
- **出處**:ADR-123。

### P-94　測試依賴真實時鐘,就會產生一個「查不到原因」的偶發紅
- **症狀**:同一份程式碼,分支上連跑兩次全過,合併到 main 後第一次就紅了
  一個**跟這次改動完全無關**的舊案例。再跑五次又全過。
- **根因**:`_quant_eval_pass()` 對分K策略有一道「給資料源 2 秒緩衝」的閘門
  (`(now_dt - boundary).total_seconds() < 2` 就 continue)。1分K 的 boundary
  就是當下這一分鐘,所以診斷只要剛好在某一分鐘的前 2 秒跑到那一行,策略
  完全不會被評估 —— 機率約 2/60 ≈ 3.3%,而且每次紅的案例不一定一樣。
- **正確做法**:
  1. **看到偶發紅,先問「這次紅的案例跟我剛改的東西有關嗎」**。無關就八成
     是環境/時序,不要急著回頭改剛寫好的程式(那會愈改愈亂)。
  2. 診斷若宣稱「與時鐘無關」(ADR-099 為此關掉 `session_gate`),就要把
     **所有**時鐘相依的窗口都堵住,堵一個不算數。本案的修法是包一層
     `eval_pass()`,秒數 < 2 就先等過去。
  3. 要改的是**測試對時鐘的依賴**,不是產品那個 2 秒緩衝 —— 它在 production
     是對的(避免抓到還沒生出來的最後一根)。
  4. **「跑很多次都沒紅」不是證明**,只是沒有反證。真正的證據是把觸發條件
     算出來(哪些秒數必定被跳過),或做成可控的確定性測試。
- **出處**:ADR-123 追記。

### P-95　新增的「獨立監控通道」很容易漏掉主路徑上早就有的閘門
- **症狀**:使用者的終極波段空單在 **15:00:01(夜盤開盤第一秒)**被平倉,
  而該策略「不會在夜盤做任何動作」。
- **根因**:`_qt_check_realtime_futures_stops()` 是後來為了補「盤中帳面已經
  虧超過停損點卻沒動作」而加的**獨立監控通道**(掛在每 3 秒的即時損益更新
  底下)。主路徑 `_quant_eval_pass()` 有完整的交易時段閘門,但這條新通道
  **一個都沒有** —— 沒有 `is_market_open`、沒有 `session_gate`、
  沒有 `futures_session`。日盤 13:45 收盤後價格不動,夜盤第一筆報價一進來就
  被拿去比停損。三條會下單的路徑裡,只有它沒有閘門。
- **正確做法**:(1) 新增「繞過主路徑的快速通道」時,把主路徑上的**每一道
  前置條件**逐條抄過來檢查一遍 —— 快速通道通常只複製了「怎麼做」,忘了
  「什麼時候可以做」;(2) 更好的是把那些判斷抽成**共用函式**(本案
  `strategy_engine.include_night_of()`),讓兩條路不可能各自為政;
  (3) 盤點時用「**哪些程式碼會真的送出委託**」當清單(grep 下單函式的呼叫
  點),而不是憑印象 —— 本案就是這樣才找到只有一條沒有閘門。
- **出處**:ADR-124。

### P-96　「危險操作」的守門條件常常只寫了想得到的那一個
- **症狀**:一檔**狀態=啟用、運轉狀態=運轉中**的策略被 🗑 刪除 直接刪掉。
- **根因**:`_qt_delete_strategy()` 只擋「仍有持倉」(`state in LONG/SHORT`),
  **完全沒有看 `enabled`**。寫的時候想到的是「有部位不能刪」,沒想到「正在跑
  也不能刪」—— 而截圖裡持倉正好是 FLAT,唯一那道檢查於是放行。
  對照組:同一個畫面的**編輯**早就擋住了(`readonly = bool(s.get('enabled'))`),
  兩個入口的規則各寫各的,於是一個對、一個漏。
- **正確做法**:(1) 危險操作(刪除/覆寫/停用)的前置條件要**列成清單逐條問**:
  「正在跑嗎?有部位嗎?有人正在編輯嗎?」而不是想到一個寫一個;
  (2) 同一個對象的**所有修改入口共用一個守門函式**(本案
  `strategy_engine.can_delete()` / `is_locked()`),不要每個入口各判各的 ——
  這是 P-95 的同一個教訓在另一個場景;(3) 守門一定要配**反向對照**測試
  (正常情況下真的刪得掉),否則把守門寫成「永遠擋」也會一片綠,而那是把功能
  鎖死,比原本的問題更難發現。
- **出處**:ADR-125。

### P-97　「跑很多次都沒紅」證明不了什麼 —— 尤其當紅不紅取決於現在幾點
- **症狀**:ADR-124 給即時停損補上交易時段閘門後,**連跑 5 次診斷全綠**就併進
  main。隔了幾小時再跑,一個跟那次改動無關的舊案例(ADR-123)紅了。
- **根因**:那個案例要求「一般期貨策略應被即時停損平倉」,而閘門加上去之後
  它就**依賴真實時鐘**了:期貨日盤 08:45~13:45 或夜盤 15:00~05:00 跑就過,
  卡在 **13:45~15:00 的空檔**跑就紅。我那 5 次剛好全都落在日盤內。
- **正確做法**:
  1. **加了閘門/前置條件之後,回頭看既有測試有沒有因此變成時鐘相依** ——
     新條件會悄悄改變舊案例的前提,而舊案例不會抱怨。
  2. 讓**測試框架**統一把時鐘相依的表面凍結住(本案 `run_case` 預設把
     `is_market_open` 固定成「開著」、`just_opened` 固定成 False),需要別的
     值的案例自己在案例內 patch。這樣新案例不可能「不小心」依賴真實時鐘。
  3. 驗證這類修正時,**刻意挑會露餡的時段跑**(本案是 13:45~15:00),
     不要只在方便的時候跑。
  4. 這是 P-94 的同一個教訓再犯一次 —— 寫下來不等於做到,**檢查清單要落到
     流程裡**(合併前在 main 重跑 + 挑對時段),不能只靠記得。
- **出處**:ADR-126。

### P-98　用「牆上時鐘 TTL」近似「資料會不會變」,近似錯了就是整天白做工
- **症狀**:使用者 Telegram 收到同一則
  「【自動交易-資料】^TWII 日K 歷史資料分 4 段在背景補齊中」——
  **09:20 與 09:55 各一次**。查下去不只是多兩則訊息:那是**整天每 30 分鐘
  重新下載一次 300 天的 ^TWII 分K 資料** (每次 4 段),一天約 11~13 回,
  而每一回抓回來的東西**保證跟上一回一模一樣**。
- **根因**:ADR-122 已經正確論證出「日K 類策略盤中重抓沒有意義」
  (`_qt_fetch_closed_bars` 會丟掉最後一根未完成的,所以『已收盤』的集合
  只在**新的一盤開始**時才會多一根),但實作時把它**近似成 30 分鐘 TTL**。
  TTL 回答的是「快取放多久了」,而真正要問的是「**造成資料改變的事件發生了
  沒有**」。兩者只要對不上,就會出現「明明資料不會變、卻每 30 分鐘重抓一次」。
  放大它的還有第二層:終極波段的看A 是 5分K → **每 5 分鐘評估一次**,
  但抓的是**日K** —— 所以 TTL 一過期,下一個 5 分鐘評估點立刻重抓。
  09:20 → 09:55 相隔 35 分鐘,正是「30 分鐘 TTL + 下一個 5 分鐘評估點」。
- **正確做法**:
  1. 快取新鮮度**照著「什麼事件會讓資料改變」去寫**,不要拿時間近似。
     本案是 `market_session.any_session_opens_between(t0, now)` ——
     期間內沒有跨過任何開盤,資料就一定沒變。
  2. 這種「事件式」判斷要**往過期的方向保守**:開盤時刻取所有商品的**聯集**、
     不看國定假日、參數異常/時鐘倒退/跨太久一律當過期。多抓一次無害,
     誤判新鮮會拿舊資料去下單。
  3. 仍然留一道**絕對上限**當保險 (本案 24 小時):週末沒有開盤,單靠事件
     規則可以讓記憶體裡的資料活過整個週末,那不划算。
  4. **不要對所有週期一視同仁**:分K 的資料每根K棒真的會變,套上事件式判斷
     會漏訊號 —— 分K 維持短 TTL,診斷要有一條守著這個區別。
- **出處**:ADR-127。

### P-99　「請稍候」類的進度訊息掛在會推播的前綴上,重試一次就變成噪音轟炸
- **症狀**:同上 —— 使用者手機一天被同一則「歷史資料在背景補齊中」敲十幾次。
- **根因**:那則訊息的前綴 `【自動交易-資料】` 會被
  `telegram_notify.is_quant_message()` 判定為要推播。訊息本身沒有錯,錯在
  它掛在一條**會被反覆走到**的路徑上 (每次快取過期都重新起一次預抓),
  而作者當初預期它「一天只會出現一次」。
- **正確做法**:
  1. 分清楚訊息的性質:**狀態變化**(值得每次通知)還是**進度/提示**
     (只有第一次有價值)。後者一律加去重,鍵值取「能代表同一件事」的最小
     組合 —— 本案是 `(cache_key, 日期)`。
  2. 去重要跟「**該不該重做那件事**」分開處理。本案修完之後,重抓仍然可能
     合法發生 (真的跨過開盤了),但**通知每天只有一次** —— 診斷刻意有一條
     「強制重抓、但不可以再推播」的斷言守著這個分離。
  3. 加任何新日誌之前,先看它的前綴會不會被推播判定抓到,以及**這一行一天
     會被走到幾次**。
- **出處**:ADR-127。

### P-100　把「內部節拍器」塞進一個語意完全不同的設定欄位,遲早會外洩給使用者
- **症狀**:使用者看著自己設定的「日K」策略,卻在系統訊息裡看到 5分K 的痕跡,
  反問「我明明設定就是看日K,為什麼哪裡有看5分K?」。查下去發現:
  終極波段的 `watch_timeframe`(語意是「A 的訊號週期」)被寫死成 `'5分K'`,
  真正的用途卻是**驅動評估迴圈的 K 棒邊界節拍**,好讓迴圈踩得進
  12:00~12:04 的二次確認窗口。訊號本身一直是日K。
- **根因**:需要一個「多久醒一次」的參數,而手邊剛好有一個會被邊界閘門讀到的
  欄位,就借用了。註解甚至老實寫著「驅動 _quant_eval_pass 每5分鐘評估一次」——
  作者知道自己在借,但沒有意識到那個欄位是**對外語意**的一部分。
- **後果不只是「命名不好看」**,四項全部發生過:
  1. 使用者在 UI 上看不到也改不了(那個策略的編輯器沒有這個欄位),
     但值會外洩到日誌/推播上 → 使用者無法自己解釋系統行為。
  2. 節拍被拉快 6 倍(10 分鐘 → 5 分鐘),整套評估跟著重跑,包含重抓歷史
     資料 —— 那是 P-98 那個「一天推播十幾次」的**上游成因**。
  3. 任何**照語意**使用那個欄位的新程式碼都會拿到錯的答案(本例:拿 5分K
     去判斷日K 策略的訊號)。這是埋著的真 bug,只是還沒有人踩到。
  4. 借用會**連帶綁住行為**:確認邏輯因此巢狀在邊界閘門底下,而邊界一天
     只放行一次 → 當天的二次確認**只有一次機會**,失敗還完全無聲(見 P-101)。
- **正確做法**:
  1. **需要新語意就給新名字**。節拍是節拍、訊號週期是訊號週期,即使兩者
     現在剛好可以共用一個數字。
  2. 若某個 kind 的欄位值是**結構上固定**的,就在 core 的 getter 裡覆寫存檔值
     (本案 `DAY_SIGNAL_KINDS`),既有資料不必遷移就正確 —— 而且讓
     「介面回答真話」與「行為正確」變成同一件事。
  3. 覆寫類的清單**不要為了看起來一樣就合併**。本專案已有三份內容相同的清單
     (`OWN_EXIT_KINDS` / `DAY_SESSION_ONLY_KINDS` / `DAY_SIGNAL_KINDS`),
     語意是三件事,合併過的寫法日後會一起改錯。有測試守著這個區分。
  4. 檢查方法:**問「使用者看得到這個值嗎?他能改嗎?他會怎麼解讀?」**
     只要答案是「看得到但改不了」,就是這個坑。
- **出處**:ADR-128。

### P-101　「一天只有一次機會」的動作,不可以掛在只放行一次的閘門底下
- **症狀**:終極波段的「隔天中午 12:00 二次確認」若在那一刻拿不到 5分K 資料,
  **當天的確認就整個丟掉,而且沒有任何日誌**。而這檔策略的進出場**完全靠**
  這個確認 —— 等於當天該進的沒進、該出的沒出。
- **根因**:確認巢狀在 K 棒邊界閘門裡,邊界在 12:00 觸發一次後就被
  `_qt_last_boundary` 記住,下一個邊界是 12:05 —— 已經在 `minute < 5` 的窗口外。
  而失敗路徑寫成 `if df is not None and not df.empty:` 的**靜默略過**,
  沒有 else、沒有日誌。兩件事疊起來:唯一的機會 + 靜默失敗。
- **正確做法**:
  1. **關鍵動作要有重試餘裕**:窗口留幾分鐘(不是「那一秒」),並掛在會反覆
     醒來的迴圈上,直到真的做成為止。本案改成獨立通道 + 每 2 秒重試。
  2. **「做成了」的判斷要明確回傳**,不要靠副作用推論。本案抽出的
     `_qt_chukuangren_confirm_one()` 回傳 True/False,語意就是「要不要再試」;
     **資料還沒到一定回 False** —— 回 True 會把當天的機會吃掉。
  3. **靜默略過要補日誌,但要防洪**:窗口內每 2 秒走一次,錯誤訊息要
     「每檔策略每天只記一次」,否則會刷 150 則(同 P-99)。
  4. 找這類問題的線索:**「一天只做一次」的動作 + 上游有 dedup/邊界/TTL 閘門**,
     兩者相遇就要問「唯一的那次失敗了會怎樣?」
- **出處**:ADR-128。

### P-102　讓使用者填「兩個幾乎一定相同的數字」,就是設計把負擔轉給使用者
- **症狀**:終極波段的停損分成「觸發點位」與「確認點位」兩個欄位。使用者實機
  填出來的是 **S1=41701 / S2=41700** —— 差 1 點。原話:「這兩個應該是要一樣,
  不需要分兩個,只需填一個 S1 就可以。」
- **根因**:設計時想給「觸發」與「確認」各自的彈性,但那個彈性**跟另一個維度
  重複了** —— 兩段式確認的緩衝已經由「隔天 12:00 才確認」這個**時間**維度提供,
  再加一個**價格**維度只是把同一件事說兩遍。作者看到的是「多一個參數更靈活」,
  使用者看到的是「又要想一個數字,而且想不出跟前一個有什麼不同」。
- **正確做法**:
  1. **多一個欄位就是多一次填錯的機會**。加參數前先問:「使用者會怎麼決定這個
     值?如果答案是『抄上一個欄位』或『大概差一點點』,那它不該存在。」
  2. 合併時把規則放在**參數讀取的邊界**(本案 `params_of()`),不要塞進狀態機:
     所有呼叫端自動一致,狀態機保留原本可離線測試的一般契約。
  3. **資料格式保留舊 key、不做遷移**,但用覆寫讓舊值失效。理由見 P-92/ADR-123:
     一個使用者看不到的欄位若還能左右出場行為,遲早會出事。
  4. 覆寫的**方向**要跟「使用者實際會填哪一個」一致(本案是觸發值),
     並用測試把方向釘住 —— 寫反了就變成「沒填的欄位覆寫填了的欄位」。
- **順帶一課(同一張截圖)**:那次儲存失敗的訊息只寫「S1 必須大於進出場分界 X」,
  使用者看不出自己哪裡錯。**驗證訊息要帶上實際填的數字 + 一句「為什麼」**
  (「做空是指數『漲上去』才要停損」),否則使用者只能猜。
- **出處**:ADR-129。

### P-103　用「設定的門檻」當「實際成交價」去推論,會推出擋掉正確用法的規則
- **症狀**:終極波段的驗證擋掉「做空的停損點位低於進出場分界 X」,理由寫得
  很順:「做空是指數漲上去才要停損,所以停損必須高於進場點」。使用者實測後
  指出這條規則本身是錯的 ——「實際進場的位置,已經離一開始設定的進場位置太遠,
  導致虧損會很大」。
- **根因**:推論的第二步偷換了概念 —— **X 不是進場價**。這個策略的進場是
  「收盤跌破 X」+「隔天 12:00 二次確認」,中間隔了一個晚上加半個交易日,
  指數本來就會跑掉(X=42500 設定,實際可能在 41800 才進場)。於是規則的實際
  效果是**擋掉合理的風控(停損 42000,虧 200 點)、只允許過寬的風控
  (停損 42500 以上,虧 700 點才停)**。
- **正確做法**:
  1. 寫「參數 A 必須大於/小於 參數 B」這類驗證前,先問:**這兩個值真的是
     同一個時間點、同一種東西嗎?** 「設定的觸發門檻」與「實際成交價」之間
     只要隔著任何延遲(次日確認、掛單成交、滑價),它們就不是同一個東西。
  2. 這類「合理性」規則要**很省著用**。它不像「零股只能限價」那種有外部
     依據的規則(交易所規定),它只是作者對使用者意圖的猜測 —— 猜錯就是
     把使用者鎖在錯的做法上,而且錯得很難解釋。
  3. 該保留的是**格式/壞資料**檢查:本案「停損必須 > 0」一定要留 ——
     做多 Y=0 → `close < 0` 永遠不成立 → **完全沒有停損**;
     做空 S1=0 → `close > 0` 永遠成立 → 每天誤判停損。
     (順帶發現:舊規則只「順便」擋到做空那半邊,做多的 Y=0 一直沒人擋。
     **一條靠副作用生效的檢查,幾乎一定有沒被覆蓋到的那一半。**)
  4. 放寬限制時要用測試把**範圍**框住:一條「不可以變成永遠觸發」、一條
     「不可以連相鄰的條件一起放寬」(本案是進場條件仍只看 X)。
     放寬最常犯的錯不是放寬本身,是順手放寬太多。
- **出處**:ADR-130(推翻 ADR-129 才剛加強過的同一條訊息)。

### P-104　tkinter 的 grid 撞格會「安靜地」讓整區設定消失
- **症狀**:使用者截圖「主圖指標參數設定」裡**布林那一整列不見了** ——
  只剩一行說明文字,旁邊露出半截「(σ2=0不畫第二組)」和一個沒有標題的色彩下拉。
- **根因**:說明文字寫成 `grid(row=8, column=0, columnspan=4)`,跟布林的
  checkbox / 期間 / σ1 / σ2 **同一個 grid 儲存格**。tkinter 的 grid 對「同一格
  放兩個 widget」**不報錯、不警告**,就是後放的蓋掉前放的。從第一版就是這樣,
  沒有人發現 —— 因為程式跑得起來、也不會有任何 log。
- **正確做法**:
  1. **把版面當成可以靜態檢查的資料**。診斷用 `ast` 取出該函式裡所有
     `.grid(...)` 的字面值 `row`/`column`/`columnspan`/`rowspan`,展開成儲存格
     集合,**任兩個 widget 不可以佔到同一格**。這檢查的是 bug 的**類別**,
     換個位置再撞一次照樣抓得到 —— 比「檢查某個字串在不在」有價值得多。
  2. 這在 headless / mock tkinter 的環境**特別重要**:量不到真實幾何,
     靜態分析是唯一能真的檢查版面的方法。
  3. 加了新的一整區 UI 之後,**視窗尺寸要一起檢查**。本案布林從 1 行變 5 行,
     沿用舊尺寸會把「確認並套用」按鈕擠出視窗外 —— 那比參數看不見更糟
     (設定根本存不了)。
  4. 一般性提醒:凡是「不報錯、只是視覺上不見了」的 API(grid 覆蓋、
     `pack` 順序、z-order、被裁切的 canvas),都不會有執行期訊號提醒你,
     只能靠**靜態規則**或**使用者截圖**發現。這次是後者。
- **出處**:ADR-131。

### P-105　「有寫進日誌」不等於「使用者收得到」——通知管道的閘門要單獨確認
- **症狀**:ADR-120 的主圖盤勢判斷偵測到型態時會寫一行
  `【盤勢判斷】... 偵測到:...`,看起來功能完整。但使用者的手機**一則都沒收到**,
  直到他主動要求「要用傳訊息到我的手機通知我」才發現。
- **根因**:Telegram 推播掛在 `log_message()` 上,而且有**兩道**閘門:
  1. 訊息前綴必須是「【自動交易」(`is_quant_message`);
  2. **自動交易總開關必須開著** (`_qt_running`)。
  盤勢判斷用的是「【盤勢判斷】」前綴,而且它是**看盤輔助**,使用者看盤時
  根本不會開自動交易 —— 兩道都不成立,訊息只到得了系統日誌。
- **正確做法**:
  1. 新增「要讓使用者知道」的訊息時,**分清楚它要走哪一條管道**,並且回頭
     確認那條管道的**每一道閘門**都會放行。日誌與推播是兩個不同的目的地,
     寫了日誌不代表送得出去。
  2. **閘門的語意要跟功能的語意對得上**。「自動交易在跑才推播」對成交回報是
     對的,對看盤提醒是錯的 —— 後者要繞過那道閘門直接送
     (本案直接呼叫 `_send_telegram_async()`)。
  3. 測試要**斷言訊息真的被送出去**,而不是「日誌裡有那行字」。本案診斷刻意
     把總開關設成 False 再斷言推播仍然送出;把它綁回總開關的突變就會紅。
  4. 反過來也要守:**沒內容的日子不要發**。一天一次的通知一旦變成噪音就沒人
     看了(同 P-99),但「只有支撐壓力、沒有型態」仍然要發 —— 使用者要的是
     「型態**或**支撐壓力」,別把條件寫成 and。
- **出處**:ADR-132。

### P-106　「使用者切到那一頁才算得出來」不是自動化,只是把工作推回給使用者
- **症狀**:ADR-120 的盤勢判斷掛在 `draw_chart()` 上,**只判斷主圖現在顯示的
  那個商品與週期**。ADR-132 我把「主圖切到 60分K 時也會出現判斷結果」當成
  新功能寫進交付說明,使用者直接回:「我要你做到**不需要我人工切換**,
  你就可以自動判斷。」
- **根因**:把「計算」掛在「畫圖」上圖方便 —— 資料現成、不用多打一次 API。
  但那等於把觸發條件綁在使用者的操作上,使用者要看 N 種組合就得手動切 N 次。
  對「盯盤提醒」這種需求,**沒被看到的那一個週期才是最需要提醒的那一個**。
- **正確做法**:
  1. 分清楚「**顯示**用的計算」與「**監控**用的計算」。前者跟著畫面走沒問題;
     後者要有自己的排程,**不依賴使用者現在在看什麼**。
  2. 監控路徑要有自己的「**要不要重算**」判準,不能靠畫面事件當節流。本案用
     「K 棒格子」(分K 對齊 K 棒邊界、日K 每 10 分鐘一格),同一格內不重算 ——
     否則就變成每分鐘重抓一次歷史資料(P-98 的同一個坑)。
  3. **抓到資料才記格子**。抓不到就讓下一輪重試,不要把這一格用掉,
     否則一次失敗就整根 K 棒都不會再判斷(P-101 的同一個教訓)。
  4. 測試要**把畫面停在完全無關的商品/週期**,再斷言監控路徑照樣運作 ——
     這條斷言就是「自動」與「不自動」的分界線。把它改回「只看主圖週期」的
     突變必須紅。
- **出處**:ADR-133。

### P-107　拿「被測物自己」當基準去比對,是最像樣的空殼斷言
- **症狀**:ADR-134 要保證「副圖指標只是搬位置,計算完全沒改」,診斷寫成
  `assert_series_equal(out['RSI'], indicators.rsi(close, p))`。看起來很嚴謹 ——
  **但兩邊都是同一個函式**。突變測試把 `indicators.rsi()` 的算式改壞,
  單元測試紅了,這條診斷卻照樣綠:被測物錯了,基準也跟著錯,兩邊永遠相等。
- **根因**:「重構前後結果一致」這種斷言需要一個**獨立於被測物**的基準,
  而抽出函式之後,最順手的基準剛好就是那個新函式。
- **正確做法**:
  1. **驗「沒改壞」時,基準要獨立**:把算式在測試裡**原地重寫一遍**,
     或存一份重構前的期望值。多打幾行字換一條真的有牙齒的斷言。
  2. 分辨兩種測試的目的:「算式對不對」可以用同一個函式(那是在測呼叫端有沒有
     接對線);「**重構有沒有改到行為**」一定要獨立基準。
  3. 判斷方法很簡單:**把被測的那個函式改壞,這條斷言會不會紅?**
     不會紅就是空殼。這正是突變測試存在的理由。
- **出處**:ADR-134(同一類問題已在 P-28 / ADR-127 / ADR-128 出現過三次,
  這次的變形是「基準與被測物同源」)。

### P-108　診斷工具自己誤報,比漏報更糟
- **症狀**:`diag_crossref.py` 回報「jae 沒有 `COL_A` 這個名稱」,但
  `core/jae.py` 明明寫著 `COL_A, COL_J, COL_E = 'JAE_A', 'JAE_J', 'JAE_E'`。
- **根因**:它收集模組公開名稱時只認 `NAME = ...` 這種單一目標的賦值,
  **漏掉元組拆解賦值**。
- **為什麼比漏報嚴重**:漏報只是少抓到一個問題;**誤報會讓人以為真的斷鏈了,
  然後去改一份本來正確的程式**。我第一反應就是想繞開它(把運算式拆成區域
  變數),那等於為了迎合工具的 bug 而扭曲產品程式碼。
- **正確做法**:
  1. 診斷工具報錯時,**先確認它是不是對的**。工具也是程式,也會有 bug。
  2. 確定是工具的問題就**修工具**,不要在產品程式碼裡繞路 —— 繞路會留下
     一句沒人看得懂的註解,而且下一個人踩到同樣的坑。
  3. 修完一定要**確認它還抓得到真的問題**(本案:把 `COL_A` 改名,
     工具必須重新報 4 處)。放寬一個檢查最容易順手把它整個關掉。
- **出處**:ADR-134。

### P-109　用原始碼切片做斷言時,切片邊界本身也要有斷言
- **症狀**:ADR-135 要驗「自訂策略編輯器有把委託方式存進策略」,診斷寫成
  `_src[index('def _qt_open_custom_editor') : index('def _qt_open_editor')]`
  再檢查裡面有沒有 `s['price_type'] =`。把那一行從自訂編輯器刪掉,**斷言照樣綠**。
- **根因**:那兩個方法**中間還夾著第三個編輯器**(終極波段),它的 `_collect`
  也有 `s['price_type'] = ...` —— 斷言被隔壁的程式碼餵飽了。
  我以為在檢查 A,實際上檢查到的是 B。
- **正確做法**:
  1. **切到「下一個同縮排的定義」為止**,不要切到某個你以為緊接在後的名字。
     檔案會長大,中間會插進新東西,而切片不會抱怨。
  2. **對切片本身下斷言**:`assert 'def 另一個函式' not in body`。
     一行防呆,換掉整類「切錯還以為測過了」的失敗。
  3. 更一般的原則:**原始碼層級的斷言要能回答「我到底在看哪一段字」**。
     能用 `ast` 精確定位就別用字串切片(ADR-131 的 grid 檢查就是用 ast)。
- **出處**:ADR-135。

### P-110　只驗「UI 有那個欄位」而不驗「存檔有寫進去」,等於沒驗
- **症狀**:同上一輪。診斷斷言原始碼裡有 `_qt_build_price_type_row`(欄位建出來了),
  但沒有斷言 `_collect()` 把值寫進策略。把 `s['price_type'] = _pt['get']()`
  刪掉,所有斷言都還是綠 —— 使用者在畫面上選得到委託方式,**存下去卻是空的**。
- **根因**:UI 有三段各自獨立的責任 —— **建出來 / 讀得到 / 存得進去**。
  只驗第一段最容易寫,也最沒有用。
- **正確做法**:新增任何設定欄位時,三件事各要一條斷言:
  1. 欄位存在(建得出來);
  2. 舊設定讀得回來(預設值/相容性);
  3. **存檔真的寫進資料**(把那一行刪掉,測試必須紅)。
  第 3 條是唯一直接對應「使用者設定會不會生效」的一條。
- **出處**:ADR-135。

### P-111　「這條路根本不經過那段程式碼」——功能在某一類策略上形同虛設
- **症狀**:使用者在自訂策略編輯器設了停損,**回測完全當作沒設**(0 筆平倉)。
  欄位存得進去、實盤的即時停損也會動,唯獨回測毫無反應。
- **根因**:停損/停利的判斷內嵌在 `evaluate_strategy()` 裡,而回測對自訂策略
  走的是另一條分支 —— `custom_strategy.decision_to_intents()`,**根本不經過**
  `evaluate_strategy()`。功能不是壞掉,是那條路上沒有它。
- **正確做法**:
  1. 新增/檢查一項「應該對所有策略都成立」的功能時,**先數清楚有幾條路**
     會走到它。本案有四條:內建實盤、內建回測、自訂實盤、自訂回測 ——
     而功能只在其中兩條上。
  2. 把共用的規則**抽成獨立函式**(本案 `check_bar_stop()`),讓每一條路
     都呼叫同一份;內嵌在某個大函式裡的邏輯,天生只服務走進那個函式的人。
  3. 測試要**針對每一條路各寫一條**。只測「純函式對不對」測不到
     「這條路有沒有呼叫它」(P-64 的同一個道理)。
  4. 找這類問題的線索:**`if kind == X: ... else: ...` 的分岔**。
     兩邊各自演化久了,一邊有的東西另一邊往往沒有。
- **出處**:ADR-136。

### P-112　回測與實盤用不同的觸發模型,回測數字就是假的
- **症狀**:實盤的停損是「盤中觸價就走」,回測卻只看**收盤價**判斷。
  同一檔策略,回測顯示的停損次數比實際少,績效被系統性高估。
- **根因**:回測的資料粒度是 K 棒,最自然的寫法就是拿 `Close` 去比 ——
  但實盤看的是即時價,兩者對「同一根 K 棒內發生什麼事」的認知不同。
- **正確做法**:
  1. **有「即時觸發」這種開關時,回測要照同一個開關切換模型**:勾了就用
     當根的 **High/Low** 判斷觸價,沒勾才只看收盤價。
  2. 觸價的成交價要**保守**:
     · 跳空穿過停損價 → 成交在**開盤價**(比停損價更差),不做「一定成交在
       停損價」的樂觀假設;
     · 同一根同時碰到停損與停利 → **算停損**,分不出誰先到就取對使用者
       不利的那一邊。
     這兩條都要有測試,而且把它們改成樂觀版本的突變必須紅 ——
     否則回測會朝「好看」的方向偏。
  3. 看A做B 時**不要**把 A 的觸價當成 B 的成交價 —— 那是兩個商品的價格。
- **出處**:ADR-136。

### P-113　測試資料的形狀要由產生它的正式程式碼決定,不要自己捏
- **症狀**:ADR-137 的測試自己拼模擬帳戶 dict,連續踩兩次 ——
  先是把 `new_account(initial_cash, name, account_id)` 當成 `new_account(id, name)`
  呼叫(帳號 id 被當成初始現金),改好之後又因為**手拼的持倉少了
  `market` / `share_per_unit`**,`paper_account.equity()` 直接 KeyError。
- **根因**:「測試資料看起來只要有我用到的那幾個欄位就好」——但被測的函式
  會讀到你沒想到的欄位,而**真實資料一定有**。手捏的假資料與真實資料的
  形狀差異,是一種只會在測試裡出現的失敗。
- **正確做法**:
  1. **用正式的建構子/寫入函式產生測試資料**(本案 `new_account()` + `apply_fill()`),
     形狀自動跟真實一致,欄位增減也會自動跟上。
  2. 手捏 dict 只用在「刻意要測壞資料」的情境,而且要註明那是刻意的。
  3. 這也是一種迴歸保護:哪天正式結構多一個必填欄位,用建構子的測試會自動
     拿到,手捏的那些會在很久以後才以奇怪的方式壞掉。
- **出處**:ADR-137。

### P-114　同一個設定要「存得起來」與「讀得回來」兩邊都放行,漏一邊就是靜默失效
- **症狀**:ADR-131 (布林中線類型/第2組)、ADR-133 (黃金切割)、ADR-134
  (KDJ 超買賣線/JAE) 的參數,使用者在對話框改好按「確認並套用」,當下圖也
  重畫了 —— 但**下次開程式全部回到程式碼預設值**,而且沒有任何錯誤訊息。
- **根因**:`config_store.load_indicator_settings()` / `save_indicator_settings()`
  都是拿 `DEFAULT_INDICATOR_SETTINGS` 當**白名單**在過濾
  (`for k in DEFAULT_INDICATOR_SETTINGS: ...`)。後來每一輪新增設定時,
  GUI 那邊的 `_collect` / `_apply` 都記得補,**唯獨沒人想到要回頭補那份
  預設 dict** —— 於是新 key 存檔時被濾掉、讀檔時也被濾掉,兩邊都靜默。
  「當下有生效」正是最會騙人的地方:tk.Variable 已經改了,圖也重畫了,
  只有跨行程的持久化沒生效。
- **正確做法**:
  1. 持久化層**不要用白名單過濾使用者的資料**。預設值的角色是「缺欄位時
     補上」,不是「決定哪些欄位可以存在」。值合不合法交給套用端逐項判斷。
  2. 新增一個設定時,**跑一次「改值 → 存檔 → 改回別的值 → 讀檔 → 套用」
     的往返測試**。中間那步「先把值改掉」不可省:少了它,「什麼都沒做」
     也會過。
  3. 診斷案例要走**完整 GUI 路徑**(`app._collect_indicator_settings()` →
     `save` → `load` → `app._apply_indicator_settings()`),純 config_store
     的單元測試測不到「GUI 那邊有沒有把這個 key 放進 dict」。
- **出處**:ADR-138。

### P-115　靜態版面檢查不分「父容器」與「互斥分支」,就會變成假警報製造機
- **症狀**:用 AST 檢查 tkinter grid 撞格 (P-104) 時,第一版對
  `_qt_open_chukuangren_editor` 報了一整串撞格,第二版對 `open_sub_settings`
  又報了一串 —— 兩次都是**假的**。
- **根因**:
  · 沒有分**父容器**:`top`、`watch_fr`、`dlg` 各有獨立的 grid 座標系,
    不同容器同時佔 (0,0) 完全合法;
  · 沒有分**互斥分支**:`if ind == "MACD": ... elif ind == "RSI": ...`
    每個分支都從 row=0 開始,但它們永遠不會同時存在。
- **正確做法**:
  1. 撞格判定的 key 一定是 **(父容器, row, col)**,不是 (row, col)。
  2. 走訪時記住「目前在哪些 `if` 的哪一條分支裡」,兩個 grid 只有在
     **分支路徑相容**時才拿來比。
  3. 認不出父容器、位置不是字面常數 (`row=i+1`) 一律**跳過**:
     這種檢查寧可漏報也不要誤報 —— **假警報的下場就是整條檢查被關掉**,
     那比沒有檢查更糟 (同 P-108)。
  4. 檢查本身要有「解析得到東西」的斷言,否則改壞成「什麼都沒掃到」時
     它會安靜地全綠。
- **出處**:ADR-138 (P-104 的工具化;ADR-131 是第一次踩,ADR-138 第二次)。

### P-116　「通知收得到」不代表「指令送得進去」:雙向管道的兩個方向要分開確認
- **症狀**:使用者回報「我在 Telegram 打 `/help` 完全沒有回應,可是系統傳給
  我的訊息我收得到」,合理懷疑是 Telegram 那邊設定有問題。實際上程式完全
  正常 —— 只是「啟用 Telegram 通知」與「⚠️ 啟用手機遠端控制」是**兩個獨立的
  勾選框**,他只勾了前者,輪詢執行緒根本沒被建立。
- **根因**:出站通知與入站指令的風險等級不同 (前者唯讀、後者能讓系統開始
  下單),ADR-108 刻意把開關拆開 —— 這個設計是對的。錯的是**沒開的時候
  完全靜默**:不建立執行緒、不記日誌、不提示,使用者手上唯一的證據是
  「打了沒反應」,而那個現象跟「token 打錯」「chat_id 不符」長得一模一樣。
- **正確做法**:
  1. 「功能沒啟用」也是一種狀態,**要說出口**。啟動時無條件寫一行日誌講清楚
     現況與該去哪裡開 (同鐵則 4 的精神:沒有的東西要標示,不要留白)。
  2. 判斷與說法收斂成一個 core 純函式 (`telegram_control.control_status()`),
     啟動時與存檔後印的是同一份,使用者不會看到兩種說法。
  3. 診斷要同時驗**沒開時有日誌**與**開了時說已啟用**(反向對照),
     否則「永遠回同一句話」也會綠。
  4. 未授權 chat_id 的指令仍然**只記日誌、不回覆**(不讓對方確認 Bot 存在),
     這條安全設計不因為「要好診斷」而放寬。
- **出處**:ADR-138。

### P-117　「測試功能」是最容易把單子送到正式環境的那條路
- **症狀**(這次是**預防**,不是事後補救):要寫一個「下單測試」功能,
  它天生就得呼叫 `place_order()`。而測試程式最自然的寫法 —— 共用現有連線、
  「反正是模擬模式」就跳過確認視窗 —— 每一項都直接打在鐵則 14 上。
- **根因**:「這是測試,不是真的下單」這個念頭會讓人自動放寬所有防護。
  但**環境是設定出來的,不是保證出來的**:`simulation` 旗標可能被改、
  連線可能被重建、物件可能被別的執行緒動到。一旦接錯,送出去的是真單。
- **正確做法**:
  1. **測試連線做成獨立型別、獨立生命週期**,絕不共用正式連線物件。
     使用者很可能正登著實盤在跑策略,共用等於把他踢下線。
  2. **每次送出前重驗環境**,不是只在建構時驗一次。這道檢查很便宜,
     它擋的是「測試單打到正式環境」。
  3. **確認視窗照跳**。可以放寬的是「哪個函式可以呼叫 place_order」,
     不可以放寬的是「使用者知不知道有東西要送出去」。
  4. 數量之類的欄位在本地就把上限鎖死(測試單就是 1),
     讓萬一接錯時的後果最小。
  5. 突變測試要逐項證明:拿掉確認視窗紅、拿掉環境重驗紅、
     把連線改成正式模式紅。少了這些,上面每一條都只是註解。
- **另一個相關的坑**:官方文件的範例常寫死**會過期的東西**(本案是期貨
  月份合約 `TXFE6`)。照抄進程式,幾個月後變成「查無此合約」,而錯誤訊息
  完全不會告訴你原因。會過期的東西一律改成執行期動態解析。
- **出處**:ADR-139。

### P-119　限制「單幀渲染量」不可以順手刪掉使用者能瀏覽的歷史
- **症狀**：資料源已載入十年以上日 K，但 `tail()` 固定只留 1,000 根，使用者實際
  只能看到約四年；效能變快的代價被誤寫成只是「viewport」。
- **根因**：把資料保存範圍、指標計算範圍與單幀渲染量混成同一個 DataFrame。
- **正確做法**：長週期最低保留十年所需根數（日 2,600／週 530／月 125）；效能問題
  用 artist 重用或真正可移動的 viewport 解決，不可再用尾端截斷冒充 viewport。
- **出處**：ADR-141。

### P-120　長歷史每天整段重抓，不是自動更新而是浪費 API
- **症狀**：每天開程式都重抓十年，載入慢又反覆觸發流量管制。
- **根因**：只有行程內 DataFrame 快取，關程式就全部消失。
- **正確做法**：所有週期 K 線以 SQLite 複合主鍵 upsert；先讀舊歷史，再用
  當次 API 返回更新重疊與新日期。本地已滿十年就不再抓深層源。
- **出處**：ADR-142。
