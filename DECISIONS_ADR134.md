# ADR-134:副圖技術指標整合成單一入口 + 新增 JAE 指標 + KDJ 超買超賣線

## 狀態

已實作,**尚未實機驗證**(分支 `claude/sub-indicators-and-jae`)。

## 需求(使用者原話)

> 1. 把主圖上方列的各種技術指數(MACD&KD&RSI…等)全部整合到一個
>    **【副圖技術指標】**中,要使用哪一個指標,就到這裡面去選擇。
>    **切記,只有移動整合,沒有改其他的功能。**
> 2. 另外,再新增一個 **KDJ 指標 & JAE 指標**。
>    JAE 的定義:**A 就是 RSI 的一條線,J 就是 KDJ 的 J 那一條線**,然後再加上
>    一條**長期趨勢線 (E)**,至於趨勢判斷這條線你再幫我想一下…交給你設計。
>    然後用這三條線利用黃金交叉或死亡交叉等來看進出場,所以這三個是可以自行
>    設定參數的。

---

## 第一部分:整合成單一入口

原本工具列上一字排開 **5 個勾選鈕 + 4 個 ⚙**,佔掉整條工具列,而且要調參數
得先認得哪顆 ⚙ 是哪個指標。改成一顆「**⚙ 副圖技術指標**」,所有開關與參數
都在同一個視窗裡。

**「只有移動整合」是這次最重要的約束**,所以:

- 用的是**原本那些 tk 變數**(`var_macd` / `macd_f` / `var_kdj` / `kd_n` …),
  計算與繪圖一個字都沒改。
- 指標清單收斂成一份 `SUB_INDICATORS` 常數,同時決定「視窗裡有哪幾列」與
  「工具列旁邊顯示哪幾個已開啟」—— 兩份各自維護遲早分歧(P-67)。
- `open_sub_settings()`(單一指標的舊視窗)**保留不刪**:它沒有壞掉的理由,
  只是不再是入口。

**勾選鈕收進視窗之後,使用者會看不出自己開了什麼**,所以工具列上留一個
小標籤即時顯示「:MACD、KDJ、JAE」,全關時顯示「:(未選)」。

### 驗證「真的只有搬位置」

診斷把 MACD/RSI/KDJ/DMI/BBW 全部打開,再把 RSI 與 KDJ 的算式**原地重寫一遍**
當基準去比對每一欄。

> **這裡踩到一個空殼斷言:** 第一版是拿 `indicators.rsi()` 當基準去比
> `out['RSI']` —— **兩邊都是同一個函式**,把那個函式改壞,斷言照樣綠。
> 突變測試抓到之後改成在診斷裡把算式獨立重寫。這類「用被測物自己當基準」
> 的比對特別容易寫出來,因為看起來很合理。

---

## 第二部分:KDJ 缺的那一項

使用者說「現有的有缺什麼」但沒有指明。我逐項查證現有 KDJ:

| 項目 | 現況 |
|---|---|
| 算式 | **正確**。RSV=(C−Ln)/(Hn−Ln)×100;K/D 用 `ewm(com=m−1)`(m=3 → α=1/3,即 K=(1/3)RSV+(2/3)K_prev),J=3K−2D —— 標準 KDJ |
| K / D / J 三條線 | **都有畫**(白 / 黃 / 紫) |
| 參數 N / M1 / M2 | **都可設** |
| **超買 / 超賣參考線** | ❌ **沒有** —— 而隔壁 RSI 副圖一直有 80 / 20 兩條虛線 |

**兩個面板之間唯一客觀的落差就是最後一項**,所以我補上超買/超賣參考線
(預設 80 / 20,可在整合視窗調)。

> **這是我的推斷,不是使用者說的。** 若你要的其實是別的(例如第二組獨立
> 參數的 KDJ、或 J 線的超買超賣區間著色),告訴我實際看到什麼,我再補。

---

## 第三部分:JAE 指標

A 與 J 由使用者定義,**E 交給我設計**。

### E 要滿足三件事

1. **跟 A/J 同尺度**。A 是 RSI(0~100),J=3K−2D(約 −50~150)。E 若用價格或
   均線,數值差好幾個數量級,三條線根本不會相交 ——「黃金交叉」就變成空話。
2. **要夠慢**,才配得上「長期趨勢」。
3. **要有天然的中性基準**,否則「向上/向下」沒有參照點。

同時滿足的最簡單選擇:**E = A(RSI)的長期 EMA**(預設 60 期)。
尺度天生一致;60 期對 14 期的 A 夠慢;**50 是 RSI 公認的多空中性線**,
直接拿來當基準,不必自己發明一個。

用「A 的 EMA」而不是「再算一次長期 RSI」:前者是**同一條線的慢速版**,
交叉的語意乾淨(同一個東西的快與慢);後者是另一條獨立的線,交叉代表什麼
說不清楚。

### 趨勢判斷:位置為主、斜率為輔

```
E ≥ 中線 + band → 向上
E ≤ 中線 - band → 向下
在 band 之內     → 看斜率 (超過 eps 才算有方向,否則盤整)
```

**我第一版寫成「位置與斜率兩個都要成立」,而且是錯的,被單元測試抓出來:**

> RSI 量的是**動能**不是價格方向。一段「等速下跌」會讓 RSI 穩定在某個低點
> (實測 34.5),E 跟著收斂成一條水平線 —— **斜率趨近 0**。
> 兩個條件都要求的話,**最典型的持續下跌反而被判成「盤整」**。

位置為主就沒有這個問題:E 遠離中線本身就是趨勢的證據;只有在中線附近
(方向真的不明時)才需要斜率決定偏哪一邊。`band`(預設 5)與 `eps`(預設 0.05)
都可調。有一條單元測試專門重現那個等速下跌的情境。

### 交叉

三條線兩兩配對,語意不同所以分開報:

| 配對 | 快 × 慢 | 意義 |
|---|---|---|
| J × A | 快 × 中 | 短線進出訊號(最靈敏,也最容易假訊號)|
| A × E | 中 × 慢 | **中期趨勢轉折(最重要的一組)** |
| J × E | 快 × 慢 | 極短線相對長期趨勢的位置 |

**黃金交叉一律定義為「比較快的那條由下往上穿過比較慢的那條」**,三組都照這個
定義,不會有的組別反過來(有測試釘住配對順序)。NaN 永遠不算交叉 ——
否則暖機期會在圖表最左邊噴一串假訊號。

### 範圍聲明

**JAE 只畫線、只寫日誌摘要,不下單。** 使用者說的是「用這三條線…來看進出場」,
所以這一輪做的是判讀工具。要不要接進自動交易策略是另一筆決定,
牽涉風控與委託路徑,不在這次範圍。

---

## 順帶修掉的一個診斷工具盲點

`diag_crossref.py` 收集模組公開名稱時只認 `NAME = ...`,**漏掉元組拆解賦值**
(`COL_A, COL_J, COL_E = 'JAE_A', 'JAE_J', 'JAE_E'`),於是誤報
「jae 沒有 COL_A 這個名稱」。**誤報比漏報更糟** —— 它會讓人以為真的斷鏈了
而去亂改程式。改成用 `ast.walk` 收集賦值目標裡的所有 Name。
修完確認它**仍然抓得到真的斷鏈**(把 `COL_A` 改名 → 4 處回報)。

## 突變測試

| 把程式改成 | 結果 |
|---|---|
| A 不用 RSI(改成均線,違反使用者定義)| 單元紅 ×3;診斷紅 |
| 交叉方向寫反(黃金/死亡對調)| 單元紅 |
| 趨勢改回「位置+斜率都要成立」 | 單元紅 ×3(等速下跌被判成盤整)|
| `rsi()` 算式改壞 | 單元紅;**診斷第一版是綠的**(空殼)→ 改成獨立基準後紅 |
| JAE 沒開也照算 | 診斷紅:`沒開 JAE 就不該算它` |

## 驗證

- `python tests/test_core.py` → **706 個全過**(原 684,新增 22)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **60 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨(並確認修好之後仍抓得到真斷鏈)
- `py_compile` 全通過

## 需使用者實機驗證

1. 工具列副圖區只剩「**⚙ 副圖技術指標**」一顆按鈕 + 一個顯示目前開啟項目的標籤。
2. 點開 → MACD / RSI / KDJ / DMI / 布林通道寬 五列,**開關與參數都在裡面**;
   勾選與參數要跟你原本的設定一致(舊設定檔會自動沿用)。
3. **重點:整合前後同一組設定畫出來的副圖要一模一樣** —— 這次只搬位置。
4. KDJ 副圖多了**超買/超賣兩條虛線**(預設 80/20,可在同一個視窗調)。
5. 勾「**顯示 JAE 副圖**」→ 出現第四條副圖:A(黃)、J(紫)、E(青,較粗)、
   中性線(灰虛線);系統日誌會出現一行【JAE】趨勢與最近的交叉。
6. 改 JAE 的任一參數 → 線要跟著變。
7. 參數打錯字(例如填 `abc`)→ **其他指標仍然要正常顯示**,不可以整張圖掛掉。

## 不在這次範圍

- JAE **不接自動交易**(見範圍聲明)。
- 既有指標的計算、顏色、面板高度、Y 軸自動縮放全部未動。
- 沒有做「第二組獨立參數的 KDJ」(等使用者確認 KDJ 缺的到底是不是超買超賣線)。
