
## ADR-069：日/周/月K 切換仍慢 (股票~15 秒/期貨~25 秒) — 日K以上也走小窗快速出圖、大範圍完整段改可中止分段下載

### 背景

ADR-068 把股票「分K」補上兩段式快速出圖後,使用者回報**日K**仍慢:「股票平均
15 秒、期貨平均 25 秒才能切換成功、滑鼠可以點 K 棒數據」。ADR-068 當時刻意
「股票日K以上維持原樣不加快速段」,這個判斷在日K上是錯的,本 ADR 修正。

---

### 根因 (日K 特有,與分K 不同)

1. **shioaji `kbars` 一律回「1 分 K」**,日K 是把 1 分 K 重採樣出來的。所以
   一張日K圖,底層要下載的是「那段期間的 1 分 K」。

2. **日K 的 shioaji 窗口很大**:`SJ_DAYS["日K"] = 365`。而日K **不**走分段
   (`is_min_tf` 不含日K),是**單一** `_download_kbars_raw(365 天)` —— 等於一次
   要 365 天的 1 分 K (股票 ≈ 10 萬根),就為了重採樣成 ~250 根日K。這一步就是
   ~15 秒。**期貨有日盤+夜盤 (每天分鐘數約股票 2 倍),365 天 1 分 K 更大 → ~25 秒**,
   正好對上「期貨比股票慢」。

3. **股票日K 完全沒有快速段** (ADR-068 只加了分K),要等上面那個 365 天單次
   下載跑完才第一次出圖。

4. **日K 的「深歷史」其實不靠這 365 天 shioaji**:`_publish` 內對日/周/月K 會用
   `_extend_with_yahoo` (股票,20 年日K) / `_extend_with_taifex` (期貨) 往前延伸。
   也就是說,畫面上的長歷史來自 yahoo/期交所,shioaji 那段大窗口主要只是「最近
   一段較權威 + 今天這根即時新鮮」——為此付 15~25 秒非常不划算。

5. **單次大請求整段獨佔 `_kbars_lock` ~15 秒,且切走也停不下來**:連點不同日K
   標的時,舊商品那個 365 天單次下載會把鎖占著,新商品的下載排在後面,體感
   還是卡。ADR-068 的 abort_cb 只能中止「分段」下載,對單次大請求無效。

### 正確做法 (本 ADR)

**一、日/周/月K 也走「小窗快速出圖」,窗口特別小**
* `want_quick` 放寬成 `asset_type in ("future","index_tw","stock")`,不再限定分K
  ——股票日/周/月K 也先搶先出圖。
* `QUICK_DAYS` 的日/周/月K 大幅縮小:日K 45→**7**、周K 240→**45**、月K 600→**120**。
  因為快速段的 `_publish` 本來就會做 yahoo/期交所延伸,shioaji 只需供「最近幾根」,
  用 7 天的小窗 (股票 ≈ 2 千根 1 分 K,~1 秒) 就能搶先畫出**完整**的 20 年日K圖。
* 效果:日K 第一次出圖從 ~15~25 秒 → 約 1~3 秒就能看、能 hover;完整 365 天
  shioaji 段在背景補。

**二、日/周/月K 的完整段改「可中止的分段下載」**
* 原本 `else` 分支對日K 是單一 `_download_kbars_raw(365 天)`。改成:非分K 且
  範圍 > 90 天時,走 `_download_kbars_chunked(chunk_days=90, abort_cb=...)`。
* 好處:(1) 段間釋放 `_kbars_lock`,不再一鎖 15 秒卡住下一檔;(2) 每段檢查
  abort_cb,使用者切走時舊商品立刻停手 (ADR-068 的過期守衛這下對日K 也有效)。
* 期貨若已匯入期交所歷史,`_taifex_plan_download` 會把 shioaji 需求砍到只剩很近
  一小段 (甚至完全不必下載),range ≤ 90 就回到單次快取路徑,不受影響。

### 為什麼這樣安全 (沒有改資料正確性)

* **深歷史來源沒變**:日/周/月K 的長歷史一直是 yahoo(股票)/期交所(期貨) 延伸來的
  (ADR-049),本 ADR 沒動延伸邏輯,只是把「搶先出圖用的 shioaji 小窗」縮小、把
  「完整 shioaji 段」改成可中止分段。`SJ_DAYS` (完整段深度 365/1095/1825) **沒有
  調降**,完整段照樣會把那段 shioaji 權威資料補進來 (背景),快取也照存。
* **快速段 vs 完整段的視角平移 (prev_len, ADR-049) 不會亂跳**:日K 兩段發布的
  總長度都被 yahoo/期交所延伸主導 (同樣 20 年),recent 段從 yahoo 換成 shioaji
  的根數差極小,`added` 接近 0,xlim 幾乎不平移。
* **快速段抓不到 (空窗/剛換約) 就自動落回原完整段流程**,不會比以前差。

### 相關程式位置 (stock_app_pro.py)

* 類別常數 `QUICK_DAYS`:日/周/月K 縮小為 7/45/120。
* `_fetch_data_worker_impl` 台股分支:
  * `want_quick` 放寬為含 stock 的全週期 (取代 ADR-068 的「stock 且分K」)。
  * 完整段的下載分派新增分支:非分K 且 range > 90 天 → 可中止分段
    (`chunk_days=90, abort_cb=_abort_if_superseded`)。

### 需使用者實機驗證 (此環境 headless,無法實測 GUI/shioaji)

1. **日K**:點一檔沒看過的股票,看日誌「⚡ 已搶先出圖…耗時 X 秒」是否降到約
   1~3 秒、且當下滑鼠已能 hover;期貨同樣測一次,回報兩個耗時數字。
2. 確認日K圖的歷史深度沒有變短 (股票仍應有多年日K、期貨有期交所延伸的長歷史)。
3. 連點不同日K標的:是否每點一下新圖都很快出來 (舊商品下載會在日誌看到
   「已停止…已切換其他標的」)。
4. 切回剛看過的商品/換週期:仍應「秒開」(走快取)。
