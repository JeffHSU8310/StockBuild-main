# 開發紀錄與鐵律 (Antigravity Rules & Logs)

這份文件用於記錄每次重大修正的細節、踩過的坑，以及為了防止未來犯相同錯誤而訂立的「鐵律」。

## 📌 鐵律 (Iron Rules)

### 1. 永豐金 API (Shioaji) 抓取資料限制
- **鐵律**：`shioaji.kbars` 抓取分鐘級別 K 線 (1分K、5分K 等) 時，單次請求**絕對不可超過 5 天**的區間。
- **原因**：若單次請求的天數過大 (例如 30 天)，API **不會報錯**，而是會「默默地」回傳被截斷的資料 (例如只剩下最近幾根 K 棒)。這會導致後續繪圖邏輯接收到極短的 DataFrame 而發生顯示異常。
- **對策**：遇到分鐘級別且區間 > 5天的狀況，**必須**使用分段下載 (`_download_kbars_chunked`)，並設定 `chunk_days=10` 與 `subsplit_days=5` 來切割請求。

### 2. Matplotlib / mplfinance 繪圖範圍 (xlim) 繼承問題
- **鐵律**：在呼叫 `ax.set_xlim()` 或讓 `mplfinance` 自動縮放前，**必須**確保新資料的長度 (`len(df)`) 與設定的視角範圍是匹配的。
- **原因**：當使用者「切換不同商品」或「切換不同週期」時，K 棒的總數量會劇烈改變。如果 `axlist` (圖表物件) 被重複使用，且未明確重置 `xlim`，會導致 `xlim` 停留在舊的範圍，或者因為 `tot <= n_c` (資料量小於預設可視範圍) 而未被設定，導致畫面只剩下殘影或是 K 棒被無限拉寬 (亂掉)。
- **對策**：
  1. 切換商品或週期時，`self.saved_xlim` 必須設為 `None`。
  2. 即使資料筆數過少 ( `tot <= n_c` )，也必須**強制設定** `xlim(0, max(1, tot))` 並手動觸發 `auto_scale_y` 與 `auto_scale_indicator_panels`，徹底洗掉舊圖的殘留狀態。

### 3. 快取 (Cache) 污染問題
- **鐵律**：存入 `_kbars_raw_cache` 時，必須將「週期等級 (`tf_class`)」考慮進去，或確定它存入的是完整的「一分K」原始檔。
- **原因**：之前從日K切換到分K時，日K 由於直接由期交所讀取，因此沒有呼叫 Shioaji 下載 (raw 為空)，卻把空的 raw 寫入到以商品代碼為 Key 的快取中。導致切回分K時，讀到空快取。

---

## 📅 修正紀錄 (Changelog)

### [2026-07-21] 修正 5 分 K 切換時 K 線圖亂掉 / 殘留舊狀態
- **現象**：切換到 5 分 K (或切換商品) 時，圖表 K 棒變得很巨大、充滿整個畫面，或只剩下幾根 K 棒 (例如只有 00:05~00:20 的四根)。
- **原因 1 (資料端)**：原先設定 5分K 讀取 30 天歷史，但因為直接使用 `_download_kbars_raw` 向永豐金 API 發出長達 30 天的單一分鐘 K 請求，觸發了 API 的無聲截斷機制，導致僅回傳最新幾根 K 棒 (約 4 根)。
- **原因 2 (繪圖端)**：當傳入的資料量太少 (4 根) 小於預設畫面 K 棒數 (120 根) 時，原程式碼**未明確設定 `xlim`**，導致 `mplfinance` 把 4 根 K 棒硬生生展開到整個畫面；同時若有舊的殘留比例，就會看起來「畫面亂掉了」。
- **解決方案**：
  1. 在 `stock_app_pro.py` 裡的 `_fetch_data_worker_impl` 新增判斷：若為分鐘級別且天數 > 5，強制走 `_download_kbars_chunked` 分段抓取，避免資料被無聲截斷。
  2. 修改 `draw_chart` 的邏輯，在 `tot <= n_c` 的 `else` 區塊中，**強制呼叫** `axlist[0].set_xlim` 與 `auto_scale`，保證無論資料多寡都能正確洗掉舊的繪圖視角。
  3. 全面放寬 `SJ_DAYS` 的天數設定，讓 1 分 K 也能往回看 15 天、5 分 K 往回看 60 天，滿足使用者檢視更多分K歷史的需求；並替分 K 補上 `QUICK_DAYS` 機制，讓近期資料能「秒出」，歷史資料在背景無縫補齊。
