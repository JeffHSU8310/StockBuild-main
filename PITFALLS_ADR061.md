
### P-73　需求裡的「持有不賣」不等於「只買一次」—— 動作與狀態要分開確認
- **症狀**：使用者說「買進後持有都不賣，也可以回測」，我實作成「進場一次後
  永不出場」。使用者更正：「不是只能進場一次，是只要進場條件成立就買進，
  但是不會賣出」——實際要的是**累積加碼**（定期定額／逢低承接）。
- **根因**：需求句描述的是**出場行為**（不賣），我卻連帶假設了**進場行為**
  （只買一次）。一句話只約束了一件事，另一件是我自己補上去的。
- **正確做法**：
  1. 交易策略的需求至少要確認三件事，缺一就問：
     **何時進場、進場幾次（單筆／可加碼）、何時出場**。
  2. 「持有」「長抱」「不賣」這類詞只約束出場；進場次數必須另外問清楚。
  3. 實作前用一句話把理解覆述回去（「所以是每次條件成立都再買一次、
     永遠不賣，對嗎？」），比做完再改便宜太多——這次連測試與診斷案例都
     一起寫死了錯誤語意，更正時要一併重寫。
- **附帶教訓**：**測試會把誤解一起固化**。ADR-059 的 5 個測試全都在斷言
  「只有一筆交易」，語意更正後它們反而變成阻力。測試是規格的表達，
  規格錯了測試就一起錯，不能因為「測試全過」就以為需求做對了。
- **出處**：ADR-061（ADR-059 的需求誤解更正）。

### P-74　部位「加碼」時，進場價必須改成加權平均成本，不可覆蓋
- **症狀**：累積買進模式下，`apply_fill` 沿用一般策略的邏輯直接覆蓋
  `runtime['entry_price']` 與 `qty`，導致成本基礎只反映最後一次買進，
  之前買的全部憑空消失，損益與停損判斷全部失真。
- **正確做法**：同方向再次開倉時
  `new_entry = (prev_px×prev_qty + fill_px×add_qty) / (prev_qty+add_qty)`、
  `new_qty = prev_qty + add_qty`。並且**只在明確允許加碼的模式下這樣做**，
  一般策略維持覆蓋語意（要有測試釘住兩種行為的差異，否則之後很容易被
  「統一一下比較乾淨」而改壞）。
- **延伸**：回測層除了維護加權平均，還要**逐筆保留每次買進的 lot**，
  否則交易明細看不到每次買在什麼價位、資金曲線的浮動損益也只會反映最後一筆。
- **出處**：ADR-061。

### P-75　彙總指標的分母，必須跟著「部位模型」一起改
- **症狀**：累積買進的報酬率顯示 **1473%**。分子是 119 筆 lot 的損益總和，
  分母卻是「每筆成本基礎的平均」（1 筆的規模）——量級對不上。
- **根因**：`_compute_metrics` 的 `base` 是為「一次一筆、進出成對」的模型設計的
  （平均每筆投入多少）。換成累積模型後，同一個公式的語意就不成立了，
  但沒人回頭檢查。ADR-059 才剛修過這個分母漏乘 `contract_size`，
  **同一個地方兩輪內錯兩次**，正說明「分母」是這類程式最容易出錯的地方。
- **正確做法**：
  1. 引入新的部位模型（加碼、分批出場、多商品）時，把所有**比率型指標**
     （報酬率、年化、回撤%、週轉率）拿出來逐一問「分母還對嗎」。
  2. 為比率型指標寫**手算對帳測試**：
     `(期末價−平均成本)×總量×單位規模−總成本 == total_pnl`，
     以及 `total_return_pct == total_pnl / 總投入 × 100`。
     恆等式測試最能抓出分母錯誤。
  3. 看到「數量級明顯不合理」的百分比就當 bug 追，不要用
     「這個指標僅供參考」自我說服（同 P-67）。
- **出處**：ADR-061。
