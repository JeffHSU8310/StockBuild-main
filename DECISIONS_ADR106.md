
## ADR-106：選股條件的歷史回測

### 背景

ADR-103 誠實限制第 5 點：「選股結果無法直接回測『這組條件過去選出的股票
表現如何』」。使用者要求補上。

### 決策一：這不是策略回測，另寫模組

`core/backtest.py` 回測的是「單一標的、逐根 K 棒進出場」；選股回測的是
**投資組合**：每隔一段時間（調倉日）重新篩選一次，把符合條件的前 N 檔等權
買入，持有到下一個調倉日再換一批。兩者的問題本質不同，硬塞進 backtest.py
只會讓兩邊都難維護，因此另寫 `core/screener_backtest.py`。

技術面／籌碼面條件仍然重用 `market_screener.screen`，語意與實際選股完全一致。

### 決策二：基本面條件預設不回測（本 ADR 最重要的正確性決定）

本地 `fundamental.csv` 只存「當前快照」，**沒有 Date 欄**（見
`data/market_store` 的設計說明）。用「今天的 EPS／本益比」去篩選三個月前的
股票，等於在當時就知道尚未公布的財報——這會讓回測績效嚴重虛高，而且是**最
難察覺的一種錯誤**：數字看起來完全合理，沒有任何異常可以提示你不對勁。

因此：

- **預設不套用基本面條件**，結果回報 `fundamental_skipped=True` 並在警告中
  說明原因。
- 使用者可用 `allow_lookahead_fundamental=True` 強制套用（研究用），但結果
  標記 `has_lookahead=True`，摘要末行會加上「⚠ 本結果含未來函數，實盤無法
  複製」，GUI 也會把警告寫進系統日誌。
- 若一組條件**只有**基本面條件，GUI 直接擋下並說明為什麼不能只靠基本面回測。

技術面與籌碼面沒有這個問題：日K 有完整歷史，逐日重算不會偷看未來；籌碼沿用
ADR-101 的防護（T 日只讀得到 T-1 日以前）。

### 決策三：進場價用「訊號日的下一個交易日開盤」

訊號是用收盤價算出來的。若用同一根的收盤價成交，等於「知道收盤價之後還能用
收盤價買到」——那是做不到的。用下一個交易日的開盤價才是實際可執行的價格。
有測試守著（`test_entry_is_next_trading_day_after_signal`）。

### 決策四：前 N 天不選股

技術指標（如 60MA）在資料開頭算不出來。硬選會得到一堆「因為資料不足而條件
不成立」的空結果，那不是策略的真實表現。`rebalance_dates()` 從第
`min_bars` 個交易日才開始取調倉日。

### 產出的指標

總損益／總報酬率、個股勝率、期間勝率、最大回撤、平均每期選中檔數、最佳／最差
單期報酬，以及**全市場等權買進持有的對照與超額報酬**——後者是判斷「這組條件
有沒有贏過隨便買」的關鍵，沒有它，一個在多頭市場賺 20% 的策略看起來很好，
但市場可能漲了 30%。

成本沿用 `core/cost_model`（手續費 + 交易稅），有測試驗證含成本的損益必然低於
不含成本。

### 實測驗證

構造 12 檔 × 80 天的市場，前 4 檔持續上漲、其餘持續下跌，用「站上 20MA」回測：

```
調倉 6 期,共 30 筆持股,平均每期選 5.0 檔
總損益 259,761 元 (+25.98%)
個股勝率 100.0% / 期間勝率 100.0%
最大回撤 0.00%
全市場買進持有 +6.54% → 超額 +19.44%
被選中: 1000/1001/1002/1003 (正是上漲的那 4 檔)
```

未來函數防護實測：預設 `fundamental_skipped=True`、`has_lookahead=False`；
強制套用時 `has_lookahead=True` 且摘要末行出現警告。

另有一條測試用 spy 攔截 `to_ohlcv`，驗證**篩選當下看到的最大日期不超過訊號
日**（`test_no_future_data_leaks_into_screening`）。

### 測試

`tests/test_core.py` 新增 12 個測試，共 **488 個全數通過**。
`diag_repro_issues.py` 新增 ADR-105/106 案例（產業下拉、產業篩選結果、回測
未來函數防護、進場日晚於訊號日、選中上漲股、GUI 紅綠上色），共 **36 案例
全數通過**；crossref 無斷鏈。

### 誠實限制

1. **基本面無法誠實回測**（見決策二）。要解除這個限制，需要開始累積基本面的
   歷史快照（每次更新時另存一份帶日期的），並在回測時取「該日當時可見的」
   那一份。這需要新的儲存設計與相當長的累積時間，未包含在本次。
2. **等權、不停損、滿倉**：目前是最單純的組合模型——每期等權買入、持有到下期、
   不設停損停利、資金全押。真實操作會有部位控制與風控，回測數字會因此偏樂觀。
3. **未考慮流動性與漲跌停**：假設隔日開盤都買得到、賣得掉。低成交量個股或
   跳空漲跌停時，實際無法以該價格成交。
4. **回測區間受本地資料限制**：預設抓 180 天，所以最多回測約半年。要更長的
   回測需要先累積更多歷史（`SC_HISTORY_DAYS` 可調，但下載時間會等比增加）。
5. **沒有滑價**：只計手續費與交易稅。實際成交價與開盤價會有落差。

### 需使用者實機驗證

1. 「選股」分頁 → 先確認「更新全市場資料」已跑完（回測需要足夠交易日，
   少於 32 天會擋下並提示）。
2. 選「技術轉強」範本 → 按「📊 回測條件」，確認結果表列出每一期的訊號日／
   買進日／賣出日／選中檔數／本期報酬，賺紅賠綠。
3. 看系統日誌，確認有總報酬、勝率、最大回撤、**買進持有對照與超額報酬**。
4. 在基本面門檻填入數值再回測，確認日誌出現「已略過 N 個基本面條件」的說明。
5. 選一個只有基本面條件的組合按回測，確認會被擋下並說明原因。
