
### P-81　風控守門的多項限制，只要有一項忘了比照其他項限定「只管 OPEN」，出場單就會被誤擋
- **症狀**：`risk_check()` 的每日次數上限、虧損熔斷都正確限定只檢查 `OPEN`
  intent，唯獨冷卻 (cooldown) 寫在共用區塊，連 `CLOSE` 也一併被擋。持倉中
  出現出場訊號時，若距離上次下單 (通常是進場) 不到 `cooldown_sec`，出場單
  會被冷卻擋下，部位卡在場上出不去。
- **根因**：同一個函式裡本來就有「這幾項限制都只管進場」的一致規則，新增
  或修改其中一項時沒有比照既有項目的寫法（都包在 `if kind=='OPEN':` 裡），
  單獨寫在共用區塊。函式自己的 docstring 與 ADR-035 都白紙黑字寫著「出場單
  不受限制」，但沒有測試真正拿 `CLOSE` intent 去驗證冷卻這一項——舊測試
  只驗證了「CLOSE 不受每日次數限制」，冷卻這項完全沒有對應案例。
- **正確做法**：
  1. 一個守門函式裡如果有「這一類限制只對某種 intent 生效」的規則，新增
     限制時要检查是不是也放進同一個 `if` 區塊，而不是分散寫在函式各處。
  2. 每新增一項風控限制，都要補一個「用它本來不該套用的 intent 種類去測」
     的案例（本例：用 CLOSE intent 測冷卻），而不是只測「它該生效的那種」。
  3. **回測會刻意關掉即時風控** (`_relax_realtime_guards` 把 cooldown/
     每日次數/熔斷全部清零)——這代表這類 bug 永遠不會在回測裡現形，
     只存在於即時 (模擬/實單) 路徑。凡是「回測與實盤行為刻意不同」的地方，
     都要想清楚「這個差異會不會讓某類 bug 永遠測不到」。
- **出處**：ADR-065。

### P-82　「平倉」更新了「進場冷卻」的時鐘，會讓緊接著的反手開倉被自己的平倉擋下
- **症狀**：ADR-053 的反手 (stop-and-reverse) 機制在同一輪評估回傳
  `[平倉, 開倉]` 兩個 intent，逐一處理時：平倉先成交、`apply_fill` 把
  `last_order_ts` 撥成「現在」，緊接著處理開倉時 `risk_check` 算出
  「距離上次下單 0 秒」，只要 `cooldown_sec > 0` 就必定被冷卻擋下——
  反手永遠只完成平倉那一半，等於重演 ADR-053 當初要解決的問題，只是
  換了冷卻這個機制觸發，而不是原本的狀態判斷缺陷。
- **根因**：`last_order_ts` 的語意含糊——它被同時當成「距離上次下單
  (不分方向) 多久」在維護，但冷卻真正該防的是「距離上次進場多久」
  (避免訊號雜訊造成過度進場)。當一個時間戳同時服務兩種不完全一致的語意時，
  只要某個新流程 (反手一次回傳兩個 intent) 打破了「一次評估最多一個 intent」
  的舊假設，語意混用就會現形。
- **正確做法**：
  1. 讓 `last_order_ts` 只在 OPEN 分支更新，明確定義成「距離上次進場的
     時間」，不受同一輪評估裡先發生的 CLOSE 影響。
  2. 設計「同一輪可能回傳多個 intent」的機制時 (本例反手一次兩個)，
     要反過來檢查所有依賴「時間戳/計數器只被上一次評估更新一次」這個
     舊假設的邏輯，是否會被「同一輪內先處理的動作」污染後續動作的判斷。
  3. 兩個問題 (P-81 + P-82) 合起來看：只要把 P-81 的冷卻檢查改成
     「只在 OPEN 生效」，這裡的反手污染問題也會自動消失——但不能只滿足
     於「表面上測試過了」，要想清楚兩個修法是否互相依賴、缺一是否會
     露餡 (若只修 P-81 不修這個，反手的開倉腿依然會被同一輪的平倉更新
     的時鐘擋下)。
- **出處**：ADR-065（ADR-053 反手機制的延伸修正）。

### P-83　條件評估的「單條隔離」設計是對的，但隔離之後如果完全不留痕跡，等於把「真錯誤」偽裝成「條件不成立」
- **症狀**：`eval_conditions()` 對每條條件獨立包 `try/except`，任何一條拋
  例外就讓那一條算 `False`——這個隔離本身沒問題（一條寫壞的條件不該拖垮
  整個策略評估）。但舊版例外被吞掉後不留任何記錄，那條條件會從此永遠算
  `False`，使用者看到「條件到了卻沒有動作」，卻連一行錯誤日誌都查不到，
  跟「條件本來就不成立」在使用者眼中完全無法區分。
- **根因**：「隔離失敗」與「隱藏失敗」是兩件不同的事，寫程式時很容易把
  兩者混為一談——為了不讓一個錯誤拖垮全局而加的 `try/except`，如果沒有
  同時留一個能讓人看到「這裡其實壞掉了」的管道，就從「容錯」變成「消音」。
  這正是本專案一貫在講的「背景失敗必須看得見」(P-58/P-70) 在另一個地方
  的重演。
- **正確做法**：
  1. 任何「為了不讓局部錯誤影響全局而做的 try/except」，都要問一句
     「使用者事後有沒有辦法知道這裡曾經失敗過」——沒有的話就要補一個
     看得見的管道 (本例：把錯誤收進 runtime 欄位，讓上層 GUI 決定要不要
     提示，而不是在 `core/` 純邏輯層直接印日誌，違反零 tkinter/日誌
     依賴的分層原則)。
  2. 分層時，「發生了什麼」(純邏輯層負責記錄) 跟「怎麼呈現給使用者」
     (GUI 層負責) 要分開——純邏輯層不能為了方便直接呼叫 GUI 的
     log_message，但也不能因此什麼都不留，把資訊透過回傳值/runtime
     欄位往上傳遞才是正確的分層方式。
- **出處**：ADR-065。
