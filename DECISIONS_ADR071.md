
## ADR-071：券商 API 斷線自動重連 (可開關) — 記憶體暫存憑證,不落地

### 背景 / 需求

使用者:「遇到系統與券商 API 斷線時,要有自動連線的機制開關。」目標是
「早上開一次、整天不用管」。

### 原本的狀況

`_mark_session_dead` 偵測到斷線只會把 `api_logged_in` 撥回 False、提示使用者
**手動**重新登入,沒有自動重連。而且登入用的「憑證密碼 (ca_pw)」從不存檔
(每次登入手動輸入),所以連要自動重連的材料都沒有。

### 決策 (含一個安全取捨)

自動重連需要憑證密碼。權衡後採「**記憶體暫存**」:登入成功時把這次輸入的
憑證 (含密碼) 只留在 RAM (`self._login_creds_mem`),**不寫進任何磁碟檔**。
- 好處:程式運行期間斷線都能自動重連 → 涵蓋盤中最常見的暫時性斷線,達成
  「開一次、整天不用管」;密碼不落地,外洩風險最低。
- 代價:整個程式關掉重開 (當機/斷電/重開機) 後,需要手動再登入一次。
- (若之後要「連重開機都全自動」,再開一條 ADR 做「加密存檔」的選項,不預先
  把密碼寫到磁碟。)

### 做法

1. **開關**:頂部大盤列新增勾選「🔄 斷線自動重連」(`auto_reconnect_var`,
   預設關)。切換時 `_on_auto_reconnect_toggle` 給明確提示 (未登入時說明要先
   登入一次、密碼只在記憶體)。
2. **暫存憑證**:`_process_broker_login_impl` 登入成功後存
   `_login_creds_mem = {api_key, secret_key, pid, ca_path, ca_pw}`。
3. **觸發**:`_mark_session_dead` 末端,若開關開著且有暫存憑證且沒有正在重連,
   就起一條 daemon 重連工作 (`_reconnecting` 防重入)。
4. **重連工作 `_reconnect_worker`**:退避間隔重試 (15→30→60→120→300 秒),
   每輪先檢查該不該停 (程式關閉 / 開關關掉 / 已登入 / 憑證被清 / 正在登入)。
   **全市場休市時改用 5 分鐘長間隔慢等** (避免半夜空轉猛打),交易時段內才積極
   重連。成功即記「✅ 已重新連線,自動交易自行恢復」並收工。
5. **手動登出優先**:`toggle_login` 手動登出時清掉 `_login_creds_mem`,自動
   重連不會把剛登出的連線又接回去 (使用者主動意圖優先)。

### 為什麼這樣能「自動恢復交易」

`quant_runner_worker` 這條 runner 開機就常駐、永遠在跑;斷線期間
`_quant_eval_pass` 因 `api_logged_in=False` 直接 return (不下單),重連成功後
`api_logged_in=True`、`_qt_running` 全程沒被動過,下一個 2 秒 tick 就自動恢復
評估下單。搭配 ADR-070 的時段閘門,等於「斷線→自動重連→回到盤中就自動繼續」。

### 安全 / 相容

- 憑證密碼只在記憶體,不寫檔;手動登出即清除。
- 重連完全走既有 `process_broker_login` (先釋放舊 session 再建全新連線,
  第八/十二輪的死循環防護沿用),不是另寫一套登入。
- 開關預設關閉:不主動改變既有使用者的行為,要用才勾。

### 相關程式位置 (stock_app_pro.py)

- `__init__`:`_login_creds_mem` / `_reconnecting` 初值。
- `create_widgets`:`auto_reconnect_var` + 勾選框。
- `_process_broker_login_impl`:登入成功暫存憑證。
- `_mark_session_dead`:觸發重連。
- `_on_auto_reconnect_toggle` / `_reconnect_worker` (新方法)。
- `toggle_login`:手動登出清憑證。

### 需使用者實機驗證

1. 登入後勾「🔄 斷線自動重連」→ 製造斷線 (例如在官網/App 重登搶佔 session):
   日誌應出現「第 N 次嘗試重新連線…」並在券商端釋放後自動連上、恢復自動交易。
2. 手動按「登出」→ 確認不會自動重連 (日誌有說明)。
3. 全市場休市時斷線:確認改成每 5 分鐘試一次,不會狂洗日誌。
4. 關掉開關:斷線後不自動重連,回到原本手動登入流程。
