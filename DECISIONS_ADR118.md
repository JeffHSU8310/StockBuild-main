
## ADR-118：籌碼單位改成台股慣用（張／億）、資料年數可設定、回測參數攤開

使用者實機操作後回報的五項。

### 一～三、單位換算（新增 `core/unit_format.py`）

官方籌碼資料的原始單位跟台灣投資人講的不一樣，原樣顯示要在腦中除以 1000 或
1 億才知道是多少，而且欄位之間單位不一致，一眼比不出大小。

| 檢視 | 原始單位 | 改成 |
|---|---|---|
| 個股法人買賣超 | 股 | **張**（÷1000） |
| 大盤法人金額 | 元 | **億**（÷1e8） |
| 融資金額 | 仟元 | **億**（÷1e5） |

**融資融券多一欄「融資增減(億)」**。官方只給當日融資金額，沒有前一日金額，
所以增減要自己用相鄰兩個交易日相減。算之前一定要**先由舊到新排序** ——
順序顛倒會讓正負號整個相反，而使用者正是靠正負號判斷「融資在增加還是減少」，
方向錯了結論就跟著錯。第一期沒有前值時顯示 `--`，不是 0。

換算全部放在 `core/unit_format.py`（純函式、零依賴）。理由：**這些數字錯了
不會有任何錯誤訊息**，只會讓使用者看到錯的量能／金額然後據此買賣，所以一定
要能離線測試（鐵則 11）。「沒有資料」一律顯示 `--` 而不是 0 —— 兩者在籌碼上
意義完全不同。

### 五、資料年數可設定

籌碼與選股各加一個「年數／歷史」下拉：

| 面板 | 選項 | 預設 |
|---|---|---|
| 籌碼 | 1／2／3／5／10 年 | 1 年 |
| 選股 | 6 個月／1／2／3／5／10 年 | **1 年**（原本寫死 180 天） |

**下載是增量的** —— `available_dates()` 會先讀本地已有的日期，只抓缺的那幾天
（ADR-100 的既有保證）。所以把年數調大只會多抓「以前沒抓過」的那一段，
不會重抓。

選股原本寫死 180 天：扣掉技術指標暖身（25 天）只剩約 100 個交易日，以 20 天
調倉一次來算才 4~5 期 —— 這正是使用者看到「回測 4 期」的原因，統計意義很薄弱。

### 四、回測參數攤開來讓人設定

使用者問「回測條件是什麼？不能自己設定嗎？期別是什麼？」——原本這個按鈕
按下去就直接用**寫死／自動推導**的參數跑，畫面上沒有任何地方說明它在做什麼。

按鈕改成先開設定視窗，裡面同時做兩件事：

**說明它在算什麼**（直接寫在視窗上）：
- 把目前這組條件拿回過去每一天重跑，看照著選出來的股票買進、持有一段時間
  再賣出，累積下來會賺還是賠
- **「期別」= 第幾次換股**。每隔一個「調倉週期」重選一次股票，一次就是一期
- 訊號日 = 條件成立那天；**買進日 = 下一個交易日的開盤**（不能當天就買到，
  那會變成偷看未來）；賣出日 = 這一期結束、換股那天

**可設定的三個參數**（留空 = 沿用原本的自動推導）：
- 調倉週期（交易日）
- 每期最多幾檔
- 暖身根數

指定值仍會夾在資料撐得住的範圍內，否則會一期都跑不出來。

### 順帶修好的診斷盲點

`diag_mock_tkinter` 的 Combobox `current(i)` 只記索引，**沒有連動 `get()`**。
真實的 `ttk.Combobox` 選了索引之後 `get()` 會回傳那一項的值；mock 不連動的話，
所有「用 `get()` 讀使用者選擇」的程式碼（年數、歷史深度…）在診斷環境都讀到
空字串而落回預設值，等於那條路徑沒被測到（同 P-57）。已修正。

### 已驗證（headless）

- `tests/test_core.py` **555 全過**（新增 11 個單位換算測試）
- `diag_repro_issues.py` **44 案例全過**（新增 1 個走 GUI 路徑）
- **突變測試**（確認斷言不是空的）：
  - `SHARES_PER_LOT` 1000→100 → FAIL「外資應為 52,784 張，實際 527,844」
  - `YI` 少一個零 → FAIL「應為 -874.85 億，實際 -8,748.46」
  - 增減方向寫反 → FAIL「應為 -231.29，實際 +231.29」

### 需使用者實機驗證

1. 籌碼三個檢視的數字是否合理（個股是張、大盤與融資是億）。
2. 融資融券多出來的「融資增減(億)」欄，正負號與您平常看的一致嗎。
3. 把「年數」調到 3 或 5 年後按「更新籌碼」，確認**只補缺的日期**（不會從頭重抓），
   以及跑完之後表格真的多出更早的資料。
4. 選股的「歷史」調大 → 更新全市場資料 → 再回測，確認**期別數變多**。
5. 「回測條件」按鈕現在會先開設定視窗，看說明是否讀得懂、三個參數是否夠用。
