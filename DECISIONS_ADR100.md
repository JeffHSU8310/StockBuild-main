
## ADR-100：籌碼資料模組 — 官方一手來源 (證交所/櫃買/期交所)

### 背景

使用者要加權指數、櫃買指數、個股、期貨的籌碼資訊。ADR-011 當初移除
FinMind 籌碼指標時明文留了條件:「若之後想恢復,需要先找到 shioaji 或其他
資料源能提供法人買賣超/資券餘額資料,並另開一筆 ADR 記錄新的資料來源,
不要直接把 FinMind 呼叫加回來」。本 ADR 即為滿足該條件的紀錄。

**shioaji 不提供籌碼資料** (已查證官方文件):資料類 API 只有串流報價、
歷史K線、快照、`credit_enquires` (查「這筆委託能不能融資」的額度,不是市場
融資餘額)、借券來源、排行榜、處置注意股。因此改用官方一手來源。

### 資料源 (全部實測通過,非推測)

| 用途 | 端點 | 格式 |
|---|---|---|
| 市場三大法人 (加權指數) | `www.twse.com.tw/rwd/zh/fund/BFI82U` | JSON |
| 上市個股三大法人 | `www.twse.com.tw/rwd/zh/fund/T86` | JSON |
| 市場融資融券 | `www.twse.com.tw/rwd/zh/marginTrading/MI_MARGN` | JSON |
| 上櫃個股三大法人 | `www.tpex.org.tw/www/zh-tw/insti/dailyTrade` | JSON |
| 期貨三大法人未平倉 | `POST www.taifex.com.tw/cht/3/futContractsDateDown` | CSV (cp950) |

`openapi.twse.com.tw` 網域實測回傳 HTML 而非 JSON,**不採用**。

### 架構 (沿用 ADR-049 期交所日K 已驗證的分工)

- `core/chips_parser.py` — 五個端點的正規化解析。零網路、零 tkinter、
  零 shioaji,可離線單元測試 (鐵則 11)。
- `data/chips_store.py` — 本地 CSV 存取。冪等 upsert、`available_dates()`。
- `stock_app_pro.py` — HTTP 下載 (背景執行緒 + `safe_after`)、籌碼分頁 UI。

**與 shioaji 完全無關**:籌碼與行情是兩條獨立路徑,不動任何 shioaji 呼叫,
鐵則 12「台股行情一律 shioaji」不受影響。

### 三個靠實測而非推測定下來的決定

**1. 櫃買欄位語意用恆等式反推並全量驗證。**
TPEx 回傳的 `fields` 有 7 組重複的「買進股數/賣出股數/買賣超股數」,JSON
本身**沒有提供分組名稱**。對應關係是用資料自身的加總關係反推、並對全部
895 列驗證出來的:
```
組2 = 組0 + 組1        (外資合計   = 外資及陸資 + 外資自營商)
組6 = 組4 + 組5        (自營商合計 = 自行買賣   + 避險)
合計 = 組2 + 組3 + 組6  (三大法人   = 外資 + 投信 + 自營商)
```
另附 `verify_tpex_layout()`,下載時逐日檢查這三條恆等式;不成立就**略過
該日並警告**,不寫入可能錯位的籌碼。籌碼欄位錯位是最難發現的錯誤類型
(數字照樣填得滿滿的,只是對應錯人),必須讓改版變成看得見的失敗。

**2. 權證在解析階段就濾掉,且用排除法而非白名單。**
T86 單日回傳 13639 筆,其中 12303 筆是權證 (實測保留 1336 筆)。全存會讓
一年份從 37MB 膨脹到 420MB。判斷用「6 碼且 03~08 開頭 = 權證」的排除法,
而不是「只留 4 碼純數字」的白名單——後者會誤殺特別股 (2883B) 與含字母的
債券 ETF (00945B)。

**3. 期貨用區間查詢,證交所/櫃買只能單日。**
期交所 `futContractsDateDown` 支援日期區間 (實測一次抓 22 個交易日),
一年只需約 12 次請求;證交所與櫃買只能單日查詢,一年各約 240 次。因此
下載器對兩者採不同策略,並以 1 秒間隔節流 (呼應鐵則 5 對 shioaji 節流的
精神,對象換成官方公開服務)。

### 儲存與資料量 (實測)

`chips/` 子目錄,純 CSV 不自行 gzip——git 內部本來就用 zlib (與 gzip 同
演算法) 壓縮 blob,實際佔用相同,但保有可讀、可 diff、可用 Excel 開的好處,
也與現有 `taifex_daily/*.csv` 慣例一致。

```
chips/market_inst.csv          市場三大法人 (每日一列)
chips/margin.csv               融資融券     (每日一列)
chips/futures_inst_YYYY.csv    期貨法人     (按年分檔)
chips/stock_inst_YYYY-MM.csv   個股法人     (按年月分檔)
```

個股按年月分檔而非單一大檔,是因為量級比 taifex 大兩個數量級 (個股一天約
2200 列 vs 期貨 69 列);切成約 3.5MB 的月檔,增量更新只需碰當月那一檔。

一年份實測總量約 **19.5MB** (repo 原本 6.8MB)。使用者已確認要納入版控,
以達成「存起來、日後不用重複抓取」。

### 「不重複抓取」怎麼成立

`available_dates()` / `_chips_missing_days()` 在下載前先讀本地已有的日期,
只抓缺的那幾天,並跳過週末。首次抓一年約 240 天 (約 16 分鐘),之後每天
只補 1 天。`upsert` 是「同日期整批覆蓋、不同日期追加」,重複匯入同一天
不會產生重複列——這是上述保證的前提,有測試守著。

### 順帶修好的診斷不穩定 (延續 ADR-099)

實作期間發現 `diag_repro_issues.py` 有**間歇性失敗**:同一份程式連跑 4 次,
FAIL 數是 2/0/2/…。追查後確認與本次改動無關,是 ADR-099 沒修完的殘留——
背景 daemon thread (`fetch_realtime_worker`) 會不定時自己呼叫
`snapshots()`/`kbars()`,污染測試裡的「下載次數」「批次抓取檔數」計數,
用「最後一筆」或「全域計數」斷言就會隨執行緒時序時而 PASS 時而 FAIL。

修法是讓斷言只認「自己這次觸發的呼叫」:kbars mock 記錄呼叫來源、新增
`chart_calls()` 只數手動查詢路徑;自選股批次改比對「呼叫前後的差集」。
修正後連跑 3 次穩定 0 FAIL / 32 PASS。

(過程中一度誤判為自選股 worker 造成,加了停用開關;用 traceback 探針查出
真正來源是 `fetch_realtime_worker` 後,已把那個多餘的改動移除。)

### 順帶修好的 mock 缺陷

`diag_mock_tkinter.py` 的 Treeview `insert()` **直接丟棄 tags**,導致所有
「依 tag 上色」的邏輯 (最典型的就是鐵則 1 紅漲綠跌) 在診斷環境完全測不到
——同 P-28 的教訓 (空殼 mock 讓被測程式碼從沒真正被執行)。已改為忠實
保存 tags,`item(iid, 'tags')` 可取回,新的籌碼案例就是靠它驗證買超紅/
賣超綠。這也讓日後其他 Treeview 上色邏輯變成可測。

### 測試

- `tests/test_core.py`:新增 25 個籌碼測試 (解析 15 + 儲存 10),
  共 **409 個全數通過**。涵蓋官方佔位符 (`--`/空字串)、四種日期格式、
  權證排除、櫃買欄位反推、恆等式失敗偵測、冪等 upsert、前導零保留
  (`0050` 不可被讀成 `50`)、跨年月分檔合併。
- `diag_repro_issues.py`:新增 ADR-100 案例 (四種檢視、紅漲綠跌、
  切分頁不觸發下載、已抓日期不重抓),共 **32 案例全數通過且穩定**。
- `diag_crossref.py` 無跨模組斷鏈;`py_compile` 全通過。

### 誠實限制

1. **全部是盤後日資料**,約 15:00 後公布,**沒有盤中即時籌碼**。想要即時
   外資買賣超需要付費資料商,官方管道做不到。
2. **期交所端點是未公開文件的下載介面**,網站改版可能失效。已設計成失敗
   時明確報錯並保留既有快取,不靜默退化。
3. **首次抓一年約 16 分鐘** (240 天 × 4 端點 × 1 秒節流)。可隨時按停止,
   已抓到的不會白費 (下次接續)。
4. 國定假日無法先驗判斷,會照樣發出請求並得到空資料;這種日子不會被記為
   「已抓」,所以每次更新都會再試一次那幾天 (影響是多幾次請求,不影響正確性)。
5. **尚未接進策略引擎**:目前只有分頁呈現與本地儲存,還不能用「外資連買
   三日」當進場條件。要做需另開 ADR (牽涉策略條件的資料來源與回測口徑)。
6. 券商分點主力進出、集保股權分散 (週資料) 這次未納入;前者官方不提供。

### 需使用者實機驗證

1. 開啟 App → 底部「籌碼」分頁 → 按「⬇ 更新籌碼 (近一年)」,確認進度
   顯示正常、可按停止、系統日誌有摘要。
2. 四種檢視 (個股法人/期貨法人/大盤法人/融資融券) 切換都正常顯示。
3. 個股檢視輸入 `2330` 查歷史;期貨檢視留空看臺股期貨、輸入「小型」看小台。
4. 確認買超顯示紅色、賣超顯示綠色 (鐵則 1)。
5. 再按一次「更新籌碼」,應顯示「本地已涵蓋此區間,無需下載」——這是
   「不重複抓取」生效的直接證據。
