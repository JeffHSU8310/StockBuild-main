
## ADR-058：券商下載流量管制的根本解、期交所歷史的「盤別口徑」問題、回測期間快選

### 一、券商 kbars 一直被擋（使用者需求 #1）

**現象**：回測/最佳化下載時反覆出現
`ServerError: kbars: request $P2P/v:bcsolace01/...`，整段整段失敗。

**釐清**：這是永豐 Solace 路由層的**流量管制**，不是資料不存在。ADR-048 已經
做過段間節流、退避重試、失敗切小段搶救——但那些全都還停留在「想辦法把請求
送出去」的層次，只要範圍夠長（20 年日K = 數十段），請求數量本身就會再次觸發
管制。

**根本解法：能不下載就不要下載。**

期貨 R1 連續合約的日/周/月K，只要本地期交所歷史已經涵蓋那段期間，就**根本
不必向券商要**。新增 `core/taifex_daily.split_coverage()` 與
`_taifex_plan_download()`：

* 期交所已完整涵蓋 → **完全跳過**券商下載。
* 只差最近幾天 → 只下載「期交所結束日之後」的尾巴。
* 判斷失敗、非期貨、非日/周/月K → 一律退回原本的全段下載，絕不因為這個
  最佳化而少抓資料。

用使用者實際提供的 `TX.csv`（1998-07-21 ~ 2026-07-17，6947 根）實測：
回測 2016-01-01~2026-07-17 → **完全略過下載**；回測到 2026-07-25 →
只需補 2026-07-18 之後，**省下約 3851 天的請求**。

副作用是好的：不吃每日 API 流量配額，重跑長期回測幾乎瞬間完成。

---

### 二、期交所歷史是「全天盤」還是「一般早盤」？（使用者需求 #3）

這是本輪最重要的問題，而且答案**兩者都不是**——它是**混合的**。

**實證方法（不靠任何欄位標示，直接看價格行為）**

* 含夜盤時：交易日 D 的 Open 是前一天 15:00 的夜盤開盤，距前一日 13:45 收盤
  只隔 75 分鐘 → 隔夜跳空很小。
* 只有日盤時：Open 是當天 08:45，距前一日 13:45 收盤隔約 19 小時，期間國際
  市場照常波動 → 隔夜跳空明顯大得多。

**用使用者提供的檔案實測（以 2017-05-15 期交所夜盤上線日切分）**

| 商品 | 上線前中位跳空 | 上線後中位跳空 | 比值 |
|---|---|---|---|
| TX（臺股期貨） | 0.3794% | 0.0644% | **5.89×** |
| MTX（小型臺指） | 0.3465% | 0.0631% | **5.49×** |
| TMF（微型臺指，2024 才上市） | — | 0.081% | 全段含夜盤 |

**結論**：2017-05-15 之前臺灣期貨市場**根本沒有夜盤**，所以那之前的資料必然
只有日盤；那之後 `build_front_month_daily` 會把「一般＋盤後」合併成近全序列。
於是同一個 `TX.csv` 的**定義在 2017-05-15 中途改變**。

這**不是資料錯誤**，是市場事實。但任何橫跨該日的長期回測，策略在前後兩段
面對的其實是**不同口徑的商品**（隔夜跳空幅度差 6 倍，跳空型策略的績效會被
嚴重扭曲）。

**系統如何正確對應**

1. **提供兩種口徑，兩份都存**
   * `TX.csv` = 近全（含夜盤，維持既有行為，已匯入的資料不受影響）
   * `TX_day.csv` = 只用日盤（忽略「盤後」列）
   匯入時**一次產生兩份**，換口徑不必重新下載。

2. **shioaji 端同步對齊**：`futures_session.resample_future_session()` 新增
   `session_basis` 參數，`'day'` 只保留 08:45–13:45 的分K 再依自然日分組。
   否則會出現「期交所端是日盤、券商端是近全」的接縫錯誤。

3. **自動偵測並警告**：`detect_session_regime()` 用上述隔夜跳空法實證判斷，
   回測完若判定為 `mixed` 就主動在日誌警告，並建議改用「只用日盤」。

4. **讓使用者選**：回測與最佳化對話框都新增「盤別口徑」選項
   （近全／只用日盤），預設 `'all'` 維持既有行為。

**誠實的取捨**：「只用日盤」放棄夜盤資訊，但換得 1998 年至今**口徑一致**的
序列；對只做日盤的策略反而更貼近實際。「近全」貼近現在的實際交易，但不適合
橫跨 2017-05-15 的長期回測。兩者沒有絕對優劣，所以交給使用者選，並把判斷
依據（實測數字）直接寫在 UI 上。

---

### 三、回測期間快選（使用者需求 #4）

回測與最佳化對話框都加上一排快選鈕：**3M / 6M / 1Y / 2Y / 3Y / 5Y / 7Y /
10Y / 15Y / 20Y**，按一下自動填入起訖日（結束日固定為今天）。

---

### 需使用者實機驗證

1. `python tests/test_core.py` → 200 項全過。
2. `python diag_repro_issues.py` → 全 PASS（含新增 ADR-058 案例）。
3. **重新匯入一次期交所歷史**（📥 期交所歷史）——這次會同時產生
   `TX.csv` 與 `TX_day.csv`，之後才能選「只用日盤」。
4. 回測 TXFR1 日K、期間按「20Y」→ 日誌應出現
   「期交所本地歷史已完整涵蓋…完全略過券商下載」，**不該再看到
   ServerError 洗版**。
5. 選「近全」回測 2010~2026 → 日誌應出現盤別混合警告；改選「只用日盤」
   重跑 → 警告消失，且兩次的績效數字應有明顯差異（這正是問題的存在證明）。

### 相關程式位置

* `core/futures_session.py`：`session_basis` 參數、`NIGHT_SESSION_START`。
* `core/taifex_daily.py`：`build_front_month_daily(session=)`、
  `detect_session_regime()`、`split_coverage()`。
* `data/taifex_store.py`：`store_path/load_daily/save_daily/has_daily` 加 `session`。
* `stock_app_pro.py`：`_taifex_plan_download()`、`_taifex_prod_of()`、
  `_taifex_load_hist(session=)`、`_taifex_merge_save` 產生雙口徑、
  `_extend_with_taifex(session=)`、`_resample_sj_df(session_basis=)`、
  兩個 worker 的 `session_basis` 參數與快取 key、兩個對話框的快選鈕與盤別選項。
* `tests/test_core.py`：`TestSessionBasisADR058`（191 → 200）。
* `diag_repro_issues.py`：新增 ADR-058 案例。
