
## ADR-064：回測 T+1 開盤成交模型正式入檔，修正因此連帶失效的 2 項測試

### 背景

這不是使用者這次回報的新需求，而是接手 session 開工前的例行檢查
（`python tests/test_core.py`）發現：237 項測試裡有 2 項失敗，且都指向
同一個根因——`core/backtest.py` 目前的實際成交時機，跟 ADR-039 當初寫下的
設計描述已經不一致，而這個改動**從未被記錄成任何一筆 ADR**。

### 現況 vs. 文件：兩者對不上

ADR-039（`run_backtest` 的原始設計 ADR）明確寫的是：

> 「逐根K棒『重放』歷史，每根都當成『剛收盤』餵給引擎，引擎回什麼意圖就照樣
> 成交」

但實際程式碼（`core/backtest.py` 主迴圈）是：

```python
eval_window = df.iloc[:i]          # 訊號判定:只看「i 之前」已收盤的資料
...
open_px = float(df['Open'].iloc[i])  # 成交價強制改成「第 i 根的開盤價」
for intent in intents:
    intent['price'] = open_px
```

也就是說，訊號用「昨天收盤」判定，成交發生在「今天開盤」——**T+1 開盤成交**，
不是 ADR-039 描述的「同根收盤即成交」。這兩處程式碼本身的中文註解寫得很清楚
（「當天判定，隔天開盤執行」），代表當初改的人知道自己在做什麼，只是**忘了
回頭寫一筆 ADR、也沒有同步修正 docstring 開頭仍照抄 ADR-039 舊描述的那幾行**。

比對 `DECISIONS.md` 的 ADR 編號序列可以發現 ADR-045～054 這個區間完全缺席
（`grep` 不到任何一個標題），推測這個改動就藏在這段遺失的區間裡（程式碼裡的
`# 【ADR-050】`、`# 【ADR-053】` 等註解證實這段期間確實有大量修改）。既然
原始 ADR 已經找不到，本 ADR 就作為這個行為**現在**的正式記錄，不再嘗試考古
「哪一筆遺失的 ADR 引入了它」。

### 為什麼這個改動本身是對的，不建議revert

T+1 開盤成交比「同根收盤即成交」更貼近真實下單流程——你不可能在還沒收盤前
就用收盤價成交。這跟 P-49（「絕不能用未完成的K棒評估訊號」）是同一種
防前視偏誤的精神，方向正確，予以保留並正式承認為現行設計，而不是回退成
ADR-039 原本的簡化模型。

### 連帶失效的 2 項測試，逐一修正

1. **`test_backtest_equals_live_logic`**：斷言「回測第一個進場點 == 引擎判定
   的金叉點」，但金叉點 `cross_i` 是用全部資料 (含當根) 算出來的，而實際
   成交在「訊號確認後的下一根」。修正為 `df.index[cross_i + 1]`，並在測試
   註解說明這是刻意的 T+1 設計，不是誤差。

2. **`test_dca_invests_close_to_planned`**（ADR-062 的定期定額投入上限保證）：
   原斷言 `bnh_total_invested <= bnh_buys * dca_amount + 1e-6`（近乎嚴格相等，
   只留浮點誤差空間）。但定期定額的股數是用「決策當根收盤價」換算的
   （`strategy_engine.py` 的 dca 分支），實際成交價卻是「下一根開盤價」——
   兩者之間的隔夜跳空會讓單期實際成本略高於或略低於預算。實測（隨機漫步
   價格、35 期、每期 10000）：預算 350,000，實際投入 350,348（超出
   0.0994%）。**這不是餘額 (carry-over) 機制本身的 bug**——carry-over 的
   精確保證已經由 `test_dca_carry_over_when_too_expensive`（固定價格、沒有
   跳空缺口）獨立驗證過。修正為相對容忍度 `<= planned * 1.01`（1%，留約
   10 倍實測值的margin），只用來抓「真的超支很多」的錯誤，不再假設
   sizing 價格與成交價格永遠相同。

### 誠實的取捨（不建議的替代方案）

有考慮過「讓 sizing 也用下一根開盤價」來讓不變式重新變嚴格，但這需要
`strategy_engine.evaluate_strategy` 在決策當下就知道「下一根」的開盤價——
這正是 P-49 要杜絕的前視偏誤，不能為了讓一個測試更漂亮就開這個後門。
放寬測試容忍度是誠實的做法：承認「用已知價格決定數量、用未知的下一根價格
成交」這個模型下，預算是「目標」不是「上限保證」。

### 需使用者實機驗證

1. `python tests/test_core.py` → 237 項全過（本輪修正前是 235 過 2 敗）。
2. `python diag_crossref.py` → 無斷鏈（未受影響，僅作收尾例行檢查）。
3. 本輪未變更任何 GUI/下單/報價邏輯，`stock_app_pro.py` 沒有修改，不需要
   額外實機驗證；純粹是 `core/backtest.py` 的註解修正 + `tests/test_core.py`
   的 2 項斷言修正。

### 相關程式位置

* `core/backtest.py`：模組 docstring 補上 ADR-064 說明段落；`run_backtest`
  內 3 處既有的 T+1 相關註解補上 `【ADR-064】` 標籤（程式邏輯本身未變動）。
* `tests/test_core.py`：`test_backtest_equals_live_logic`（`cross_i` →
  `cross_i + 1`）、`test_dca_invests_close_to_planned`（`+1e-6` →
  `* 1.01` 容忍度）。
