
## ADR-101：籌碼條件接入策略引擎

### 背景

ADR-100 完成了籌碼的下載、儲存與呈現,但誠實限制第 5 點明列「尚未接進策略
引擎,要做需另開 ADR」。使用者要求把籌碼變成策略條件之一,本 ADR 即為此。

### 決策一:籌碼併進 df,條件函式簽名不變

既有條件函式的簽名是 `func(df, params)`,只吃 K 棒 df。與其改動全部既有條件
的簽名 (侵入性大、風險高),不如在**資料準備階段**把籌碼併成 df 的額外欄位
(`ChipForeign`/`ChipTrust`/`ChipDealer`/`ChipInstTotal`/`ChipMarginBalance`/
`ChipFutForeignOI` 等)。

這個選擇帶來三個好處:
1. **實盤與回測自動共用同一條路**——實盤走 `_qt_fetch_closed_bars`、回測走
   `_qt_prepare_df`,兩者都呼叫同一個 `_qt_attach_chips` →
   `chips_features.attach_chips`,不會出現「回測對、實盤錯」的雙軌問題。
2. **GUI 零改動就會出現新條件**——策略編輯器是自動讀 `CONDITIONS` 渲染的
   (`cond_keys = list(strategy_engine.CONDITIONS.keys())`),新增條件即自動上架。
3. `core/` 維持零網路、可離線測試 (鐵則 11)。

### 決策二:未來函數防護 (本 ADR 最重要的部分)

三大法人買賣超、融資融券餘額都是**當日盤後 (約 15:00 後) 才公布**的。
如果讓 T 日的 K 棒讀到 T 日籌碼,回測績效會虛高到實盤無法複製——這是回測
最經典的致命錯誤 (look-ahead bias)。

**預設 `chips_allow_same_day=False`:T 日只讀得到 T-1 日(含)以前的籌碼。**
實作在 `chips_features.attach_chips()`:用 `merge_asof(direction='backward')`
對齊,預設把比對鍵往前推一天。逐根驗證過 (見測試
`test_default_never_reads_same_day_chips`):第 1 根為 NaN (沒有前一日),
第 i 根讀到的一律是第 i-1 天的籌碼。

依使用者決定,保留進階開關:策略編輯器有一個橘色警示的
「⚠ 籌碼允許讀當日 (含未來函數,僅供研究)」勾選框,預設關閉。

### 決策三:僅日K 以上可用

籌碼是日頻資料,分K 策略的當日每一根都只會讀到同一筆前一交易日的值,
沒有意義。依使用者決定,`validate_strategy` 在存檔時就擋下並說明原因,
而不是讓使用者以為「分K 也有籌碼訊號」。

### 決策四:缺資料要出聲,不可靜默

`chips_features.series_of()` 在「欄位不存在」或「全為 NaN」時拋
`ChipDataMissing`。`eval_conditions` 的既有 try/except 會把訊息收進
`errors`,GUI 就能提示「本地缺這段期間的籌碼,請先到籌碼分頁更新」。

這是刻意的:**「沒資料」與「條件沒到」對使用者的意義完全不同**。若靜默
回 False,使用者會對著一個永遠不成立的條件猜原因 (ADR-065 的教訓)。

回測不會因此中斷:回測透過 `evaluate_strategy` → `eval_conditions`,
例外一定被接住 (有測試 `test_chip_conditions_never_break_backtest_via_eval_conditions`
守著這個保證)。

### 新增的 15 個條件

外資/投信連續買超賣超 N 日、三大法人買超賣超 ≥X 張、外資由賣轉買/由買轉賣、
外資買超佔成交量 ≥X%、融資餘額連續 N 日增減、期貨外資未平倉 ≥/≤X 口、
期貨外資未平倉由空翻多/由多翻空。

單位處理:個股籌碼原始單位是**股**,使用者參數用**張**(與下單單位一致),
比較時乘 `SHARES_PER_LOT=1000`。資料本身保持原始股數不失真。

「外資買超佔成交量 %」刻意用相對比例而非絕對張數:外資買 500 張對台積電
是雜訊,對中小型股可能是關鍵買盤,絕對門檻會讓大小股失真。

### 調整既有測試的理由 (誠實交代)

`test_every_condition_is_callable_and_safe_on_short_df` 要求「所有條件在
資料太短時回 False 而不是拋例外」。籌碼條件在**沒有 Chip 欄位**時會拋
`ChipDataMissing`,與之衝突。

我沒有放寬這條安全網,而是**修正它測的東西**:改成餵籌碼條件「有 Chip
欄位但只有兩根」的 df。理由是——欄位不存在代表的是「使用者還沒下載籌碼」,
那是另一種狀況 (刻意設計成要出聲);而回測初期真正會遇到的是「欄位由
attach_chips 建好了,只是前幾根 NaN」,那才是這條測試的原意。同時另補一條
測試,證明即使完全沒有籌碼欄位,走 `eval_conditions` 也只會讓那一條變 False
並記錄原因,不中斷回測。

### 測試

- `tests/test_core.py` 新增 20 個測試 (chips_features 10 + 籌碼條件 10),
  共 **429 個全數通過**。涵蓋逐根驗證未來函數防護、進階選項、籌碼缺日沿用
  最後已知值、不就地修改輸入 df、期貨依身份別分流、張/股單位換算、
  翻多僅在轉折那根成立、分K 擋下、預設值不得為 True。
- `diag_repro_issues.py` 新增 ADR-101 案例 (GUI 端讀本地籌碼併入、防護在
  GUI 這條路同樣生效、分K 擋下),共 **33 案例全數通過**。

### 誠實限制

1. **籌碼不會自動下載**。自動交易的評估迴圈每 2 秒跑一次,在裡面發網路請求
   會拖垮迴圈,也違反 ADR-100「下載由使用者明確發動」的設計。使用者必須先到
   籌碼分頁按更新;沒有資料時條件不成立並在日誌說明。
2. **回測報告目前不會標示「含未來函數」**。勾選進階選項後,回測結果本身沒有
   醒目提示 (只有編輯器裡的橘色警語)。要在報告加標示需另做,這次未包含。
3. 個股籌碼只涵蓋**上市+上櫃股票**;興櫃、美股沒有這類資料,條件會因缺資料
   而不成立。
4. 期貨籌碼目前只對應到 `CHIPS_FUT_PRODUCT_MAP` 列出的五個商品家族
   (TXF/MXF/TMF/EXF/FXF),其餘期貨會 fallback 到臺股期貨,可能不符預期。
5. 「外資買超佔成交量%」的成交量用的是 K 棒 Volume;台股個股 Volume 單位是
   **股**(與籌碼一致),但若日後資料源改變單位,這個比例會失真——已在測試
   中固定此假設。

### 需使用者實機驗證

1. 先到「籌碼」分頁按更新下載近一年資料 (ADR-100)。
2. 新增日K 策略 → 進場條件下拉選單應可看到 15 個 `[籌碼]` 開頭的條件。
3. 建一個「外資連續買超 3 日」進場、「外資由買轉賣」出場的策略,跑回測,
   確認有交易產生且報告正常。
4. 試著把該策略週期改成 5分K 存檔,應被擋下並顯示原因。
5. 在「還沒下載籌碼」的情況下啟用籌碼策略,確認系統日誌會提示缺資料,
   而不是靜默不動作。
