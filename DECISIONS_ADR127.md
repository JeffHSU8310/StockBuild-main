# ADR-127:日K 類快取的新鮮度改問「有沒有跨過開盤」+ 補資料通知每天只一次

## 狀態

已實作,**尚未實機驗證**(分支 `claude/day-cache-session-freshness`)。

## 背景:使用者的 Telegram 截圖

```
09:20  【自動交易-資料】^TWII 日K 歷史資料分 4 段在背景補齊中,補完後策略會自動開始評估。
09:55  【自動交易-資料】^TWII 日K 歷史資料分 4 段在背景補齊中,補完後策略會自動開始評估。
```

> 「為什麼會出現這個訊息?是哪裡有問題嗎?」

**先直接回答:這則訊息本身不是錯誤**,它是 ADR-122 刻意加的 ——
第一次要抓 300 天的 ^TWII 資料時,分段下載需要時間,所以告訴使用者
「策略還沒開始評估,不是壞了,等一下」。

**但它出現兩次,就暴露了兩個真的問題。**

---

## 重現:兩次的間隔為什麼剛好是 35 分鐘

用當天的參數實跑,時間軸完全對得上:

| 時刻 | 發生什麼 |
|---|---|
| 09:20:30 | 第一次抓 → 起背景預抓 → **推播第一則** → 快取寫入 |
| 09:50:30 | `QT_CACHE_TTL_DAY_TF = 1800` (30 分鐘) 到期 |
| 09:50:00 | 這一次評估**還在 TTL 內** → 用快取,沒事 |
| **09:55:00** | 快取已過期 → **重抓** → **推播第二則** |

35 分鐘 = **30 分鐘 TTL + 下一個 5 分鐘評估點**。

5 分鐘從哪來?終極波段的**看A 是加權指數,`watch_timeframe='5分K'`**,
所以評估迴圈每 5 分鐘走一次 —— 但它抓的是**日K**。兩個週期錯開,
就長出這個 35 分鐘的節奏。

### 問題一:整天每 30 分鐘白抓 300 天的資料

ADR-122 當時的論證本身是**對的**:

> `_qt_fetch_closed_bars` 會**丟掉最後一根**視為未完成,所以日K 策略「評估用
> 的資料」在盤中完全不變,重抓沒有任何意義。

錯的是**實作把它近似成 30 分鐘 TTL**。TTL 回答的是「放多久了」,
而該問的是「**會讓資料改變的事件發生了沒有**」。近似一旦對不上,結果就是:

- 一天約 **11~13 次**重新下載 ^TWII 的 300 天資料,每次 **4 段**
- 每一次抓回來的東西**保證跟上一次一模一樣**
- 而每次分段下載都是 4 個 `kbars()` 大請求 —— 正是 ADR-121 那個
  `ShioajiTimeoutError` 的風險來源

### 問題二:一則「請稍候」訊息被推播了十幾次

`【自動交易-資料】` 這個前綴會被 `telegram_notify.is_quant_message()` 判定要
推播。訊息內容沒錯,錯在它掛在一條**會被反覆走到**的路徑上 ——
作者(我)當初預期「一天只會出現一次」。

---

## 決定

### A. 新鮮度改成事件式判斷(core 純函式)

```python
# core/market_session.py
def session_open_minutes():
    """所有可能的開盤時刻,取聯集後排序 (08:45 / 09:00 / 09:10 / 15:00)。"""

MAX_CACHE_SPAN_DAYS = 7

def any_session_opens_between(t0, t1):
    """(t0, t1] 之間有沒有跨過任何一個交易時段的開盤時刻。"""
```

`(t0, t1]` 左開右閉是刻意的:剛好等於 `t0` 的那個開盤,它的資料已經反映在
快取裡了。

**保守方向寫死在設計裡**(這是安全性的關鍵,不是省事):

| 做法 | 效果 | 為什麼選它 |
|---|---|---|
| 開盤時刻取**所有商品的聯集** | 快取早一點過期 | 少列一個開盤時刻 → 拿舊資料去評估 → 下錯單 |
| **不看國定假日** | 假日也算「跨過開盤」 | 同上;假日多抓一次只是慢一點 |
| `None` / 時鐘倒退 / 跨超過 7 天 | 一律回 `True` | 判斷不出來就當過期 |

### B. GUI 側:日K 類走 `fresh_check`,分K 類**維持原樣**

```python
if kbars_plan.is_min_timeframe(tf):
    raw, fresh = self._kbars_cache_get(key, start_dt)              # 30 秒 TTL,不動
else:
    raw, fresh = self._kbars_cache_get(key, start_dt,
                                       fresh_check=self._qt_day_cache_fresh)
```

`_kbars_cache_get` 多一個 `fresh_check` 參數(不傳 = 完全照舊,**主圖行為
不變**);`fresh_check` 自己拋例外時一律當過期。

**分K 類刻意不套用**:分K 的資料每根K棒真的會變,套上「跨過開盤才過期」
會漏訊號。診斷有一條專門守著這個區別。

### C. 絕對上限當保險

```python
QT_CACHE_MAX_AGE_SEC = 24 * 3600
```

週末沒有開盤,單靠事件規則可以讓記憶體裡的資料活過整個週末。那期間確實不會
有新的日K,但「資料放了三天沒重新確認過」風險報酬不划算,寧可週一多抓一次。

### D. 「背景補齊中」每個 `(key, 日期)` 只通知一次

```python
_today = f"{datetime.now():%Y-%m-%d}"
if self._qt_prefetch_noted.get(key) != _today:
    self._qt_prefetch_noted[key] = _today
    self.safe_after(0, self.log_message, ...)
```

**去重跟「該不該重抓」是分開的兩件事**:修完之後重抓仍然可能合法發生
(真的跨過開盤了),但通知每天只有一次。診斷刻意有一條
「強制重抓、但**不可以**再推播」的斷言把這個分離釘住。

### 刻意保留 `QT_CACHE_TTL_DAY_TF`

策略路徑已經不用它了,但它仍是 `_kbars_cache_get` 的預設值(`tf_class=='day'`
時)。留著不刪,免得其他呼叫端無聲改變行為。

---

## 突變測試(這次抓到一個空殼斷言)

| 把程式改成 | 結果 |
|---|---|
| `any_session_opens_between` 永遠回 `False` | 單元測試紅 ×4 |
| `any_session_opens_between` 永遠回 `True` | 單元測試紅 ×3;診斷紅(永遠不收斂)|
| **還原成 ADR-122 的 30 分鐘 TTL** | 診斷紅:`實際多抓了 4 次` ← **重現使用者的截圖** |
| 拿掉每天只通知一次的守門 | 診斷紅:`不可以重複推播,即使真的重抓了` |
| 拿掉絕對上限 | **第一次是綠的** → 見下 → 修好斷言後紅 |
| 絕對上限縮成 19 小時 | 診斷紅(證明反向對照有牙齒)|
| 分K 也套 session 判斷 | 診斷紅:`分K 類必須維持短 TTL` |
| `session_open_minutes()` 漏掉夜盤 15:00 | 單元測試紅 ×2 |

### 空殼斷言:「拿掉絕對上限」那個突變是綠的

第一版的斷言是:

```python
_age_cache(app.QT_CACHE_MAX_AGE_SEC + 60)    # 往前撥 24 小時 + 1 分鐘
_fetch_and_wait()
assert calls['n'] > n_before, "超過絕對上限應重抓"
```

看起來合理,實際上**測不到上限**:往前撥 24 小時的區間**本來就跨過了好幾個
真實開盤**(昨天 15:00、今天 08:45……),`any_session_opens_between` 自己就
回 `True` 了。斷言成立的理由跟絕對上限**完全無關**,所以把上限拿掉照樣綠。

修法是先模擬「期間內沒有任何開盤」(= 週末),再配一組**成對**斷言:

1. 沒跨開盤 + **未超過**上限(20 小時)→ **不可以**重抓
2. 沒跨開盤 + **超過**上限 → **必須**重抓

只有上限能造成這兩者的差異。改完之後,「拿掉上限」與「上限縮成 19 小時」
兩個方向的突變都紅了。

> 同 P-28 / ADR-124:斷言通過時要問「**它是因為我想測的那個原因才通過的嗎**」。
> 「往前撥時間」這種手法特別容易踩 —— 撥過去的區間會順便滿足別的條件。

## 驗證

- `python tests/test_core.py` → **616 個全過**(原 609,新增 7)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **53 案例全過,0 FAIL**
- `python diag_crossref.py` → 乾淨

診斷用「把快取時間戳往前撥」模擬時間經過,**不依賴真實時鐘**(P-94/P-97);
`any_session_opens_between` 要別的值時在案例內 patch 並在 `finally` 還原。

診斷案例走**完整的 GUI 路徑**(`_qt_fetch_closed_bars` →
`_qt_start_kbars_prefetch` → 背景執行緒 join),不是只測純函式 ——
「呼叫端有沒有真的用到新的判斷」只有走完整路徑才測得到(P-64)。

### 一個實作上的坑(記在診斷註解裡)

第一版把案例設成 `QT_PREFETCH_SYNC = True`(其他 ADR-122 案例都這樣),結果
「第一次應通知一次」當場紅 —— 因為那則訊息**只存在於非同步預抓那條路**
(`_qt_start_kbars_prefetch`),`SYNC=True` 走的是原地分段下載,根本不經過它。
改成 `SYNC=False` + 攔 `KBarsPending` + join 背景執行緒。

## 需使用者實機驗證

1. **早上第一次啟動策略時,「歷史資料分 N 段在背景補齊中」只收到一則** ——
   之後一整天不該再收到同一則。
2. 補完之後**策略確實開始評估**(不會因為快取被判新鮮就卡住不動)。
3. **隔天**再看:新的一天應該會再收到一則(每天一次是預期行為)。
4. **分K 策略的訊號沒有變慢/漏掉** —— 分K 走的還是 30 秒 TTL。

## 不在這次範圍

- `QT_TF_DAYS['日K'] = 300` 的天數本身沒動(300 天 ≈ 205 個交易日,對 MA240
  仍不足)。那是另一個問題,使用者尚未回報,先不動。
- 兩檔同名策略在日誌上分不出來(ADR-123 已記)。
- `paper_account.json` 不動(使用者已決定保留紀錄)。
