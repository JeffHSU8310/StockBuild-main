# ADR-120:盤勢/型態偵測從「終極波段策略」搬到主圖【盤勢判斷】

## 狀態

已實作,**尚未實機驗證**(分支 `claude/regime-panel`)。

## 背景:使用者回報

> 1. 圖片中的盤勢功能,要拉到主圖來判斷主圖加權指數K線圖,因此,在主圖上的
>    支撐壓力改為【盤勢判斷】,在【盤勢判斷】中裡面有包含目前有的盤勢+支撐
>    壓力,同樣都是可以勾選。如果,主圖加權指數K線圖有出現盤勢中的型態,會
>    自動通知我。原本在終極波段的盤勢功能,就不需要。
> 2. 只要打開程式,預設主圖K線圖就是加權指數的日K。

## 問題出在哪

盤勢/型態偵測原本掛在**終極波段策略的編輯器**裡(`_qt_pattern_check` /
`_qt_pattern_intraday_preview`),意思是:

- 要先建一個終極波段策略、
- 而且要把量化總開關打開、策略要在跑,

才會評估盤勢。但**看盤跟跑策略是兩件事** —— 使用者想在看加權指數 K 線圖的
當下就知道「現在是什麼盤、有沒有出現型態」,不想為了看盤勢而養一個策略。

同時主圖上已經有一顆「支撐壓力」(ADR-102)。支撐壓力跟盤勢/型態其實是同一
類東西:**都是對著現在這張 K 線圖做出的判斷**,分成兩處(一個在主圖、一個
在策略編輯器)本身就是分類錯誤。

## 決定

### 1. 主圖的「支撐壓力」改名為【盤勢判斷】,底下兩個子項

```
主圖工具列:  ☑ 盤勢判斷  [⚙]
                        └─ ① 量價支撐壓力 (畫線)        ← 原 ADR-102
                           ② 盤勢/型態偵測 (通知)       ← 從終極波段搬來
```

總開關 + 子項都要開才會作用。原本掛在工具列上的「計算範圍」下拉選單移進
⚙ 對話框(工具列已經很擠,而且新增的參數不只一個)。

### 2. 型態偵測的觸發時機:主圖畫完就評估

掛在 `draw_chart()` 裡、緊接在 `_draw_sr_levels()` 之後,**共用同一份
`raw_df`**,不用多打一次 API(鐵則 5 的精神:能不打就不打)。

三個閘門(`core/regime_panel.should_evaluate`):

| 閘門 | 為什麼 |
|---|---|
| 總開關 + 型態子項都要開 | 沒開這個功能的人行為完全不變 |
| **週期必須是日K** | 型態的定義(頸線/缺口/區間)都以「一天一根」為前提,套到 5 分 K 上算出來的東西沒有意義 |
| 預設**只在加權指數** | 使用者的需求就是加權指數;不限制的話,翻閱自選股時會被一堆個股型態洗版。可在 ⚙ 取消這個限制 |

### 3. 通知去重是這次最關鍵的設計

**主圖每縮放/平移一次就重畫一次。** 如果照 `evaluate_all()` 的結果直接寫
日誌,同一個型態會在同一根 K 棒上被通知幾十次 —— 而且畫面上不會有任何錯誤
訊息,只會看到日誌一直捲。

去重規則抽成純函式 `core/regime_panel.plan_notifications()`:

- **一次性型態**(M頭/W底/頭肩頂底/破底翻/假突破/島型反轉/區間突破跌破/
  N字形):同一根 K 棒內只通知一次;換到新的一根重新確認就是新事件,要再通知。
- **持續狀態**(盤勢分類/接近區間邊界/三角形楔形):內容沒變就不再通知,
  跨到新的一根也一樣 —— 盤勢連續 20 天都是「區間整理」不該連發 20 則。

原本住在 `stock_app_pro.QT_PATTERN_PERSISTENT_IDS` 的持續狀態清單一併搬進
`core/regime_panel.PERSISTENT_PATTERN_IDS`。

### 4. 設定持久化與正規化

面板設定存進 `app_settings.json` 的 `regime_panel` 一整包 dict。**刻意不攤平
成十幾個 key**:欄位的預設值與值域檢查全部收斂在
`core/regime_panel.normalize()`,`data/config_store.py` 不需要知道裡面有哪些
欄位,日後加欄位也不用改那個檔。

`normalize()` 對「設定檔壞掉」的態度:**永遠回一份能用的設定**。設定檔可能
是空的(第一次啟動)、可能是舊版本存的(缺欄位)、也可能被手動改壞(型別
不對、數值離譜)。絕不可以讓「設定檔壞掉」變成「主圖畫不出來」。

有一個**刻意的區分**:`pattern_list` 這個 key 不存在(舊設定檔)→ 用預設的
全部型態;key 存在但是空 list → 使用者真的把型態全部取消勾選了,要尊重他的
選擇,不可以「好心」幫他變回全開。

### 5. 終極波段策略那一區整個移除

移除的東西:編輯器的「📈 盤勢/型態提醒」整區、`_qt_pattern_check`、
`_qt_pattern_intraday_preview`、`QT_PATTERN_PERSISTENT_IDS`、`_collect()` 裡
的 `pattern_*` 欄位。

**舊策略檔裡殘留的 `pattern_*` 不主動清掉** —— 不再被讀取,留著無害;萬一
使用者想降版回舊程式,資料還在。

順帶消失的是「60分K盤中預告」(`_qt_pattern_intraday_preview`)。那是為了
「策略在跑、想提早知道日K會不會收在區間邊界」設計的,離開策略脈絡後就沒有
對應的使用情境了;主圖上看的是日K收盤後的正式判定。若之後想要盤中預告,
應該做成主圖上的獨立功能,而不是把舊的搬過來。

### 6. 啟動預設 = 加權指數 日K

- `self.current_symbol` 與代碼輸入框的預設值改為
  `regime_panel.DEFAULT_INDEX_SYMBOL`(`^TWII`)。
- 週期預設值本來就是「日K」(`self.timeframe_var`),不用改。
- 市場模式預設「台股」,`^` 開頭走的就是台股指數這條路,不用改。

## 新增/變更的檔案

| 檔案 | 變更 |
|---|---|
| `core/regime_panel.py` | **新增**。面板設定正規化 + 通知去重(純函式,零 tkinter) |
| `core/volume_profile.py` | 新增 `RANGE_VISIBLE`/`RANGE_FIXED`/`RANGE_MODES` 常數(原本寫死在 GUI 類別裡,設定正規化也要用,收斂成唯一出處) |
| `data/config_store.py` | `DEFAULT_APP_SETTINGS` 新增 `regime_panel` |
| `stock_app_pro.py` | 工具列改名 + ⚙ 對話框、`_regime_check`/`_regime_toggle`/`open_regime_settings`/`_sr_active`、移除終極波段那一區、啟動預設改 `^TWII` |
| `tests/test_core.py` | 新增 `TestRegimePanel`(20 個) |
| `diag_repro_issues.py` | 新增 ADR-120 案例;ADR-102 案例配合改成「總開關+子項」 |

## 驗證

離線全過:

- `python tests/test_core.py` → **575 個全過**(原 555,新增 20)
- `python tests/test_brokers.py` → 42 個全過
- `python diag_repro_issues.py` → **46 案例全過,0 FAIL**
- `python diag_crossref.py` → 無斷鏈、無重複定義

### 突變測試(每個新斷言都確認「改壞就會紅」)

| 把程式改成 | 結果 |
|---|---|
| 去掉「同一根K棒不重複」的去重 | 單元測試紅 |
| 去掉持續狀態「內容沒變不通知」 | 單元測試 + 診斷都紅 |
| `should_evaluate` 不看週期 | 單元測試 + 診斷都紅 |
| `should_evaluate` 不看 `index_only` | 單元測試 + 診斷都紅 |
| `normalize` 不夾值域 | 單元測試紅 |
| `_sr_active` 不看總開關 | 診斷紅 |
| `draw_chart` 不呼叫 `_regime_check` | 診斷紅 |

## 需使用者實機驗證

這次的改動**版面與互動佔一大半,headless 完全測不到**,請重點看:

1. **開程式,主圖是不是直接就是加權指數的日K。**(不用登入也該看到代碼框
   是 `^TWII`;圖要等登入後才抓得到。)
2. **主圖工具列**:原本的「支撐壓力 + 計算範圍下拉」變成「☑ 盤勢判斷 ⚙」,
   工具列有沒有擠壞。
3. **勾選【盤勢判斷】**:支撐壓力線要照舊畫出來(壓力紅、支撐綠),而且
   系統日誌會出現一則「【盤勢判斷】加權指數 …… 日K偵測到:目前盤勢:……」。
4. **重點:縮放/平移主圖幾次,日誌不可以一直重複同一則通知。** 這是這次
   最容易出錯的地方,離線測過但實機節奏不同,請實際拖幾下。
5. **⚙ 設定對話框**:兩個子項各自勾/取消要立即生效;「套用並儲存」後關掉
   程式再開,設定要留著。
6. 打開終極波段策略編輯器,確認「📈 盤勢/型態提醒」那一區已經不見,而且
   **儲存策略仍然正常**(舊策略載入後儲存不可出錯)。
7. 切到 5分K/60分K,確認**不會**再跳盤勢通知(型態只在日K評估)。
