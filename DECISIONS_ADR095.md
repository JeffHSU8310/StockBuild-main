
## ADR-095：量化交易獨立視窗「有時候關閉不了」— 診斷與修正

### 背景

使用者回報:量化交易獨立視窗 (策略管理/回測/參數最佳化) 有時候會關閉
不了。這裡記錄診斷過程與找到、修正的根因，以及一個已知但這次不動的
既有架構限制。

### 根因1 (已修正)：訊息框 parent 寫死成主視窗，被獨立視窗擋住看不見

`_build_quant_panel()` 這段程式碼是「主視窗底部分頁」跟「獨立視窗
(open_quant_window)」共用的同一份，裡面好幾處 `messagebox.showinfo/
askyesno(..., parent=self)` 把 parent 寫死成主視窗根 (`self`)。當使用者
實際在操作的是獨立視窗 (`win`) 時：

- 自訂 Python 策略編輯器、終極波段策略編輯器的「儲存成功」訊息框
  (parent=self)，跟 3 個編輯器中第 3 個 (內建條件策略編輯器，已經
  正確寫 `parent=dlg`) 的行為不一致。
- 「請先選取策略」「僅支援自訂策略」「策略比較無資料」「已有回測/
  最佳化進行中，要不要強制終止」這幾個從獨立視窗按鈕觸發的訊息框，
  也都寫死 `parent=self`。

`messagebox` 是整個應用程式共用的 modal grab——跳出來的訊息框如果因為
parent 設定錯誤而被其他視窗擋住 (使用者看不到)，並不代表它沒有跳出來，
而是使用者完全找不到那個在等待「確定/是/否」的隱形視窗，感覺上就是
「所有視窗 (含獨立量化視窗的關閉鈕) 都沒反應，像是卡死關不掉」。

**修正**：
- 兩個編輯器的「儲存成功」訊息框改成 `parent=dlg` (存檔完成前 `dlg`
  都還存在，訊息框顯示完才 `dlg.destroy()`，跟第3個編輯器一致)。
- 新增 `self._qt_dialog_parent()`：獨立視窗還開著就回傳它，否則回傳
  主視窗。其餘 4 處從獨立視窗按鈕觸發、但寫死 `parent=self` 的訊息框
  改用這個函式動態決定 parent。

### 根因2 (已知限制，這次不動)：大量K棒圖表渲染會同步卡住整個UI執行緒

matplotlib 的 `canvas.draw()` 是同步、且必須在主執行緒呼叫的重運算，
Tkinter 整個應用程式 (含所有 Toplevel 獨立視窗) 共用同一個單執行緒
事件迴圈——主圖在渲染大量K棒/多指標面板時，這整段時間**所有視窗都
會沒有反應**，包括量化交易獨立視窗的關閉鈕，因為關閉鈕的點擊事件
要排隊等主執行緒有空才處理得到。

這不是新發現的問題，程式裡本來就有自我診斷的機制 (`stock_app_pro.py`
約 4780 行)：單次繪圖渲染超過 350ms 就會在系統日誌記一行
「本次繪圖渲染 XXX ms (YYY 根K棒)；若切換/縮放偏慢多半卡在此」——
使用者這次截圖的量化視窗狀態列剛好就顯示了這一類訊息 (1604ms /
1078 根K棒)，跟回報的「有時候關不掉」時間點相符，很可能是同一次
發作的其中一個成因：不是視窗真的壞掉關不掉，是在那 1~2 秒內整個
應用程式的UI執行緒在忙著畫圖，所有點擊都要排隊。

**這次刻意不動這個問題**：要徹底解決需要把 matplotlib 渲染搬到背景
執行緒或子行程，這是牽動主圖整個繪圖管線的大改動，風險與影響面
遠超過這次回報的範圍，且沒有在有畫面的環境無法驗證修改後圖表是否
還能正常顯示——如果使用者確認這是主要原因、想進一步處理，需要另外
開一輪討論評估怎麼做 (例如降低預設抓取的K棒根數上限、或圖表渲染
搬到子執行緒) 才動工。

### 使用者之後回報時如何分辨是哪一種

- 系統日誌/最新訊息在「關不掉」的當下前後，**有**看到「本次繪圖
  渲染 XXX ms」這類訊息 → 大概率是根因2 (等它畫完，通常1~2秒內
  應該就會恢復正常，不需要強制關閉行程)。
- **沒有**看到繪圖渲染訊息、且等了好幾秒還是完全沒反應 → 可能還有
  其他隱藏原因，請截圖當下系統日誌的最後幾行回報。

### 測試

純 UI 邏輯修正，不影響 `core/`/`data/` 任何可離線測試的模組，
`tests/test_core.py` 384 個測試不受影響、全數通過。GUI 層需要在有
畫面的機器上實機驗證：

1. 開啟量化交易獨立視窗，選一個策略按「🎯 參數最佳化」但不符合條件
   (例如非自訂策略)，確認跳出的提示視窗有正常顯示在最前面 (不是
   被獨立視窗擋住)。
2. 開啟終極波段策略/自訂Python策略編輯器，儲存後確認「儲存成功」
   訊息框正常顯示在編輯器視窗上方，關掉後編輯器也正常關閉。
3. 若之後又遇到「關不掉」，依上面「如何分辨」的方法確認是不是根因2，
   並回報當下系統日誌內容以便進一步排查。
